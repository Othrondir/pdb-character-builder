---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - apps/planner/src/features/level-progression/prestige-gate-build.ts
  - apps/planner/src/components/shell/level-sub-steps.tsx
  - packages/data-extractor/src/cli.ts
  - apps/planner/src/components/ui/confirm-dialog.tsx
  - apps/planner/src/styles/app.css
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Either (A) ConfirmDialog primitive + its app.css style block are removed because pre-flight grep confirmed 0 callers, OR (B) the audit finding is re-classified as a false positive with evidence pinned in 13-02-SUMMARY.md because save-slot-dialog.tsx imports + renders ConfirmDialog and 3 phase-08 tests exercise it. NEITHER outcome is a failure — the binding success is that the decision is sourced from grep evidence, not from the audit's stale assertion."
    - "computeHighestClassLevel function AND ARCANE_SPELLCASTER_IDS constant are both removed from prestige-gate-build.ts (these ARE verifiably dead — grep confirms zero callers outside the helper body itself per 12.8-REVIEW IN-01)."
    - "level-sub-steps.tsx aria-label no longer reads the generic static literal 'Sub-pasos del nivel' — instead it interpolates the active level (the template literal uses the level prop). The level prop is PRESERVED because it is consumed by claseComplete/habilidadesComplete/dotesComplete predicates at lines 40-54 (grep-verified). The ROADMAP's 'unused level prop dropped' wording reflects a misreading of the audit source; the actionable finding is the generic aria-label, which IS fixed."
    - "In packages/data-extractor/src/cli.ts the unconditional 2DA-parse calls loadClassLabels(...) + loadSpellsColumnNames(...) (currently at cli.ts:311-312) are moved INSIDE the if (EMIT_MAGIC_CATALOGS) block (currently opening at cli.ts:388) so default extractor runs skip the wasted parse. classLabelsByRow is consumed only inside the spells branch (line 402). spellIdsByRow's let declaration stays in outer scope because the domains branch (line 424) reads the value after the spells branch assigns it, but both branches are already guarded; that guard is preserved."
    - "pnpm typecheck still green (baseline 4 pre-existing errors preserved, NO new errors added by any deletion or edit)."
    - "pnpm test (vitest run) still green against the baseline tolerance: 6 pre-existing failures (phase-08 + phase-12.4 documented out-of-scope per 12.8-VERIFICATION.md line 115) permitted; 0 NEW failures from this plan."
    - "Zero-diff gates hold: git diff origin/master on apps/planner/src/features/persistence/, packages/rules-engine/, apps/planner/src/data/compiled-*, apps/planner/src/styles/tokens.css all empty."
  artifacts:
    - path: "apps/planner/src/features/level-progression/prestige-gate-build.ts"
      provides: "Prestige gate build state without the dead computeHighestClassLevel helper + ARCANE_SPELLCASTER_IDS constant"
      contains_not: "computeHighestClassLevel"
      contains_not_alt: "ARCANE_SPELLCASTER_IDS"
    - path: "apps/planner/src/components/shell/level-sub-steps.tsx"
      provides: "level-aware aria-label on the sub-steps container"
      contains: "Sub-pasos del nivel "
    - path: "packages/data-extractor/src/cli.ts"
      provides: "EMIT_MAGIC_CATALOGS-gated 2DA parse — default runs skip loadClassLabels + loadSpellsColumnNames"
      contains: "if (EMIT_MAGIC_CATALOGS)"
    - path: ".planning/phases/13-verification-orphan-sweep/13-02-SUMMARY.md"
      provides: "Records pre-flight ConfirmDialog grep result + the branch taken (delete vs false-positive re-classification)"
      contains: "ConfirmDialog callsite audit"
  key_links:
    - from: "prestige-gate-build.ts post-deletion"
      to: "packages/rules-engine/src/progression/prestige-gate.ts PrestigeGateInput"
      via: "highestArcaneSpellLevel / highestClassLevel via computeHighestSpellLevel (the LIVE code path)"
      pattern: "computeHighestSpellLevel|highestArcaneSpellLevel"
    - from: "level-sub-steps.tsx aria-label"
      to: "LevelSubSteps container accessible name"
      via: "template literal interpolating the level prop"
      pattern: "Sub-pasos del nivel "
    - from: "cli.ts main() default branch"
      to: "spells/domains branches"
      via: "EMIT_MAGIC_CATALOGS gate subsumes loadClassLabels + loadSpellsColumnNames"
      pattern: "if \\(EMIT_MAGIC_CATALOGS\\)"
---

<objective>
Close the dead-code / orphan-sweep tech-debt items from `v1.0-MILESTONE-AUDIT.md`
sections 07.2 + 12.8 + 05.1 (IN-06). All items are refactoring hygiene within
already-Complete requirements — no REQ-IDs reopened, no new behavior, no new
trust boundaries.

Purpose: Close ROADMAP Phase 13 Success Criteria #2..#6 subject to the
pre-flight callsite gate on #2. SC #3–#6 are verifiably safe (grep-confirmed at
plan time).

Output: Source edits under `apps/planner/src/` + `packages/data-extractor/src/`
that remove unreferenced symbols, fix the aria-label interpolation, and gate an
efficiency-wasted 2DA parse — with `pnpm typecheck` + `vitest run` remaining
green and all four zero-diff gates holding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@CLAUDE.md

@.planning/phases/12.8-uat-04-23-residuals/12.8-REVIEW.md
@.planning/phases/07.2-magic-ui-descope/07.2-REVIEW.md

@apps/planner/src/features/level-progression/prestige-gate-build.ts
@apps/planner/src/components/shell/level-sub-steps.tsx
@apps/planner/src/components/shell/creation-stepper.tsx
@apps/planner/src/features/summary/save-slot-dialog.tsx
@apps/planner/src/components/ui/confirm-dialog.tsx
@tests/phase-08/save-slot-dialog.spec.tsx
@packages/data-extractor/src/cli.ts

<interfaces>
<!-- Planner pre-flight findings — executor should treat as authoritative -->
<!-- guidance, but re-run the grep at execute time to catch any drift since -->
<!-- planning on 2026-04-24. -->

Finding A (ConfirmDialog callsite audit — ROADMAP Success Criterion #2):
- ROADMAP asserts "0 callers post-magic-purge" — this is FALSE on master at plan time.
- Grep on 2026-04-24 found:
    apps/planner/src/features/summary/save-slot-dialog.tsx:3   — import { ConfirmDialog } from '@planner/components/ui/confirm-dialog';
    apps/planner/src/features/summary/save-slot-dialog.tsx:109 — <ConfirmDialog ...
    tests/phase-08/save-slot-dialog.spec.tsx:81,124,150        — 3 test cases exercise the overwrite-confirm branch.
- Rationale: save-slot-dialog.tsx reuses ConfirmDialog for the Guardar slot-name-collision OVERWRITE flow (not magic-related; added in Phase 08, not 07.2).
- Action: re-run the grep in Task 1; if 0 callers outside confirm-dialog.tsx itself, delete. If 1+ callers, flag the audit item as a false positive and record in 13-02-SUMMARY.md. DO NOT delete a live primitive.

Finding B (level-sub-steps `level` prop — ROADMAP Success Criterion #4):
- ROADMAP wording "unused level prop dropped; aria-label interpolates active level" conflates two facts.
- Grep confirms the prop IS USED internally: lines 40-54 pass `level` to isClaseLevelComplete(progressionState, level), isHabilidadesLevelComplete(..., level), isDotesLevelComplete(..., level).
- What IS broken: the <div role="group" aria-label="Sub-pasos del nivel"> literal (line 71) is a generic static string — no level number interpolated.
- Action: KEEP the prop, FIX the aria-label to interpolate the level value.

Finding C (cli.ts IN-06 line numbers):
- ROADMAP cites cli.ts:263-273 but the actual symbols drifted. On 2026-04-24:
    cli.ts:311 — const classLabelsByRow = loadClassLabels(nwsyncReader, baseGameReader);      (unconditional; consumed only at :402 inside if (EMIT_MAGIC_CATALOGS))
    cli.ts:312 — const spellsColumnNames = loadSpellsColumnNames(nwsyncReader, baseGameReader); (unconditional; consumed only at :402 — same branch)
    cli.ts:321 — let spellIdsByRow = new Map<number, string>();                                (unconditional init; mutated at :407 spells branch, read at :424 domains branch — both already magic-gated)
- Task 4 below specifies the refactor by SYMBOL, not line number, so drift does not invalidate the task.

Dead-code evidence for prestige-gate-build.ts (ROADMAP Success Criterion #3):
- Grep 2026-04-24 shows computeHighestClassLevel + ARCANE_SPELLCASTER_IDS referenced ONLY inside the helper body itself (line 336 reads the set inside the now-unreachable helper). Phase 12.8-REVIEW.md IN-01 + 12.8-02-SUMMARY.md explicitly mark both as "2-line removal deferred to a future housekeeping pass".
- Safe to delete; Task 2 does so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight ConfirmDialog callsite audit — delete OR re-classify as false positive (ROADMAP SC #2)</name>
  <files>
    apps/planner/src/components/ui/confirm-dialog.tsx (conditional delete — Branch A only)
    apps/planner/src/styles/app.css (conditional — remove the /* ── ConfirmDialog ── */ block near line 1238 ONLY if the .tsx is deleted)
    .planning/phases/13-verification-orphan-sweep/13-02-SUMMARY.md (record the branch taken; create the file if missing, append otherwise)
  </files>
  <read_first>
    - apps/planner/src/features/summary/save-slot-dialog.tsx (confirm the current import + render — planning found 2 hits at lines 3 and 109; if still present, the primitive is LIVE)
    - tests/phase-08/save-slot-dialog.spec.tsx lines 60-170 (3 test cases at lines 81, 124, 150 exercise the overwrite ConfirmDialog flow)
    - apps/planner/src/components/ui/confirm-dialog.tsx (the candidate for deletion)
    - apps/planner/src/styles/app.css lines 1230-1280 (the /* ── ConfirmDialog ── */ style block — delete ONLY IF the .tsx is deleted)
  </read_first>
  <action>
    STEP 1 — Pre-flight grep (MANDATORY, do this FIRST):

        rg -n "ConfirmDialog|from.*confirm-dialog|@planner/components/ui/confirm-dialog" apps/planner/src packages tests

    Exclude the self-reference line in apps/planner/src/components/ui/confirm-dialog.tsx (grep will match the class declaration itself; that is not a caller).

    STEP 2 — Branch on grep result:

    Branch A (0 callers outside confirm-dialog.tsx itself) — proceed with deletion:
    1. git rm apps/planner/src/components/ui/confirm-dialog.tsx
    2. Delete the CSS block in apps/planner/src/styles/app.css that starts with the marker comment /* ── ConfirmDialog ─ (currently near line 1238); delete through the closing brace of the last .confirm-dialog selector in the block. Preserve all other CSS byte-identical.
    3. Append to 13-02-SUMMARY.md (create if missing): "ConfirmDialog callsite audit: Branch A executed — 0 callers confirmed, primitive + CSS block deleted. Grep output at execution time: <paste>."

    Branch B (1+ live callers — EXPECTED based on planner pre-flight: save-slot-dialog.tsx + 3 test cases) — DO NOT delete:
    1. Leave confirm-dialog.tsx and its CSS block BYTE-IDENTICAL.
    2. Append to 13-02-SUMMARY.md (create if missing): "ConfirmDialog callsite audit: Branch B executed — 1+ callers confirmed on master, ROADMAP SC #2 'removed' requirement reclassified as false-positive audit finding. Evidence: <paste grep output showing save-slot-dialog.tsx + test file hits>. The Phase 07.2 magic purge correctly removed magic-related ConfirmDialog usages; Phase 08 (SaveSlotDialog overwrite flow) independently re-introduced a legitimate caller that post-dates the 07.2 descope. Leaving in place; v1.0-MILESTONE-AUDIT.md tech_debt entry for 07.2 will be updated to strike this line in a follow-up housekeeping doc pass."
    3. No source-code edits for this task.

    STEP 3 — Regardless of branch:
    - Do NOT edit save-slot-dialog.tsx, the confirm-dialog spec, or any other file.
    - The 13-02-SUMMARY.md append is the sole artifact in Branch B; the .tsx + CSS deletions are the only artifacts in Branch A.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='apps/planner/src/components/ui/confirm-dialog.tsx'; const saveUses=fs.readFileSync('apps/planner/src/features/summary/save-slot-dialog.tsx','utf8').includes('ConfirmDialog'); const tsxExists=fs.existsSync(p); if (saveUses &amp;&amp; tsxExists) { console.log('Branch B OK: primitive preserved while live caller exists'); } else if (!saveUses &amp;&amp; !tsxExists) { console.log('Branch A OK: primitive deleted with zero callers'); } else { console.error('INVARIANT BROKEN: saveUses=', saveUses, ' tsxExists=', tsxExists); process.exit(1); }" &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Invariant — exactly one of these two states holds:
      * Branch A: `test ! -f apps/planner/src/components/ui/confirm-dialog.tsx` AND `grep -c 'ConfirmDialog' apps/planner/src/features/summary/save-slot-dialog.tsx` returns 0 AND `grep -c '── ConfirmDialog' apps/planner/src/styles/app.css` returns 0
      * Branch B: `test -f apps/planner/src/components/ui/confirm-dialog.tsx` AND `grep -c 'ConfirmDialog' apps/planner/src/features/summary/save-slot-dialog.tsx` returns 2 or more AND 13-02-SUMMARY.md contains the phrase "Branch B" or "false-positive"
    - `pnpm typecheck` exits 0 (baseline 4 pre-existing errors preserved; 0 new)
    - `pnpm test -- tests/phase-08/save-slot-dialog.spec.tsx` exits 0 (3 overwrite-ConfirmDialog cases stay green if Branch B; or the spec has already been migrated off ConfirmDialog and still green if Branch A)
    - `git diff origin/master -- apps/planner/src/features/persistence/ packages/rules-engine/ apps/planner/src/styles/tokens.css` is EMPTY (zero-diff gates hold)
    - `git diff origin/master -- apps/planner/src/data/compiled-classes.ts apps/planner/src/data/compiled-races.ts apps/planner/src/data/compiled-skills.ts apps/planner/src/data/compiled-feats.ts` is EMPTY (zero-diff on compiled-* data)
    - `git diff origin/master -- apps/planner/src/features/summary/save-slot-dialog.tsx tests/phase-08/save-slot-dialog.spec.tsx` is EMPTY (this task never edits these)
  </acceptance_criteria>
  <done>ConfirmDialog callsite audit run; decision branch (A or B) executed; evidence recorded in 13-02-SUMMARY.md; typecheck green; zero-diff gates hold.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead computeHighestClassLevel + ARCANE_SPELLCASTER_IDS from prestige-gate-build.ts (ROADMAP SC #3)</name>
  <files>apps/planner/src/features/level-progression/prestige-gate-build.ts</files>
  <read_first>
    - apps/planner/src/features/level-progression/prestige-gate-build.ts (lines 180-200 for the ARCANE_SPELLCASTER_IDS constant around line 188, and lines 330-365 for the computeHighestClassLevel helper around line 346)
    - .planning/phases/12.8-uat-04-23-residuals/12.8-REVIEW.md lines 79-83 (IN-01 documents the dead-code finding)
    - .planning/phases/12.8-uat-04-23-residuals/12.8-02-SUMMARY.md line 44 + lines 159-160 (explicitly defers the 2-line removal to a future housekeeping pass)
  </read_first>
  <action>
    Pre-flight grep (MANDATORY, prove helpers remain unreferenced):

        rg -n "computeHighestClassLevel|ARCANE_SPELLCASTER_IDS" apps packages tests --glob '!*.md' --glob '!prestige-gate-build.ts'

    Expected result: ZERO hits. If any hit surfaces outside prestige-gate-build.ts, STOP and re-scope — the helper is not actually dead.

    If pre-flight grep passes (0 hits):
    1. Open apps/planner/src/features/level-progression/prestige-gate-build.ts.
    2. Delete the full `const ARCANE_SPELLCASTER_IDS = new Set<CanonicalId>([...]);` declaration (currently starting at line 188). Delete through the closing `]);`.
    3. Delete the full `function computeHighestClassLevel(...) { ... }` declaration (currently starting at line 346). Delete through the closing `}` of the function body.
    4. If either deletion leaves an orphan import (CanonicalId may still be used by remaining code — verify before removing), leave imports BYTE-IDENTICAL. If TS strict surfaces an "unused import" error (TS6133 / TS6196), delete ONLY that specific unused import line. Do NOT prune imports speculatively.
    5. Preserve computeHighestSpellLevel + all other functions + PrestigeGateBuildState exports BYTE-IDENTICAL. This is a surgical delete.
    6. Run `pnpm typecheck` — must exit 0 with baseline 4 pre-existing errors preserved; 0 NEW errors.
    7. Run `pnpm test -- tests/phase-12.8 tests/phase-12.4` (nearest suites consuming prestige-gate) — must all pass as before.

    Commit when green. Suggested message: `refactor(13-02): delete dead computeHighestClassLevel + ARCANE_SPELLCASTER_IDS from prestige-gate-build.ts (12.8-REVIEW IN-01 closure)`.
  </action>
  <verify>
    <automated>grep -c 'computeHighestClassLevel\|ARCANE_SPELLCASTER_IDS' apps/planner/src/features/level-progression/prestige-gate-build.ts; test $? -eq 1 &amp;&amp; pnpm typecheck &amp;&amp; pnpm test -- tests/phase-12.8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'computeHighestClassLevel' apps/planner/src/features/level-progression/prestige-gate-build.ts` returns 0
    - `grep -c 'ARCANE_SPELLCASTER_IDS' apps/planner/src/features/level-progression/prestige-gate-build.ts` returns 0
    - `rg -l 'computeHighestClassLevel|ARCANE_SPELLCASTER_IDS' apps/ packages/ tests/ --glob '!*.md'` prints ZERO file paths
    - `grep -c 'computeHighestSpellLevel' apps/planner/src/features/level-progression/prestige-gate-build.ts` returns 1 or more (live helper preserved)
    - `pnpm typecheck` exits 0 (baseline 4 pre-existing errors preserved; 0 new)
    - `pnpm test -- tests/phase-12.8` exits 0 (prestige-gate-schema + prestige-gate-overrides specs still green)
    - `git diff origin/master -- packages/rules-engine/ apps/planner/src/data/compiled- apps/planner/src/styles/tokens.css apps/planner/src/features/persistence/` is EMPTY
  </acceptance_criteria>
  <done>Both symbols deleted from prestige-gate-build.ts; other exports preserved; typecheck + phase-12.8 tests green; zero-diff gates hold.</done>
</task>

<task type="auto">
  <name>Task 3: Level-aware aria-label on level-sub-steps.tsx (ROADMAP SC #4 — corrected)</name>
  <files>apps/planner/src/components/shell/level-sub-steps.tsx</files>
  <read_first>
    - apps/planner/src/components/shell/level-sub-steps.tsx (full file — 85 lines; planner already read it)
    - apps/planner/src/components/shell/creation-stepper.tsx line 179 (the sole call site; passes expandedLevel as the level prop — confirms level is a live, consumed integer)
  </read_first>
  <action>
    ROADMAP SC #4 wording mixes two ideas. Planner grep proved the level prop is USED internally (lines 40-54 pass it to three predicates). The ACTIONABLE delta is the generic aria-label at line 71.

    Single edit in apps/planner/src/components/shell/level-sub-steps.tsx line 71:

    Replace:
        aria-label={`Sub-pasos del nivel`}

    With:
        aria-label={`Sub-pasos del nivel ${level}`}

    That is the only edit. DO NOT:
    - Drop the level prop (it is consumed at lines 40-54 — removing would break three predicate calls).
    - Change the LevelSubStepsProps interface.
    - Touch any other file (unless Task 3 test-harness alignment under STEP 2 below surfaces a spec that asserts the old literal).
    - Modify the component's children or internal logic.

    STEP 2 — test-harness alignment (Rule 3):
    - Run `rg "Sub-pasos del nivel" tests/` before claiming done. If any spec asserts the old exact literal (e.g. `getByLabelText('Sub-pasos del nivel')`), update it to the new interpolated form matching the live expandedLevel value in the fixture (e.g. `getByLabelText('Sub-pasos del nivel 1')`). This IS in scope — it is direct test-harness drift caused by this edit, not scope creep.

    Run `pnpm typecheck` — must stay green.
    Run `pnpm test` — full suite, tolerating the baseline 6 pre-existing failures documented in 12.8-VERIFICATION.md line 115; 0 NEW failures.

    Commit: `fix(a11y)(13-02): level-sub-steps aria-label interpolates active level (ROADMAP Phase-13 SC #4 corrected — prop is used; only the aria-label was generic)`.
  </action>
  <verify>
    <automated>grep -c 'Sub-pasos del nivel \${level}' apps/planner/src/components/shell/level-sub-steps.tsx &amp;&amp; grep -c 'level }: LevelSubStepsProps' apps/planner/src/components/shell/level-sub-steps.tsx &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sub-pasos del nivel \${level}' apps/planner/src/components/shell/level-sub-steps.tsx` returns 1 (the interpolated form present)
    - `grep -c 'aria-label={`Sub-pasos del nivel`}' apps/planner/src/components/shell/level-sub-steps.tsx` returns 0 (the old static literal is gone)
    - `grep -c 'level }: LevelSubStepsProps' apps/planner/src/components/shell/level-sub-steps.tsx` returns 1 (prop preserved in destructured signature)
    - `grep -c 'isClaseLevelComplete(progressionState, level)' apps/planner/src/components/shell/level-sub-steps.tsx` returns 1 (predicate call preserved — proves prop still consumed)
    - `pnpm typecheck` exits 0
    - `pnpm test` — 0 NEW failures beyond the 6 pre-existing documented in 12.8-VERIFICATION.md
    - `git diff origin/master -- apps/planner/src/features/persistence/ packages/rules-engine/ apps/planner/src/data/compiled- apps/planner/src/styles/tokens.css` is EMPTY
  </acceptance_criteria>
  <done>aria-label interpolates level; prop preserved; typecheck green; no new test failures; zero-diff gates hold.</done>
</task>

<task type="auto">
  <name>Task 4: Gate cli.ts wasted 2DA parse behind EMIT_MAGIC_CATALOGS (ROADMAP SC #5 / IN-06)</name>
  <files>packages/data-extractor/src/cli.ts</files>
  <read_first>
    - packages/data-extractor/src/cli.ts lines 260-430 (full main() body — see the EMIT_MAGIC_CATALOGS gate at 270, the unconditional loadClassLabels + loadSpellsColumnNames at 311-312, the spellIdsByRow init at 321, the spells branch at 388-416 where classLabelsByRow is consumed at 402, and the domains branch at 418-434 where spellIdsByRow is read at 424)
    - .planning/phases/07.2-magic-ui-descope/07.2-REVIEW.md section IN-06 line 234 (original flag of the waste)
    - .planning/phases/07.2-magic-ui-descope/07.2-VERIFICATION.md line 137 (reproduces the finding at 07.2 close)
    - .planning/phases/05.1-data-extractor-pipeline/05.1-VERIFICATION.md line 132 (re-affirms as Info / pre-existing)
  </read_first>
  <action>
    Refactor the magic-gated section of main() so the wasted 2DA parse is skipped when EMIT_MAGIC_CATALOGS is off (default runs). Work by SYMBOL, not line number (numbers may drift):

    STEP 1 — Confirm the symbol layout:

        rg -n "loadClassLabels|loadSpellsColumnNames|EMIT_MAGIC_CATALOGS|spellIdsByRow" packages/data-extractor/src/cli.ts

    Expect:
    - ONE declaration of `const EMIT_MAGIC_CATALOGS = ...` at top of main() (around line 270).
    - ONE `const classLabelsByRow = loadClassLabels(...)` at around line 311.
    - ONE `const spellsColumnNames = loadSpellsColumnNames(...)` at around line 312.
    - ONE `let spellIdsByRow = new Map<number, string>();` at around line 321.
    - Consumers: classLabelsByRow ONLY inside `if (EMIT_MAGIC_CATALOGS)` at line 402 (spells branch). spellIdsByRow mutated at line 407 and read at line 424 (both inside magic-gated branches).

    STEP 2 — Apply the refactor (preferred shape, surgical):

    Move the two unconditional 2DA parse calls inside the existing spells branch, preserving the outer `let spellIdsByRow = new Map<number, string>();` declaration because the domains branch consumes it:

    Before (paraphrased — exact text at execute time may differ by whitespace only):

        // Load auxiliary data for cross-assembler linking
        const classLabelsByRow = loadClassLabels(nwsyncReader, baseGameReader);
        const spellsColumnNames = loadSpellsColumnNames(nwsyncReader, baseGameReader);

        // -------------------------------------------------------------------------
        // d. Run assemblers in dependency order
        // -------------------------------------------------------------------------
        ...
        let featIdsByRow = new Map<number, string>();
        let spellIdsByRow = new Map<number, string>();
        ...
        // 5. Spells (needs classRows from classes) — gated behind EMIT_MAGIC_CATALOGS
        if (EMIT_MAGIC_CATALOGS) {
          try {
            ...
            const spellClassRows = classCatalog
              ? buildSpellClassRows(classCatalog.classes, classLabelsByRow, spellsColumnNames)
              : ...;
            ...

    After:

        // Phase 13-02 (IN-06): loadClassLabels + loadSpellsColumnNames moved inside
        // the spells branch — they are consumed only by buildSpellClassRows. Default
        // runs (EMIT_MAGIC_CATALOGS=0) now skip a 2DA parse round-trip each.

        // -------------------------------------------------------------------------
        // d. Run assemblers in dependency order
        // -------------------------------------------------------------------------
        ...
        let featIdsByRow = new Map<number, string>();
        let spellIdsByRow = new Map<number, string>();
        ...
        // 5. Spells (needs classRows from classes) — gated behind EMIT_MAGIC_CATALOGS
        if (EMIT_MAGIC_CATALOGS) {
          try {
            const classLabelsByRow = loadClassLabels(nwsyncReader, baseGameReader);
            const spellsColumnNames = loadSpellsColumnNames(nwsyncReader, baseGameReader);
            ...
            const spellClassRows = classCatalog
              ? buildSpellClassRows(classCatalog.classes, classLabelsByRow, spellsColumnNames)
              : ...;
            ...

    STEP 3 — DO NOT:
    - Change the signature of loadClassLabels / loadSpellsColumnNames / buildSpellClassRows.
    - Remove the outer `let spellIdsByRow = new Map<number, string>();` declaration — the domains branch consumes it.
    - Remove the `// Load auxiliary data for cross-assembler linking` comment if its siblings still apply; if the comment becomes vestigial (both its referents moved inside the branch), delete it too. Judgement call at edit time.
    - Alter any other catalog (classes, races, skills, feats, deities) — those are NOT magic-gated.

    STEP 4 — Verify:
    - `pnpm typecheck` — must exit 0 (baseline 4 pre-existing errors preserved).
    - `pnpm test -- tests/phase-12 tests/phase-05.1 tests/phase-07.2` or the nearest suite covering the extractor emitter plan — must all pass. Specifically tests/phase-12/extract-counter-magic-off.spec.ts: note from STATE.md it has a pre-existing better-sqlite3 module-not-found baseline failure; tolerate that. Run buildEmitterPlan unit tests if present — they should not care about this refactor.
    - Attempt a dry extractor run if environment permits: `EMIT_MAGIC_CATALOGS=0 node --experimental-vm-modules packages/data-extractor/src/cli.ts --dry-run` — if no --dry-run flag exists, skip this step; the unit tests are sufficient.

    Commit: `refactor(13-02): gate cli.ts classLabelsByRow + spellsColumnNames behind EMIT_MAGIC_CATALOGS (IN-06 closure; default runs skip wasted 2DA parse)`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('packages/data-extractor/src/cli.ts','utf8'); const magicBranchIdx=s.indexOf('if (EMIT_MAGIC_CATALOGS)'); const classLabelsIdx=s.indexOf('const classLabelsByRow = loadClassLabels'); const spellsColIdx=s.indexOf('const spellsColumnNames = loadSpellsColumnNames'); if (classLabelsIdx < magicBranchIdx || spellsColIdx < magicBranchIdx) { console.error('FAIL: 2DA parse still outside EMIT_MAGIC_CATALOGS gate — classLabelsIdx=', classLabelsIdx, ' spellsColIdx=', spellsColIdx, ' magicBranchIdx=', magicBranchIdx); process.exit(1); } console.log('OK: both 2DA parse calls are now INSIDE the EMIT_MAGIC_CATALOGS branch');" &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const classLabelsByRow = loadClassLabels' packages/data-extractor/src/cli.ts` returns 1 (call preserved — just relocated)
    - `grep -c 'const spellsColumnNames = loadSpellsColumnNames' packages/data-extractor/src/cli.ts` returns 1 (call preserved — just relocated)
    - The character index of `const classLabelsByRow = loadClassLabels` in cli.ts is GREATER than the character index of the first `if (EMIT_MAGIC_CATALOGS)` occurrence inside main() (proves the declaration moved inside the guard)
    - The character index of `const spellsColumnNames = loadSpellsColumnNames` is likewise GREATER than the `if (EMIT_MAGIC_CATALOGS)` index
    - `pnpm typecheck` exits 0
    - `pnpm test -- tests/phase-12` exits 0 OR matches the documented baseline (only the pre-existing better-sqlite3 module-not-found failure in tests/phase-12/extract-counter-magic-off.spec.ts; 0 NEW failures)
    - `git diff origin/master -- apps/planner/ packages/rules-engine/ apps/planner/src/data/compiled- apps/planner/src/styles/tokens.css` is EMPTY (zero-diff gates hold)
  </acceptance_criteria>
  <done>2DA parse calls gated behind EMIT_MAGIC_CATALOGS; extractor default runs skip the waste; typecheck + extractor tests green; zero-diff gates hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal refactor scope | No new trust boundaries in this phase. No network endpoints, user input paths, data persistence formats, URL parsers, import/export formats, or auth surfaces are touched. All edits are within a single compiled bundle consumed by the same browser/runtime as before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | ConfirmDialog deletion breaks live overwrite flow | mitigate | Task 1 executes a pre-flight callsite grep and refuses to delete under Branch B (live callers present). Runtime evidence: tests/phase-08/save-slot-dialog.spec.tsx exercises the flow and runs in `pnpm test` — any regression fails CI before merge. |
| T-13-02-02 | Tampering | computeHighestClassLevel deletion breaks a hidden caller | mitigate | Task 2 pre-flight grep rejects the deletion if any caller exists outside the helper body itself. `pnpm test -- tests/phase-12.8` re-confirms prestige-gate-schema + prestige-gate-overrides green post-delete. |
| T-13-02-03 | Information disclosure | aria-label exposes data-sensitive level context | accept | `Sub-pasos del nivel 1..20` is not sensitive; it is the same integer already rendered visibly in the progression row header and announced by multiple neighboring elements. |
| T-13-02-04 | Denial of service | Extractor refactor breaks pnpm extract default runs | mitigate | Task 4 requires `pnpm typecheck` green and preserves the existing `if (EMIT_MAGIC_CATALOGS)` guard shape (only moves two local consts inside it). buildEmitterPlan logic + all 5 non-magic emitters (classes, races, skills, feats, deities) left byte-identical. |
| T-13-02-05 | Elevation of privilege | Executor silently expands scope by editing files outside `files_modified` | mitigate | Zero-diff gates encoded in every task's acceptance_criteria as explicit `git diff origin/master -- <path>` emptiness checks on persistence/, rules-engine/, compiled-*, tokens.css. Executor cannot merge with drift in those trees. |
| T-13-02-06 | Repudiation | ConfirmDialog re-classification lacks audit trail | mitigate | Task 1 Branch B mandates an append to 13-02-SUMMARY.md recording the grep output and the reasoning — v1.0-MILESTONE-AUDIT.md tech_debt entry remains consistent with on-disk reality after this note. |
</threat_model>

<verification>
After all four tasks complete:
1. `pnpm typecheck` → exits 0 (baseline 4 pre-existing errors preserved; zero NEW errors)
2. `pnpm test` → 0 NEW failures beyond the 6 pre-existing documented in 12.8-VERIFICATION.md line 115
3. `git diff origin/master -- apps/planner/src/features/persistence/` → empty (zero-diff gate)
4. `git diff origin/master -- packages/rules-engine/` → empty (zero-diff gate)
5. `git diff origin/master -- apps/planner/src/data/compiled-classes.ts apps/planner/src/data/compiled-races.ts apps/planner/src/data/compiled-skills.ts apps/planner/src/data/compiled-feats.ts apps/planner/src/data/compiled-deities.ts` → empty (zero-diff gate on compiled-* data)
6. `git diff origin/master -- apps/planner/src/styles/tokens.css` → empty (zero-diff gate on design tokens)
7. 13-02-SUMMARY.md exists and records: the ConfirmDialog audit branch taken (A or B), the prestige-gate deletion, the aria-label fix, and the cli.ts gate refactor. Include the actual grep outputs from each pre-flight step.
</verification>

<success_criteria>
- Task 1 decision (Branch A delete or Branch B preserve) executed consistently with pre-flight grep evidence; 13-02-SUMMARY.md records the choice.
- computeHighestClassLevel + ARCANE_SPELLCASTER_IDS removed from prestige-gate-build.ts with surgical delete; zero other changes to the file.
- level-sub-steps.tsx aria-label interpolates the active level prop; the prop itself is preserved because it is consumed by three predicate calls.
- cli.ts loadClassLabels + loadSpellsColumnNames calls relocated inside the existing if (EMIT_MAGIC_CATALOGS) branch; default extractor runs skip the wasted 2DA parse.
- All four zero-diff gates hold (persistence, rules-engine, compiled-*, tokens.css).
- `pnpm typecheck` green; `pnpm test` 0 NEW failures beyond baseline.
- Closes ROADMAP Phase 13 Success Criteria #2..#6. SC #2 closed via evidence-based branch decision; SC #3..#5 closed via edits; SC #6 closed via gate preservation.

This plan depends_on 13-01 only for ordering hygiene (author VERIFICATION.md files first so the re-audit state matches reality as edits land). Since the 13-01 work is pure documentation with no source overlap, 13-02 could in principle run in parallel; staging it in Wave 2 avoids any accidental race where a partial 13-02 state is described in the 13-01 retrospective.
</success_criteria>

<output>
After completion, finalize `.planning/phases/13-verification-orphan-sweep/13-02-SUMMARY.md` recording:
- Task 1 branch decision (A or B) with the grep output captured at execution time.
- Task 2 pre-delete grep result (expect zero hits) and the two deleted constructs' byte counts.
- Task 3 aria-label diff showing old literal → new interpolated form plus any test-harness alignments.
- Task 4 before/after character-index proof that both 2DA-parse calls moved inside the EMIT_MAGIC_CATALOGS guard.
- Full `pnpm typecheck` output (expect baseline 4 pre-existing errors; zero new).
- Full `pnpm test` summary (expect 6 pre-existing failures; zero new).
- `git diff --stat origin/master` filtered to show only the authorized files touched.
</output>
