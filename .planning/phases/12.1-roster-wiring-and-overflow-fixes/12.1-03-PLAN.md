---
phase: 12.1-roster-wiring-and-overflow-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/styles/app.css
autonomous: false
requirements: [FLOW-02]
tags: [css, overflow, selection-screen, layout-fix]

must_haves:
  truths:
    - "`.selection-screen__content` allows vertical scrolling when its grid content exceeds the viewport, so the full L1 class picker + race picker are reachable once the expanded roster ships from plans 12.1-01 and 12.1-02."
    - "No horizontal scrollbar appears (`overflow-x: hidden`) — the two-column minmax(0,1fr) grid still contains itself horizontally."
    - "No other CSS selector is touched — D-04 lock: only `.selection-screen__content` changes."
  artifacts:
    - path: "apps/planner/src/styles/app.css"
      provides: "`.selection-screen__content` declaration (app.css:1149) flipped from `overflow: hidden` to `overflow-y: auto; overflow-x: hidden;`"
      contains: "overflow-y: auto"
  key_links:
    - from: "apps/planner/src/styles/app.css:1149"
      to: "apps/planner/src/components (whichever component renders .selection-screen__content — unchanged)"
      via: "CSS class selector; component JSX not modified"
      pattern: "overflow-y:\\s*auto"
---

<objective>
Restore vertical scrollability on `.selection-screen__content` so the expanded 46-race + 41-class rosters (shipped by plans 12.1-01 and 12.1-02) do not clip silently when they exceed viewport height. One-line CSS patch per D-04.

Purpose: Close Phase 11 UAT observation — users could not see entries beyond the visible grid when rosters overflowed the panel; the clip was silent (no scroll affordance).
Output: A single CSS declaration change in `apps/planner/src/styles/app.css` at the `.selection-screen__content` rule (line 1149 today), replacing `overflow: hidden` with `overflow-y: auto; overflow-x: hidden;`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12.1-roster-wiring-and-overflow-fixes/12.1-CONTEXT.md

# Source file the executor will read
@apps/planner/src/styles/app.css

<interfaces>
<!-- Current declaration at app.css:1144–1150 (read once, edit once): -->
<!-- .selection-screen__content { -->
<!--   display: grid; -->
<!--   flex: 1; -->
<!--   grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); -->
<!--   min-height: 0; -->
<!--   overflow: hidden;   <-- THIS LINE CHANGES -->
<!-- } -->
<!-- -->
<!-- Target declaration after the fix: -->
<!-- .selection-screen__content { -->
<!--   display: grid; -->
<!--   flex: 1; -->
<!--   grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); -->
<!--   min-height: 0; -->
<!--   overflow-y: auto; -->
<!--   overflow-x: hidden; -->
<!-- } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace `.selection-screen__content` overflow declaration (app.css:1149)</name>
  <files>apps/planner/src/styles/app.css</files>
  <action>
    1. Read `apps/planner/src/styles/app.css` lines 1140–1155 to confirm the exact current shape of the `.selection-screen__content` rule. Line numbers may have drifted since 2026-04-18 — verify the rule still opens with `display: grid;` and declares `overflow: hidden;`.
    2. Use the Edit tool to replace the single line `  overflow: hidden;` inside the `.selection-screen__content { ... }` block with two lines:
       ```
         overflow-y: auto;
         overflow-x: hidden;
       ```
    3. Per D-04 — do NOT modify any other selector. If a search for `overflow: hidden` returns other matches in the file (expected: `planner-layout__stepper`, `planner-layout__center`, `.character-sheet`, `.character-sheet__stat-grid`, `.character-sheet__derived dt`), leave them untouched. Those are intentional grid/flex/ellipsis containers per CONTEXT.md `<decisions>` D-04.
    4. Commit: `fix(12.1-03): restore overflow-y on SelectionScreen`.
  </action>
  <verify>
    <automated>grep -A7 "\.selection-screen__content {" apps/planner/src/styles/app.css | grep -q "overflow-y:\s*auto" &amp;&amp; grep -A7 "\.selection-screen__content {" apps/planner/src/styles/app.css | grep -q "overflow-x:\s*hidden" &amp;&amp; ! grep -A7 "\.selection-screen__content {" apps/planner/src/styles/app.css | grep -qE "^\s*overflow:\s*hidden;"</automated>
  </verify>
  <done>
    - `grep -A1 "\.selection-screen__content" apps/planner/src/styles/app.css` shows the rule contains `overflow-y: auto;` and `overflow-x: hidden;`.
    - The `.selection-screen__content` block no longer contains `overflow: hidden;`.
    - Count of `overflow: hidden` occurrences in `app.css` decreased by exactly 1 vs. pre-edit baseline (other untouched selectors retain theirs).
    - `pnpm typecheck` clean (CSS change; no TS impact expected).
    - `pnpm build` (Vite) succeeds — CSS still parses.
    - Single atomic commit: `fix(12.1-03): restore overflow-y on SelectionScreen`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — SelectionScreen scrolls under expanded rosters</name>
  <what-built>
    Plan 12.1-03 patched `.selection-screen__content` from `overflow: hidden` to `overflow-y: auto; overflow-x: hidden;`. This is paired (conceptually, not mechanically) with plans 12.1-01 + 12.1-02 which expand the rosters that will actually cause overflow. The CSS fix alone is correct-by-construction, but visual confirmation closes the UAT observation.
  </what-built>
  <how-to-verify>
    Per CONTEXT.md `<specifics>` final bullet ("Manual UAT after execution"):
    1. Ensure plans 12.1-01 and 12.1-02 have landed first (full roster visible in pickers).
    2. Run `pnpm --filter @planner dev` and open the dev server (default http://localhost:5173).
    3. Navigate to the Construcción route (L1 class picker path) — the SelectionScreen should render the two-column grid.
    4. Verify a vertical scrollbar appears on `.selection-screen__content` when the class list exceeds the viewport; scroll to the bottom and confirm the final class is reachable.
    5. Repeat for the race picker screen — confirm the 46-race parent list scrolls to the bottom.
    6. Confirm NO horizontal scrollbar appears (the `overflow-x: hidden` keeps the minmax(0,1fr) grid contained).
    7. Confirm the neighboring panels (`planner-layout__stepper`, `planner-layout__center`, `.character-sheet`) still render correctly — no double-scroll, no ellipsis regression.
    This matches the UAT protocol used in Phase 11 plan 11-01 (scripted MCP Chrome walk-through is acceptable substitute at user's discretion).
  </how-to-verify>
  <resume-signal>Type "approved" after visual confirmation, or describe any clipping/scrolling regression you observed.</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "\.selection-screen__content" apps/planner/src/styles/app.css` resolves to the rule block.
- `grep -A7 "\.selection-screen__content {" apps/planner/src/styles/app.css` contains both `overflow-y: auto;` and `overflow-x: hidden;`, and does NOT contain the bare `overflow: hidden;`.
- `pnpm build --filter @planner` (or the repo's equivalent Vite build) succeeds.
- Diff vs. HEAD on `apps/planner/src/styles/app.css` shows exactly one hunk: the `.selection-screen__content` rule's overflow declaration. No other selector changed.
</verification>

<success_criteria>
1. `.selection-screen__content` renders `overflow-y: auto; overflow-x: hidden;` (single-line replacement per D-04).
2. Only one CSS selector modified — no collateral changes to `planner-layout__stepper`, `planner-layout__center`, `.character-sheet`, `.character-sheet__stat-grid`, `.character-sheet__derived dt`.
3. Manual UAT confirms the expanded roster scrolls vertically inside the SelectionScreen after plans 12.1-01 + 12.1-02 land.
4. Single atomic commit: `fix(12.1-03): restore overflow-y on SelectionScreen`.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-roster-wiring-and-overflow-fixes/12.1-03-SUMMARY.md` following the Phase 12 SUMMARY pattern. Note the manual UAT outcome (pass/issues) in the Accomplishments section.
</output>
