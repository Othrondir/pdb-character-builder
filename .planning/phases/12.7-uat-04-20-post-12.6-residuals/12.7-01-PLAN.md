---
phase: 12.7-uat-04-20-post-12.6-residuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/components/shell/creation-stepper.tsx
  - apps/planner/src/features/level-progression/level-progression-row.tsx
  - vitest.config.ts
  - tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx
autonomous: true
requirements: [PROG-05]
tags: [level-progression, shell, stepper, advance-bar, mount-hoist, ui]
must_haves:
  truths:
    - "User sees LevelEditorActionBar on the Progresión-Clase sub-step for the active level"
    - "User sees LevelEditorActionBar on the Progresión-Habilidades sub-step for the active level (R1 fix)"
    - "User sees LevelEditorActionBar on the Progresión-Dotes sub-step for the active level (R1 fix)"
    - "Transitioning Clase → Habilidades → Dotes does NOT remount the action bar (stable React key)"
    - "Action bar returns null at L20 (PROGRESSION_LEVEL_CAP terminal invariant preserved)"
    - "[data-testid=\"advance-to-level-{N+1}\"] testid contract from 12.4-09 survives verbatim"
  artifacts:
    - path: "apps/planner/src/components/shell/creation-stepper.tsx"
      provides: "New mount site for <LevelEditorActionBar key={expandedLevel} />, gated on expandedLevel !== null, sibling of .creation-stepper__bottom region"
      contains: "import { LevelEditorActionBar }"
    - path: "apps/planner/src/features/level-progression/level-progression-row.tsx"
      provides: "Old mount at line 243 DELETED + unused import at line 36 REMOVED"
      contains: "// line 36 import removed; line 243 <LevelEditorActionBar /> removed"
    - path: "vitest.config.ts"
      provides: "environmentMatchGlobs entry for tests/phase-12.7/**/*.spec.tsx → jsdom"
      contains: "tests/phase-12.7"
    - path: "tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx"
      provides: "5-suite RTL jsdom spec proving bar presence on Clase/Habilidades/Dotes + L20 null + stable-key remount proof"
      min_lines: 120
  key_links:
    - from: "apps/planner/src/components/shell/creation-stepper.tsx"
      to: "apps/planner/src/features/level-progression/level-editor-action-bar.tsx"
      via: "import + JSX mount"
      pattern: "import \\{ LevelEditorActionBar \\} from '@planner/features/level-progression/level-editor-action-bar'"
    - from: "apps/planner/src/components/shell/creation-stepper.tsx"
      to: "usePlannerShellStore"
      via: "existing expandedLevel subscription (line 102 pre-existing, no new subscription)"
      pattern: "usePlannerShellStore\\(\\(state\\) => state\\.expandedLevel\\)"
---

<objective>
Close F7 (BLOCKER): user cannot advance L1 → L2 because `LevelEditorActionBar` only mounts inside `<LevelProgressionRow>`'s `{isActive && ...}` expanded slot. When the user navigates to Habilidades or Dotes via the creation stepper, BuildProgressionBoard unmounts and the action bar disappears.

Hoist `<LevelEditorActionBar>` to a stepper-global mount in `creation-stepper.tsx` (per CONTEXT D-01) so it persists across Clase / Habilidades / Dotes sub-steps for the active level. Delete the old mount in `level-progression-row.tsx` (per D-03) to prevent double-render. Use stable React `key={expandedLevel}` (per D-02) so sub-step transitions do NOT remount the bar.

Purpose: Restore the user's ability to reach L2 at every sub-step — the core UX blocker of Phase 12.7.
Output: Bar visible on 3 sub-steps, testid contract preserved, no remount on sub-step change, null at L20.
</objective>

<execution_context>
@C:\Users\pzhly\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pzhly\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-SPEC.md
@.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-CONTEXT.md
@.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-PATTERNS.md
@.planning/UAT-FINDINGS-2026-04-20-post-12.6.md
@apps/planner/src/components/shell/creation-stepper.tsx
@apps/planner/src/features/level-progression/level-progression-row.tsx
@apps/planner/src/features/level-progression/level-editor-action-bar.tsx
@vitest.config.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

From apps/planner/src/features/level-progression/level-editor-action-bar.tsx (UNCHANGED):
```typescript
export function LevelEditorActionBar(): JSX.Element | null;
// Subscribes to: useLevelProgressionStore, usePlannerShellStore, useFeatStore, useSkillStore, useCharacterFoundationStore
// Returns null when `level >= PROGRESSION_LEVEL_CAP` (currently 20 per UAT-2026-04-20 P6)
// Renders <footer data-testid="level-editor-action-bar"> with <NwnButton data-testid={`advance-to-level-${nextLevel}`}>
// Click dispatch order (ATOMIC — DO NOT change): setActiveLevelSubStep('class') → setActiveLevel(next) → setExpandedLevel(next)
```

From apps/planner/src/components/shell/creation-stepper.tsx:102 (pre-existing subscription — REUSE):
```typescript
const expandedLevel = usePlannerShellStore((state) => state.expandedLevel);
```

From apps/planner/src/features/level-progression/level-progression-row.tsx:36 + line 243 (DELETE both):
```typescript
// line 36 — import to delete
import { LevelEditorActionBar } from './level-editor-action-bar';
// line 243 — JSX mount to delete (inside {isActive && (<div>...</div>)})
<LevelEditorActionBar />
```

From vitest.config.ts:13-18 (extend):
```typescript
environmentMatchGlobs: [
  ['tests/phase-02/**/*.spec.{ts,tsx}', 'jsdom'],
  ['tests/phase-08/**/*.spec.tsx', 'jsdom'],
  ['tests/phase-12.4/**/*.spec.tsx', 'jsdom'],
  ['tests/phase-12.6/**/*.spec.tsx', 'jsdom'],
  // APPEND ONE ROW
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write stepper-mount regression spec + wire vitest phase-12.7 jsdom glob</name>
  <files>tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx, vitest.config.ts</files>
  <read_first>
    - apps/planner/src/components/shell/creation-stepper.tsx (FULL)
    - apps/planner/src/features/level-progression/level-editor-action-bar.tsx (FULL)
    - tests/phase-12.6/level-progression-scan.spec.tsx (full — mirror setup helpers, afterEach cleanup + zustand reset)
    - vitest.config.ts (FULL)
    - .planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-PATTERNS.md § "R1 — tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx"
    - apps/planner/src/features/character-foundation/store.ts (setRace shape + reset pattern)
    - apps/planner/src/features/level-progression/store.ts (setLevelClassId, setActiveLevel signature)
    - apps/planner/src/state/planner-shell.ts (setState shape — activeLevelSubStep, expandedLevel fields)
  </read_first>
  <behavior>
    - Suite A1: With fixture Elfo + L1 Guerrero + activeLevelSubStep='class' + expandedLevel=1, render <CreationStepper />; container.querySelector('[data-testid="level-editor-action-bar"]') !== null.
    - Suite A2: Same fixture, activeLevelSubStep='skills'; bar MUST be present (currently FAILS at RED — regression baseline).
    - Suite A3: Same fixture, activeLevelSubStep='feats'; bar MUST be present (currently FAILS at RED).
    - Suite A4: expandedLevel=20 + activeLevel=20; container.querySelector('[data-testid="level-editor-action-bar"]') === null (terminal-level invariant).
    - Suite A5 (stable key): Initial render at sub-step 'class' captures bar node; setState sub-step → 'skills'; rerender; container.querySelector('[data-testid="level-editor-action-bar"]') === same node reference (key stable = expandedLevel, NOT activeLevelSubStep).
    - Suite A6 (testid contract preservation): Bar contains <button data-testid="advance-to-level-2"> when L1 fixture enables progression.
  </behavior>
  <action>
    1. Edit `vitest.config.ts` to append one line inside `environmentMatchGlobs` array (AFTER line 17, BEFORE line 18):
       `['tests/phase-12.7/**/*.spec.tsx', 'jsdom'],`
       Final array count = 5 entries. Indent = 6 spaces (match existing).

    2. Create `tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` with header `// @vitest-environment jsdom` as first line.

    3. Imports (mirror `tests/phase-12.6/level-progression-scan.spec.tsx` lines 19-34 verbatim where applicable):
       ```tsx
       import { afterEach, beforeEach, describe, expect, it } from 'vitest';
       import { cleanup, render } from '@testing-library/react';
       import { CreationStepper } from '@planner/components/shell/creation-stepper';
       import { useCharacterFoundationStore } from '@planner/features/character-foundation/store';
       import { useLevelProgressionStore } from '@planner/features/level-progression/store';
       import { useFeatStore } from '@planner/features/feats/store';
       import { useSkillStore } from '@planner/features/skills/store';
       import { usePlannerShellStore } from '@planner/state/planner-shell';
       import type { CanonicalId } from '@rules-engine/contracts/canonical-id';
       import type { ProgressionLevel } from '@planner/features/level-progression/progression-fixture';
       ```

    4. Add `setupElfoGuerreroL1()` helper (race: 'race:elf', level1: 'class:fighter', expandedLevel: 1, activeLevelSubStep: 'class'). Grep the planner-shell store for the exact default-state field names before writing (usePlannerShellStore.setState requires full state or partial spread).

    5. Add `afterEach(() => { cleanup(); /* reset all 5 stores */ })` — mirror `tests/phase-12.6/level-progression-scan.spec.tsx` cleanup block.

    6. Write 6 suites matching `<behavior>` above. Each suite calls `setupElfoGuerreroL1()`, mutates the one field under test, renders `<CreationStepper />`, queries via `container.querySelector`.

    7. Suite A5 stable-key proof: after initial render, capture `const before = container.querySelector('[data-testid="level-editor-action-bar"]')`. Update planner-shell to `activeLevelSubStep: 'skills'` via `usePlannerShellStore.setState((s) => ({ ...s, activeLevelSubStep: 'skills' }))`. Call `rerender(<CreationStepper />)`. `const after = container.querySelector('[data-testid="level-editor-action-bar"]')`. Assert `expect(after).toBe(before)` (literal same DOM node → React preserved via stable key).

    8. Run `pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` and CONFIRM Suite A1 + A4 PASS, Suites A2 + A3 + A5 + A6 FAIL (RED baseline: current master has bar only on 'class' sub-step and A6 needs render state making advance possible — expected).

    9. Atomic commit: `test(12.7-01): RED — advance-bar stepper-mount regression spec + phase-12.7 jsdom glob`
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx 2>&amp;1 | grep -E "Suite A[1-6]|FAIL|PASS|\d+ (passed|failed)"</automated>
    Expected: A1 + A4 pass; A2 + A3 + A5 fail with baseline assertion mismatch (no assertion errors like "CreationStepper is not a function"; the test MUST reach assertions).
  </verify>
  <acceptance_criteria>
    - `grep -c "tests/phase-12.7/\*\*/\*.spec.tsx" vitest.config.ts` returns 1 (exactly one new glob entry)
    - `grep -c "^// @vitest-environment jsdom" tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` returns 1
    - `grep -c "data-testid=\"level-editor-action-bar\"" tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` returns ≥ 5 (one per suite A1-A5)
    - `grep -c "expect(after).toBe(before)" tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` returns ≥ 1 (stable-key proof)
    - `pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` exits with failing suites (non-zero exit) BUT imports resolve (no TS errors, no ReferenceErrors)
    - Git commit subject contains `RED` and `12.7-01`
  </acceptance_criteria>
  <done>
    RED baseline committed atomically; failing assertions reproduce the F7 regression (no bar on Habilidades/Dotes) and lock the stable-key contract. Vitest config extended with phase-12.7 jsdom glob. No production-code changes in this commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Hoist LevelEditorActionBar to creation-stepper + delete old mount</name>
  <files>apps/planner/src/components/shell/creation-stepper.tsx, apps/planner/src/features/level-progression/level-progression-row.tsx</files>
  <read_first>
    - apps/planner/src/components/shell/creation-stepper.tsx (FULL — lines 99-146 is render body)
    - apps/planner/src/features/level-progression/level-progression-row.tsx (lines 1-260 — locate import at line 36 + JSX at line 243)
    - .planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-PATTERNS.md § "R1 — creation-stepper.tsx" and § "R1 — level-progression-row.tsx"
    - .planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-CONTEXT.md § "Mount Site" (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    1. **Add to `creation-stepper.tsx`** (per D-01, D-02):
       - Add import alongside line 14 (after `import { LevelSubSteps }`):
         `import { LevelEditorActionBar } from '@planner/features/level-progression/level-editor-action-bar';`
       - Inside `CreationStepper()` JSX, AFTER the `</section>` closing line 134 (end of `creation-stepper__progression` section) and BEFORE `<div className="creation-stepper__bottom">` line 136, insert:
         ```tsx
         {expandedLevel !== null ? (
           <LevelEditorActionBar key={expandedLevel} />
         ) : null}
         ```
       - D-02 invariant: `key={expandedLevel}` — NOT `key={activeLevelSubStep}`. This is the stable-key contract that keeps the bar from remounting on sub-step transition.
       - No new `usePlannerShellStore` subscription — `expandedLevel` is already subscribed on line 102.

    2. **Delete from `level-progression-row.tsx`** (per D-03):
       - Delete line 36 verbatim: `import { LevelEditorActionBar } from './level-editor-action-bar';`
       - Delete the `<LevelEditorActionBar />` JSX (currently at line 243, inside `{isActive && (<div>...)}` block — verify exact line via `grep -n "<LevelEditorActionBar" apps/planner/src/features/level-progression/level-progression-row.tsx` first since other edits may have shifted the number).

    3. **Update doc block** in `level-progression-row.tsx` lines 1-26: change "expanded slot hosts … AbilityIncreaseControl + LevelEditorActionBar" → "expanded slot hosts … AbilityIncreaseControl". Delete the sentence reading "non-active rows render header-only — they never mount ClassPicker or LevelEditorActionBar" and replace with "non-active rows render header-only — they never mount ClassPicker." LevelEditorActionBar now lives in creation-stepper.tsx per Phase 12.7-01 (F7 hoist).

    4. Run `pnpm tsc --noEmit` — confirm NO unused-import error for `LevelEditorActionBar` in `level-progression-row.tsx`.

    5. Run `pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` — confirm all 6 suites PASS.

    6. Run `pnpm vitest run tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` — confirm GREEN (testid contract preserved per D-04). If RED, investigate before committing.

    7. Atomic commit: `feat(12.7-01): hoist LevelEditorActionBar to creation-stepper + delete row mount (F7 R1)`
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx 2>&amp;1 | tail -20</automated>
    Expected: all new + existing E2E suites pass; zero regressions.
  </verify>
  <acceptance_criteria>
    - `grep -c "LevelEditorActionBar" apps/planner/src/components/shell/creation-stepper.tsx` returns exactly 2 (import + JSX mount)
    - `grep -c "LevelEditorActionBar" apps/planner/src/features/level-progression/level-progression-row.tsx` returns 0 (both import AND JSX deleted — doc-block mention also removed)
    - `grep -c "key={expandedLevel}" apps/planner/src/components/shell/creation-stepper.tsx` returns 1 (D-02 stable key invariant)
    - `grep -cE "key=\{activeLevelSubStep\}" apps/planner/src/components/shell/creation-stepper.tsx` returns 0 (D-02 prohibits this)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` → 6/6 suites pass
    - `pnpm vitest run tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` → 1/1 pass (testid contract preserved)
    - Full suite: `pnpm vitest run` exits 0 (or same baseline skipped/todo count as Phase 12.6 close: 651 pass + 2 skipped + 4 todo)
  </acceptance_criteria>
  <done>
    Action bar renders in creation-stepper, visible across Clase/Habilidades/Dotes sub-steps for active level, returns null at L20, stable React key prevents remount on sub-step transition. 12.4-09 E2E spec remains green — testid contract preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → planner-shell store | Sub-step / active-level / expanded-level mutations — existing gate in setExpandedLevel |
| React render tree → DOM testid attributes | E2E selectors `[data-testid="advance-to-level-N"]` and `[data-testid="level-editor-action-bar"]` are public automation contracts |
| Stepper conditional render → LevelEditorActionBar lifecycle | Mount/unmount governs which level drives the bar's selector inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-01-01 | Tampering | `key={activeLevelSubStep}` regression could be introduced by a future refactor, causing bar to remount on every sub-step change and re-fire initial-render selectors | mitigate | Suite A5 stable-key assertion in spec file literally asserts `expect(after).toBe(before)` — any future key change to `activeLevelSubStep` fails this test immediately |
| T-12.7-01-02 | Denial of Service | Double-render regression if old mount in `level-progression-row.tsx` is not fully deleted — two bars would fight for focus + the lower bar's testid would collide with the hoisted one | mitigate | D-03 mandates delete of both import + JSX; acceptance grep asserts 0 occurrences of `LevelEditorActionBar` in `level-progression-row.tsx`. Playwright-style unique testid would fail if double-rendered |
| T-12.7-01-03 | Information Disclosure | `expandedLevel !== null` gate exposes no user data; bar simply hidden when no Progresión level is active | accept | No PII / secrets in scope; conditional render is UX, not a trust boundary |
| T-12.7-01-04 | Tampering | Testid rename (`advance-to-level-{N+1}` → anything else) would silently break Phase 12.4-09 E2E spec | mitigate | D-04 locks testid verbatim; Task 2 verify runs 12.4-09 E2E spec — failure blocks commit |
| T-12.7-01-05 | Elevation of Privilege | Bar could skip atomic dispatch order (`setActiveLevelSubStep('class')` → `setActiveLevel` → `setExpandedLevel`) if someone refactors the handler — locked by Phase 12.4-09 Suite B1 | accept | Component internals unchanged in this plan; 12.4-09 E2E spec is regression guard |

All mitigations are enforced by tests in this plan's deliverable spec OR by the pre-existing 12.4-09 E2E spec. No manual review gate required.
</threat_model>

<verification>
Full suite green:
- `pnpm tsc --noEmit` → 0 errors
- `pnpm vitest run` → same baseline as Phase 12.6 close (651 pass + 2 skipped + 4 todo) + 6 new suites in phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx

Grep invariants (run as one-liner before commit):
- `grep -c "LevelEditorActionBar" apps/planner/src/components/shell/creation-stepper.tsx` → 2
- `grep -c "LevelEditorActionBar" apps/planner/src/features/level-progression/level-progression-row.tsx` → 0
- `grep -c "key={expandedLevel}" apps/planner/src/components/shell/creation-stepper.tsx` → 1
- `grep -cE "font-weight:\s*700" apps/planner/src/components/shell/creation-stepper.tsx` → 0 (zero new)
- `grep -cE "#[0-9a-fA-F]{3,6}" apps/planner/src/components/shell/creation-stepper.tsx` → 0 (zero new hex)

Threat-model enforcement:
- T-12.7-01-01 guarded by Suite A5 stable-key proof (fails on key rename to activeLevelSubStep)
- T-12.7-01-02 guarded by 0-occurrence grep against level-progression-row.tsx
- T-12.7-01-04 guarded by pnpm vitest run tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx
</verification>

<success_criteria>
- User visits L1 + navigates Clase → Habilidades → Dotes: action bar visible on all 3 sub-steps
- User clicks enabled bar: atomic dispatch moves to L2 + sub-step 'class' (preserved from 12.4-09)
- L20: no bar rendered
- Sub-step transition: bar DOM node identity preserved (no remount flash)
- Phase 12.4-09 E2E spec green
- Zero new CSS tokens / font-weight:700 / hex colors (DEF-12.4-02 invariant)
</success_criteria>

<output>
After completion, create `.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-01-SUMMARY.md` documenting:
- Files touched (3 modified + 1 new test file)
- RED→GREEN commit hashes
- Acceptance grep outputs (all invariants satisfied)
- Phase 12.4-09 E2E confirm-still-green evidence
- Any deviation from CONTEXT D-01..D-04 (expected: none)
</output>
