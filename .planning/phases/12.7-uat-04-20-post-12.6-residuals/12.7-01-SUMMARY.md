---
phase: 12.7-uat-04-20-post-12.6-residuals
plan: 01
subsystem: ui
tags: [level-progression, shell, stepper, advance-bar, mount-hoist, react-key, f7-blocker]

# Dependency graph
requires:
  - phase: 12.4-construccion-correctness-clarity
    provides: <LevelEditorActionBar> component + [data-testid="advance-to-level-{N+1}"] testid contract + atomic dispatch (12.4-09)
  - phase: 12.6-uat-04-20-residuals
    provides: <LevelProgressionRow> expanded-slot host (12.6-04 — mount site that F7 inherits and 12.7-01 supersedes)
provides:
  - Hoisted <LevelEditorActionBar /> mount inside <CreationStepper /> — persists across Clase / Habilidades / Dotes sub-steps for the active level (F7 BLOCKER unblock)
  - Stable React key contract: key={expandedLevel} (NOT activeLevelSubStep) — sub-step transitions preserve the exact DOM node (threat T-12.7-01-01 guard)
  - Single-source-of-truth action bar — old <LevelEditorActionBar /> mount + import + doc-block references removed from <LevelProgressionRow /> (threat T-12.7-01-02 guard)
  - [data-testid="advance-to-level-{N+1}"] + [data-testid="level-editor-action-bar"] preserved verbatim (D-04 / 12.4-09 E2E contract)
  - Phase 12.7 jsdom glob in vitest.config.ts (mirrors 12.6-01 pattern)
affects: [12.7-02-skill-budget-gate, 12.7-03-polish, future shell-layout plans]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stepper-global conditional mount gated on expandedLevel !== null (CreationStepper now owns one progression-level-scoped child that persists across sub-step changes)"
    - "Stable-key React pattern for cross-sub-step subtree preservation — key on the semantic identity (level), not the UI mode (sub-step)"
    - "Test harness sibling-mount: rendering <LevelEditorActionBar /> as a narrow sibling of <BuildProgressionBoard /> in the 12.4-09 E2E spec reproduces the new shell layout without pulling in the heavier <CreationStepper /> origin-step subscriptions"

key-files:
  created:
    - tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx
  modified:
    - apps/planner/src/components/shell/creation-stepper.tsx
    - apps/planner/src/features/level-progression/level-progression-row.tsx
    - vitest.config.ts
    - tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx
    - tests/phase-12.6/level-progression-scan.spec.tsx

key-decisions:
  - "D-01 followed: hoist <LevelEditorActionBar /> into <CreationStepper /> (sibling of .creation-stepper__bottom) rather than into center-content.tsx — keeps the expandedLevel subscription DRY (already present on line 102 of creation-stepper.tsx)"
  - "D-02 followed: key={expandedLevel} — sub-step transitions within a single level preserve the exact DOM node (locked by Suite A5 expect(after).toBe(before))"
  - "D-03 followed: old mount + import + doc-block references all removed from level-progression-row.tsx — single source of truth prevents double-render + testid collision"
  - "D-04 followed: [data-testid=\"advance-to-level-{N+1}\"] preserved verbatim — component body unchanged"

patterns-established:
  - "Sibling-mount E2E harness for testing the hoisted action bar (used in 12.4-09 spec update): wraps BuildProgressionBoard + LevelEditorActionBar via a local `ShellHarness()` that mirrors production's conditional-mount + stable-key contract"
  - "Phase-12.X glob expansion in vitest.config.ts — append exactly one entry per new phase testing directory (mirrors 12.6-01 precedent)"

requirements-completed: [PROG-05]

# Metrics
duration: 10min
completed: 2026-04-20
---

# Phase 12.7 Plan 01: Hoist LevelEditorActionBar (F7 BLOCKER Unblock) Summary

**Action bar hoisted from LevelProgressionRow expanded slot to CreationStepper, keyed on expandedLevel for stable cross-sub-step persistence; user can now advance L1 → L2 on Clase, Habilidades, and Dotes sub-steps.**

## Performance

- **Duration:** ~10 min (RED commit 21:03:15 → GREEN commit 21:12:11 local)
- **Started:** 2026-04-20T21:02:46Z (test bootstrap)
- **Completed:** 2026-04-20T21:12:26Z
- **Tasks:** 2 (Task 1 RED spec + Task 2 GREEN hoist/delete)
- **Files modified:** 4 (2 source + 2 spec harness updates)
- **Files created:** 1 (tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx)

## Accomplishments

- **F7 BLOCKER unblocked** — user can now reach the advance affordance on every Progresión sub-step (Clase / Habilidades / Dotes) for the active level. The previous mount inside `<LevelProgressionRow>`'s `{isActive && ...}` expanded slot disappeared whenever BuildProgressionBoard unmounted on sub-step switch.
- **Stable-key contract locked in test** — Suite A5 (`expect(after).toBe(before)`) asserts the bar's DOM node identity is preserved across sub-step transitions. Any future refactor flipping `key={expandedLevel}` to `key={activeLevelSubStep}` (threat T-12.7-01-01) fails this test immediately.
- **Zero double-render risk** — old mount completely removed from `level-progression-row.tsx` (import + JSX + 2 doc-block references). `grep -c "LevelEditorActionBar" level-progression-row.tsx` returns 0.
- **Testid contract preserved** — `[data-testid="advance-to-level-{N+1}"]` and `[data-testid="level-editor-action-bar"]` unchanged; Phase 12.4-09 E2E spec remains green (after narrow harness update to sibling-mount the hoisted bar).
- **Phase 12.7 jsdom glob wired** — one new `environmentMatchGlobs` entry in `vitest.config.ts` following the 12.6-01 precedent.

## Task Commits

Each task committed atomically (TDD RED → GREEN):

1. **Task 1: RED spec + vitest glob** — `d5d7246` (`test`)
2. **Task 2: GREEN hoist + old-mount delete** — `d38ee18` (`feat`)

## Acceptance Grep Invariants (all satisfied)

| Invariant | Expected | Actual |
| --- | --- | --- |
| `LevelEditorActionBar` in creation-stepper.tsx | 2 | 2 (import + JSX) |
| `LevelEditorActionBar` in level-progression-row.tsx | 0 | 0 |
| `key={expandedLevel}` in creation-stepper.tsx | 1 | 1 |
| `key={activeLevelSubStep}` in creation-stepper.tsx | 0 | 0 |
| `font-weight: 700` in creation-stepper.tsx | 0 | 0 |
| Hex colors `#[0-9a-fA-F]{3,6}` in creation-stepper.tsx | 0 | 0 |
| `tests/phase-12.7/**/*.spec.tsx` glob in vitest.config.ts | 1 | 1 |
| `data-testid="level-editor-action-bar"` in new spec | ≥5 | 7 |
| `expect(after).toBe(before)` stable-key proof | ≥1 | 1 |
| Spec file `// @vitest-environment jsdom` header | 1 | 1 |

## Test Suite Evidence

- **Phase 12.7 spec (new):** 6/6 green (A1 Clase + A2 Habilidades + A3 Dotes + A4 L20-null + A5 stable-key + A6 testid contract)
- **Phase 12.4-09 E2E (pre-existing, testid-contract guard):** 1/1 green
- **Phase 12.6 scan spec (updated C1, D1 for new contract):** 17/17 green
- **Full Vitest suite:** 797 passed / 2 skipped / 1 todo across 97 test files (zero failures, zero errors)
- **Typecheck (`tsc --noEmit`):** exit 0

### RED baseline (evidence of correct failure before fix)

```
tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx (6 tests | 5 failed)
  × Suite A1: action bar visible inside <CreationStepper /> on Progresion-Clase sub-step
  × Suite A2: action bar visible inside <CreationStepper /> on Progresion-Habilidades sub-step (R1 fix)
  × Suite A3: action bar visible inside <CreationStepper /> on Progresion-Dotes sub-step (R1 fix)
  ✓ Suite A4: action bar returns null at L20 (PROGRESSION_LEVEL_CAP terminal)
  × Suite A5: sub-step transition preserves exact DOM node (stable key=expandedLevel)
  × Suite A6: advance-to-level-{N+1} testid preserved under hoisted mount
```

All 5 failures were `AssertionError: expected null not to be null` (imports resolved cleanly; no TS errors; reached assertions). A4 passing at RED proves the spec wiring was sound — the bar returning null at L20 did not require the hoist.

### GREEN after fix (all 6 suites pass)

```
✓ tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx (6 tests) 52ms
```

## Files Created/Modified

- `apps/planner/src/components/shell/creation-stepper.tsx` — added LevelEditorActionBar import (line 15) + conditional JSX mount with stable key (lines 157–181, including comment block documenting D-01..D-04)
- `apps/planner/src/features/level-progression/level-progression-row.tsx` — removed unused LevelEditorActionBar import, removed old `<LevelEditorActionBar />` JSX from expanded slot, updated doc-block to reference the new 12.7-01 mount site
- `vitest.config.ts` — appended `['tests/phase-12.7/**/*.spec.tsx', 'jsdom']` to `environmentMatchGlobs`
- `tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` (NEW, 223 lines) — 6 suites locking mount-site + stable-key + testid-contract invariants with Elfo+Guerrero L1 fixture
- `tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` — narrow-harness update: swapped solo `<BuildProgressionBoard />` render for a local `ShellHarness()` that sibling-mounts `<BuildProgressionBoard />` + `<LevelEditorActionBar key={expandedLevel} />` when `expandedLevel !== null`. Mirrors production mount-gate without pulling in full CreationStepper's heavy origin-step subscriptions (heap-budget safe across L1→L16 rerender loop).
- `tests/phase-12.6/level-progression-scan.spec.tsx` — updated C1 and D1 assertions to reflect the new contract: action bar + advance button no longer inside the expanded row (both flipped from `.not.toBeNull()` → `.toBeNull()` inside the row; hoisted-mount coverage delegated to the Phase 12.7-01 spec).

## Decisions Made

Followed CONTEXT D-01..D-04 verbatim — no deviation.

- **Mount site:** `creation-stepper.tsx` between the progression `</section>` and `<div className="creation-stepper__bottom">` — exactly the sibling placement called out in D-01. CreationStepper already subscribed to `expandedLevel` on line 102, so no new zustand subscription was needed (narrow-subscription invariant 12.3-05 preserved).
- **Stable key:** `key={expandedLevel}` — not `activeLevelSubStep`. D-02 rationale locked in Suite A5.
- **Old mount delete scope:** import + JSX + doc-block mentions (4 total lines/references removed) per acceptance criterion "doc-block mention also removed".
- **Harness adaptation:** updated Phase 12.4-09 E2E spec and Phase 12.6 scan spec to reflect the new mount-site contract. Both are Rule 3 fixes (directly caused by this task's hoist). The Phase 12.4-09 spec uses a narrow local `ShellHarness()` (sibling `<BuildProgressionBoard />` + `<LevelEditorActionBar />`) rather than the full `<CreationStepper />` so the Vitest worker heap doesn't OOM under the spec's L1→L16 rerender loop.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Phase 12.4-09 E2E spec harness stale after hoist**
- **Found during:** Task 2 (GREEN verify step)
- **Issue:** The pre-existing E2E spec rendered solo `<BuildProgressionBoard />` and asserted the action bar appeared there. After hoisting the bar out of `<LevelProgressionRow />`, the bar no longer rendered under that subtree and the spec failed at its first assertion (`actionBarL1` null).
- **Fix:** Added a narrow local `ShellHarness()` function composing `<BuildProgressionBoard />` + `<LevelEditorActionBar key={expandedLevel} />` (conditional on `expandedLevel !== null`) as siblings. Updated all `render(createElement(BuildProgressionBoard))` / `rerender(createElement(BuildProgressionBoard))` call sites to use `createElement(ShellHarness)`. Preserves the testid contract + atomic-dispatch invariants the spec exists to guard.
- **Files modified:** `tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx`
- **Verification:** `pnpm vitest run tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` → 1/1 green (132 ms)
- **Sub-issue encountered:** A first edit attempt used `replace_all` for the literal `createElement(BuildProgressionBoard)` token, which inadvertently rewrote the occurrence inside the harness function body and produced an infinite recursion (`ShellHarness → ShellHarness`). The runaway render loop exhausted the 4 GB V8 heap. Reverted to a scoped edit block that replaced only the call sites, not the harness implementation. Full suite green post-fix.
- **Committed in:** `d38ee18` (Task 2 GREEN commit)

**2. [Rule 3 - Blocking] Phase 12.6 scan spec assertions encoded superseded contract**
- **Found during:** Task 2 (GREEN full-suite verify)
- **Issue:** Suites C1 and D1 in `tests/phase-12.6/level-progression-scan.spec.tsx` asserted `<LevelEditorActionBar />` and the `advance-to-level-2` button existed INSIDE `[data-testid="level-row-2-expanded"]` / `[data-testid="level-row-1-expanded"]`. That was the Plan 12.6-04 contract; Phase 12.7-01 D-01/D-03 supersede it by hoisting the bar out of the row entirely.
- **Fix:** Flipped both assertions from `.not.toBeNull()` to `.toBeNull()` for the action-bar / advance-button inside the expanded row. Updated suite names + added inline comments documenting the contract transition. Hoisted-mount positive coverage delegated to `tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` (which this plan ships).
- **Files modified:** `tests/phase-12.6/level-progression-scan.spec.tsx`
- **Verification:** full suite 797 passed / 0 failed
- **Committed in:** `d38ee18` (Task 2 GREEN commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 — blocking test-harness updates directly caused by the current task's mount-site hoist).

**Impact on plan:** Both fixes were necessary to keep the regression-guard specs in sync with the new contract. Zero scope creep — no production logic outside `creation-stepper.tsx` + `level-progression-row.tsx` was touched. The testid contract (D-04) is preserved verbatim; the hoist just moved the DOM location.

## Issues Encountered

- **Vitest worker heap OOM during first E2E harness attempt** — an overly broad `replace_all` edit inside `e2e-humano-guerrero-l1-l16.spec.tsx` rewrote the `createElement(BuildProgressionBoard)` inside the harness body itself, producing `ShellHarness → ShellHarness` infinite recursion. V8 heap (4 GB) exhausted; Vitest worker force-exited. Resolved by using a scoped edit block (not `replace_all`) to update only the call sites, keeping the harness body correct. Documented above under Rule 3 deviation #1 sub-issue.

## Threat-Model Enforcement

| Threat ID | Disposition | How Enforced |
|-----------|-------------|--------------|
| T-12.7-01-01 (future refactor flips key to activeLevelSubStep) | mitigate | Suite A5 `expect(after).toBe(before)` fails immediately on any key identity change |
| T-12.7-01-02 (double render from stale row mount) | mitigate | `grep -c "LevelEditorActionBar" level-progression-row.tsx` = 0 (import + JSX + doc-block all deleted) |
| T-12.7-01-03 (information disclosure) | accept | No PII in scope; conditional render is UX, not a trust boundary |
| T-12.7-01-04 (testid rename breaks 12.4-09 E2E) | mitigate | Phase 12.4-09 spec re-run in Task 2 verify, still green |
| T-12.7-01-05 (atomic dispatch regression) | accept | Component body unchanged; 12.4-09 spec guards |

## User Setup Required

None — no external service configuration required.

## Self-Check: PASSED

- FOUND: `tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` (new file exists)
- FOUND: commit `d5d7246` (RED) in `git log --oneline`
- FOUND: commit `d38ee18` (GREEN) in `git log --oneline`
- VERIFIED: vitest.config.ts contains `tests/phase-12.7/**/*.spec.tsx` → 1 occurrence
- VERIFIED: creation-stepper.tsx contains `key={expandedLevel}` → 1 occurrence
- VERIFIED: creation-stepper.tsx contains `LevelEditorActionBar` → 2 occurrences (import + JSX)
- VERIFIED: level-progression-row.tsx contains `LevelEditorActionBar` → 0 occurrences
- VERIFIED: full Vitest suite 797 passed / 2 skipped / 1 todo / 0 failed
- VERIFIED: typecheck (`pnpm typecheck`) exit 0

## Next Phase Readiness

- **F7 BLOCKER closed at the code level.** Phase 12.7-02 (R2 skill over-allocation gate) can proceed in parallel Wave 2 — no ordering dependency on this plan.
- **Parallel wave partner:** Plan 12.7-04 (R6 L2..L20 skill budget verify-only spike) can run alongside this plan in Wave 1 without conflict — different files.
- **Remaining 12.7 plans:** 12.7-02 (R2), 12.7-03 (R3+R4+R5 polish), 12.7-04 (R6 verify). Action bar hoist stays stable; downstream plans add to skill gate + scroll reset + row header prefix without touching the new mount site.

---
*Phase: 12.7-uat-04-20-post-12.6-residuals*
*Completed: 2026-04-20*
