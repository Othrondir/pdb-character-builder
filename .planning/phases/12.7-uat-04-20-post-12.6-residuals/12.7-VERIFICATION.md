---
phase: 12.7-uat-04-20-post-12.6-residuals
verified: 2026-04-24T21:57:00Z
status: passed
score: "4/5 UAT tests pass at 12.7 close + T3 scroll-reset gap closed cross-phase by 12.8-03 D-15 marker (5/5 net)"
overrides_applied: 0
source:
  - 12.7-UAT.md
  - 12.7-01-SUMMARY.md
  - 12.7-02-SUMMARY.md
  - 12.7-03-SUMMARY.md
  - 12.7-04-SUMMARY.md
reverified: false
retro_authored: true
retro_reason: "v1.0 re-audit 2026-04-24 flagged unverified_phases.12.7 — UAT + SUMMARYs complete, T3 scroll-reset gap delegated cross-phase to Phase 12.8-03 which landed D-15 Closure marker in 12.7-UAT.md on 2026-04-24. This document closes the verification gap from on-disk evidence; zero source changes."
cross_phase_closure:
  - gap: "T3 Habilidades scroll resets on entry + level change"
    closed_by: "12.8-01 retargeted scrollerRef to .skill-board .selection-screen__content + deleted app.css scroll-snap rules; 12.8-03 appended ## Closure (2026-04-24) marker to 12.7-UAT.md"
    evidence: "12.7-UAT.md lines 87-102 contain 'Closed by Phase 12.8' + 'T3 gap status: resolved'"
---

# Phase 12.7: UAT-2026-04-20 Post-12.6 Residuals Verification Report

**Phase Goal:** Close 6 of 7 findings (F1, F2, F3, F4, F6, F7) surfaced by agent-driven UAT against master 2026-04-20 immediately after Phase 12.6 closure, restoring the user's ability to advance from L1 → L2 (F7 BLOCKER — `LevelEditorActionBar` mounted only inside the Progresión-Clase sub-step host) and gating skill over-allocation at the `+` button site (F4); F5 SKILL-CARRYOVER explicitly deferred to Phase 12.8 due to data-blocker on Puerta server source.
**Verified:** 2026-04-24T21:57:00Z
**Status:** passed
**Re-verification:** retro-authored 2026-04-24; T3 gap resolved cross-phase by Phase 12.8-03 D-15 closure marker (see 12.7-UAT.md lines 87-102). Zero source-code changes from this verification pass.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (R1/F7) | Action bar visible on Clase / Habilidades / Dotes sub-steps; DOM node identity preserved across sub-step transitions | ✓ VERIFIED | 12.7-UAT.md Test 1: Clase `barExists=true`, `barParentClass="nwn-frame creation-stepper"`, advanceBtnExists=true (`testid "advance-to-level-2"`); Habilidades `barExists=true, stableNode=true`; Dotes `barExists=true, stableNode=true`. Plan 12.7-01 hoisted `<LevelEditorActionBar key={expandedLevel}>` into `<CreationStepper>` (commits `d5d7246` + `d38ee18`); Suite A5 `expect(after).toBe(before)` locks stable-key contract |
| 2 (R2/F4) | Skill `+` button disabled when budget exhausted; `Puntos restantes` never reaches -1 | ✓ VERIFIED | 12.7-UAT.md Test 2: Fixture Elfo + Guerrero L1, INT 8, Puntos disponibles = 4. Click 1: rest 3 / Click 2: rest 2 / Click 3: rest 1 / Click 4: rest 0, disabledAfter true / Click 5: blocked (disabledBefore true). Post-exhaustion: 39/39 `+` buttons disabled; rest never negative. Plan 12.7-02 shipped pure `canIncrementSkill` predicate + boundary adapter `buildSkillBudgetSnapshotFromSheet` (commits `90e8caf` RED, `79b5e77` GREEN, `7e7afd9` integration) |
| 3 (R3/F2) | Habilidades scroll resets on entry + level change | ✓ RESOLVED-CROSS-PHASE | At 12.7 close (2026-04-23), 12.7-UAT.md Test 3 reported `result: issue` because jsdom passed (12.7-03 useLayoutEffect + scrollerRef on `<aside class="skill-sheet">`) but real browser scroller was `.selection-screen__content` (clientHeight===scrollHeight===1982 on `.skill-sheet`; scrollTop=206 on fresh entry). CSS `scroll-snap-type: y` (`app.css:473`) + `.skill-sheet__row scroll-snap-align: start` (`app.css:477`) snapped past 206px header region. **Closed cross-phase by Phase 12.8-01:** retargeted `useLayoutEffect` to `.skill-board .selection-screen__content` (real overflow owner) + deleted both snap rules + shipped Playwright regression spec (`tests/phase-12.8/skill-scroll-snap.e2e.spec.ts`, 5 cases). **Phase 12.8-03 appended `## Closure (2026-04-24)` marker to 12.7-UAT.md lines 87-102 with `T3 gap status: resolved` per D-15 cross-phase closure pattern.** Net status at 2026-04-24: ✓ VERIFIED |
| 4 (R4/F3) | Per-row Clase/Transclase label removed; section headings preserved; `Solo entrenada` badge unchanged | ✓ VERIFIED | 12.7-UAT.md Test 4: Section headings present (`Habilidades de clase · coste 1 pt/rango` + `Habilidades transclase · coste 2 pts/rango`); `offendingSpanCount=0` (zero standalone Clase/Transclase spans inside row-label/meta-inline); `trainedBadgeCount=14` ([data-trained-only="true"] preserved). Plan 12.7-03 dropped `<span>{row.costTypeLabel}</span>` JSX consumer with comment-block scope-out (commit `c856da7`); RTL spec `tests/phase-12.7/skill-sheet-row-label.spec.tsx` D1+D2+D3 green |
| 5 (R5/F1) | Row header shows `Nivel N` prefix; token order Nivel N → class → N/M dotes → N/M pts → glyph | ✓ VERIFIED | 12.7-UAT.md Test 5: level-row-1 strong=`Nivel 1`, header=`Nivel 1Guerrero0/2 dotes4/4 pts⚠`; level-row-2 strong=`Nivel 2`, header=`Nivel 2Sin clase0/0 dotes0/0 pts⚠`; token order matches spec. Plan 12.7-03 added `shellCopyEs.progression.rowLevelPrefix(level)` template fn + JSX swap `<strong>{level}</strong> → <strong>{shellCopyEs.progression.rowLevelPrefix(level)}</strong>` (commit `c856da7`); RTL spec E1+E2+E3+E4 green |

**Score:** 5/5 truths verified (4 passed at 12.7 close + 1 cross-phase closed by 12.8-03 D-15).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/planner/src/components/shell/creation-stepper.tsx` | LevelEditorActionBar import + JSX mount with stable `key={expandedLevel}` | ✓ VERIFIED | Plan 12.7-01 (commit `d38ee18`) — line 15 import + lines 157-181 JSX mount + D-01..D-04 doc-block; `grep -c "key={expandedLevel}"` = 1; `grep -c "key={activeLevelSubStep}"` = 0 (T-12.7-01-01 forward-guard) |
| `apps/planner/src/features/level-progression/level-progression-row.tsx` | Old LevelEditorActionBar mount + import + doc-block all removed | ✓ VERIFIED | Plan 12.7-01 (commit `d38ee18`) — `grep -c "LevelEditorActionBar"` = 0; threat T-12.7-01-02 mitigated; row keeps `<strong>{shellCopyEs.progression.rowLevelPrefix(level)}</strong>` from Plan 12.7-03 |
| `vitest.config.ts` | Phase-12.7 jsdom glob entry appended | ✓ VERIFIED | Plan 12.7-01 (commit `d38ee18`) — appended `['tests/phase-12.7/**/*.spec.tsx', 'jsdom']` to `environmentMatchGlobs` mirroring 12.6-01 pattern |
| `tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` | F7 stepper-global mount + stable-key + testid contract guard (6 suites A1-A6) | ✓ VERIFIED | Plan 12.7-01 (commit `d5d7246` RED → `d38ee18` GREEN) — 223 lines, 6/6 green (A1 Clase + A2 Habilidades + A3 Dotes + A4 L20-null + A5 stable-key + A6 testid contract) |
| `tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` | Harness updated to sibling-mount LevelEditorActionBar via local ShellHarness | ✓ VERIFIED | Plan 12.7-01 (commit `d38ee18`) — Rule 3 deviation #1 in summary; `ShellHarness()` composes `<BuildProgressionBoard />` + `<LevelEditorActionBar key={expandedLevel} />` siblings; 1/1 green |
| `tests/phase-12.6/level-progression-scan.spec.tsx` | Suites C1+D1 contract-transition assertions flipped (`.not.toBeNull()` → `.toBeNull()` inside expanded row) | ✓ VERIFIED | Plan 12.7-01 (commit `d38ee18`) — Rule 3 deviation #2 in summary; hoisted-mount positive coverage delegated to phase-12.7 spec; full Vitest suite 797 passed / 0 failed |
| `packages/rules-engine/src/skills/skill-budget.ts` | Pure `canIncrementSkill` + `nextIncrementCost` + `SkillBudgetSnapshot` interface; zero imports | ✓ VERIFIED | Plan 12.7-02 (commit `79b5e77`) — 85 lines, zero imports (framework-purity invariant 12.4-03 preserved); mirrors 12.3-01 `canIncrementAttribute` doc-block + signature shape verbatim |
| `apps/planner/src/features/skills/selectors.ts` | Boundary adapter `buildSkillBudgetSnapshotFromSheet(activeSheet): SkillBudgetSnapshot` | ✓ VERIFIED | Plan 12.7-02 (commit `79b5e77`) — D-07 honored; iterates `activeSheet.groups[*].rows[*]` mapping `costType==='class'` to `costPerRank: 1` else `2`; zero leak of planner types into rules-engine |
| `apps/planner/src/features/skills/skill-sheet.tsx` | 3-clause disabled chain + scrollerRef + costTypeLabel JSX removed | ✓ VERIFIED | Plan 12.7-02 (commit `79b5e77`) — `disabled = row.disabled \|\| row.currentRank + row.step > row.maxAssignableRank \|\| !canIncrementSkill(row.skillId, level, snapshot)`; Plan 12.7-03 (commit `c856da7`) added `useLayoutEffect`+`scrollerRef` (later retargeted by Plan 12.8-01) + dropped `<span>{row.costTypeLabel}</span>` JSX consumer |
| `tests/phase-12.7/skill-budget-gate.spec.ts` | Pure-node Vitest 13 tests across 3 describe blocks | ✓ VERIFIED | Plan 12.7-02 (commit `90e8caf`) — 162 lines; Block 1 nextIncrementCost / Block 2 canIncrementSkill / Block 3 UAT F4 repro; 13/13 green |
| `tests/phase-12.7/skill-sheet-disabled-gate.spec.tsx` | jsdom RTL F4 R2 integration (B1 full spend → all disabled, B2 sanity, B3 belt-and-braces) | ✓ VERIFIED | Plan 12.7-02 (commit `7e7afd9`) — 204 lines; 3/3 green |
| `apps/planner/src/lib/copy/es.ts` | `progression.rowLevelPrefix(level)` template fn appended | ✓ VERIFIED | Plan 12.7-03 (commit `c856da7`) — `rowLevelPrefix: (level: number) => \`Nivel ${level}\``; mirrors `attributes.pointBuyMissing` precedent at copy/es.ts:148; D-12 honored |
| `tests/phase-12.7/skill-sheet-scroll-reset.spec.tsx` | jsdom RTL R3 scroll reset (C1 unmount-then-remount + C2 level-change + C3 forward-guard no scrollIntoView) | ✓ VERIFIED | Plan 12.7-03 (commits `61fb9f5` RED + `c856da7` GREEN) — 3/3 green; **note:** jsdom-only — Plan 12.8-01 added Playwright real-layout regression after this spec passed jsdom but live browser failed |
| `tests/phase-12.7/skill-sheet-row-label.spec.tsx` | jsdom RTL R4 row-label drop + section heading preservation + Solo entrenada badge unchanged | ✓ VERIFIED | Plan 12.7-03 (commit `c856da7`) — 3/3 green |
| `tests/phase-12.7/level-progression-row-prefix.spec.tsx` | jsdom RTL R5 Nivel-N prefix at L1 + L8 (locked) + L16 (terminal) | ✓ VERIFIED | Plan 12.7-03 (commit `c856da7`) — 4/4 green; regex `Nivel ${level}[^\\d/]+\\d+\\/\\d+ dotes.*\\d+\\/\\d+ pts.*[⚠✓✗🔒]` locks token ORDER without textContent separator assumption (Rule 3 deviation #2 in plan summary) |
| `tests/phase-12.7/skill-budget-l2-l20-formula.spec.ts` | Verify-only spike R6 — table-driven 1,254 assertions across 11 classes × 6 INT × 19 levels | ✓ VERIFIED | Plan 12.7-04 (commit `ef8133a`) — 131 lines, pure-node env; 1,254/1,254 GREEN; raceId:null isolation; classBase read from `compiledClassCatalog`; R6 closed as no-op (zero production-code changes) |
| `.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-UAT.md` | D-15 closure marker appended at lines 87-102 | ✓ VERIFIED | Phase 12.8-03 cross-phase closure — `## Closure (2026-04-24)` block contains `Closed by Phase 12.8 — UAT-2026-04-23 Residuals` + bullet citations to 12.8-01-PLAN.md (Playwright spec + scrollerRef retarget + snap rule deletion) + `T3 gap status: resolved.` |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `<CreationStepper>` | sibling-mount `<LevelEditorActionBar key={expandedLevel} />` | conditional mount gated on `expandedLevel !== null`; subscribed to `expandedLevel` already on line 102 (no new zustand subscription, narrow-subscription invariant 12.3-05 preserved) | ✓ WIRED |
| Cross-sub-step DOM-node stability | Suite A5 `expect(after).toBe(before)` | Stable React key `key={expandedLevel}` (NOT `activeLevelSubStep`); locked in test as forward-guard for T-12.7-01-01 | ✓ WIRED |
| `<SkillSheet>` skill `+` button | budget-exhaustion guard | `canIncrementSkill(row.skillId, level, snapshot)` (3-clause `\|\|` chain at line 87) + `buildSkillBudgetSnapshotFromSheet(activeSheet)` composed once per render and threaded as prop | ✓ WIRED |
| `<SkillSheet>` scroller element | `.skill-board .selection-screen__content` (post-12.8-01 retarget) | `useLayoutEffect` + `document.querySelector` (Plan 12.7-03 attached to `.skill-sheet` initially; cross-phase retargeted by Plan 12.8-01 after live browser UAT exposed the wrong overflow owner) | ✓ WIRED-VIA-12.8-01 |
| `<LevelProgressionRow>` header strong | `shellCopyEs.progression.rowLevelPrefix(level)` template | Single-param template fn (`(level: number) => \`Nivel ${level}\``) consumed at JSX site `<strong>{shellCopyEs.progression.rowLevelPrefix(level)}</strong>`; D-11 zero-new-CSS invariant preserved (existing `.level-progression-row__header` `gap: var(--space-sm)` at app.css:2191) | ✓ WIRED |
| `compiledClassCatalog.classes[].skillPointsPerLevel` | `computePerLevelBudget` skill-points field | L2..L20 formula `max(1, classBase + intMod)` verified table-driven across 1,254 (class × INT × level) triples; raceId:null isolates from Humano +1/level canon (locked separately by 12.4 fixture) | ✓ WIRED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PROG-05 | 12.7-01 (frontmatter `requirements-completed: [PROG-05]`) | Stepper-global advance bar (F7 BLOCKER unblock) — bar visible on every Progresión sub-step for active level, stable React key prevents remount | ✓ SATISFIED | 12.7-01-SUMMARY.md frontmatter explicit; commit `d38ee18` hoists action bar; Suite A5 locks stable-key contract; full Vitest suite 797 passed |
| SKILL-01 | 12.7-04 (frontmatter `requirements-completed: [SKILL-01]`) | L2..L20 skill-budget formula `max(1, classBase + intMod)` verification (D&D 3.5e canon) — verify-only spike, R6 closed as no-op | ✓ SATISFIED | 12.7-04-SUMMARY.md frontmatter explicit; 1,254 GREEN assertions; spec stays as regression guard for Phase 12.8 F5 SKILL-CARRYOVER work |

Note: Plans 12.7-02 + 12.7-03 emit no `requirements-completed` array in their frontmatter — they ship UAT-driven defect-class fixes (F4 over-allocation gate; F2/F3/F1 polish triplet) rather than headline REQ closure. Functional intent IS verified by the UAT pass + dedicated specs, but the requirements traceability for these surfaces lives upstream in SKIL-01..03 / FEAT-02 closure recorded by their owning phases. NOT a gap; consistent with 12.7 being a UAT residuals cleanup phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app.css` | 113 | Pre-existing DEF-12.4-02 `font-weight: 700` | ℹ Info (carry-forward) | Tracked tech debt; explicitly out of 12.7 scope per 12.7-SPEC.md Boundaries |
| `skill-sheet.tsx` | 150-154 (post-12.8-01) | Unscoped `document.querySelector('.skill-board .selection-screen__content')` | ℹ Info (REVIEW WR-02, documented in 12.8-REVIEW.md) | Low — class currently unique; future surfaces could collide; no-op on correctness |
| `skill-sheet.tsx` | (12.7-03 implementation) | `useLayoutEffect`/scrollerRef pointed at `.skill-sheet` (wrong overflow owner) | ⚠ Major (RESOLVED-CROSS-PHASE) | Caused T3 gap; retargeted to real scroller by Plan 12.8-01 + snap rules deleted; Playwright regression added |
| `e2e-humano-guerrero-l1-l16.spec.tsx` | (test harness) | Initial `replace_all` rewrite caused `ShellHarness → ShellHarness` infinite recursion / V8 heap OOM | ℹ Info (SELF-RESOLVED) | Sub-issue documented in 12.7-01-SUMMARY deviation #1; reverted to scoped edit; full suite green post-fix |

No blocker anti-patterns persisting at phase close. T3 anti-pattern (wrong scroller target) was the single major issue; resolved cross-phase via Plan 12.8-01 + 12.8-03 D-15 marker.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase-12.7 Vitest suite at Plan 01 close | `pnpm vitest run tests/phase-12.7/level-editor-action-bar-stepper-mount.spec.tsx` | 6/6 green (A1..A6) | ✓ PASS |
| Phase 12.4-09 E2E harness post-12.7-01 hoist | `pnpm vitest run tests/phase-12.4/e2e-humano-guerrero-l1-l16.spec.tsx` | 1/1 green (132 ms; ShellHarness sibling-mount) | ✓ PASS |
| Phase 12.6 scan spec contract-transition | `pnpm vitest run tests/phase-12.6/level-progression-scan.spec.tsx` | 17/17 green (C1+D1 flipped to `.toBeNull()` for hoisted-out contract) | ✓ PASS |
| Full Vitest suite at 12.7-01 close | `pnpm test` | 797 passed / 2 skipped / 1 todo / 0 failed across 97 files | ✓ PASS |
| Phase-12.7 suite at 12.7-02 close | `npx vitest run tests/phase-12.7` | 4 files / 1276 passed (includes 12.7-01 stepper-mount + 12.7-04 L2..L20 formula) | ✓ PASS |
| Phase-12.7 suite at 12.7-03 close | full Phase 12.7 suite | 1286/1286 across 7 test files | ✓ PASS |
| Phase-12.7 suite at 12.7-04 close | `vitest run tests/phase-12.7/skill-budget-l2-l20-formula.spec.ts` | 1254/1254 green (44 ms; 11 classes × 6 INT × 19 levels) | ✓ PASS |
| Typecheck across all 4 plans | `pnpm typecheck` (`tsc -p tsconfig.base.json --noEmit`) | exit 0 in touched files (pre-existing dexie/better-sqlite3 worktree-only failures out of scope) | ✓ PASS |
| Framework purity invariant | `grep -cE "^import.*(react\|@planner\|@data-extractor\|zustand)" packages/rules-engine/src/skills/skill-budget.ts` | 0 (zero imports entirely) | ✓ PASS |
| DEF-12.4-02 invariant (no new font-weight 700) | `grep -cE "font-weight:\s*700" apps/planner/src/features/skills/skill-sheet.tsx` | 0 (no new occurrences) | ✓ PASS |
| Zero new CSS / hex / tokens (Plan 12.7-03 D-13) | `git diff apps/planner/src/styles/` | empty (R5 is text-prefix-only via rowLevelPrefix) | ✓ PASS |
| Agent-driven MCP Chrome UAT 2026-04-23 | manual probe localhost:5173 master HEAD `809ff11` fixture Elfo+Neutral puro+Atributos default+Guerrero L1 | 4/5 pass + T3 issue (later RESOLVED-CROSS-PHASE by Phase 12.8-01 + 12.8-03 D-15) | ✓ PASS (post-closure) |

### Gaps Summary

One gap reported at 12.7 close (T3 Habilidades scroll reset — jsdom vs real layout engine divergence: Plan 12.7-03 added `useLayoutEffect`+scrollerRef on `<aside class="skill-sheet">`, which jsdom accepts but real Chrome treats as non-scrollable because `.selection-screen__content` is the actual overflow owner; CSS `scroll-snap-type: y` on `.selection-screen__content` + `scroll-snap-align: start` on `.skill-sheet__row` compounded the regression by snapping past the 206px header). Closed cross-phase by Phase 12.8-01 (source retarget to `.skill-board .selection-screen__content` + CSS snap rule deletion + Playwright regression spec `tests/phase-12.8/skill-scroll-snap.e2e.spec.ts` 5 cases) and cross-phase marker appended to 12.7-UAT.md by Phase 12.8-03 (D-15 pattern). **Net verification state at 2026-04-24 = passed, 5/5 truths verified, 0 open gaps. No 12.7 plan carries residual debt into Phase 13+.** F5 SKILL-CARRYOVER remains an explicit out-of-scope deferral to Phase 12.8 per 12.7-SPEC.md Boundaries — data-blocked at 12.7 plan time on Puerta server source for the per-class/per-level carryover cap.

---

*Verified: 2026-04-24T21:57:00Z (retro-authored from on-disk evidence; T3 gap closed cross-phase by 12.8-01 + 12.8-03 D-15)*
*Verifier: Claude (gsd-planner, Phase 13-01 retro-verification)*
