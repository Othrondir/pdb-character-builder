# Phase 12.7: UAT-2026-04-20 Post-12.6 Residuals - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-20
**Phase:** 12.7-uat-04-20-post-12.6-residuals
**Areas discussed:** Mount site (R1), Scroll reset (R3), Row pill separator (R5), Skill gate location (R2)

---

## G1 — R1 Mount Site (LevelEditorActionBar hoist)

| Option | Description | Selected |
|--------|-------------|----------|
| a) creation-stepper.tsx | Top of stepper, persistent across sub-steps | ✓ (recommended) |
| b) center-content.tsx | Per-route host | |
| c) level-sub-steps.tsx | Per-level sub-step nav (narrowest scope) | |
| d) New ProgressionAdvanceBar wrapper | Owned by progression feature | |

**User's choice:** "tu decides" → a) creation-stepper.tsx
**Notes:** Sibling of `<CenterContent>` mount, conditional on `expandedLevel !== null`, stable React key `key={expandedLevel}` to avoid sub-step remount churn. Old mount at `level-progression-row.tsx:243` deleted in same commit.

---

## G2 — R3 Habilidades Scroll-Reset Shape

| Option | Description | Selected |
|--------|-------------|----------|
| a) useLayoutEffect + ref | Pre-paint reset, no flash | ✓ (recommended) |
| b) useEffect | Post-paint reset | |
| c) Remove auto-focus root cause | No reset needed | |

**User's choice:** "tu decides" → a) useLayoutEffect + ref
**Notes:** Reset on mount AND on `level` dependency change. Auto-focus root cause NOT touched (may serve a11y).

---

## G3 — R5 Row Pill Separator Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| a) Text ` · ` between pills + "Nivel N" prefix | | |
| b) CSS gap on flex container + "Nivel N" prefix | Cleaner | ✓ (recommended) |
| c) Both belt-and-braces | | |

**User's choice:** "tu decides" → b) CSS gap + "Nivel N" prefix
**Notes:** `gap: var(--space-xs)` (or nearest existing token) on header flex container; literal `Nivel ${level}` prefix via new `shellCopyEs.progression.rowLevelPrefix` template. Zero new tokens, zero new hex colors, zero new font-weight:700.

---

## G4 — R2 canIncrementSkill Module Location

| Option | Description | Selected |
|--------|-------------|----------|
| a) packages/rules-engine/src/skills/skill-budget.ts (new pure module) | Mirrors 12.3-01 attribute-budget pattern | ✓ (recommended) |
| b) Extend packages/rules-engine/src/foundation/ | | |
| c) Planner-side selector only | | |

**User's choice:** "tu decides" → a) New skills/skill-budget.ts module
**Notes:** Sibling `nextIncrementCost` helper for naming consistency with `ability-budget.ts`. Wired at `skill-sheet.tsx:77` extending the existing `disabled` expression with a third `||` condition.

---

## Claude's Discretion

- Researcher chooses exact selector name for snapshot composition (`selectSkillBudgetSnapshot` vs equivalent) at planning time after grep audit.
- Planner sequences the 4 waves; Wave 1 (R1 mount hoist) MUST land first to unblock the user. Wave 4 (R6 verify-only spike) independent; can land parallel to Wave 2 / Wave 3.
- CSS gap token (`--space-xs` vs `--space-sm`) — pick whichever matches existing 12.6-03 pill spacing for visual parity.

## Deferred Ideas

- **F5 SKILL-CARRYOVER** → Phase 12.8 (data-blocked on Puerta source for per-class/per-level carryover cap rule).
- **F6 fix path** → If verify-only spike reveals L2..L20 broken, fix lands in Phase 12.8 (not 12.7).
- **Dotes UX redesign beyond R1 advance-bar visibility** → future Dotes-focused phase (per-section budget hint copy, family-row affordances, scroll-on-completion).
