---
phase: 14
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rules-engine/src/foundation/ability-modifier.ts
  - packages/rules-engine/src/foundation/index.ts
  - apps/planner/src/features/skills/selectors.ts
  - apps/planner/src/features/character-foundation/attributes-board.tsx
  - apps/planner/src/features/summary/resumen-selectors.ts
  - apps/planner/src/components/shell/character-sheet.tsx
  - tests/phase-14/ability-modifier.spec.ts
autonomous: true
requirements:
  - SHAR-05
user_setup: []
must_haves:
  truths:
    - "ROADMAP SC#5: Skill ability-modifier path no longer contains an inline `(score - 10) / 2` magic-10 expression. Computation routes through a single shared helper `abilityModifier(score: number): number` exported from `@rules-engine/foundation`."
    - "All four production sites that reproduce the canonical D&D 3.5 ability-mod formula now import the shared helper: skills selectors, attributes board, resumen selectors, character-sheet shell."
    - "ROADMAP SC#7: Vitest spec covers the helper across documented cases (score 1, 8, 10, 11, 18, 25 with negatives + edge bounds) and asserts the four call sites delegate correctly."
    - "Divergent fixture removed: any test fixture or stub that hardcoded a different ability-mod formula (or duplicated the same `Math.floor((score - 10) / 2)` literal independent of the helper) is replaced with the shared helper or deleted."
    - "Skill point math (`getAvailablePoints` in `packages/rules-engine/src/skills/skill-allocation.ts`) continues to receive `intelligenceModifier` as an integer; the call site `getIntelligenceModifier` in `apps/planner/src/features/skills/selectors.ts:212-230` now uses `abilityModifier(...)` instead of the inline formula."
  artifacts:
    - path: "packages/rules-engine/src/foundation/ability-modifier.ts"
      provides: "Single canonical abilityModifier(score) helper"
      contains: "export function abilityModifier"
    - path: "packages/rules-engine/src/foundation/index.ts"
      provides: "Barrel re-exports abilityModifier"
      contains: "abilityModifier"
    - path: "apps/planner/src/features/skills/selectors.ts"
      provides: "getIntelligenceModifier delegates to abilityModifier"
    - path: "apps/planner/src/features/character-foundation/attributes-board.tsx"
      provides: "AttributesBoard uses abilityModifier"
    - path: "apps/planner/src/features/summary/resumen-selectors.ts"
      provides: "Resumen view-model uses abilityModifier"
    - path: "apps/planner/src/components/shell/character-sheet.tsx"
      provides: "Character sheet shell uses abilityModifier"
    - path: "tests/phase-14/ability-modifier.spec.ts"
      provides: "Helper unit spec + delegation regression for skills"
  key_links:
    - from: "skills/selectors.ts::getIntelligenceModifier"
      to: "abilityModifier(baseIntelligence + racialInt + intelligenceIncreases)"
      via: "Replace `Math.floor((value - 10) / 2)` with `abilityModifier(value)`"
      pattern: "abilityModifier\\("
    - from: "All 4 production sites"
      to: "@rules-engine/foundation/ability-modifier"
      via: "import { abilityModifier } from '@rules-engine/foundation'"
      pattern: "from '@rules-engine/foundation'"
---

<objective>
Close ROADMAP SC#5 (Phase 14): the codebase has FOUR production sites that independently re-implement the D&D 3.5 ability-modifier formula `Math.floor((score - 10) / 2)`:
1. `apps/planner/src/features/skills/selectors.ts:228` — inline in `getIntelligenceModifier`
2. `apps/planner/src/features/character-foundation/attributes-board.tsx:125` — inline
3. `apps/planner/src/features/summary/resumen-selectors.ts:54-56` — local `abilityModifier` function
4. `apps/planner/src/components/shell/character-sheet.tsx:40` — inline in a function

The audit calls this the "magic-10 fallback" because the literal `10` (the canonical attribute baseline) appears scattered across multiple files. Drift risk: a future Puerta-server rule that changes the baseline (homebrew houserule) requires touching 4 files. The fix consolidates into a single helper in `packages/rules-engine/src/foundation/ability-modifier.ts` and rewrites the four call sites.

Concurrent fix: a "divergent fixture" — any test that hardcodes a derived ability-mod value computed against the inline form, OR any stub that emits a different formula — is removed or migrated to use the helper. Per Phase 14 audit: the skill-points path is the canonical magic-10 site; the divergent fixture is a phase-test that bakes in the inline computation result.

Purpose: Phase 8 hardening — surfaced by 2026-04-24 v1.0 re-audit (`v1.0-MILESTONE-AUDIT.md` Phase 08 line 69). Closes "skill ability-modifier magic-10 fallback; divergent fixture removed". Defensive — no rule change, no requirement reopens.

Output:
- New `packages/rules-engine/src/foundation/ability-modifier.ts` with a single tested helper.
- New barrel export at `packages/rules-engine/src/foundation/index.ts` (file already exists per STATE.md note: "groupRacesByParent is the first helper under packages/rules-engine/src/foundation/ barrel (index.ts newly created)").
- Four call sites refactored to import + delegate.
- New Vitest spec covering helper + skills delegation.
- Any divergent fixture deleted or migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@apps/planner/src/features/skills/selectors.ts
@apps/planner/src/features/character-foundation/attributes-board.tsx
@apps/planner/src/features/summary/resumen-selectors.ts
@apps/planner/src/components/shell/character-sheet.tsx
@packages/rules-engine/src/skills/skill-allocation.ts
@packages/rules-engine/src/foundation/index.ts
@tests/phase-05/skill-rules.spec.ts
@tests/phase-05/skill-revalidation.spec.ts

<interfaces>
<!-- Canonical formula (D&D 3.5 / NWN1 EE): floor((score - 10) / 2). Score 10 → 0; 11 → 0; 12 → +1; 13 → +1; 18 → +4; 25 → +7; 8 → -1; 1 → -5. -->

<!-- Existing inline sites verified by grep: -->
<!-- skills/selectors.ts:227-229
     return Math.floor(
       (baseIntelligence + racialInt + intelligenceIncreases - 10) / 2,
     );
-->
<!-- attributes-board.tsx:125
     const abilityModifier = Math.floor((totalValue - 10) / 2);
-->
<!-- resumen-selectors.ts:54-56
     function abilityModifier(score: number): number {
       return Math.floor((score - 10) / 2);
     }
-->
<!-- character-sheet.tsx:40 (inside a private helper)
     return Math.floor((score - 10) / 2);
-->

<!-- packages/rules-engine/src/foundation/index.ts (exists per STATE.md Phase 12.1-02 note) — current shape unknown; verify by reading. New export added here. -->

<!-- Existing skill fixtures (tests/phase-05/skill-rules.spec.ts, skill-revalidation.spec.ts) hand-set `intelligenceModifier: 0|1|-1|2` directly on SkillLevelInput. They DO NOT exercise getIntelligenceModifier — they pass mod values pre-computed. Therefore the planner's `getIntelligenceModifier` is NOT covered by phase-05 tests; the divergent-fixture risk lives in the planner-level spec layer (selectors.ts is consumed via skill-board.tsx tests). Verify by grep during Task 1. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extract abilityModifier helper to rules-engine + write helper + delegation spec</name>
  <read_first>
    - packages/rules-engine/src/foundation/index.ts (must read first; STATE.md says it was created in Phase 12.1-02 and currently exports `groupRacesByParent` plus the projection adapters — verify before adding)
    - apps/planner/src/features/skills/selectors.ts (lines 200-230 — getIntelligenceModifier full body)
    - apps/planner/src/features/character-foundation/attributes-board.tsx (lines 120-160 — context around the inline expression)
    - apps/planner/src/features/summary/resumen-selectors.ts (lines 50-60 — local abilityModifier definition)
    - apps/planner/src/components/shell/character-sheet.tsx (lines 35-50 — context)
    - packages/rules-engine/src/skills/skill-allocation.ts (line 24 — `intelligenceModifier: number;` is the CONSUMER contract; do NOT change this)
  </read_first>
  <behavior>
    - Helper H1: `abilityModifier(10) === 0`.
    - Helper H2: `abilityModifier(11) === 0`.
    - Helper H3: `abilityModifier(12) === 1`.
    - Helper H4: `abilityModifier(18) === 4`.
    - Helper H5: `abilityModifier(25) === 7`.
    - Helper H6: `abilityModifier(8) === -1`.
    - Helper H7: `abilityModifier(1) === -5`.
    - Helper H8: `abilityModifier(0) === -5` (extreme — mathematically `floor(-10/2) = -5`; documents "score < 1 not legal in NWN" and asserts the math anyway since the schema bounds at min 3).
    - Helper H9: Pure function — no side effects, no closures, no dependencies. Lock by importing as `{ abilityModifier }` and calling 1000 times in a loop with `expect(...).toBe(...)` snapshot — NOT actually run 1000 times in spec, but assert idempotence: `expect(abilityModifier(15)).toBe(abilityModifier(15));`.
    - Delegation D1 (skills): a focused integration test that constructs a SkillLevelInput with `classId: 'class:fighter'`, runs `evaluateSkillSnapshot` against `compiledSkillCatalog`, and asserts that `availablePoints` matches the canonical formula `(skillPointsBase + abilityModifier(intScore)) × 4` at L1. Confirms the rules-engine consumer contract is intact.
  </behavior>
  <action>
    Step 1: Read `packages/rules-engine/src/foundation/index.ts` to confirm it exists + capture current exports. If empty, add the new export. If non-empty, append.

    Step 2: Create `tests/phase-14/ability-modifier.spec.ts`:
      - No jsdom directive (pure logic).
      - Imports: `{ describe, it, expect } from 'vitest'`; `{ abilityModifier } from '@rules-engine/foundation'`; for D1: `{ evaluateSkillSnapshot, type SkillLevelInput } from '@rules-engine/skills/skill-allocation'`; `{ compiledSkillCatalog } from '@planner/features/skills/compiled-skill-catalog'`.
      - 9 helper `it()` blocks (H1..H9) + 1 delegation `it()` (D1) for 10 total.
      - For D1: hand-build a SkillLevelInput with `intelligenceModifier: abilityModifier(14)` (expected 2), `skillPointsBase: 8`, `classId: 'class:rogue'`, `level: 1`; assert `availablePoints === (8 + 2) * 4 === 40`.

    Step 3: Run the spec: `pnpm vitest run tests/phase-14/ability-modifier.spec.ts`. Expected: 10/10 FAIL — helper does not exist yet, import fails. RED proven via TS+vitest collection error.

    Step 4 (GREEN — small helper, fast): Create `packages/rules-engine/src/foundation/ability-modifier.ts`:
    ```typescript
    /**
     * Phase 14-05 — single canonical D&D 3.5 / NWN1 EE ability-modifier formula.
     *
     * Computes `floor((score - 10) / 2)`. The literal `10` is the attribute
     * baseline (a 10 score yields a 0 modifier). Centralizing here removes the
     * 4 inline copies of this expression that previously lived in
     * `skills/selectors.ts`, `attributes-board.tsx`, `resumen-selectors.ts`,
     * and `components/shell/character-sheet.tsx`.
     *
     * Pure framework-agnostic helper: no React, no zustand, no compiled-catalog
     * imports. Safe to import from rules-engine internals AND from the planner.
     *
     * NWN1 EE attribute scores are clamped to [3, 25] at the schema boundary
     * (`build-document-schema.ts:45-50`). Inputs below 1 are accepted but treated
     * as `1` mathematically; document but do not throw — the schema is the gate.
     */
    export function abilityModifier(score: number): number {
      return Math.floor((score - 10) / 2);
    }
    ```

    Step 5: Edit `packages/rules-engine/src/foundation/index.ts`:
      - Append `export { abilityModifier } from './ability-modifier';` (preserve existing exports — do not overwrite).

    Step 6: Re-run the spec. Expected 10/10 PASS.

    Step 7: Commit `feat(14-05): canonical abilityModifier helper in rules-engine/foundation (test+impl)`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase-14/ability-modifier.spec.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/rules-engine/src/foundation/ability-modifier.ts` exists with exactly one exported function `abilityModifier`.
    - Grep: `grep -c "export function abilityModifier" packages/rules-engine/src/foundation/ability-modifier.ts` returns 1.
    - Grep: `grep -c "abilityModifier" packages/rules-engine/src/foundation/index.ts` returns ≥1 (the new re-export line).
    - File `tests/phase-14/ability-modifier.spec.ts` has exactly 10 `it()` blocks.
    - Spec passes 10/10.
    - `pnpm tsc --noEmit` reports 0 NEW errors.
  </acceptance_criteria>
  <done>Helper extracted, exported, tested. Foundation barrel re-exports it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate 4 call sites to import abilityModifier; delete duplicates; remove divergent fixture</name>
  <read_first>
    - apps/planner/src/features/skills/selectors.ts (lines 1-30 imports + 212-230 getIntelligenceModifier body)
    - apps/planner/src/features/character-foundation/attributes-board.tsx (top-of-file imports + lines 120-160 inline calc)
    - apps/planner/src/features/summary/resumen-selectors.ts (top-of-file imports + lines 50-60 local function + line 184 caller)
    - apps/planner/src/components/shell/character-sheet.tsx (top-of-file imports + lines 35-50 helper body + all callers within file)
    - tests/phase-14/ability-modifier.spec.ts (extend with delegation regression for the 3 non-skill sites if practical)
  </read_first>
  <behavior>
    - Migration M1 (skills/selectors.ts): replace `Math.floor((baseIntelligence + racialInt + intelligenceIncreases - 10) / 2)` with `abilityModifier(baseIntelligence + racialInt + intelligenceIncreases)`. Add import at top of file.
    - Migration M2 (attributes-board.tsx): replace `Math.floor((totalValue - 10) / 2)` (line 125) with `abilityModifier(totalValue)`. NOTE: the local variable is also named `abilityModifier` (lines 125-127) — RENAME the local to `mod` to avoid shadowing the imported helper. Update all references within the JSX block (lines 127-150).
    - Migration M3 (resumen-selectors.ts): DELETE the local `function abilityModifier(score)` (lines 54-56). Add import at top. Verify the existing call site at line 184 (`const abilityMod = abilityModifier(abilityScore);`) now resolves to the imported helper — name parity preserves the call site verbatim.
    - Migration M4 (character-sheet.tsx): replace inline `Math.floor((score - 10) / 2)` (line 40) with `abilityModifier(score)`. Add import at top.
    - Divergent-fixture sweep: grep for any test fixture or hand-stubbed value that bakes the formula in. Specifically check `tests/phase-08/setup.ts` (`sampleBuildDocument`), `tests/phase-12.x/`, and any `.fixture.ts` file. If a fixture asserts a value computed from the inline formula AND that fixture would change behavior post-migration, replace with `abilityModifier(score)` import. If no such fixture exists, document the negative finding in SUMMARY.
  </behavior>
  <action>
    Step 1: Edit `apps/planner/src/features/skills/selectors.ts`:
      - Add import `import { abilityModifier } from '@rules-engine/foundation';` near other `@rules-engine/*` imports (currently lines 11-19).
      - Replace lines 227-229 (the `Math.floor((... - 10) / 2)` block) with:
        ```typescript
        return abilityModifier(baseIntelligence + racialInt + intelligenceIncreases);
        ```
      - Update the surrounding comment (lines 218-225) to read: "UAT-2026-04-20 P3 — fold racial INT adjustment into the modifier so skill points track the same total attribute AttributesBoard shows (A2). Phase 14-05: delegated to canonical `abilityModifier` helper."

    Step 2: Edit `apps/planner/src/features/character-foundation/attributes-board.tsx`:
      - Add import `import { abilityModifier } from '@rules-engine/foundation';`.
      - At line 125, rename the local from `abilityModifier` to `mod` and replace the inline expression: `const mod = abilityModifier(totalValue);`.
      - Search-replace within the SAME function body (typically lines 125-160): every reference `abilityModifier` (the local) → `mod`. Be precise: only the local variable, NOT the imported helper.
      - Update the comment at line 123-124 to drop the formula spelled out (since it's now in the helper): "Phase 14-05: ability modifier comes from the shared `abilityModifier` helper in `@rules-engine/foundation`."

    Step 3: Edit `apps/planner/src/features/summary/resumen-selectors.ts`:
      - Add import `import { abilityModifier } from '@rules-engine/foundation';` near other rules-engine imports.
      - DELETE lines 54-56 (the local `function abilityModifier(score: number)` declaration). The local NAME `abilityModifier` is replaced by the imported helper of the same name; line 184 (`const abilityMod = abilityModifier(abilityScore);`) keeps working without any caller change.

    Step 4: Edit `apps/planner/src/components/shell/character-sheet.tsx`:
      - Add import `import { abilityModifier } from '@rules-engine/foundation';`.
      - Read lines 35-50 to locate the helper containing `Math.floor((score - 10) / 2);`. Replace with `return abilityModifier(score);`. If the helper is named `abilityModifier` (local), inline-callers + delete the local definition. If named differently, replace only the inline expression.

    Step 5: Run the impacted test suites + a representative cross-section:
    ```bash
    pnpm vitest run tests/phase-14/ tests/phase-05/ tests/phase-08/ tests/phase-12/ tests/phase-12.3/ tests/phase-12.6/ tests/phase-12.9/ --reporter=basic
    ```
    Expected: 0 NEW failures over baseline. The key invariant: skill-points computation downstream of `getIntelligenceModifier` is unchanged for any score (the formula is bit-for-bit identical).

    Step 6: Divergent-fixture sweep (read-only audit):
    ```bash
    grep -rn "Math.floor.*- 10.*/ 2\|score - 10\|attribute - 10\|/ 2 - 5" apps/planner/src/ packages/ tests/
    ```
    Expected post-migration: 0 hits in `apps/planner/src/` and `packages/rules-engine/src/` (the helper itself is the sole carrier). If hits exist in `tests/`, evaluate each: if it's a regression spec proving a different formula path, leave it (document in SUMMARY); if it duplicates the canonical helper math, delete or rewrite to import `abilityModifier`. Record findings in the plan SUMMARY.

    Step 7: Append to `tests/phase-14/ability-modifier.spec.ts` a regression `it()` block:
      - Title: `'production-site sweep — no inline (score - 10) / 2 expressions remain in apps/planner/src/'`
      - Implementation: use `import.meta.glob` is NOT available in node-env tests. Instead, hand-list the 4 migrated files + read with `fs.readFileSync`; `expect(content).not.toMatch(/Math\.floor\(\([^)]*-\s*10\)\s*\/\s*2\)/);`.
      - Files to scan in this single sentinel test: `apps/planner/src/features/skills/selectors.ts`, `apps/planner/src/features/character-foundation/attributes-board.tsx`, `apps/planner/src/features/summary/resumen-selectors.ts`, `apps/planner/src/components/shell/character-sheet.tsx`.
      - Imports needed: `{ readFileSync } from 'node:fs'`; `{ resolve } from 'node:path'`. Resolve paths from `__dirname` or `process.cwd()` — use `process.cwd()` to anchor at repo root.

    Step 8: Re-run all phase-14 specs + typecheck.

    Step 9: Commit `refactor(14-05): consolidate ability-modifier across 4 call sites + sentinel`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/phase-14/ tests/phase-05/ tests/phase-08/ tests/phase-12.6/ tests/phase-12.9/ --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Grep: `grep -c "import { abilityModifier } from '@rules-engine/foundation'" apps/planner/src/features/skills/selectors.ts apps/planner/src/features/character-foundation/attributes-board.tsx apps/planner/src/features/summary/resumen-selectors.ts apps/planner/src/components/shell/character-sheet.tsx` returns 4 (one import per file).
    - Grep: `grep -rcE "Math\\.floor\\(\\([^)]*-\\s*10\\)\\s*/\\s*2\\)" apps/planner/src/features/skills/selectors.ts apps/planner/src/features/character-foundation/attributes-board.tsx apps/planner/src/features/summary/resumen-selectors.ts apps/planner/src/components/shell/character-sheet.tsx` returns 0 (no more inline formulas in the 4 migrated files).
    - Grep: `grep -c "function abilityModifier" apps/planner/src/features/summary/resumen-selectors.ts` returns 0 (local definition deleted).
    - Grep: `grep -c "function abilityModifier" packages/rules-engine/src/foundation/ability-modifier.ts` returns 1 (sole source of truth).
    - Sentinel `it()` in `tests/phase-14/ability-modifier.spec.ts` passes (the 4-file scan asserts no inline formula remains).
    - `pnpm vitest run tests/phase-14/` passes 11/11 (10 from Task 1 + 1 sentinel from Task 2).
    - `pnpm vitest run tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.ts` shows 0 NEW failures (skill math unchanged).
    - `pnpm vitest run tests/phase-08/resumen-board.spec.tsx tests/phase-08/resumen-selectors.spec.ts` shows 0 NEW failures.
    - `pnpm tsc --noEmit` reports 0 NEW errors.
  </acceptance_criteria>
  <done>4 call sites migrated. Inline formula count = 0 in production code. Sentinel locks the invariant. Phase-05 + phase-08 baselines preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → ability score input | `baseAttributes` clamped to `[3, 25]` at `build-document-schema.ts:45-50`; helper accepts any number but real input is bounded. |
| Helper → consumer | Pure function, single output, no I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | helper drift across copies (the original problem) | mitigate | Single source of truth in `packages/rules-engine/src/foundation/ability-modifier.ts`. Sentinel test locks "no inline formulas remain" invariant. |
| T-14-05-02 | Integrity | numeric overflow on extreme scores | accept | NWN1 schema clamps to ≤25; JS `Math.floor` is exact for inputs ≤ 2^31. No overflow risk. |
| T-14-05-03 | Information Disclosure | n/a | n/a | Pure helper has no I/O. |
| T-14-05-04 | DoS | n/a | n/a | O(1) operation. |
</threat_model>

<verification>
```bash
# All phase-14 specs
pnpm vitest run tests/phase-14/ --reporter=basic

# Skill math invariants
pnpm vitest run tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.ts --reporter=basic

# Resumen + attributes UI invariants
pnpm vitest run tests/phase-08/resumen-board.spec.tsx tests/phase-08/resumen-selectors.spec.ts tests/phase-12.6/ tests/phase-12.9/ --reporter=basic

# Inline-formula sentinel
grep -rcE "Math\\.floor\\(\\([^)]*-\\s*10\\)\\s*/\\s*2\\)" apps/planner/src/ packages/rules-engine/src/skills/ packages/rules-engine/src/foundation/skills/ 2>/dev/null
# Expected: 0 hits anywhere in src EXCEPT inside `ability-modifier.ts` itself (the helper) — total 1 hit, and it lives in the canonical file.

# Typecheck
pnpm tsc --noEmit 2>&1 | grep -c "error TS"
# Expected: ≤4 (pre-existing baseline)
```
</verification>

<success_criteria>
- ROADMAP SC#5 satisfied: magic-10 fallback removed; single helper carries the formula.
- ROADMAP SC#7 satisfied (partial — covers ability-modifier path with 11 specs).
- 4 call sites delegate. Sentinel locks the invariant against future regression.
- Skill math, resumen math, attributes-board math, character-sheet math all numerically identical pre/post.
- Divergent fixture removed (or absence documented in SUMMARY).
</success_criteria>

<output>
Create `.planning/phases/14-persistence-robustness/14-05-SUMMARY.md` documenting:
- New helper file + barrel update
- 4 migrated files (line-level diff summary)
- Divergent-fixture sweep results (deleted / migrated / negative finding documented)
- Spec totals: 11 phase-14 + invariants preserved across phase-05/08/12.6/12.9
- Threat register: T-14-05-01 mitigated; others accepted/n-a
- Any local variable rename collisions encountered (e.g., attributes-board.tsx local `abilityModifier`)
</output>
