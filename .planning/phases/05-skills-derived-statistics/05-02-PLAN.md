---
phase: 05-skills-derived-statistics
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/planner/src/routes/skills.tsx
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/skills/selectors.ts
  - apps/planner/src/features/skills/store.ts
  - apps/planner/src/features/skills/skill-board.tsx
  - apps/planner/src/features/skills/skill-summary-strip.tsx
  - apps/planner/src/features/skills/skill-rail.tsx
  - apps/planner/src/features/skills/skill-sheet.tsx
  - tests/phase-05/skill-allocation-flow.spec.tsx
autonomous: true
requirements: [SKIL-01, SKIL-02]
user_setup: []
must_haves:
  truths:
    - "El usuario puede abrir `Habilidades`, elegir un nivel del 1 al 16 y editar solo la hoja activa de ese nivel."
    - "Cada fila de habilidad muestra costes, topes y estado usando el snapshot dataset-driven creado en 05-01."
    - "Si una decision previa rompe el reparto, los niveles posteriores siguen visibles y quedan marcados para reparacion en el rail y la hoja."
  artifacts:
    - path: "apps/planner/src/features/skills/selectors.ts"
      provides: "Proyeccion compartida para rail, hoja activa y resumen tecnico local"
      contains: "selectSkillBoardView"
    - path: "apps/planner/src/routes/skills.tsx"
      provides: "Pantalla real de Habilidades"
      contains: "SkillBoard"
    - path: "tests/phase-05/skill-allocation-flow.spec.tsx"
      provides: "Cobertura de rail, hoja activa y reparacion"
      contains: "Habilidades 1-16"
  key_links:
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "packages/rules-engine/src/skills/skill-allocation.ts"
      via: "shared board view model derived from compiled catalog evaluation"
      pattern: "evaluateSkillSnapshot"
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "packages/rules-engine/src/skills/skill-revalidation.ts"
      via: "repair metadata for preserved downstream edits"
      pattern: "inheritedFromLevel"
    - from: "apps/planner/src/routes/skills.tsx"
      to: "apps/planner/src/features/skills/skill-board.tsx"
      via: "route ownership for Habilidades"
      pattern: "SkillBoard"
    - from: "apps/planner/src/features/skills/skill-sheet.tsx"
      to: "apps/planner/src/features/skills/store.ts"
      via: "active-level rank edits call mutable skill setters before selectors recompute spent points, remaining points, and status"
      pattern: "setSkillRank|incrementSkillRank|decrementSkillRank"
---

<objective>
Wire the dataset-driven Phase 5 backbone into the routed `Habilidades` editor, keeping the screen level-focused and consistent with the Phase 4 rail-plus-sheet interaction model.

Purpose: satisfy the editable UI portion of SKIL-01 and SKIL-02 without mixing rules truth into JSX or reopening the stats route scope.
Output: selectors, routed `Habilidades` board, Spanish copy and styling hooks, and route-level interaction coverage.
</objective>

<execution_context>
@C:/Users/pzhly/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pzhly/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-derived-statistics/05-CONTEXT.md
@.planning/phases/05-skills-derived-statistics/05-RESEARCH.md
@.planning/phases/05-skills-derived-statistics/05-UI-SPEC.md
@.planning/phases/05-skills-derived-statistics/05-01-PLAN.md
@apps/planner/src/routes/skills.tsx
@apps/planner/src/features/level-progression/selectors.ts
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/lib/copy/es.ts
@apps/planner/src/styles/app.css

<interfaces>
Expected from Plan 05-01:
```ts
export const compiledSkillCatalog: SkillCatalog;
export function evaluateSkillSnapshot(...args): SkillSnapshot;
export function revalidateSkillSnapshotAfterChange(...args): RevalidatedSkillLevel[];
```

From `apps/planner/src/features/level-progression/selectors.ts`:
```ts
export interface LevelRailEntryView {
  active: boolean;
  classId: CanonicalId | null;
  classLabel: string | null;
  inheritedFromLevel: number | null;
  issueCount: number;
  level: ProgressionLevel;
  status: ProgressionStatus;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared Habilidades selectors and route-owned board</name>
  <files>apps/planner/src/routes/skills.tsx, apps/planner/src/features/skills/selectors.ts, apps/planner/src/features/skills/store.ts, apps/planner/src/features/skills/skill-board.tsx, apps/planner/src/features/skills/skill-summary-strip.tsx, apps/planner/src/features/skills/skill-rail.tsx, apps/planner/src/features/skills/skill-sheet.tsx</files>
  <action>Per D-01, D-03, D-04, D-05, and D-06, replace the placeholder `skills` route with `SkillBoard` and create `apps/planner/src/features/skills/selectors.ts` as the only projection layer for the `Habilidades` screen. `selectSkillBoardView`, `selectSkillRail`, `selectActiveSkillSheetView`, and `selectSkillSummaryStrip` must consume the compiled skill catalog, raw skill store state, progression state, and the pure rules helpers from 05-01. In the same task, extend `apps/planner/src/features/skills/store.ts` with the mutable setters the board needs for per-level rank edits, then wire `apps/planner/src/features/skills/skill-sheet.tsx` so every increment, decrement, or direct rank edit on the active level calls those setters instead of mutating selector output or staying local to component state. The board layout must stay `summary strip -> left rail -> right active sheet`, with no dense spreadsheet fallback and no route-local legality math. The rail and active sheet must expose blocked or invalid repair state from `inheritedFromLevel` so later levels stay visible when upstream edits break legality, and a successful edit from the active sheet must immediately change store state plus recompute `Puntos gastados`, `Puntos restantes`, and the evaluated status shown in the sheet or rail.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-allocation-flow.spec.tsx</automated>
  </verify>
  <done>`Habilidades` becomes a real route-owned editor driven entirely by shared selectors over the compiled dataset and rules pipeline from 05-01, and editing a rank from `skill-sheet.tsx` changes the active-level skill store record and updates remaining points and status without a reload.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Spanish copy, NWN1 board styling, and route interaction coverage</name>
  <files>apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/skills/skill-board.tsx, apps/planner/src/features/skills/skill-rail.tsx, apps/planner/src/features/skills/skill-sheet.tsx, tests/phase-05/skill-allocation-flow.spec.tsx</files>
  <action>Per D-03, D-04, D-05, and the Phase 5 UI spec, extend Spanish copy and CSS so `Habilidades` renders the exact level-ledger language required by the phase: `Habilidades 1-16`, `Hoja de habilidades`, `Puntos disponibles`, `Puntos gastados`, `Puntos restantes`, `Clase`, `Transclase`, `Tope`, `Siguiente coste`, and the repair callout text from the UI contract. Keep the dark-and-gold board language, bronze blocked framing, and dark-red invalid framing from `05-UI-SPEC.md`. Finish `tests/phase-05/skill-allocation-flow.spec.tsx` so it proves the rail switches the active level, the sheet reflects class or transclase cost information from the shared selectors, and upstream changes preserve but block or invalidate later allocations instead of deleting them.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot</automated>
  </verify>
  <done>The `Habilidades` route matches the locked rail-plus-sheet interaction, Spanish-first copy, and preserved-repair behavior for Phase 5.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm vitest --run tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- `Habilidades` is the only editing surface for per-level skill allocation
- Route UI consumes the compiled catalog and pure rules output from 05-01 instead of hand-authored runtime fixtures
- The rail and active sheet preserve downstream edits and show explicit repair states after upstream changes
- The visual and copy contract stays inside the NWN1-inspired Phase 4 shell language
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-derived-statistics/05-02-SUMMARY.md`
</output>
