---
phase: 05-skills-derived-statistics
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/planner/src/routes/stats.tsx
  - apps/planner/src/components/shell/summary-panel.tsx
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/skills/selectors.ts
  - apps/planner/src/features/skills/skill-stats-board.tsx
  - packages/rules-engine/src/skills/skill-derived-stats.ts
  - tests/phase-05/skill-stats-sync.spec.tsx
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03]
user_setup: []
must_haves:
  truths:
    - "La pantalla `Estadísticas` muestra un resumen técnico de solo lectura calculado desde el mismo snapshot de habilidades que usa `Habilidades`."
    - "Los topes, costes, totales y bloqueos visibles en `Estadísticas` coinciden con los que ve el usuario al editar habilidades."
    - "El resumen persistente del shell refleja el estado más grave del reparto de habilidades sin perder la prioridad de foundation y progression cuando estén bloqueadas."
  artifacts:
    - path: "packages/rules-engine/src/skills/skill-derived-stats.ts"
      provides: "Proyección pura de estadísticas derivadas de habilidades"
      contains: "deriveSkillStatsView"
    - path: "apps/planner/src/routes/stats.tsx"
      provides: "Pantalla real de Estadísticas técnicas"
      contains: "SkillStatsBoard"
    - path: "apps/planner/src/components/shell/summary-panel.tsx"
      provides: "Integración del shell summary con severidad de habilidades"
      contains: "Habilidades listas"
    - path: "tests/phase-05/skill-stats-sync.spec.tsx"
      provides: "Prueba de sincronización entre Habilidades, Estadísticas y resumen"
      contains: "Estadísticas técnicas"
  key_links:
    - from: "packages/rules-engine/src/skills/skill-derived-stats.ts"
      to: "apps/planner/src/features/skills/selectors.ts"
      via: "derived stats projection"
      pattern: "deriveSkillStatsView"
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "apps/planner/src/routes/stats.tsx"
      via: "read-only stats board view model"
      pattern: "selectSkillStatsView"
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "apps/planner/src/components/shell/summary-panel.tsx"
      via: "skill summary severity and plan-state labels"
      pattern: "selectSkillSummary"
---

<objective>
Complete Phase 5 by wiring derived skill statistics into the read-only `Estadísticas` route and synchronizing the shell summary with the skill evaluation pipeline from Plan 05-01.

Purpose: finish the phase outcome so skill totals and explanatory derived state stay consistent across editor, technical view, and persistent shell summary.
Output: pure derived-stats helper, routed `Estadísticas` board, summary-panel integration, and synchronization tests.
</objective>

<execution_context>
@C:/Users/pzhly/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pzhly/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-derived-statistics/05-CONTEXT.md
@.planning/phases/05-skills-derived-statistics/05-RESEARCH.md
@.planning/phases/05-skills-derived-statistics/05-UI-SPEC.md
@.planning/phases/05-skills-derived-statistics/05-01-PLAN.md
@apps/planner/src/routes/stats.tsx
@apps/planner/src/components/shell/summary-panel.tsx
@apps/planner/src/features/skills/selectors.ts
@apps/planner/src/lib/copy/es.ts
@packages/rules-engine/src/skills/skill-allocation.ts

<interfaces>
Expected from Plan 05-01:
```ts
export function selectSkillBoardView(...args): SkillBoardView;
export function evaluateSkillSnapshot(...args): SkillSnapshot;
export function revalidateSkillSnapshotAfterChange(...args): RevalidatedSkillLevel[];
```

Existing shell summary contract from `apps/planner/src/components/shell/summary-panel.tsx`:
```ts
const validationLabels = {
  blocked: 'Bloqueada',
  illegal: 'Inválida',
  legal: 'Válida',
  pending: shellCopyEs.summaryValues.validation,
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared derived-statistics projection and the read-only stats route</name>
  <files>apps/planner/src/routes/stats.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/skills/selectors.ts, apps/planner/src/features/skills/skill-stats-board.tsx, packages/rules-engine/src/skills/skill-derived-stats.ts, tests/phase-05/skill-stats-sync.spec.tsx</files>
  <read_first>apps/planner/src/routes/stats.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/skills/selectors.ts, packages/rules-engine/src/skills/skill-allocation.ts, apps/planner/src/routes/skills.tsx, apps/planner/src/features/skills/skill-board.tsx, .planning/phases/05-skills-derived-statistics/05-UI-SPEC.md, .planning/phases/05-skills-derived-statistics/05-01-PLAN.md</read_first>
  <action>Per D-02, D-07, and D-08, create `packages/rules-engine/src/skills/skill-derived-stats.ts` exporting the exact function `deriveSkillStatsView(input)`. The returned object must contain exactly three groups named `totals`, `capsAndCosts`, and `penalties`, because `Estadísticas` must stay focused and technical. `totals` must expose per-skill total ranks plus `totalRanks`, `spentPoints`, and `remainingPoints`; `capsAndCosts` must expose `classLabel`, `cap`, and `nextIncrementCost` for the active level; `penalties` must expose `restrictionCode`, `reason`, and `status` for any blocked or illegal override such as `puerta-heavy-armor-tumble`. Extend `apps/planner/src/features/skills/selectors.ts` with the exact selectors `selectSkillStatsView` and `selectSkillSummary`. Replace `PlannerSectionView` in `apps/planner/src/routes/stats.tsx` with `SkillStatsBoard`, and create `apps/planner/src/features/skills/skill-stats-board.tsx` with the exact heading `Estadísticas técnicas` and the exact read-only panel headings `Totales de habilidades`, `Topes y costes`, and `Penalizaciones y bloqueos`. Extend `apps/planner/src/lib/copy/es.ts` with the exact strings `Estadísticas técnicas`, `Totales de habilidades`, `Topes y costes`, `Penalizaciones y bloqueos`, `Total de rangos`, `Coste siguiente`, `Motivo de bloqueo`, and `Penalización`. Extend `apps/planner/src/styles/app.css` with the exact class hooks `.skill-stats-board`, `.skill-stats-group`, `.skill-stats-row`, and `.skill-stats-row--blocked`, preserving the Phase 5 dark technical-board language. Finish `tests/phase-05/skill-stats-sync.spec.tsx` so it proves `Estadísticas` stays read-only, renders the three exact groups, and shows the same cap, next cost, and blocked reason as the skill editor for the active level.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-05/skill-stats-sync.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules-engine/src/skills/skill-derived-stats.ts` contains `export function deriveSkillStatsView`
    - `packages/rules-engine/src/skills/skill-derived-stats.ts` contains `totals`
    - `packages/rules-engine/src/skills/skill-derived-stats.ts` contains `capsAndCosts`
    - `packages/rules-engine/src/skills/skill-derived-stats.ts` contains `penalties`
    - `apps/planner/src/routes/stats.tsx` contains `SkillStatsBoard`
    - `apps/planner/src/features/skills/selectors.ts` contains `selectSkillStatsView`
    - `apps/planner/src/features/skills/skill-stats-board.tsx` contains `Estadísticas técnicas`
    - `apps/planner/src/features/skills/skill-stats-board.tsx` contains `Topes y costes`
    - `apps/planner/src/lib/copy/es.ts` contains `Totales de habilidades`
    - `apps/planner/src/styles/app.css` contains `.skill-stats-board`
    - `tests/phase-05/skill-stats-sync.spec.tsx` contains `Penalizaciones y bloqueos`
    - `tests/phase-05/skill-stats-sync.spec.tsx` contains `puerta-heavy-armor-tumble`
  </acceptance_criteria>
  <done>`Estadísticas` is now a real read-only technical view backed by the same skill evaluation pipeline as the editor.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize shell summary severity with the Phase 5 skill state</name>
  <files>apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/features/skills/selectors.ts, tests/phase-05/skill-stats-sync.spec.tsx</files>
  <read_first>apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/features/skills/selectors.ts, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/routes/skills.tsx, apps/planner/src/routes/stats.tsx, .planning/phases/05-skills-derived-statistics/05-CONTEXT.md, .planning/phases/05-skills-derived-statistics/05-UI-SPEC.md</read_first>
  <action>Per D-02, D-05, D-06, D-07, and D-08, extend `apps/planner/src/features/skills/selectors.ts` with `selectSkillSummary`, returning the exact fields `highestConfiguredSkillLevel`, `planState`, `summaryStatus`, `spentPoints`, `remainingPoints`, and `blockedLevels`. Add these exact plan-state labels to `apps/planner/src/lib/copy/es.ts`: `Sin reparto de habilidades`, `Habilidades en curso`, `Habilidades en reparación`, and `Habilidades listas`. Update `apps/planner/src/components/shell/summary-panel.tsx` so the shell chooses summary state in this exact order: foundation summary when foundation is not `legal`; otherwise progression summary when progression is `blocked`, `illegal`, or has no configured levels; otherwise skill summary from `selectSkillSummary`. The shell must display `Habilidades en reparación` when any skill level is inherited-blocked, `Habilidades listas` when all configured skill levels are coherent, and keep the existing exact validation labels `Bloqueada`, `Inválida`, and `Válida`. Extend `tests/phase-05/skill-stats-sync.spec.tsx` so one assertion edits skills in `Habilidades`, another reads `Estadísticas`, and a final assertion checks the persistent summary panel for the exact label `Habilidades en reparación` after an upstream break and `Habilidades listas` after repair.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-05/skill-stats-sync.spec.tsx tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/features/skills/selectors.ts` contains `selectSkillSummary`
    - `apps/planner/src/features/skills/selectors.ts` contains `blockedLevels`
    - `apps/planner/src/lib/copy/es.ts` contains `Sin reparto de habilidades`
    - `apps/planner/src/lib/copy/es.ts` contains `Habilidades en reparación`
    - `apps/planner/src/lib/copy/es.ts` contains `Habilidades listas`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `Habilidades en reparación`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `Habilidades listas`
    - `tests/phase-05/skill-stats-sync.spec.tsx` contains `Habilidades en reparación`
    - `tests/phase-05/skill-stats-sync.spec.tsx` contains `Habilidades listas`
    - `tests/phase-05/skill-stats-sync.spec.tsx` contains `Estadísticas técnicas`
  </acceptance_criteria>
  <done>The shell summary, skill editor, and technical stats route all project the same Phase 5 severity and plan-state vocabulary.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm vitest run tests/phase-05 --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- `Estadísticas` stays read-only and technical while mirroring the exact skill snapshot
- Summary, editor, and stats stay synchronized after both skill edits and upstream progression changes
- Phase 5 ends with explicit blocked/illegal/ready messaging instead of divergent route-local math
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-derived-statistics/05-02-SUMMARY.md`
</output>
