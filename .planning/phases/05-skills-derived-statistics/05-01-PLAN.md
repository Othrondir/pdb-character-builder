---
phase: 05-skills-derived-statistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - apps/planner/src/routes/skills.tsx
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/skills/skill-fixture.ts
  - apps/planner/src/features/skills/store.ts
  - apps/planner/src/features/skills/selectors.ts
  - apps/planner/src/features/skills/skill-board.tsx
  - apps/planner/src/features/skills/skill-summary-strip.tsx
  - apps/planner/src/features/skills/skill-rail.tsx
  - apps/planner/src/features/skills/skill-sheet.tsx
  - packages/rules-engine/src/skills/skill-allocation.ts
  - packages/rules-engine/src/skills/skill-revalidation.ts
  - tests/phase-05/skill-rules.spec.ts
  - tests/phase-05/skill-revalidation.spec.tsx
  - tests/phase-05/skill-allocation-flow.spec.tsx
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03]
user_setup: []
must_haves:
  truths:
    - "El usuario puede abrir `Habilidades`, elegir un nivel del 1 al 16 y editar solo la hoja activa de ese nivel."
    - "Cada rango gastado usa la clase del nivel activo, los costes clase/transclase y los topes exactos del snapshot actual de la build."
    - "Si cambia una decisión anterior, los repartos posteriores se conservan y quedan marcados como `Bloqueada` o `Inválida` hasta repararlos."
  artifacts:
    - path: "apps/planner/src/features/skills/store.ts"
      provides: "Estado bruto por nivel para reparto de habilidades"
      contains: "setSkillRanks"
    - path: "packages/rules-engine/src/skills/skill-allocation.ts"
      provides: "Evaluador puro de costes, topes, puntos y restricciones"
      contains: "evaluateSkillSnapshot"
    - path: "packages/rules-engine/src/skills/skill-revalidation.ts"
      provides: "Revalidación preserve-first para cambios en niveles anteriores"
      contains: "revalidateSkillSnapshotAfterChange"
    - path: "apps/planner/src/routes/skills.tsx"
      provides: "Pantalla real de Habilidades"
      contains: "SkillBoard"
    - path: "tests/phase-05/skill-allocation-flow.spec.tsx"
      provides: "Prueba de flujo rail + hoja activa"
      contains: "Habilidades 1-16"
  key_links:
    - from: "apps/planner/src/features/skills/store.ts"
      to: "packages/rules-engine/src/skills/skill-allocation.ts"
      via: "raw per-level allocations evaluated into derived skill state"
      pattern: "evaluateSkillSnapshot"
    - from: "packages/rules-engine/src/skills/skill-revalidation.ts"
      to: "apps/planner/src/features/skills/selectors.ts"
      via: "inherited repair metadata"
      pattern: "inheritedFromLevel"
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "apps/planner/src/features/skills/skill-board.tsx"
      via: "shared rail, strip, and active sheet projection"
      pattern: "selectSkillBoardView"
---

<objective>
Deliver the Phase 5 skill-editing backbone: raw per-level skill state, pure legality and revalidation helpers, and the routed `Habilidades` editor that mirrors the proven Phase 4 rail-plus-sheet workflow.

Purpose: satisfy the editable half of the phase first so skill allocation becomes possible before the read-only technical stats surface is wired.
Output: skill fixture and store, pure rules-engine evaluators, preserved-downstream repair model, routed `Habilidades` UI, and Phase 5 skill tests.
</objective>

<execution_context>
@C:/Users/pzhly/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pzhly/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-derived-statistics/05-CONTEXT.md
@.planning/phases/05-skills-derived-statistics/05-RESEARCH.md
@.planning/phases/05-skills-derived-statistics/05-UI-SPEC.md
@.planning/phases/04-level-progression-class-path/04-CONTEXT.md
@.planning/phases/04-level-progression-class-path/04-02-SUMMARY.md
@.planning/phases/04-level-progression-class-path/04-03-SUMMARY.md
@apps/planner/src/routes/skills.tsx
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/features/level-progression/selectors.ts
@apps/planner/src/features/level-progression/progression-fixture.ts
@apps/planner/src/components/shell/summary-panel.tsx
@apps/planner/src/lib/copy/es.ts
@packages/rules-engine/src/contracts/canonical-id.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
@packages/rules-engine/src/progression/progression-revalidation.ts
@vitest.config.ts

<interfaces>
From `apps/planner/src/features/level-progression/store.ts`:
```ts
export interface LevelProgressionStoreState {
  activeLevel: ProgressionLevel;
  datasetId: string;
  lastEditedLevel: ProgressionLevel | null;
  levels: ProgressionLevelRecord[];
  resetProgression: () => void;
  setActiveLevel: (level: ProgressionLevel) => void;
  setLevelAbilityIncrease: (
    level: ProgressionLevel,
    abilityIncrease: AttributeKey | null,
  ) => void;
  setLevelClassId: (level: ProgressionLevel, classId: CanonicalId | null) => void;
}
```

From `apps/planner/src/features/level-progression/progression-fixture.ts`:
```ts
export type ProgressionStatus = 'blocked' | 'illegal' | 'legal' | 'pending';

export interface ProgressionLevelRecord {
  abilityIncrease: AttributeKey | null;
  classId: CanonicalId | null;
  level: ProgressionLevel;
}
```

From `packages/rules-engine/src/contracts/validation-outcome.ts`:
```ts
export type ValidationStatus = 'legal' | 'illegal' | 'blocked';
export type ValidationOutcome = z.infer<typeof validationOutcomeSchema>;

export function resolveValidationOutcome(
  input: ResolveValidationOutcomeInput,
): ValidationOutcome
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 5 skill contracts, fixture, and test harness</name>
  <files>vitest.config.ts, apps/planner/src/features/skills/skill-fixture.ts, apps/planner/src/features/skills/store.ts, tests/phase-05/skill-rules.spec.ts, tests/phase-05/skill-revalidation.spec.tsx, tests/phase-05/skill-allocation-flow.spec.tsx</files>
  <read_first>vitest.config.ts, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/level-progression/progression-fixture.ts, apps/planner/src/lib/copy/es.ts, packages/rules-engine/src/contracts/canonical-id.ts, .planning/phases/05-skills-derived-statistics/05-CONTEXT.md, .planning/phases/05-skills-derived-statistics/05-RESEARCH.md, .planning/phases/05-skills-derived-statistics/05-UI-SPEC.md</read_first>
  <action>Per D-03 and D-04, create `apps/planner/src/features/skills/store.ts` with the exact interface `SkillStoreState { activeLevel: ProgressionLevel; datasetId: string; lastEditedLevel: ProgressionLevel | null; levels: SkillAllocationRecord[]; resetSkills(): void; setActiveLevel(level: ProgressionLevel): void; setSkillRanks(level: ProgressionLevel, skillId: CanonicalId, ranks: number): void; }`. Use `createEmptySkillAllocations()` to prebuild a fixed 16-level array matching the Phase 4 preserve-first model. Create `apps/planner/src/features/skills/skill-fixture.ts` with the exact exported records `skill:discipline`, `skill:tumble`, `skill:concentration`, `skill:lore`, and `skill:heal`. Each record must include the exact fields `id`, `label`, `abilityKey`, `category`, `trainedOnly`, `defaultClassIds`, and `restrictionCode`. Set `skill:tumble.restrictionCode` to the exact string `puerta-heavy-armor-tumble`; set its `defaultClassIds` to `['class:monk', 'class:rogue']`; set `skill:discipline.defaultClassIds` to `['class:fighter', 'class:paladin']`; set `skill:concentration.defaultClassIds` to `['class:bard', 'class:cleric', 'class:paladin', 'class:sorcerer', 'class:wizard']`; set `skill:lore.defaultClassIds` to `['class:bard', 'class:wizard']`; set `skill:heal.defaultClassIds` to `['class:cleric', 'class:druid', 'class:ranger']`. Update `vitest.config.ts` so `tests/phase-05/**/*.spec.tsx` runs in `jsdom`. Create the three Phase 5 test files with exact describe blocks for `skill allocation rules`, `skill preserve-first revalidation`, and `skills route flow`, and make the assertions reference the exact Spanish copy strings `Habilidades 1-16`, `Hoja de habilidades`, `Puntos gastados`, `Puntos restantes`, and `Este nivel conserva sus rangos, pero depende de corregir decisiones anteriores.`.</action>
  <verify>
    <automated>corepack pnpm exec tsc -p tsconfig.base.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `tests/phase-05/**/*.spec.tsx`
    - `apps/planner/src/features/skills/store.ts` contains `export interface SkillStoreState`
    - `apps/planner/src/features/skills/store.ts` contains `setSkillRanks`
    - `apps/planner/src/features/skills/skill-fixture.ts` contains `skill:tumble`
    - `apps/planner/src/features/skills/skill-fixture.ts` contains `puerta-heavy-armor-tumble`
    - `apps/planner/src/features/skills/skill-fixture.ts` contains `defaultClassIds`
    - `tests/phase-05/skill-rules.spec.ts` contains `skill allocation rules`
    - `tests/phase-05/skill-revalidation.spec.tsx` contains `Este nivel conserva sus rangos, pero depende de corregir decisiones anteriores.`
    - `tests/phase-05/skill-allocation-flow.spec.tsx` contains `Habilidades 1-16`
    - `tests/phase-05/skill-allocation-flow.spec.tsx` contains `Hoja de habilidades`
  </acceptance_criteria>
  <done>The repo has Phase 5 source-of-truth skill records, a dedicated per-level skill store, and runnable test files prepared for the real implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure skill evaluation and preserved-downstream revalidation</name>
  <files>packages/rules-engine/src/skills/skill-allocation.ts, packages/rules-engine/src/skills/skill-revalidation.ts, tests/phase-05/skill-rules.spec.ts, tests/phase-05/skill-revalidation.spec.tsx</files>
  <read_first>packages/rules-engine/src/contracts/validation-outcome.ts, packages/rules-engine/src/progression/progression-revalidation.ts, apps/planner/src/features/skills/skill-fixture.ts, apps/planner/src/features/skills/store.ts, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/level-progression/progression-fixture.ts, .planning/phases/05-skills-derived-statistics/05-CONTEXT.md, .planning/phases/05-skills-derived-statistics/05-RESEARCH.md</read_first>
  <action>Per D-05, D-06, and D-07, create `packages/rules-engine/src/skills/skill-allocation.ts` exporting the exact functions `evaluateSkillLevel(input)` and `evaluateSkillSnapshot(input)`. `evaluateSkillLevel` must calculate `availablePoints`, `spentPoints`, `remainingPoints`, `status`, `issues`, and `skills` for one level using these exact rules: class skills cap at `level + 3`, cross-class skills cap at `Math.floor((level + 3) / 2)`, the next increment cost is `1` for `Clase` skills and `2` for `Transclase` skills, and overspending or exceeding a cap resolves `illegal` through `resolveValidationOutcome`. The input shape must include the exact field `restrictionOverrides: Array<{ skillId: CanonicalId; code: string; status: 'blocked' | 'illegal'; reason: string; }>` so SKIL-03 stays dataset-driven. When a restriction override exists for `skill:tumble` with code `puerta-heavy-armor-tumble`, return the exact override status and reason instead of a warning. Create `packages/rules-engine/src/skills/skill-revalidation.ts` exporting `revalidateSkillSnapshotAfterChange(input)` and `RevalidatedSkillLevel`, with exact output fields `level`, `status`, `issues`, `inheritedFromLevel`, `availablePoints`, `spentPoints`, and `remainingPoints`. The helper must preserve every later allocation record after an earlier class or INT change and mark inherited breakage with `inheritedFromLevel` instead of deleting entries. Extend `tests/phase-05/skill-rules.spec.ts` so it proves a class-skill rank spends `1`, a cross-class rank spends `2`, an over-cap allocation is `illegal`, and a `puerta-heavy-armor-tumble` override becomes `illegal` or `blocked` exactly as the override says. Extend `tests/phase-05/skill-revalidation.spec.tsx` so it proves changing an earlier class leaves later level records in place and emits `inheritedFromLevel` plus the exact repair callout copy.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules-engine/src/skills/skill-allocation.ts` contains `export function evaluateSkillLevel`
    - `packages/rules-engine/src/skills/skill-allocation.ts` contains `export function evaluateSkillSnapshot`
    - `packages/rules-engine/src/skills/skill-allocation.ts` contains `restrictionOverrides`
    - `packages/rules-engine/src/skills/skill-allocation.ts` contains `Math.floor((level + 3) / 2)`
    - `packages/rules-engine/src/skills/skill-revalidation.ts` contains `export function revalidateSkillSnapshotAfterChange`
    - `packages/rules-engine/src/skills/skill-revalidation.ts` contains `inheritedFromLevel`
    - `tests/phase-05/skill-rules.spec.ts` contains `puerta-heavy-armor-tumble`
    - `tests/phase-05/skill-rules.spec.ts` contains `Transclase`
    - `tests/phase-05/skill-revalidation.spec.tsx` contains `inheritedFromLevel`
    - `tests/phase-05/skill-revalidation.spec.tsx` contains `Este nivel conserva sus rangos, pero depende de corregir decisiones anteriores.`
  </acceptance_criteria>
  <done>The project has one pure skill-allocation truth for caps, costs, remaining points, server overrides, and preserve-first downstream repair.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the placeholder skills route with the rail-plus-sheet editor</name>
  <files>apps/planner/src/routes/skills.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/skills/selectors.ts, apps/planner/src/features/skills/skill-board.tsx, apps/planner/src/features/skills/skill-summary-strip.tsx, apps/planner/src/features/skills/skill-rail.tsx, apps/planner/src/features/skills/skill-sheet.tsx, tests/phase-05/skill-allocation-flow.spec.tsx</files>
  <read_first>apps/planner/src/routes/skills.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/skills/store.ts, apps/planner/src/features/skills/skill-fixture.ts, packages/rules-engine/src/skills/skill-allocation.ts, packages/rules-engine/src/skills/skill-revalidation.ts, .planning/phases/05-skills-derived-statistics/05-UI-SPEC.md</read_first>
  <action>Per D-01, D-03, D-04, D-05, and D-06, replace `PlannerSectionView` in `apps/planner/src/routes/skills.tsx` with `SkillBoard`. Create `apps/planner/src/features/skills/selectors.ts` exporting the exact functions `selectSkillBoardView`, `selectSkillRail`, `selectActiveSkillSheetView`, and `selectSkillSummaryStrip`, all driven from `foundation + progression + skill store -> evaluateSkillSnapshot -> revalidateSkillSnapshotAfterChange`. Create `SkillBoard` with the exact layout `summary strip -> left rail -> right active sheet`. Create `SkillSummaryStrip` showing the exact fields `Clase actual`, `Puntos disponibles`, `Puntos gastados`, `Puntos restantes`, and `Estado actual`. Create `SkillRail` with the exact heading `Habilidades 1-16` and level entries that render `Bloqueada`, `Inválida`, or `Pendiente` directly in the row. Create `SkillSheet` with the exact heading `Hoja de habilidades`; each row must render current ranks for the active level, `Clase` or `Transclase`, `Tope`, `Siguiente coste`, and an inline blocked/invalid reason when editing is disallowed. Extend `apps/planner/src/lib/copy/es.ts` with the exact labels `Habilidades 1-16`, `Hoja de habilidades`, `Puntos disponibles`, `Puntos gastados`, `Puntos restantes`, `Clase`, `Transclase`, `Tope`, `Siguiente coste`, `Ajustar habilidades`, `Las habilidades siguen bloqueadas`, `Completa una progresión válida en Construcción para repartir rangos por nivel.`, `Reparto bloqueado: revisa la clase, la Inteligencia o la restricción marcada y corrige el nivel anterior para continuar.`, and `Este nivel conserva sus rangos, pero depende de corregir decisiones anteriores.`. Extend `apps/planner/src/styles/app.css` with the exact class hooks `.skills-board`, `.skills-summary-strip`, `.skill-rail`, `.skill-sheet`, `.skill-row__status`, `.skill-row__controls`, and `.skill-sheet__repair-callout`, keeping blocked states bronze-muted and illegal states dark red per the UI spec. Finish `tests/phase-05/skill-allocation-flow.spec.tsx` so it proves the rail switches the active level, the sheet shows `Clase` or `Transclase` and `Puntos restantes`, and preserved later rows become `Bloqueada` or `Inválida` after an upstream progression change.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-05/skill-allocation-flow.spec.tsx tests/phase-05/skill-revalidation.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/routes/skills.tsx` contains `SkillBoard`
    - `apps/planner/src/features/skills/selectors.ts` contains `selectSkillBoardView`
    - `apps/planner/src/features/skills/selectors.ts` contains `selectActiveSkillSheetView`
    - `apps/planner/src/features/skills/skill-rail.tsx` contains `Habilidades 1-16`
    - `apps/planner/src/features/skills/skill-sheet.tsx` contains `Hoja de habilidades`
    - `apps/planner/src/features/skills/skill-sheet.tsx` contains `Siguiente coste`
    - `apps/planner/src/lib/copy/es.ts` contains `Ajustar habilidades`
    - `apps/planner/src/lib/copy/es.ts` contains `Reparto bloqueado: revisa la clase, la Inteligencia o la restricción marcada y corrige el nivel anterior para continuar.`
    - `apps/planner/src/styles/app.css` contains `.skills-board`
    - `apps/planner/src/styles/app.css` contains `.skill-sheet__repair-callout`
    - `tests/phase-05/skill-allocation-flow.spec.tsx` contains `Puntos restantes`
    - `tests/phase-05/skill-allocation-flow.spec.tsx` contains `Bloqueada`
  </acceptance_criteria>
  <done>The `Habilidades` route is a real level-focused skill editor that reuses the established rail-plus-sheet model and preserves later work under repair.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm vitest run tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.tsx tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- `Habilidades` owns all skill editing for levels 1-16
- Costs, caps, and server restrictions resolve through shared pure helpers instead of JSX conditionals
- Earlier-level changes preserve later skill allocations and surface repair state explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-derived-statistics/05-01-SUMMARY.md`
</output>
