---
phase: 05-skills-derived-statistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/data-extractor/src/contracts/skill-catalog.ts
  - apps/planner/src/features/skills/compiled-skill-catalog.ts
  - apps/planner/src/features/skills/store.ts
  - packages/rules-engine/src/skills/skill-allocation.ts
  - packages/rules-engine/src/skills/skill-revalidation.ts
  - tests/phase-05/skill-dataset-contract.spec.ts
  - tests/phase-05/skill-rules.spec.ts
  - tests/phase-05/skill-revalidation.spec.ts
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03]
user_setup: []
must_haves:
  truths:
    - "El evaluador de habilidades de Phase 5 resuelve en runtime todo el catalogo de habilidades de Puerta recibido en el dataset compartido, no solo un subconjunto fijo."
    - "Las restricciones especificas del servidor se bloquean usando el payload compilado y sus overrides compartidos, sin depender de constantes declaradas solo en la UI."
    - "Si cambia una decision anterior, los repartos posteriores se conservan y quedan marcados como `Bloqueada` o `Invalida` hasta repararlos."
  artifacts:
    - path: "packages/data-extractor/src/contracts/skill-catalog.ts"
      provides: "Contrato tipado del catalogo compilado de habilidades y overrides"
      contains: "skillCatalogSchema"
    - path: "apps/planner/src/features/skills/compiled-skill-catalog.ts"
      provides: "Payload runtime con datasetId, skill records completos y restriction overrides"
      contains: "compiledSkillCatalog"
    - path: "packages/rules-engine/src/skills/skill-allocation.ts"
      provides: "Evaluador puro de puntos, topes, costes y restricciones"
      contains: "evaluateSkillSnapshot"
    - path: "packages/rules-engine/src/skills/skill-revalidation.ts"
      provides: "Revalidacion preserve-first para cambios aguas arriba"
      contains: "revalidateSkillSnapshotAfterChange"
  key_links:
    - from: "apps/planner/src/features/skills/compiled-skill-catalog.ts"
      to: "packages/data-extractor/src/contracts/skill-catalog.ts"
      via: "runtime payload validated against extractor contract"
      pattern: "skillCatalogSchema"
    - from: "packages/rules-engine/src/skills/skill-allocation.ts"
      to: "apps/planner/src/features/skills/store.ts"
      via: "raw per-level allocations evaluated against compiled skill definitions"
      pattern: "evaluateSkillSnapshot"
    - from: "packages/rules-engine/src/skills/skill-revalidation.ts"
      to: "tests/phase-05/skill-revalidation.spec.ts"
      via: "preserved downstream repair contract"
      pattern: "inheritedFromLevel"
---

<objective>
Define the dataset-driven Phase 5 backbone before any route UI work: compiled Puerta skill catalog contracts, runtime catalog consumption, pure legality helpers, preserved-downstream revalidation, and focused test scaffolding.

Purpose: address SKIL-01, SKIL-02, and SKIL-03 with runtime truth that comes from compiled dataset outputs plus override metadata instead of a hand-picked fixture.
Output: extractor-facing skill catalog contract, runtime compiled skill catalog module, per-level skill store, pure rules helpers, and Phase 5 dataset or rules tests.
</objective>

<execution_context>
@C:/Users/pzhly/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pzhly/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-derived-statistics/05-CONTEXT.md
@.planning/phases/05-skills-derived-statistics/05-RESEARCH.md
@.planning/phases/05-skills-derived-statistics/05-VALIDATION.md
@.planning/phases/01-canonical-puerta-dataset/01-02-SUMMARY.md
@apps/planner/src/features/character-foundation/foundation-fixture.ts
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/features/level-progression/progression-fixture.ts
@packages/data-extractor/src/contracts/dataset-manifest.ts
@packages/data-extractor/src/contracts/override-registry.ts
@packages/rules-engine/src/contracts/canonical-id.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
@packages/rules-engine/src/progression/progression-revalidation.ts

<interfaces>
From `apps/planner/src/features/level-progression/store.ts`:
```ts
export interface LevelProgressionStoreState {
  activeLevel: ProgressionLevel;
  datasetId: string;
  lastEditedLevel: ProgressionLevel | null;
  levels: ProgressionLevelRecord[];
  resetProgression: () => void;
  setActiveLevel: (level: ProgressionLevel) => void;
  setLevelAbilityIncrease: (
    level: ProgressionLevel,
    abilityIncrease: AttributeKey | null,
  ) => void;
  setLevelClassId: (level: ProgressionLevel, classId: CanonicalId | null) => void;
}
```

From `packages/data-extractor/src/contracts/dataset-manifest.ts`:
```ts
export const datasetIdSchema = z.string().regex(datasetIdRegex, {
  message: 'datasetId must match puerta-ee-YYYY-MM-DD+<hash>.',
});
```

From `packages/rules-engine/src/contracts/validation-outcome.ts`:
```ts
export type ValidationStatus = 'legal' | 'illegal' | 'blocked';

export function resolveValidationOutcome(
  input: ResolveValidationOutcomeInput,
): ValidationOutcome
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the compiled skill catalog contract and runtime catalog input</name>
  <files>packages/data-extractor/src/contracts/skill-catalog.ts, apps/planner/src/features/skills/compiled-skill-catalog.ts, apps/planner/src/features/skills/store.ts, tests/phase-05/skill-dataset-contract.spec.ts</files>
  <action>Per D-01, D-04, and D-07, create `packages/data-extractor/src/contracts/skill-catalog.ts` as the extractor-facing schema for the full compiled Puerta skill catalog. The schema must carry `datasetId`, `schemaVersion`, `skills`, and `restrictionOverrides`, and each skill record must include at least `id`, `label`, `abilityKey`, `category`, `trainedOnly`, `defaultClassIds`, and provenance-friendly restriction metadata. Create `apps/planner/src/features/skills/compiled-skill-catalog.ts` as the runtime compiled dataset output consumed by the browser for Phase 5. It must explicitly represent the full catalog payload for the active dataset and expose override metadata for server restrictions such as heavy-armor tumble; do not create or reference `skill-fixture.ts`, and do not limit runtime truth to a hand-picked subset of skills. Create `apps/planner/src/features/skills/store.ts` with a per-level raw allocation state matching the Phase 4 store shape and carrying `datasetId` from the compiled skill catalog. Any fixture-style helper introduced in this plan must stay under tests only; runtime imports must read from `compiled-skill-catalog.ts` and nowhere else. Add `tests/phase-05/skill-dataset-contract.spec.ts` to prove the compiled runtime payload conforms to `skillCatalogSchema`, contains more than the prior five-skill subset, and surfaces restriction override metadata without relying on UI-local constants.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-dataset-contract.spec.ts</automated>
  </verify>
  <done>The phase has a dataset-driven runtime skill catalog contract and store input, and any fixture usage is limited to test scaffolding instead of runtime truth.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure skill evaluation and preserved-downstream revalidation against the compiled catalog</name>
  <files>packages/rules-engine/src/skills/skill-allocation.ts, packages/rules-engine/src/skills/skill-revalidation.ts, apps/planner/src/features/skills/store.ts, tests/phase-05/skill-rules.spec.ts, tests/phase-05/skill-revalidation.spec.ts</files>
  <action>Per D-05, D-06, and D-07, implement `packages/rules-engine/src/skills/skill-allocation.ts` so `evaluateSkillLevel(...)` and `evaluateSkillSnapshot(...)` read skill definitions and restriction overrides from the compiled catalog input created in Task 1. Compute available points, spent points, remaining points, class or transclase costs, topes, and blocked or illegal outcomes through `resolveValidationOutcome(...)`, with no route-level constants and no narrow runtime fixture. Preserve the Phase 4 repair model in `packages/rules-engine/src/skills/skill-revalidation.ts` via `revalidateSkillSnapshotAfterChange(...)`, keeping later allocation records intact after upstream class or INT changes and surfacing `inheritedFromLevel` instead of truncating data. Extend `tests/phase-05/skill-rules.spec.ts` and `tests/phase-05/skill-revalidation.spec.ts` so they prove class versus transclase costs, level caps, compiled restriction overrides, and preserve-first downstream repair against the dataset-driven input from Task 1.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.ts --reporter=dot</automated>
  </verify>
  <done>The repo has one pure, dataset-driven skill legality pipeline plus a preserve-first revalidation helper that downstream UI plans can consume directly.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm vitest --run tests/phase-05/skill-dataset-contract.spec.ts tests/phase-05/skill-rules.spec.ts tests/phase-05/skill-revalidation.spec.ts --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
</verification>

<success_criteria>

- Runtime skill truth comes from a compiled Puerta catalog payload, not a narrow hand-authored fixture
- Server restriction metadata is available to rules helpers through compiled dataset input
- Preserve-first revalidation keeps later skill edits visible after upstream changes
- Phase 05 has dataset and rules tests that can run before any `Habilidades` UI work
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-derived-statistics/05-01-SUMMARY.md`
</output>
