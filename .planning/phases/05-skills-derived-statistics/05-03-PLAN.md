---
phase: 05-skills-derived-statistics
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - apps/planner/src/routes/stats.tsx
  - apps/planner/src/components/shell/summary-panel.tsx
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/skills/selectors.ts
  - apps/planner/src/features/skills/skill-stats-board.tsx
  - packages/rules-engine/src/skills/skill-derived-stats.ts
  - tests/phase-05/skill-stats-sync.spec.tsx
autonomous: true
requirements: [SKIL-02, SKIL-03]
user_setup: []
must_haves:
  truths:
    - "La pantalla `Estadisticas` muestra un resumen tecnico de solo lectura calculado desde el mismo snapshot dataset-driven que usa `Habilidades`."
    - "Los topes, costes, totales y bloqueos visibles en `Estadisticas` coinciden con los que ve el usuario al editar habilidades."
    - "El resumen persistente del shell refleja el estado mas grave del reparto de habilidades sin ocultar foundation o progression cuando sigan bloqueadas."
  artifacts:
    - path: "packages/rules-engine/src/skills/skill-derived-stats.ts"
      provides: "Proyeccion pura para totales, topes, costes y penalizaciones"
      contains: "deriveSkillStatsView"
    - path: "apps/planner/src/routes/stats.tsx"
      provides: "Pantalla real de Estadisticas tecnicas"
      contains: "SkillStatsBoard"
    - path: "tests/phase-05/skill-stats-sync.spec.tsx"
      provides: "Cobertura de sincronizacion entre editor, stats y summary"
      contains: "Estadisticas tecnicas"
  key_links:
    - from: "packages/rules-engine/src/skills/skill-derived-stats.ts"
      to: "apps/planner/src/features/skills/selectors.ts"
      via: "read-only stats projection over the evaluated skill snapshot"
      pattern: "deriveSkillStatsView"
    - from: "apps/planner/src/features/skills/selectors.ts"
      to: "apps/planner/src/components/shell/summary-panel.tsx"
      via: "shared summary status and plan-state labels"
      pattern: "selectSkillSummary"
    - from: "apps/planner/src/routes/stats.tsx"
      to: "apps/planner/src/features/skills/skill-stats-board.tsx"
      via: "route ownership for read-only Estadisticas"
      pattern: "SkillStatsBoard"
---

<objective>
Finish Phase 5 by projecting the shared skill evaluation pipeline into the read-only `Estadisticas` route and the persistent shell summary.

Purpose: complete SKIL-02 and SKIL-03 with synchronized derived totals, caps, costs, and blocked-rule explanations that never diverge from `Habilidades`.
Output: pure derived-stat projection, routed `Estadisticas` board, summary-panel integration, and synchronization coverage.
</objective>

<execution_context>
@C:/Users/pzhly/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/pzhly/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-skills-derived-statistics/05-CONTEXT.md
@.planning/phases/05-skills-derived-statistics/05-RESEARCH.md
@.planning/phases/05-skills-derived-statistics/05-UI-SPEC.md
@.planning/phases/05-skills-derived-statistics/05-02-PLAN.md
@apps/planner/src/routes/stats.tsx
@apps/planner/src/components/shell/summary-panel.tsx
@apps/planner/src/lib/copy/es.ts
@apps/planner/src/styles/app.css

<interfaces>
Expected from previous plans:
```ts
export function evaluateSkillSnapshot(...args): SkillSnapshot;
export function deriveSkillStatsView(...args): SkillStatsView;
export function selectSkillBoardView(...args): SkillBoardView;
export function selectSkillSummary(...args): SkillSummaryView;
```

Existing summary labels in `apps/planner/src/components/shell/summary-panel.tsx`:
```ts
const validationLabels = {
  blocked: 'Bloqueada',
  illegal: 'Inválida',
  legal: 'Válida',
  pending: shellCopyEs.summaryValues.validation,
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared derived-statistics projection and the read-only stats route</name>
  <files>packages/rules-engine/src/skills/skill-derived-stats.ts, apps/planner/src/features/skills/selectors.ts, apps/planner/src/routes/stats.tsx, apps/planner/src/features/skills/skill-stats-board.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, tests/phase-05/skill-stats-sync.spec.tsx</files>
  <action>Per D-02, D-07, and D-08, create `packages/rules-engine/src/skills/skill-derived-stats.ts` with `deriveSkillStatsView(...)` returning exactly the three read-only groups required by the UI spec: totals, caps and costs, and penalties or blocked reasons. Extend `apps/planner/src/features/skills/selectors.ts` with `selectSkillStatsView` so the route consumes the same evaluated snapshot used by `Habilidades`; do not recalculate totals or restriction messages inside JSX. Replace the placeholder stats route with `SkillStatsBoard`, and keep the route strictly read-only and technical. Finish `tests/phase-05/skill-stats-sync.spec.tsx` so it proves `Estadisticas` shows the same cap, next cost, and blocked reason as the active `Habilidades` level for the current dataset-driven snapshot.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-stats-sync.spec.tsx --reporter=dot</automated>
  </verify>
  <done>`Estadisticas` becomes a real read-only view over the same skill snapshot as the editor, with no duplicated route-local math.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize shell summary severity with the Phase 5 skill state</name>
  <files>apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/features/skills/selectors.ts, apps/planner/src/lib/copy/es.ts, tests/phase-05/skill-stats-sync.spec.tsx</files>
  <action>Per D-02, D-05, D-06, D-07, and D-08, add `selectSkillSummary(...)` to the shared selectors and update `summary-panel.tsx` so summary priority stays foundation first, then progression, then skills. The skill summary must expose the highest configured level, current status, blocked levels, spent points, remaining points, and Phase 5 plan-state labels such as `Sin reparto de habilidades`, `Habilidades en curso`, `Habilidades en reparacion`, and `Habilidades listas`. Extend `tests/phase-05/skill-stats-sync.spec.tsx` so one assertion edits skills in `Habilidades`, one reads `Estadisticas`, and the final assertion checks the shell summary for `Habilidades en reparacion` after an upstream break and `Habilidades listas` after repair.</action>
  <verify>
    <automated>corepack pnpm vitest --run tests/phase-05/skill-stats-sync.spec.tsx tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot</automated>
  </verify>
  <done>The shell summary, `Habilidades`, and `Estadisticas` all project the same Phase 5 severity and plan-state vocabulary from one shared selector pipeline.</done>
</task>

</tasks>

<verification>
- [ ] `corepack pnpm vitest --run tests/phase-05/skill-stats-sync.spec.tsx tests/phase-05/skill-allocation-flow.spec.tsx --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- `Estadisticas` stays read-only and technical while mirroring the evaluated skill snapshot from `Habilidades`
- Summary, editor, and stats remain synchronized after both skill edits and upstream progression changes
- Phase 5 exposes blocked or illegal server restrictions through shared selector output rather than route-local constants
</success_criteria>

<output>
After completion, create `.planning/phases/05-skills-derived-statistics/05-03-SUMMARY.md`
</output>
