---
phase: 03-character-origin-base-attributes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/routes/root.tsx
  - apps/planner/src/routes/abilities.tsx
  - apps/planner/src/components/shell/summary-panel.tsx
  - packages/rules-engine/src/contracts/canonical-id.ts
  - apps/planner/src/features/character-foundation/foundation-fixture.ts
  - apps/planner/src/features/character-foundation/store.ts
  - apps/planner/src/features/character-foundation/selectors.ts
  - apps/planner/src/features/character-foundation/origin-board.tsx
  - apps/planner/src/features/character-foundation/locked-abilities-panel.tsx
  - tests/phase-01/schema-contract.spec.ts
  - tests/phase-03/origin-flow.spec.tsx
  - tests/phase-03/summary-status.spec.tsx
autonomous: true
requirements: [CHAR-01, CHAR-02, CHAR-03]
user_setup: []
must_haves:
  truths:
    - "Phase 3 keeps origin editing in `Construcción` and blocks `Atributos` until the foundation state is coherent enough to continue."
    - "Foundation state stores canonical IDs plus an explicit dataset identifier instead of localized labels."
    - "The persistent summary panel stops being placeholder-only and reflects the selected origin state."
  artifacts:
    - path: "apps/planner/src/features/character-foundation/foundation-fixture.ts"
      provides: "Static compiled Phase 3 origin and attribute data source"
      contains: "puerta-ee-2026-03-30+phase03"
    - path: "packages/rules-engine/src/contracts/canonical-id.ts"
      provides: "Expanded canonical entity-kind contract for Phase 3 foundation entities"
      contains: "alignment"
    - path: "apps/planner/src/features/character-foundation/store.ts"
      provides: "Dedicated character-foundation state separate from shell chrome"
      contains: "useCharacterFoundationStore"
    - path: "apps/planner/src/features/character-foundation/origin-board.tsx"
      provides: "Route-owned stepped origin UI"
      contains: "Sin deidad"
    - path: "tests/phase-03/origin-flow.spec.tsx"
      provides: "Render coverage for stepped origin flow and `Atributos` lock state"
      contains: "El origen del personaje sigue incompleto"
  key_links:
    - from: "apps/planner/src/features/character-foundation/foundation-fixture.ts"
      to: "apps/planner/src/features/character-foundation/store.ts"
      via: "shared dataset id and canonical option catalogs"
      pattern: "datasetId"
    - from: "packages/rules-engine/src/contracts/canonical-id.ts"
      to: "apps/planner/src/features/character-foundation/foundation-fixture.ts"
      via: "new entity kinds required for origin, subrace, alignment, and deity IDs"
      pattern: "deity"
    - from: "apps/planner/src/features/character-foundation/selectors.ts"
      to: "apps/planner/src/routes/abilities.tsx"
      via: "origin readiness gate for the attributes screen"
      pattern: "selectOriginReadyForAbilities"
    - from: "apps/planner/src/features/character-foundation/selectors.ts"
      to: "apps/planner/src/components/shell/summary-panel.tsx"
      via: "summary projection and validation status sync"
      pattern: "selectFoundationSummary"
---

<objective>
Create the Phase 3 character-foundation dataset adapter, state model, stepped origin screen, and summary-panel wiring so the planner can capture race, subrace, alignment, and deity before attribute rules are applied.

Purpose: land the route-owned state and UI backbone first, without mixing in the full restriction or budget engine yet.
Output: compiled foundation fixture, dedicated state slice, `Construcción` origin board, locked `Atributos` panel, summary projection, and Phase 3 render tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-character-origin-base-attributes/03-CONTEXT.md
@.planning/phases/03-character-origin-base-attributes/03-RESEARCH.md
@.planning/phases/03-character-origin-base-attributes/03-VALIDATION.md
@.planning/phases/03-character-origin-base-attributes/03-UI-SPEC.md
@packages/rules-engine/src/contracts/canonical-id.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
@apps/planner/src/state/planner-shell.ts
@apps/planner/src/components/shell/summary-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the compiled foundation fixture and dedicated Phase 3 state slice</name>
  <files>packages/rules-engine/src/contracts/canonical-id.ts, apps/planner/src/features/character-foundation/foundation-fixture.ts, apps/planner/src/features/character-foundation/store.ts, apps/planner/src/features/character-foundation/selectors.ts, tests/phase-01/schema-contract.spec.ts</files>
  <read_first>packages/rules-engine/src/contracts/canonical-id.ts, packages/data-extractor/src/contracts/canonical-record.ts, tests/phase-01/schema-contract.spec.ts, apps/planner/src/state/planner-shell.ts, packages/rules-engine/src/contracts/validation-outcome.ts, .planning/phases/03-character-origin-base-attributes/03-CONTEXT.md, .planning/phases/03-character-origin-base-attributes/03-RESEARCH.md, .planning/phases/03-character-origin-base-attributes/03-VALIDATION.md, .planning/phases/03-character-origin-base-attributes/03-UI-SPEC.md, AGENTS.md</read_first>
  <action>Extend `packages/rules-engine/src/contracts/canonical-id.ts` so `ENTITY_KINDS` and `EntityKind` include the exact new values `subrace`, `alignment`, and `deity` in addition to the existing Phase 1 kinds. Keep `canonicalIdRegex` unchanged so the grammar remains `kind:value`. Update `tests/phase-01/schema-contract.spec.ts` to prove the canonical schema accepts `subrace:moon-elf`, `alignment:neutral-good`, and `deity:none` alongside the existing accepted ids. Then create `apps/planner/src/features/character-foundation/foundation-fixture.ts` as the temporary compiled dataset adapter for Phase 3. Export `FOUNDATION_DATASET_ID = 'puerta-ee-2026-03-30+phase03'`, `ATTRIBUTE_KEYS = ['str', 'dex', 'con', 'int', 'wis', 'cha'] as const`, and a `phase03FoundationFixture` object with exact top-level fields `datasetId`, `races`, `subraces`, `alignments`, `deities`, and `attributeRules`. Each option record must store canonical ids with the prefixes `race:`, `alignment:`, `deity:`, and `subrace:` and include visible Spanish `label` text. Include the explicit sentinel entry `deity:none` with label `Sin deidad`. Inside `attributeRules`, define the exact fields `budget`, `minimum`, `maximum`, `baseScore`, and `costByScore`. Create `apps/planner/src/features/character-foundation/store.ts` exporting `useCharacterFoundationStore`, a `createBaseAttributes()` helper, and actions `setRace`, `setSubrace`, `setAlignment`, `setDeity`, `setBaseAttribute`, and `resetFoundation`. The store must keep `datasetId`, `raceId`, `subraceId`, `alignmentId`, `deityId`, and `baseAttributes` separate from `usePlannerShellStore`. Create `apps/planner/src/features/character-foundation/selectors.ts` exporting `selectOriginOptions`, `selectOriginReadyForAbilities`, `selectFoundationSummary`, and `selectAttributeBudgetSnapshot`. `selectOriginReadyForAbilities` must only return `true` when `raceId`, `alignmentId`, and `deityId` are selected and any chosen `subraceId` belongs to the selected race.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-01/schema-contract.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules-engine/src/contracts/canonical-id.ts` contains `subrace`
    - `packages/rules-engine/src/contracts/canonical-id.ts` contains `alignment`
    - `packages/rules-engine/src/contracts/canonical-id.ts` contains `deity`
    - `tests/phase-01/schema-contract.spec.ts` contains `subrace:moon-elf`
    - `tests/phase-01/schema-contract.spec.ts` contains `alignment:neutral-good`
    - `tests/phase-01/schema-contract.spec.ts` contains `deity:none`
    - `apps/planner/src/features/character-foundation/foundation-fixture.ts` contains `puerta-ee-2026-03-30+phase03`
    - `apps/planner/src/features/character-foundation/foundation-fixture.ts` contains `deity:none`
    - `apps/planner/src/features/character-foundation/foundation-fixture.ts` contains `costByScore`
    - `apps/planner/src/features/character-foundation/store.ts` contains `useCharacterFoundationStore`
    - `apps/planner/src/features/character-foundation/store.ts` contains `setBaseAttribute`
    - `apps/planner/src/features/character-foundation/selectors.ts` contains `selectOriginReadyForAbilities`
    - `apps/planner/src/features/character-foundation/selectors.ts` contains `selectFoundationSummary`
  </acceptance_criteria>
  <done>The planner has a canonical-ID-based foundation dataset and a dedicated state slice ready for route integration.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder routes with the stepped origin board, locked abilities panel, and summary projection</name>
  <files>apps/planner/src/lib/copy/es.ts, apps/planner/src/routes/root.tsx, apps/planner/src/routes/abilities.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/features/character-foundation/origin-board.tsx, apps/planner/src/features/character-foundation/locked-abilities-panel.tsx, tests/phase-03/origin-flow.spec.tsx, tests/phase-03/summary-status.spec.tsx</files>
  <read_first>apps/planner/src/lib/copy/es.ts, apps/planner/src/routes/root.tsx, apps/planner/src/routes/abilities.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/features/character-foundation/store.ts, apps/planner/src/features/character-foundation/selectors.ts, .planning/phases/03-character-origin-base-attributes/03-CONTEXT.md, .planning/phases/03-character-origin-base-attributes/03-UI-SPEC.md, .planning/phases/03-character-origin-base-attributes/03-RESEARCH.md</read_first>
  <action>Create `apps/planner/src/features/character-foundation/origin-board.tsx` as the `Construcción` surface with the ordered visible steps `Raza`, `Subraza`, `Alineamiento`, and `Deidad`. The component must keep the deity step visible at all times and render the explicit option label `Sin deidad`. Create `apps/planner/src/features/character-foundation/locked-abilities-panel.tsx` with the exact heading `El origen del personaje sigue incompleto` and the exact body `Selecciona raza, subraza, alineamiento y deidad en Construcción para desbloquear Atributos.`. Update `apps/planner/src/lib/copy/es.ts` to add the exact Phase 3 copy strings `Raza`, `Subraza`, `Alineamiento`, `Deidad`, `Sin deidad`, `Confirmar origen`, `El origen del personaje sigue incompleto`, and `Selecciona raza, subraza, alineamiento y deidad en Construcción para desbloquear Atributos.`. Update `apps/planner/src/routes/root.tsx` to render `OriginBoard` instead of `PlannerSectionView`. Update `apps/planner/src/routes/abilities.tsx` to render `LockedAbilitiesPanel` whenever `selectOriginReadyForAbilities` is `false`. Update `apps/planner/src/components/shell/summary-panel.tsx` to read the foundation summary projection from `selectFoundationSummary`, replace the placeholder character value with selected origin labels, and drive the shell validation badge from Phase 3 foundation severity. Add `tests/phase-03/origin-flow.spec.tsx` that asserts the ordered step labels, `Sin deidad`, and the locked `Atributos` copy render in the app shell. Add `tests/phase-03/summary-status.spec.tsx` that asserts the summary starts in a blocked or incomplete state and then shows origin identity fields after store-backed selections are applied.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-03/origin-flow.spec.tsx tests/phase-03/summary-status.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/lib/copy/es.ts` contains `Confirmar origen`
    - `apps/planner/src/lib/copy/es.ts` contains `El origen del personaje sigue incompleto`
    - `apps/planner/src/features/character-foundation/origin-board.tsx` contains `Sin deidad`
    - `apps/planner/src/features/character-foundation/origin-board.tsx` contains `Raza`
    - `apps/planner/src/features/character-foundation/locked-abilities-panel.tsx` contains `Selecciona raza, subraza, alineamiento y deidad en Construcción para desbloquear Atributos.`
    - `apps/planner/src/routes/root.tsx` contains `OriginBoard`
    - `apps/planner/src/routes/abilities.tsx` contains `LockedAbilitiesPanel`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `useCharacterFoundationStore`
    - `tests/phase-03/origin-flow.spec.tsx` contains `Sin deidad`
    - `tests/phase-03/summary-status.spec.tsx` contains `Bloqueada`
  </acceptance_criteria>
  <done>The `Construcción`, `Atributos`, and summary routes now reflect real Phase 3 origin state instead of shell placeholders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm vitest run tests/phase-01/schema-contract.spec.ts --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm vitest run tests/phase-03/origin-flow.spec.tsx tests/phase-03/summary-status.spec.tsx --reporter=dot`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- `Construcción` captures race, subrace, alignment, and deity through a stepped route-owned board
- `Atributos` stays visibly blocked until the origin is coherent enough to continue
- Summary chrome reflects real foundation state and dataset identity
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-origin-base-attributes/03-01-SUMMARY.md`
</output>
