---
phase: 03-character-origin-base-attributes
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/rules-engine/src/foundation/origin-rules.ts
  - packages/rules-engine/src/foundation/ability-budget.ts
  - apps/planner/src/features/character-foundation/selectors.ts
  - apps/planner/src/features/character-foundation/origin-board.tsx
  - apps/planner/src/features/character-foundation/attributes-board.tsx
  - apps/planner/src/routes/abilities.tsx
  - apps/planner/src/components/shell/summary-panel.tsx
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/tokens.css
  - apps/planner/src/styles/app.css
  - tests/phase-03/foundation-validation.spec.ts
  - tests/phase-03/attribute-budget.spec.tsx
  - tests/phase-03/summary-status.spec.tsx
autonomous: true
requirements: [CHAR-04, ABIL-01]
user_setup: []
must_haves:
  truths:
    - "Origin restrictions and starting-attribute legality resolve through pure helpers instead of route-local JSX booleans."
    - "Inline blocked or illegal feedback and the summary-panel status are derived from the same Phase 3 validation projection."
    - "The attributes screen becomes budget-led once origin prerequisites are satisfied, matching the approved UI contract."
  artifacts:
    - path: "packages/rules-engine/src/foundation/origin-rules.ts"
      provides: "Pure Phase 3 origin legality resolver"
      contains: "evaluateOriginSelection"
    - path: "packages/rules-engine/src/foundation/ability-budget.ts"
      provides: "Pure Phase 3 attribute budget resolver"
      contains: "calculateAbilityBudgetSnapshot"
    - path: "apps/planner/src/features/character-foundation/attributes-board.tsx"
      provides: "Interactive base-attribute board with budget feedback"
      contains: "Puntos restantes"
    - path: "tests/phase-03/foundation-validation.spec.ts"
      provides: "Contract coverage for blocked and illegal origin paths"
      contains: "deity:none"
  key_links:
    - from: "packages/rules-engine/src/foundation/origin-rules.ts"
      to: "apps/planner/src/features/character-foundation/selectors.ts"
      via: "foundation validation projection"
      pattern: "evaluateOriginSelection"
    - from: "packages/rules-engine/src/foundation/ability-budget.ts"
      to: "apps/planner/src/features/character-foundation/attributes-board.tsx"
      via: "spent and remaining budget output"
      pattern: "calculateAbilityBudgetSnapshot"
    - from: "apps/planner/src/features/character-foundation/selectors.ts"
      to: "apps/planner/src/components/shell/summary-panel.tsx"
      via: "shared severity status for inline feedback and summary badge"
      pattern: "summaryStatus"
---

<objective>
Implement the pure legality and budget engine for origin plus starting attributes, then wire the results into the `Construcción`, `Atributos`, and summary-panel experiences with inline Spanish feedback and the approved Phase 3 visual contract.

Purpose: finish the mechanical behavior that makes Phase 3 server-valid instead of only structurally present.
Output: rules-engine helpers, attributes editor, inline incompatibility feedback, budget readout, NWN1 board styling, and automated Phase 3 legality tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-character-origin-base-attributes/03-CONTEXT.md
@.planning/phases/03-character-origin-base-attributes/03-RESEARCH.md
@.planning/phases/03-character-origin-base-attributes/03-VALIDATION.md
@.planning/phases/03-character-origin-base-attributes/03-UI-SPEC.md
@.planning/phases/03-character-origin-base-attributes/03-01-PLAN.md
@packages/rules-engine/src/contracts/canonical-id.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
@apps/planner/src/features/character-foundation/foundation-fixture.ts
@apps/planner/src/features/character-foundation/store.ts
@apps/planner/src/features/character-foundation/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure origin-restriction and attribute-budget resolvers with contract tests</name>
  <files>packages/rules-engine/src/foundation/origin-rules.ts, packages/rules-engine/src/foundation/ability-budget.ts, tests/phase-03/foundation-validation.spec.ts</files>
  <read_first>packages/rules-engine/src/contracts/canonical-id.ts, packages/rules-engine/src/contracts/validation-outcome.ts, apps/planner/src/features/character-foundation/foundation-fixture.ts, .planning/phases/03-character-origin-base-attributes/03-CONTEXT.md, .planning/phases/03-character-origin-base-attributes/03-RESEARCH.md, .planning/phases/03-character-origin-base-attributes/03-VALIDATION.md, .planning/phases/03-character-origin-base-attributes/03-01-PLAN.md</read_first>
  <action>Create `packages/rules-engine/src/foundation/origin-rules.ts` exporting `evaluateOriginSelection(input)` and `getAllowedSubraces(input)`. `evaluateOriginSelection` must accept `raceId`, `subraceId`, `alignmentId`, `deityId`, and the Phase 3 dataset records, then return an object with exact fields `summaryStatus`, `issues`, and `requiredDeityResolved`. Use `resolveValidationOutcome` from `packages/rules-engine/src/contracts/validation-outcome.ts` so blocked and illegal cases stay on the project-standard vocabulary. Create `packages/rules-engine/src/foundation/ability-budget.ts` exporting `calculateAbilityBudgetSnapshot(input)` with exact output fields `spentPoints`, `remainingPoints`, `status`, and `issues`. The budget helper must read the exact `attributeRules` fields `budget`, `minimum`, `maximum`, `baseScore`, and `costByScore` from the Phase 3 fixture. Missing prerequisite origin data must resolve to `blocked`, overspending or out-of-range attributes must resolve to `illegal`, and satisfied rules must resolve to `legal`. Add `tests/phase-03/foundation-validation.spec.ts` that asserts `deity:none` is legal when deity is optional, missing required deity does not return `legal`, a subrace outside the selected race resolves to a non-legal status, and attribute overspend produces negative or zero-safe remaining-point logic with an `illegal` result.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-03/foundation-validation.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules-engine/src/foundation/origin-rules.ts` contains `evaluateOriginSelection`
    - `packages/rules-engine/src/foundation/origin-rules.ts` contains `requiredDeityResolved`
    - `packages/rules-engine/src/foundation/origin-rules.ts` contains `resolveValidationOutcome`
    - `packages/rules-engine/src/foundation/ability-budget.ts` contains `calculateAbilityBudgetSnapshot`
    - `packages/rules-engine/src/foundation/ability-budget.ts` contains `remainingPoints`
    - `tests/phase-03/foundation-validation.spec.ts` contains `deity:none`
    - `tests/phase-03/foundation-validation.spec.ts` contains `illegal`
  </acceptance_criteria>
  <done>Phase 3 legality and budget rules are encoded in pure, testable helpers instead of UI-local conditionals.</done>
</task>

<task type="auto">
  <name>Task 2: Render inline legality feedback, budget-led attribute editing, and UI-SPEC-compliant board styling</name>
  <files>apps/planner/src/features/character-foundation/selectors.ts, apps/planner/src/features/character-foundation/origin-board.tsx, apps/planner/src/features/character-foundation/attributes-board.tsx, apps/planner/src/routes/abilities.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/tokens.css, apps/planner/src/styles/app.css, tests/phase-03/attribute-budget.spec.tsx, tests/phase-03/summary-status.spec.tsx</files>
  <read_first>apps/planner/src/features/character-foundation/selectors.ts, apps/planner/src/features/character-foundation/origin-board.tsx, apps/planner/src/routes/abilities.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/styles/tokens.css, apps/planner/src/styles/app.css, packages/rules-engine/src/foundation/origin-rules.ts, packages/rules-engine/src/foundation/ability-budget.ts, .planning/phases/03-character-origin-base-attributes/03-UI-SPEC.md, .planning/phases/03-character-origin-base-attributes/03-RESEARCH.md</read_first>
  <action>Create `apps/planner/src/features/character-foundation/attributes-board.tsx` with six editable rows for `FUE`, `DES`, `CON`, `INT`, `SAB`, and `CAR`, plus exact budget labels `Puntos gastados` and `Puntos restantes`. Update `apps/planner/src/features/character-foundation/selectors.ts` to combine `evaluateOriginSelection` and `calculateAbilityBudgetSnapshot` into one shared summary or inline-state projection consumed by both route components and `SummaryPanel`. Update `apps/planner/src/features/character-foundation/origin-board.tsx` to render inline blocked or illegal reasons beside the affected control and keep locked steps visible when prerequisites are missing. Update `apps/planner/src/routes/abilities.tsx` to swap `LockedAbilitiesPanel` for `AttributesBoard` once `selectOriginReadyForAbilities` is `true`. Update `apps/planner/src/components/shell/summary-panel.tsx` so the validation badge reflects the most severe current Phase 3 result: `Bloqueada` for missing or blocked prerequisites and `Inválida` for rule failures. Extend `apps/planner/src/lib/copy/es.ts` with the exact strings `Puntos gastados`, `Puntos restantes`, `Elección bloqueada: completa el paso anterior o cambia la opción marcada para continuar.`, and `Reiniciar base: se perderán el origen y las características iniciales.`. Extend `apps/planner/src/styles/tokens.css` and `apps/planner/src/styles/app.css` with the exact color values `#050403`, `#17120f`, `#9f7a31`, `#41506a`, and `#7a1f1f`, plus Phase 3 classes `.foundation-board`, `.foundation-step`, `.foundation-option.is-blocked`, `.attribute-budget`, `.attribute-control`, and `.summary-status--blocked` so the board matches the approved left-control/right-sheet NWN1 presentation. Add `tests/phase-03/attribute-budget.spec.tsx` that asserts budget totals update as attributes change and illegal allocations surface inline text. Update `tests/phase-03/summary-status.spec.tsx` so it asserts the summary badge changes when an illegal attribute or blocked origin combination exists.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-03/attribute-budget.spec.tsx tests/phase-03/summary-status.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/features/character-foundation/attributes-board.tsx` contains `Puntos restantes`
    - `apps/planner/src/features/character-foundation/attributes-board.tsx` contains `FUE`
    - `apps/planner/src/features/character-foundation/origin-board.tsx` contains `Elección bloqueada: completa el paso anterior o cambia la opción marcada para continuar.`
    - `apps/planner/src/routes/abilities.tsx` contains `AttributesBoard`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `Inválida`
    - `apps/planner/src/styles/tokens.css` contains `#050403`
    - `apps/planner/src/styles/tokens.css` contains `#41506a`
    - `apps/planner/src/styles/app.css` contains `.foundation-board`
    - `tests/phase-03/attribute-budget.spec.tsx` contains `Puntos gastados`
    - `tests/phase-03/summary-status.spec.tsx` contains `Inválida`
  </acceptance_criteria>
  <done>The planner now enforces and visualizes origin plus base-attribute legality through the routed UI and shared summary status.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm vitest run tests/phase-03 --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- Invalid or blocked origin and deity combinations are explained inline and reflected in the summary panel
- The attribute screen enforces the supported creation rule with visible spent and remaining budget
- The Phase 3 board presentation matches the approved NWN1-inspired contract on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-origin-base-attributes/03-02-SUMMARY.md`
</output>
