---
phase: 12.2-roster-detail-and-race-modifiers
plan: 04
subsystem: level-progression
tags: [dedupe, class-fixture, first-wins, tdd, react-key-hygiene]
requires:
  - 12.1-02 (dedupeByCanonicalId race-fixture pattern; UNIQUE-IDs spec-floor convention)
  - 12.2-03 (projectCompiledClass + decodeAlignRestrict + BASE_CLASS_ALLOWLIST shipped on class-fixture.ts)
provides:
  - dedupeCompiledClassesByCanonicalId helper (first-wins, sourceRow-aware warning)
  - extended projectCompiledClasses pipeline (dedupe precedes projection)
affects:
  - tests/phase-12.2/class-catalog-dedupe.spec.ts (new regression spec, 5 assertions)
tech-stack:
  added: []
  patterns:
    - first-wins dedupe at the projection boundary (mirror of foundation-fixture.ts:135)
    - dedupe-before-projection (operate on CompiledClass so sourceRow is visible for the warning log without smuggling metadata through PlannerClassRecord)
    - prefixed console.warn (`[12.2-04]`) for extractor backlog traceability
key-files:
  created:
    - tests/phase-12.2/class-catalog-dedupe.spec.ts
  modified:
    - apps/planner/src/features/level-progression/class-fixture.ts
decisions:
  - "Dedupe the raw CompiledClass array BEFORE map(projectCompiledClass), not after. Keeps sourceRow in scope for the warning log and avoids smuggling a __sourceRow field through PlannerClassRecord."
  - "First-wins (lowest sourceRow) — matches race-fixture semantics and matches the pre-existing React render behavior (first occurrence was already the one surviving the key collision visually; dedupe makes it deterministic and silent)."
  - "console.warn prefixed [12.2-04] so the extractor backlog surfaces the dropped rows in test stderr and dev-server console. Not silenced in test runs — two warnings fire at module-load (harper sourceRow=54, shadowadept sourceRow=55) and are expected."
  - "No 12.1-01 floor-spec ripple needed. Both duplicate rows are isBase=false (prestige), so the base-class unique-id count in tests/phase-12.1/class-roster-wiring.spec.ts is unchanged post-dedupe. The plan's contingency for an in-place floor rewrite was not triggered."
metrics:
  duration: "~10 min"
  completed: "2026-04-19T00:31:00Z"
  tests_added: 5
  tests_total: 445
  commits: 2 (excl. docs)
---

# Phase 12.2 Plan 04: Class-Fixture First-Wins Dedupe Summary

**One-liner:** Add a first-wins `dedupeCompiledClassesByCanonicalId` helper at the `projectCompiledClasses` boundary in `class-fixture.ts` — mirrors the race-fixture dedupe pattern from 12.1-02, drops `class:harper` (sourceRow 54) and `class:shadowadept` (sourceRow 55) duplicates with a prefixed `console.warn`, and eliminates React duplicate-key warnings in the L1 class picker.

## Outcome

Closes Phase 12.1 UAT Bug 4 at the adapter layer:

- `phase04ClassFixture.classes.length` drops from 41 raw → 39 unique, exactly matching `new Set(compiledClassCatalog.classes.map(c => c.id)).size`.
- `class:harper` and `class:shadowadept` each appear exactly once in the projected fixture (first-wins: the Arcano variants at rows 338 + 702 survive; the Divino duplicates at rows 842 + 870 are dropped).
- `console.warn` fires exactly twice at module-load with the `[12.2-04]` prefix and the dropped `sourceRow` so the extractor backlog stays visible until slug-disambiguation lands upstream.
- No regressions: full Vitest suite 445/445 green (was 440/440; +5 new dedupe contract assertions).
- No ripple to 12.1-01 base-class floor spec — both duplicate rows are `isBase=false` (prestige), so the unique base-id count is unchanged; the plan's contingency rewrite was not triggered.

## Commits (this plan)

| # | Hash    | Message                                                                              |
|---|---------|--------------------------------------------------------------------------------------|
| 1 | 8065f45 | test(12.2-04): add failing class-catalog first-wins dedupe spec                      |
| 2 | ea1dbea | fix(12.2-04): add first-wins dedupe to class projection with sourceRow warning log   |

## Tasks

### Task 1 — RED spec (commit 8065f45)
Created `tests/phase-12.2/class-catalog-dedupe.spec.ts` (82 lines, 5 assertions):
- Test 1: every projected id unique (`new Set(ids).size === ids.length`).
- Test 2: `class:harper` count === 1 (with falsifiability guard against a future extractor fix).
- Test 3: `class:shadowadept` count === 1 (with falsifiability guard).
- Test 4 (UNIQUE floor): `phase04ClassFixture.classes.length === new Set(compiled).size` — mirrors the 12.1-02 race-fixture floor convention.
- Test 5 (first-wins determinism): surviving `class:harper` `description` matches the first compiled occurrence.

Initial run: 4 failed / 1 passed against the pre-dedupe projection (41 vs 39, harper 2, shadowadept 2). Test 5 trivially passed because the first Harper was already at index 0 in the mapped array; the dedupe makes this survival explicit rather than incidental.

### Task 2 — GREEN dedupe helper (commit ea1dbea)
Edited `apps/planner/src/features/level-progression/class-fixture.ts`:

- Added `dedupeCompiledClassesByCanonicalId(entries: readonly CompiledClass[]): CompiledClass[]` — module-scoped helper, mirrors the race-fixture pattern at `foundation-fixture.ts:135`. Uses `Set<string>` to track seen canonical IDs; on collision, emits `console.warn` with the `[12.2-04]` prefix and the dropped row's `sourceRow` before `continue`.
- Rewrote `projectCompiledClasses(catalog)` body from `catalog.classes.map(projectCompiledClass)` to `dedupeCompiledClassesByCanonicalId(catalog.classes).map(projectCompiledClass)`.
- No other exports touched: `projectCompiledClass`, `CLASS_SERVER_RULE_OVERLAY`, `BASE_CLASS_ALLOWLIST`, `decodeAlignRestrict`, `decodePrerequisiteMaxLevel`, `getPhase04ClassRecord`, and `phase04ClassFixture` call-site all unchanged.

## Verification

- `pnpm typecheck` — clean.
- `pnpm vitest run tests/phase-12.2/class-catalog-dedupe.spec.ts` — 5/5 GREEN.
- `pnpm vitest run tests/phase-12.1/class-roster-wiring.spec.ts` — 6/6 GREEN (floor-spec ripple guard).
- `pnpm vitest run tests/phase-12.2/prestige-filter-l1.spec.ts` — 16/16 GREEN.
- `pnpm vitest run tests/phase-12.2/picker-descriptions.spec.ts` — 8/8 GREEN.
- `pnpm vitest run` full suite — 445/445 GREEN (was 440/440).
- `grep -n 'dedupeCompiledClassesByCanonicalId' apps/planner/src/features/level-progression/class-fixture.ts` — 2 hits (declaration line 263 + call line 282).
- stderr at module-load contains exactly two `[12.2-04] Dropped duplicate class id` warnings (harper sourceRow=54, shadowadept sourceRow=55).

## Deviations from Plan

None — plan executed exactly as written. The contingency floor-spec rewrite in Task 2's `<action>` was inspected and determined unnecessary: both duplicate canonical IDs (`class:harper`, `class:shadowadept`) are prestige classes (`isBase: false`), so the 12.1-01 base-class count-floor (`compiledClassCatalog.classes.filter(c => c.isBase).length`) is unchanged post-dedupe. `tests/phase-12.1/class-roster-wiring.spec.ts` passed without modification.

## Deferred / Upstream Follow-up

The real fix lives in the extractor: emit slug-disambiguated IDs (e.g. `class:harper-arcane` / `class:harper-divine`, `class:shadowadept-arcane` / `class:shadowadept-divine`) so the compiled catalog no longer collides on canonical ID. Tracked in `.planning/phases/12.2-roster-detail-and-race-modifiers/12.2-CONTEXT.md` `<deferred>` alongside the broader PreReqTable-decoding + `reachableAtLevelOne:boolean` extractor enrichment line. When that lands, `dedupeCompiledClassesByCanonicalId` becomes a no-op guard and can be deleted; the regression spec's falsifiability branches will switch from the `count === 1` assertion to the `count === compiledCount` pass-through.

## Self-Check: PASSED

- `tests/phase-12.2/class-catalog-dedupe.spec.ts` — exists.
- `apps/planner/src/features/level-progression/class-fixture.ts` — modified (helper + call).
- Commit `8065f45` — exists.
- Commit `ea1dbea` — exists.
