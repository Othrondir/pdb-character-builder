---
phase: 12.2-roster-detail-and-race-modifiers
plan: 01
subsystem: character-foundation + level-progression
tags: [roster-detail, tlk-description, projection-adapter, tdd, detail-panel]

# Dependency graph
requires:
  - phase: 05.1-data-extractor-pipeline
    provides: compiledRaceCatalog[].description + compiledClassCatalog[].description populated from TLK prose (Spanish)
  - phase: 12.1-roster-wiring-and-overflow-fixes
    provides: projectCompiledRace / projectCompiledClass adapter boundaries
provides:
  - Race picker DetailPanel body renders selectedRaceDescription (TLK prose) when a race is selected
  - Class picker DetailPanel body renders selectedClassRecord.description (TLK prose) at every progression level
  - FoundationRaceOption.description (required string)
  - PlannerClassRecord.description (required string)
  - Count-based regression spec locking non-empty description on every projected race + class
affects: [12.2-02-race-ability-modifiers, 12.2-03-prestige-filter, 12.2-04-class-dup-key]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Surgical adapter-field addition (new required field on FoundationRaceOption + PlannerClassRecord, one-line map in each adapter) — lowest-blast-radius way to surface already-extracted TLK data through the existing projection pipeline
    - ID-based re-lookup pattern in board components (origin-board reads phase03FoundationFixture.races; build-progression-board uses getPhase04ClassRecord) — keeps view-row shapes (OptionItem / classOptions[]) stable while giving DetailPanel access to the full record

key-files:
  created:
    - tests/phase-12.2/picker-descriptions.spec.ts
    - .planning/phases/12.2-roster-detail-and-race-modifiers/12.2-01-SUMMARY.md
  modified:
    - apps/planner/src/features/character-foundation/foundation-fixture.ts
    - apps/planner/src/features/level-progression/class-fixture.ts
    - apps/planner/src/features/character-foundation/origin-board.tsx
    - apps/planner/src/features/level-progression/build-progression-board.tsx
    - tests/phase-03/foundation-validation.spec.ts

key-decisions:
  - "Added `description: string` as a REQUIRED field on FoundationRaceOption + PlannerClassRecord (not optional). Rationale: extractor always emits a string (empty string for missing TLK refs rather than omission), so the contract is always-present; making it required forces future adapters to confront the field instead of silently dropping it again."
  - "Chose ID-based re-lookup in origin-board.tsx (`phase03FoundationFixture.races.find(...)`) over extending `FoundationOptionView` shape in selectors.ts — keeps the D-01 4-file blast radius and avoids touching the selector layer. Same reasoning for build-progression-board.tsx using the existing `getPhase04ClassRecord(id)` helper."
  - "Scoped description rendering to the race step only (`activeStep === 'race' && selectedOption`) in origin-board — the alignment step has no TLK description source today; it falls back to the existing `${label} seleccionado.` placeholder or the validation issue message."

patterns-established:
  - "Adapter field pass-through + consumer re-lookup: when compiled-extractor data carries a field the view-row shape doesn't (e.g. description on FoundationRaceOption but not on FoundationOptionView), re-lookup by ID at the render site rather than widening every intermediate shape. Keeps boundary contracts tight and blast radius surgical."
  - "Source-swap ripple: adding a required field to a projected shape triggers test-fixture updates for any inline literal that previously stubbed the shape. Update the literal in-place — do not remove the behavioral assertion."

requirements-completed: []

# Metrics
duration: 10min
completed: 2026-04-18
---

# Phase 12.2 Plan 01: Wire TLK descriptions through race + class picker DetailPanels Summary

**Plumbed the `description` field from `compiledRaceCatalog` + `compiledClassCatalog` through the two projection adapters into both picker DetailPanel bodies — closing Phase 12.1 UAT Bug 1 where detail panes showed only label echoes (`${label} seleccionado.` / `Clase seleccionada: ${label}`) instead of the Spanish TLK prose already present in the extractor payload.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-18T23:47:00Z (approx)
- **Completed:** 2026-04-18T23:57:00Z (approx)
- **Tasks:** 2 (TDD RED + GREEN)
- **Files:** 2 created, 5 modified

## Accomplishments

- Race picker DetailPanel body now renders the selected race's TLK description (Spanish prose) when a race is chosen — validation issue message still wins when present; placeholder still wins when nothing is selected.
- Class picker DetailPanel body now renders the selected class's TLK description at every progression level (L1-L16) via `getPhase04ClassRecord(selectedClass.id)?.description`; `activeSheet.placeholderBody` still wins when nothing is selected.
- `FoundationRaceOption.description: string` + `PlannerClassRecord.description: string` are now required fields carried verbatim from the compiled catalog through `projectCompiledRace` / `projectCompiledClass`.
- Regression spec `tests/phase-12.2/picker-descriptions.spec.ts` (8 tests) locks the wiring contract: canary IDs `race:dwarf/elf/human` + `class:fighter/cleric/wizard` all carry non-empty descriptions distinct from their labels, AND every projected race + class in both fixtures carries a non-empty description (count-based floor).
- Source-swap ripple applied in `tests/phase-03/foundation-validation.spec.ts`: inline test race literal gained a `description` field to satisfy the newly-required interface contract (D-01 standard ripple, no behavioral change).

## Task Commits

1. **Task 1: RED — picker-description regression spec** — `fd6a709` (test)
   - `tests/phase-12.2/picker-descriptions.spec.ts` — 8 tests, all failing with "description is undefined" (TypeError on `.length` access / AssertionError on `toBeTypeOf('string')`).

2. **Task 2: GREEN — wire TLK descriptions through adapters + DetailPanels** — `af4b427` (fix)
   - `apps/planner/src/features/character-foundation/foundation-fixture.ts` — FoundationRaceOption gains `description: string`; projectCompiledRace maps `description: compiled.description`.
   - `apps/planner/src/features/level-progression/class-fixture.ts` — PlannerClassRecord gains `description: string`; projectCompiledClass maps `description: compiled.description`.
   - `apps/planner/src/features/character-foundation/origin-board.tsx` — import `phase03FoundationFixture`; compute `selectedRaceDescription` for the race step only; DetailPanel body prioritizes issue > description > label-echo > placeholder.
   - `apps/planner/src/features/level-progression/build-progression-board.tsx` — import `getPhase04ClassRecord`; compute `selectedClassRecord` via ID lookup; DetailPanel body now reads `selectedClassRecord.description` when a class is selected.
   - `tests/phase-03/foundation-validation.spec.ts` — source-swap ripple: inline test race literal gains `description`.

## Verification Results

- `pnpm typecheck` — clean (after test-fixture ripple applied).
- `pnpm vitest run tests/phase-12.2/picker-descriptions.spec.ts` — 8/8 GREEN.
- `pnpm vitest run` — 414/414 GREEN (up from 406/406; +8 new tests, zero regressions).
- `grep -nE "description: compiled\.description" apps/planner/src/features/character-foundation/foundation-fixture.ts apps/planner/src/features/level-progression/class-fixture.ts` → 2 hits (one per adapter).
- `grep "description: string"` in target fixtures → 2 hits (one interface per fixture file).
- `grep "selectedRaceDescription|selectedClassRecord\.description"` → both present in their respective boards.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] Source-swap ripple: tests/phase-03/foundation-validation.spec.ts**
- **Found during:** Task 2 GREEN typecheck (Step E).
- **Issue:** `pnpm typecheck` failed with TS2322/TS2353 — the inline test race literal at line 79-84 no longer matched the widened `FoundationRaceOption` (missing `description`), so TypeScript widened the array type and the excess-property check on `label` vs `RaceRuleRecord` fired.
- **Fix:** Added `description: 'Test race with required deity (spec-local).'` to the inline literal. Behavior unchanged — this is the same standard ripple pattern 12.1-02 used when adding dedupe (noted in its SUMMARY under "Dropped hand-picked 'subrace:moon-elf' assertions").
- **Files modified:** `tests/phase-03/foundation-validation.spec.ts`
- **Commit:** `af4b427` (folded into GREEN).

No architectural deviations. No authentication gates. No checkpoints.

## Known Stubs

None. Both description fields flow live TLK data from the compiled catalogs — no placeholder / empty-string fallbacks wired to UI.

## Follow-ups

None from this plan — the three remaining Phase 12.2 items (race ability modifiers, prestige-filter fix, class duplicate-key fix) are scoped for plans 12.2-02 / -03 / -04 per CONTEXT.md.

## Self-Check: PASSED

- `tests/phase-12.2/picker-descriptions.spec.ts` — FOUND
- `apps/planner/src/features/character-foundation/foundation-fixture.ts` (modified) — FOUND (contains `description: string` + `description: compiled.description`)
- `apps/planner/src/features/level-progression/class-fixture.ts` (modified) — FOUND (contains `description: string` + `description: compiled.description`)
- `apps/planner/src/features/character-foundation/origin-board.tsx` (modified) — FOUND (contains `selectedRaceDescription`)
- `apps/planner/src/features/level-progression/build-progression-board.tsx` (modified) — FOUND (contains `selectedClassRecord.description`)
- Commit `fd6a709` (RED) — FOUND in git log
- Commit `af4b427` (GREEN) — FOUND in git log
