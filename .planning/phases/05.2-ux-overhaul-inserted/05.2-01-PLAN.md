---
phase: 05.2-ux-overhaul-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/styles/tokens.css
  - apps/planner/src/styles/fonts.css
  - apps/planner/src/styles/app.css
  - apps/planner/src/components/ui/nwn-frame.tsx
  - apps/planner/src/components/ui/nwn-button.tsx
  - apps/planner/src/components/ui/action-bar.tsx
  - tests/phase-05.2/theme-contract.spec.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "The planner has a dark background (#0a0a0a) with golden borders (#c8a84e) and light text (#e8d5a0)"
    - "No rounded corners exist anywhere in the UI (border-radius: 0 globally)"
    - "No drop shadows exist in the UI"
    - "Font weights are 400 and 600 only -- no 700 weight imported or used"
    - "Typography uses 18px uppercase titles with 0.15em letter-spacing, not 36px display sizing"
    - "NwnFrame renders a sharp-cornered double-border panel with inner inset pseudo-element"
    - "NwnButton renders primary (gold border), secondary (muted border), and auxiliary variants"
  artifacts:
    - path: "apps/planner/src/styles/tokens.css"
      provides: "Dark NWN1 palette tokens"
      contains: "--color-surface: #0a0a0a"
    - path: "apps/planner/src/components/ui/nwn-frame.tsx"
      provides: "Shared double-border panel component"
      exports: ["NwnFrame"]
    - path: "apps/planner/src/components/ui/nwn-button.tsx"
      provides: "NWN1-style button with variants"
      exports: ["NwnButton"]
    - path: "apps/planner/src/components/ui/action-bar.tsx"
      provides: "Bottom action bar with Aceptar/Cancelar"
      exports: ["ActionBar"]
    - path: "tests/phase-05.2/theme-contract.spec.ts"
      provides: "Wave 0 test scaffold for theme tokens"
  key_links:
    - from: "apps/planner/src/styles/tokens.css"
      to: "apps/planner/src/styles/app.css"
      via: "CSS custom property references"
      pattern: "var\\(--color-"
    - from: "apps/planner/src/components/ui/nwn-frame.tsx"
      to: "apps/planner/src/styles/app.css"
      via: "BEM class .nwn-frame"
      pattern: "\\.nwn-frame"
---

<objective>
Overhaul the entire visual design system from the current light parchment theme to the dark NWN1 replica palette, rebuild the CSS stylesheet, create the foundational UI primitive components, and establish the Wave 0 test scaffolds.

Purpose: This is the atomic foundation layer. Every subsequent plan depends on the correct token values, the rebuilt CSS, and the NwnFrame/NwnButton primitives. Executing tokens first makes the palette inversion instant and global. The UI primitives establish the sharp-cornered, gold-bordered NWN1 panel pattern that all containers will use.

Output: Fully inverted dark theme visible on every existing page (even if layout is still old), three reusable UI primitive components, and test infrastructure for the phase.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-ux-overhaul-inserted/05.2-UI-SPEC.md
@.planning/phases/05.2-ux-overhaul-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-ux-overhaul-inserted/05.2-PATTERNS.md

<interfaces>
<!-- Current tokens.css (24 lines) -- to be completely rewritten -->
From apps/planner/src/styles/tokens.css:
```css
:root {
  --color-parchment: #e3d8c1;
  --color-parchment-shadow: #c8b89a;
  --color-ink: #2f241d;
  --color-ink-soft: #5d4d42;
  --color-bronze: #9f7a31;
  --color-destructive: #7a1f1f;
  --color-foundation-canvas: #050403;
  --color-foundation-panel: #17120f;
  --color-panel-edge: rgba(63, 45, 32, 0.65);
  --color-panel-glow: rgba(159, 122, 49, 0.18);
  --color-selection-fill: rgba(159, 122, 49, 0.28);
  --font-display: "Cormorant Garamond", "Times New Roman", serif;
  --font-body: "Spectral", Georgia, serif;
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
  --shadow-panel: 0 18px 48px rgba(31, 22, 18, 0.16);
  --border-frame: 1px solid rgba(79, 58, 41, 0.45);
}
```

From apps/planner/src/styles/fonts.css:
```css
@import "@fontsource/cormorant-garamond/600.css";
@import "@fontsource/cormorant-garamond/700.css";
@import "@fontsource/spectral/400.css";
@import "@fontsource/spectral/600.css";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CSS tokens, fonts, and full app.css for dark NWN1 theme</name>
  <files>
    apps/planner/src/styles/tokens.css,
    apps/planner/src/styles/fonts.css,
    apps/planner/src/styles/app.css,
    tests/phase-05.2/theme-contract.spec.ts
  </files>
  <read_first>
    apps/planner/src/styles/tokens.css,
    apps/planner/src/styles/fonts.css,
    apps/planner/src/styles/app.css,
    .planning/phases/05.2-ux-overhaul-inserted/05.2-UI-SPEC.md
  </read_first>
  <action>
**tokens.css -- complete rewrite per UI-SPEC token migration table (per D-09, D-10, D-11):**

Replace the entire file with these exact values:
```css
:root {
  /* Palette: dark NWN1 replica */
  --color-surface: #0a0a0a;
  --color-panel: #141010;
  --color-gold: #c8a84e;
  --color-text: #e8d5a0;
  --color-text-muted: #8b7355;
  --color-destructive: #7a1f1f;
  --color-panel-edge: rgba(200, 168, 78, 0.35);
  --color-selection: rgba(200, 168, 78, 0.18);
  --color-selection-border: rgba(200, 168, 78, 0.75);
  --color-blocked: rgba(200, 168, 78, 0.55);

  /* Borders */
  --border-frame: 2px solid var(--color-gold);
  --border-frame-inner: 1px solid var(--color-panel-edge);
  --border-button: 1px solid var(--color-gold);
  --border-button-muted: 1px solid var(--color-text-muted);

  /* Fonts (families unchanged) */
  --font-display: "Cormorant Garamond", "Times New Roman", serif;
  --font-body: "Spectral", Georgia, serif;

  /* Spacing (unchanged) */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
}
```

Tokens REMOVED (do not carry forward): `--color-parchment`, `--color-parchment-shadow`, `--color-ink`, `--color-ink-soft`, `--color-bronze`, `--color-foundation-canvas`, `--color-foundation-panel`, `--color-panel-glow`, `--color-selection-fill`, `--shadow-panel`.

**fonts.css -- remove 700 weight, add 400 for Cormorant Garamond (per D-13):**
```css
@import "@fontsource/cormorant-garamond/400.css";
@import "@fontsource/cormorant-garamond/600.css";
@import "@fontsource/spectral/400.css";
@import "@fontsource/spectral/600.css";
```
The 700 weight is no longer used (UI-SPEC specifies only 400 and 600).

**app.css -- full rewrite (929 lines). Key transformations:**

1. **Body background**: Replace the light parchment gradient `radial-gradient(circle at top, rgba(255, 244, 220, 0.75)...)` with:
   ```css
   body {
     background: var(--color-surface);
     color: var(--color-text);
     font-family: var(--font-body);
     font-size: 14px;
     line-height: 1.5;
   }
   ```

2. **Global border-radius sweep**: Search for every `border-radius` declaration in the file. There are 17 distinct values ranging from `14px` to `999px`. Set ALL to `0` (NWN1 uses sharp corners per D-10). The ONLY exception: `.sr-only` utility can keep `border-radius` if it exists.

3. **Hardcoded color replacement**: The file has ~40+ hardcoded `rgba(...)` values that bypass tokens. Replace ALL of them:
   - `rgba(159, 122, 49, ...)` variants (old bronze shades) -> use `var(--color-gold)` or `var(--color-selection)` as appropriate
   - `rgba(255, 248, 236, ...)` and `rgba(255, 244, 220, ...)` (old parchment-white) -> use `var(--color-text)` or remove
   - `rgba(32, 24, 18, ...)` and `rgba(31, 22, 18, ...)` (old dark-panel) -> use `var(--color-panel)` or `var(--color-surface)`
   - `rgba(63, 45, 32, ...)` (old edge) -> use `var(--color-panel-edge)`
   - Any `#e3d8c1`, `#c8b89a`, `#2f241d`, `#5d4d42`, `#9f7a31` references -> replace with new token `var()` references

4. **Drop shadow removal**: Remove ALL `box-shadow` declarations referencing the old panel shadows. NWN1 UI has no drop shadows.

5. **Font weight 700 -> 600**: Replace every `font-weight: 700` with `font-weight: 600`.

6. **Typography density (per D-13, D-14)**: Update heading sizes to GUI-density:
   - `.planner-shell__topbar h1` and similar large headings: change from `font-size: 1.75rem` / `2rem` etc. to `font-size: 18px`; add `text-transform: uppercase; letter-spacing: 0.15em; font-weight: 600; line-height: 1.2;`
   - `.planner-nav h2`: same treatment as panel title bars: `font-size: 18px; text-transform: uppercase; letter-spacing: 0.15em;`
   - `.planner-section-view h1`: `font-size: 18px; text-transform: uppercase; letter-spacing: 0.15em; text-align: center;`
   - Section headings (h3): `font-size: 16px; text-transform: uppercase; letter-spacing: 0.10em; font-weight: 600;`
   - Body text: `font-size: 14px; line-height: 1.5; font-weight: 400;`
   - Labels and buttons: `font-size: 14px; font-weight: 600; letter-spacing: 0.02em;`

7. **Panel frame replacement**: Replace `.planner-panel` styles:
   - Remove `background: linear-gradient(...)` or `background: var(--color-parchment)` patterns
   - Replace with: `background: var(--color-panel); border: var(--border-frame); border-radius: 0;`
   - Replace `.planner-panel::before` inner glow/line with: `content: ""; position: absolute; inset: 8px; border: var(--border-frame-inner); pointer-events: none;`

8. **NwnFrame CSS class**: Add to app.css:
   ```css
   .nwn-frame {
     background: var(--color-panel);
     border: 2px solid var(--color-gold);
     border-radius: 0;
     position: relative;
   }
   .nwn-frame::before {
     border: 1px solid var(--color-panel-edge);
     content: "";
     inset: 8px;
     pointer-events: none;
     position: absolute;
   }
   ```

9. **NwnButton CSS classes**: Add to app.css:
   ```css
   .nwn-button {
     background: transparent;
     border-radius: 0;
     cursor: pointer;
     font-family: var(--font-body);
     font-size: 14px;
     font-weight: 600;
     letter-spacing: 0.02em;
     line-height: 1.3;
     min-height: 44px;
     padding: var(--space-sm) var(--space-md);
     transition: background-color 150ms ease, border-color 150ms ease;
   }
   .nwn-button:hover { background: var(--color-selection); }
   .nwn-button:disabled { cursor: not-allowed; opacity: 0.45; }
   .nwn-button:focus-visible { outline: 2px solid var(--color-gold); outline-offset: 2px; }

   .nwn-button--primary {
     border: 2px solid var(--color-gold);
     color: var(--color-text);
   }
   .nwn-button--secondary {
     border: 1px solid var(--color-text-muted);
     color: var(--color-text-muted);
   }
   .nwn-button--auxiliary {
     border: 1px solid var(--color-text-muted);
     color: var(--color-text-muted);
     font-weight: 400;
   }
   .nwn-button--icon {
     background: rgba(232, 213, 160, 0.08);
     border: 1px solid var(--color-gold);
     color: var(--color-text);
     min-height: 36px;
     padding: var(--space-xs);
   }
   ```

10. **ActionBar CSS**: Add to app.css:
    ```css
    .action-bar {
      align-items: center;
      display: flex;
      gap: var(--space-sm);
      justify-content: flex-end;
      padding: var(--space-md);
    }
    .action-bar__left {
      margin-right: auto;
    }
    ```

11. **Selection and status modifiers**: Update existing `.is-selected`, `.is-blocked`, `.is-illegal`, `.is-active` classes to use new tokens:
    - `.is-selected`: `background: var(--color-selection); border-color: var(--color-selection-border);`
    - `.is-blocked`: `border-color: var(--color-blocked);`
    - `.is-illegal`: `border-color: var(--color-destructive);`
    - `.is-active`: `background: var(--color-selection); border-color: var(--color-selection-border);`

12. **Reduced motion**: Add:
    ```css
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
      }
    }
    ```

13. **Scrollbar styling** for dark theme:
    ```css
    ::-webkit-scrollbar { width: 8px; }
    ::-webkit-scrollbar-track { background: var(--color-surface); }
    ::-webkit-scrollbar-thumb { background: var(--color-text-muted); border-radius: 0; }
    ::-webkit-scrollbar-thumb:hover { background: var(--color-gold); }
    ```

**tests/phase-05.2/theme-contract.spec.ts -- Wave 0 test scaffold:**

Create a unit test (node environment, reads CSS files as strings) that validates:
- `tokens.css` contains `--color-surface: #0a0a0a`
- `tokens.css` contains `--color-gold: #c8a84e`
- `tokens.css` contains `--color-text: #e8d5a0`
- `tokens.css` does NOT contain `--color-parchment` (old token removed)
- `tokens.css` does NOT contain `--shadow-panel` (shadow removed)
- `tokens.css` does NOT contain `--color-panel-glow` (removed)
- `tokens.css` contains `--border-frame: 2px solid`
- `tokens.css` contains `--border-frame-inner: 1px solid`
- `app.css` contains `.nwn-frame`
- `app.css` contains `.nwn-button`
- `app.css` does NOT contain `border-radius: 22px` (old rounded panels gone)
- `app.css` does NOT contain `border-radius: 14px` (no old radius values)
- `app.css` does NOT contain `#e3d8c1` (no hardcoded old parchment)
- `app.css` does NOT contain `#c8b89a` (no hardcoded old shadow)
- `app.css` does NOT contain `font-weight: 700` (no 700 weight)
- `fonts.css` does NOT contain `700.css` (700 weight import removed)
- `fonts.css` contains `400.css` for Cormorant Garamond (400 weight added)
- `app.css` contains `prefers-reduced-motion` media query

Pattern: follow the exact same `readFileSync` + `describe` + `expect` structure used in `tests/phase-02/theme-contract.spec.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/phase-05.2/theme-contract.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css contains `--color-surface: #0a0a0a` and `--color-gold: #c8a84e` and `--color-text: #e8d5a0`
    - tokens.css does NOT contain `--color-parchment`, `--shadow-panel`, `--color-panel-glow`
    - tokens.css contains `--border-frame: 2px solid` and `--border-frame-inner: 1px solid`
    - fonts.css does NOT contain `700.css`; fonts.css DOES contain `cormorant-garamond/400.css`
    - app.css does NOT contain any of: `border-radius: 22px`, `border-radius: 14px`, `#e3d8c1`, `#c8b89a`, `font-weight: 700`
    - app.css contains `.nwn-frame` class with `border: 2px solid var(--color-gold)` and `border-radius: 0`
    - app.css contains `.nwn-frame::before` with `inset: 8px`
    - app.css contains `.nwn-button--primary` with `border: 2px solid var(--color-gold)`
    - app.css contains `.nwn-button--secondary` with `border: 1px solid var(--color-text-muted)`
    - app.css contains `.action-bar` class
    - app.css contains `prefers-reduced-motion` media query
    - `npx vitest run tests/phase-05.2/theme-contract.spec.ts -x` exits 0
  </acceptance_criteria>
  <done>Dark NWN1 palette is the global theme, all hardcoded old values are gone, border-radius is globally 0, font weight 700 is eliminated, NwnFrame/NwnButton/ActionBar CSS classes exist, and the Wave 0 theme test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create NwnFrame, NwnButton, and ActionBar React components</name>
  <files>
    apps/planner/src/components/ui/nwn-frame.tsx,
    apps/planner/src/components/ui/nwn-button.tsx,
    apps/planner/src/components/ui/action-bar.tsx
  </files>
  <read_first>
    apps/planner/src/styles/app.css,
    apps/planner/src/components/shell/section-view.tsx,
    apps/planner/src/features/character-foundation/origin-board.tsx,
    .planning/phases/05.2-ux-overhaul-inserted/05.2-PATTERNS.md
  </read_first>
  <action>
**Create `apps/planner/src/components/ui/` directory and three components:**

**nwn-frame.tsx (per D-10, UI-SPEC Panel Frame Treatment):**
```tsx
import type { ReactNode } from 'react';

interface NwnFrameProps {
  as?: 'aside' | 'div' | 'nav' | 'section';
  children: ReactNode;
  className?: string;
}

export function NwnFrame({ as: Tag = 'div', children, className }: NwnFrameProps) {
  return (
    <Tag className={`nwn-frame${className ? ` ${className}` : ''}`}>
      {children}
    </Tag>
  );
}
```
This wraps content in a sharp-cornered double-border NWN1 frame. The `::before` pseudo-element from CSS handles the inner border line. The `as` prop allows semantic HTML elements (nav, aside, section).

**nwn-button.tsx (per D-03, D-10, UI-SPEC Button Styles):**
```tsx
import type { ComponentPropsWithoutRef } from 'react';

type NwnButtonVariant = 'auxiliary' | 'icon' | 'primary' | 'secondary';

interface NwnButtonProps extends ComponentPropsWithoutRef<'button'> {
  variant?: NwnButtonVariant;
}

export function NwnButton({ className, variant = 'primary', ...props }: NwnButtonProps) {
  return (
    <button
      className={`nwn-button nwn-button--${variant}${className ? ` ${className}` : ''}`}
      type="button"
      {...props}
    />
  );
}
```
Uses `ComponentPropsWithoutRef<'button'>` spread pattern from the existing `OriginOptionButton` in origin-board.tsx. Default variant is `primary` (gold border). Default `type` is `"button"` (can be overridden via props spread).

**action-bar.tsx (per D-03, UI-SPEC Bottom Action Bar):**
```tsx
import type { ReactNode } from 'react';
import { NwnButton } from './nwn-button';

interface ActionBarProps {
  acceptDisabled?: boolean;
  acceptLabel?: string;
  cancelLabel?: string;
  leftContent?: ReactNode;
  onAccept?: () => void;
  onCancel?: () => void;
}

export function ActionBar({
  acceptDisabled = false,
  acceptLabel = 'Aceptar',
  cancelLabel = 'Cancelar',
  leftContent,
  onAccept,
  onCancel,
}: ActionBarProps) {
  return (
    <div className="action-bar">
      {leftContent && <div className="action-bar__left">{leftContent}</div>}
      {onCancel && (
        <NwnButton onClick={onCancel} variant="secondary">
          {cancelLabel}
        </NwnButton>
      )}
      {onAccept && (
        <NwnButton disabled={acceptDisabled} onClick={onAccept} variant="primary">
          {acceptLabel}
        </NwnButton>
      )}
    </div>
  );
}
```
The `leftContent` slot is for optional auxiliary buttons like "Recomendado". Default button labels come from the UI-SPEC copywriting contract. `onCancel` and `onAccept` are optional -- if not provided, that button is not rendered.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/planner/tsconfig.json 2>&1; echo TSC_EXIT_CODE:$?</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/planner/src/components/ui/nwn-frame.tsx` exists and exports `NwnFrame`
    - File `apps/planner/src/components/ui/nwn-button.tsx` exists and exports `NwnButton`
    - File `apps/planner/src/components/ui/action-bar.tsx` exists and exports `ActionBar`
    - `NwnFrame` accepts `as`, `children`, `className` props
    - `NwnButton` accepts `variant` prop with values `'primary' | 'secondary' | 'auxiliary' | 'icon'`
    - `ActionBar` imports and uses `NwnButton`
    - `npx tsc --noEmit` reports no type errors for these files
  </acceptance_criteria>
  <done>Three reusable UI primitive components (NwnFrame, NwnButton, ActionBar) exist, type-check cleanly, and are ready to be consumed by shell and feature components in subsequent plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A -- static CSS/component restructure, no new attack surface. This plan changes visual tokens, font imports, CSS rules, and creates presentational React components. No user input processing, no network requests, no data storage changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | N/A | CSS tokens / UI components | accept | Pure visual change; no security-relevant behavior introduced |
</threat_model>

<verification>
1. `npx vitest run tests/phase-05.2/theme-contract.spec.ts -x` passes
2. `npx tsc --noEmit --project apps/planner/tsconfig.json` reports no errors
3. `npx vite build` completes without errors (apps/planner)
4. No occurrences of old token names (`--color-parchment`, `--color-ink`, `--color-bronze`, `--shadow-panel`) in tokens.css
5. No occurrences of `border-radius: 22px` or `border-radius: 14px` in app.css
6. No occurrences of `font-weight: 700` in app.css
</verification>

<success_criteria>
The planner loads with a dark background, golden borders, and light-on-dark text. All panels have sharp corners. Typography is GUI-density (18px uppercase titles, 14px body). Three new UI primitive components (NwnFrame, NwnButton, ActionBar) are exported and type-safe. The Wave 0 theme test validates the token contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ux-overhaul-inserted/05.2-01-SUMMARY.md`
</output>
