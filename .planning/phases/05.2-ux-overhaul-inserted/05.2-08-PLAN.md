---
phase: 05.2-ux-overhaul-inserted
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/skills/skill-sheet.tsx
  - apps/planner/src/features/level-progression/level-sheet.tsx
  - apps/planner/src/features/character-foundation/locked-abilities-panel.tsx
  - apps/planner/src/features/level-progression/ability-increase-control.tsx
autonomous: true
gap_closure: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "No dead CSS rules for deleted components remain in app.css"
    - "All feature files use current NWN1 pattern class names, not old planner-section-view/planner-chip names"
    - "Application renders correctly after CSS cleanup"
  artifacts:
    - path: "apps/planner/src/styles/app.css"
      provides: "Clean CSS without dead rules for deleted components"
    - path: "apps/planner/src/features/skills/skill-sheet.tsx"
      provides: "Uses NWN1 pattern classes instead of planner-section-view__description"
    - path: "apps/planner/src/features/level-progression/level-sheet.tsx"
      provides: "Uses NWN1 pattern classes instead of planner-section-view/planner-chip"
    - path: "apps/planner/src/features/character-foundation/locked-abilities-panel.tsx"
      provides: "Uses NWN1 nwn-frame + selection-screen patterns instead of planner-panel/section-fade"
    - path: "apps/planner/src/features/level-progression/ability-increase-control.tsx"
      provides: "Uses NWN1 pattern classes instead of planner-section-view__highlights/planner-chip"
  key_links:
    - from: "level-sheet.tsx"
      to: "app.css"
      via: "CSS class references"
      pattern: "level-sheet__"
    - from: "skill-sheet.tsx"
      to: "app.css"
      via: "CSS class references"
      pattern: "skill-sheet__"
---

<objective>
Close Gap 2 (WARNING): Remove dead CSS rules for deleted components from app.css and migrate 4 feature files from old class names to NWN1 pattern equivalents.

Purpose: app.css still contains ~30 CSS rules for components deleted during the UX overhaul (section-nav, summary-panel, section-view). Additionally, 4 feature files reference old class names that only work because the dead CSS still exists. Removing the dead CSS without updating the class names would break rendering.

Output: Clean app.css without dead rules, 4 feature files using current NWN1 pattern class names.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-ux-overhaul-inserted/05.2-VERIFICATION.md

<interfaces>
<!-- NWN1 pattern CSS classes currently in use (from app.css) -->

Active class patterns to use as replacements:
- `.detail-panel__body` — for description/body text (color: var(--color-text), font-size: 14px, line-height: 1.5)
- `.nwn-frame` — for panel containers (replaces .planner-panel with double-border)
- `.nwn-button` — for button elements
- `.option-list__item` — for selectable items in lists
- `.level-sheet__*` — existing level sheet classes (already in app.css)
- `.skill-sheet__*` — existing skill sheet classes (already in app.css)
- `.class-option` — for class choice chips (already exists, replaces .planner-chip usage)

Dead class names to remove from app.css:
- `.planner-nav__list`, `.planner-nav__link`, `.planner-nav__icon`, `.planner-nav__eyebrow` (section-nav deleted)
- `.planner-summary`, `.planner-summary__grid` (summary-panel deleted)
- `.planner-section-view` and all sub-classes (section-view deleted)
- `.planner-chip` (replaced by .class-option)
- `.section-fade` animation and `.section-fade` class (deleted component)
- `@keyframes section-fade` (no longer used)

Classes to KEEP (still referenced by active components):
- `.planner-shell`, `.planner-layout*` (shell frame uses these)
- `.planner-panel`, `.planner-panel--inner`, `.planner-panel--nav` (used by level-sheet, skill-sheet, etc.)
- `.planner-section-view__callout` (used by locked-abilities-panel -- will be migrated)
- `.foundation-*`, `.attribute-*`, `.class-option`, `.level-*`, `.skill-*` (active feature styles)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 feature files from old class names to NWN1 equivalents</name>
  <files>
    apps/planner/src/features/skills/skill-sheet.tsx,
    apps/planner/src/features/level-progression/level-sheet.tsx,
    apps/planner/src/features/character-foundation/locked-abilities-panel.tsx,
    apps/planner/src/features/level-progression/ability-increase-control.tsx
  </files>
  <read_first>
    apps/planner/src/features/skills/skill-sheet.tsx,
    apps/planner/src/features/level-progression/level-sheet.tsx,
    apps/planner/src/features/character-foundation/locked-abilities-panel.tsx,
    apps/planner/src/features/level-progression/ability-increase-control.tsx,
    apps/planner/src/styles/app.css
  </read_first>
  <action>
    **skill-sheet.tsx:**
    - Line 115: `className="planner-panel planner-panel--inner level-sheet skill-sheet"` -- keep as-is, `.planner-panel` is still active CSS
    - Line 118: `className="planner-section-view__description"` -- change to `className="detail-panel__body"` (same visual: color muted, 14px, 1.5 line-height)

    **level-sheet.tsx:**
    - Line 26: `className="planner-panel planner-panel--inner level-sheet"` -- keep as-is, `.planner-panel` is still active
    - Line 29: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`
    - Line 36: `className="planner-section-view__highlights"` -- change to `className="level-sheet__class-options"` (new class to add to app.css)
    - Line 41: `className={\`planner-chip class-option is-...\`}` -- change to `className={\`class-option is-...\`}` (remove `planner-chip`, `.class-option` already has all needed styles)
    - Line 72: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`
    - Line 84: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`
    - Line 89: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`
    - Line 105: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`

    **locked-abilities-panel.tsx:**
    - Line 5: `className="planner-panel section-fade planner-section-view"` -- change to `className="nwn-frame locked-abilities-panel"` (use NWN1 frame + dedicated class)
    - Line 6: `className="planner-section-view__header"` -- change to `className="locked-abilities-panel__header"` (new class)
    - Line 7: `className="planner-section-view__eyebrow"` -- change to `className="locked-abilities-panel__eyebrow"` (new class)
    - Line 9: `className="planner-section-view__description"` -- change to `className="detail-panel__body"`
    - Line 14: `className="planner-section-view__callout planner-panel planner-panel--inner"` -- change to `className="locked-abilities-panel__callout nwn-frame"`

    **ability-increase-control.tsx:**
    - Line 30: `className="planner-section-view__highlights"` -- change to `className="level-sheet__class-options"` (same layout as level-sheet class options)
    - Line 34: `className={\`planner-chip class-option...\`}` -- change to `className={\`class-option...\`}` (remove `planner-chip`, `.class-option` has the styles)

    Add new CSS rules to app.css for the new class names:

    ```css
    /* ── Level Sheet Class Options ───────────────────────────── */
    .level-sheet__class-options {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-sm);
    }

    /* ── Locked Abilities Panel ──────────────────────────────── */
    .locked-abilities-panel { padding: var(--space-lg); }
    .locked-abilities-panel__header { margin-bottom: var(--space-md); }
    .locked-abilities-panel__eyebrow {
      color: var(--color-text-muted);
      font-size: 14px;
      font-weight: 400;
      letter-spacing: 0.16em;
      margin: 0 0 var(--space-xs);
      text-transform: uppercase;
    }
    .locked-abilities-panel__callout { padding: var(--space-lg); }
    ```

    Place these new rules after the existing "Level Sheet" section in app.css (around line 880).
  </action>
  <verify>
    <automated>cd apps/planner && npx tsc --noEmit --project tsconfig.json && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - grep "planner-section-view" apps/planner/src/features/ returns 0 matches (recursive)
    - grep "planner-chip" apps/planner/src/features/ returns 0 matches (recursive)
    - grep "section-fade" apps/planner/src/features/ returns 0 matches (recursive)
    - skill-sheet.tsx contains "detail-panel__body" instead of "planner-section-view__description"
    - level-sheet.tsx contains "level-sheet__class-options" and "class-option" without "planner-chip"
    - locked-abilities-panel.tsx contains "nwn-frame" and "locked-abilities-panel" classes
    - ability-increase-control.tsx contains "level-sheet__class-options" and "class-option" without "planner-chip"
    - TypeScript compilation succeeds
    - Vite build succeeds
  </acceptance_criteria>
  <done>All 4 feature files use NWN1 pattern class names, no old class name references remain</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead CSS rules from app.css</name>
  <files>apps/planner/src/styles/app.css</files>
  <read_first>apps/planner/src/styles/app.css</read_first>
  <action>
    Remove the following dead CSS rule blocks from app.css. Use line ranges from the current file as reference.

    **Lines 46-57 (heading rules for deleted components):**
    Remove `.planner-nav h2`, `.planner-summary h2`, `.planner-section-view h1`, `.planner-section-view h2` from the combined selector. Keep only selectors that are still used. If none of these selectors are used elsewhere, delete the entire block.

    **Lines 59-67 (eyebrow rules for deleted components):**
    Remove `.planner-nav__eyebrow` and `.planner-section-view__eyebrow` from the selector. If the entire block becomes unused, delete it.

    **Lines 155-169 (.planner-panel--nav):**
    Check if `.planner-panel--nav` is used anywhere. If not, delete lines 162-169.

    **Lines 171-213 (Nav section):**
    Delete the entire `/* -- Nav */` section: `.planner-nav__list`, `.planner-nav__link`, `.planner-nav__link:hover`, `.planner-nav__link.is-active`, `.planner-nav__icon`. These components no longer exist.

    **Lines 215-239 (Summary section):**
    Delete `.planner-summary` and `.planner-summary__grid` with dt/dd rules IF no active component uses them. Check first: `.planner-summary__grid` is still used by `skill-sheet.tsx` line 124 and `attribute-budget` section. KEEP `.planner-summary__grid` styles. Delete only `.planner-summary` (the standalone block at lines 216-220) if no component uses it.

    **Lines 241-276 (Section View section):**
    Delete the entire `/* -- Section View */` section: `.planner-section-view`, `.planner-section-view h1`, `.planner-section-view h3`, `.planner-section-view__description`, `.planner-section-view__highlights`. After Task 1 migrates the feature files, nothing references these classes.

    **Lines 278-286 (Chip section):**
    Delete `/* -- Chip */` section: `.planner-chip`. After Task 1, `.class-option` provides all chip styling.

    **Lines 330-332 (.foundation-step .planner-section-view__description, .foundation-sheet .planner-section-view__description):**
    Delete these rules since `.planner-section-view__description` is removed.

    **Lines 395 (.attribute-budget .planner-summary__grid):**
    KEEP this rule -- `.planner-summary__grid` is still used.

    **Lines 476-478 (progression-summary-strip .planner-section-view__eyebrow etc.):**
    Remove `.planner-section-view__eyebrow` from the selector if used, or `.planner-section-view__description` from the selector at line 478. Keep the `.planner-summary__grid dt` selector if `.planner-summary__grid` is still active in the progression strip.

    **Line 524 (.progression-summary-strip .planner-chip):**
    Delete this rule -- `.planner-chip` is being removed.

    **Lines 628-631 (.skill-summary-strip .planner-summary__grid dd):**
    KEEP if `.planner-summary__grid` is still used.

    **Line 906 (.planner-section-view__callout):**
    Delete this rule -- the callout class is being migrated to `.locked-abilities-panel__callout`.

    **Lines 1394-1417 (section-fade animation):**
    Delete `.section-fade` class (line 1394-1396) and `@keyframes section-fade` (lines 1409-1417). The `.section-fade` class was used by locked-abilities-panel which is being migrated.

    **IMPORTANT:** Before deleting each block, verify no active component references it by searching the `apps/planner/src/` directory. Only delete rules confirmed as dead.

    After all deletions, verify the file is syntactically valid CSS by running the Vite build.
  </action>
  <verify>
    <automated>cd apps/planner && npx vite build && npx vitest run tests/phase-05.2/theme-contract.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - app.css does NOT contain ".planner-nav__list" or ".planner-nav__link" or ".planner-nav__icon"
    - app.css does NOT contain ".planner-section-view" as a standalone class (may appear in comments)
    - app.css does NOT contain ".planner-chip" as a standalone rule
    - app.css does NOT contain ".section-fade" class
    - app.css does NOT contain "@keyframes section-fade"
    - app.css DOES still contain ".planner-panel" (still used)
    - app.css DOES still contain ".planner-summary__grid" (still used by skill-sheet and attribute-budget)
    - app.css DOES contain ".level-sheet__class-options" (new rule from Task 1)
    - app.css DOES contain ".locked-abilities-panel" (new rule from Task 1)
    - Vite build succeeds
    - Theme contract tests pass
  </acceptance_criteria>
  <done>Dead CSS removed, app.css contains only active rules, build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- CSS and component class name changes only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-08-01 | n/a | CSS/JSX only | accept | No runtime security impact from CSS class renaming |
</threat_model>

<verification>
After both tasks complete:
1. `npx vite build` in apps/planner succeeds
2. `npx tsc --noEmit --project apps/planner/tsconfig.json` exits 0
3. `grep -r "planner-section-view" apps/planner/src/features/` returns 0 matches
4. `grep -r "planner-chip" apps/planner/src/features/` returns 0 matches
5. `grep -r "section-fade" apps/planner/src/features/` returns 0 matches
6. `npx vitest run tests/phase-05.2/theme-contract.spec.ts` passes
7. `grep "planner-nav__list" apps/planner/src/styles/app.css` returns 0 matches
</verification>

<success_criteria>
- Dead CSS rules for deleted components removed from app.css
- 4 feature files migrated to NWN1 pattern class names
- Build succeeds, no TypeScript errors
- Theme contract tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ux-overhaul-inserted/05.2-08-SUMMARY.md`
</output>
