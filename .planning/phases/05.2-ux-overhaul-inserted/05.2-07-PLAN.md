---
phase: 05.2-ux-overhaul-inserted
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-02/navigation-copy.spec.ts
  - tests/phase-02/shell-routes.spec.ts
  - tests/phase-05.2/stepper-navigation.spec.tsx
  - tests/phase-05.2/stepper-states.spec.tsx
  - tests/phase-03/foundation-validation.spec.ts
  - tests/phase-03/origin-flow.spec.tsx
  - tests/phase-03/summary-status.spec.tsx
  - tests/phase-03/attribute-budget.spec.tsx
  - tests/phase-04/build-progression-shell.spec.tsx
  - tests/phase-04/level-sheet-gains.spec.tsx
  - tests/phase-04/level-timeline.spec.tsx
  - tests/phase-04/progression-revalidation.spec.tsx
  - tests/phase-05/skill-allocation-flow.spec.tsx
  - tests/phase-05/skill-stats-sync.spec.tsx
autonomous: true
gap_closure: true
requirements:
  - FLOW-01
  - FLOW-02

must_haves:
  truths:
    - "All 14 test files pass after deity references are removed"
    - "No test file references setDeity, deity:none, or Deidad label"
    - "Full test suite passes with 0 failures and 0 errors"
  artifacts:
    - path: "tests/phase-02/navigation-copy.spec.ts"
      provides: "Stepper label assertions without Deidad"
      contains: "Raza.*Alineamiento.*Atributos"
    - path: "tests/phase-02/shell-routes.spec.ts"
      provides: "Origin steps array without deity"
      contains: "race.*alignment.*attributes"
    - path: "tests/phase-05.2/stepper-navigation.spec.tsx"
      provides: "3 origin step buttons (not 4)"
      contains: "3 origin step"
    - path: "tests/phase-05.2/stepper-states.spec.tsx"
      provides: "No deity step state assertions"
    - path: "tests/phase-03/foundation-validation.spec.ts"
      provides: "Origin evaluation without deity argument"
    - path: "tests/phase-03/origin-flow.spec.tsx"
      provides: "Origin flow without deity step or setDeity"
    - path: "tests/phase-03/summary-status.spec.tsx"
      provides: "Summary status without setDeity calls"
    - path: "tests/phase-03/attribute-budget.spec.tsx"
      provides: "Attribute budget without setDeity in primeOrigin"
    - path: "tests/phase-04/build-progression-shell.spec.tsx"
      provides: "Build progression without setDeity in primeOrigin"
    - path: "tests/phase-04/level-sheet-gains.spec.tsx"
      provides: "Level sheet gains without setDeity in primeFoundation"
    - path: "tests/phase-04/level-timeline.spec.tsx"
      provides: "Level timeline without setDeity in primeOrigin"
    - path: "tests/phase-04/progression-revalidation.spec.tsx"
      provides: "Progression revalidation without setDeity in primeFoundation"
    - path: "tests/phase-05/skill-allocation-flow.spec.tsx"
      provides: "Skill allocation without setDeity in primeFoundation"
    - path: "tests/phase-05/skill-stats-sync.spec.tsx"
      provides: "Skill stats sync without setDeity in primeFoundation"
  key_links:
    - from: "tests/phase-02/shell-routes.spec.ts"
      to: "apps/planner/src/lib/sections.ts"
      via: "originSteps import"
      pattern: "originSteps.*race.*alignment.*attributes"
---

<objective>
Close Gap 1 (BLOCKER): Remove all deity references from 14 test files across phases 02-05.2 that broke after Plan 06 removed the deity selection step.

Purpose: Plan 06 legitimately removed the deity step from the origin flow (deity catalog is null per server data). However, 14 test files still call `setDeity()` (which no longer exists on the store) or expect `'Deidad'` labels in the stepper. This causes 24 test failures and 2 unhandled errors.

Output: All 14 test files updated, full suite passes with 0 failures.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-ux-overhaul-inserted/05.2-VERIFICATION.md

<interfaces>
<!-- Current production state: deity has been removed from the foundation store and sections -->

From apps/planner/src/features/character-foundation/store.ts:
- CharacterFoundationStoreState has: setRace, setAlignment, setSubrace, setBaseAttribute, resetFoundation
- NO setDeity method exists
- NO deityId field exists

From apps/planner/src/lib/sections.ts:
- OriginStep = 'race' | 'alignment' | 'attributes' (NO 'deity')
- originSteps array has 3 entries: race, alignment, attributes

From packages/rules-engine/src/foundation/origin-rules.ts:
- EvaluateOriginSelectionInput still accepts deityId and deities fields (rules engine unchanged)
- Tests calling evaluateOriginSelection directly must still pass deityId/deities
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deity from Phase 02 and Phase 05.2 stepper/shell tests</name>
  <files>
    tests/phase-02/navigation-copy.spec.ts,
    tests/phase-02/shell-routes.spec.ts,
    tests/phase-05.2/stepper-navigation.spec.tsx,
    tests/phase-05.2/stepper-states.spec.tsx
  </files>
  <read_first>
    tests/phase-02/navigation-copy.spec.ts,
    tests/phase-02/shell-routes.spec.ts,
    tests/phase-05.2/stepper-navigation.spec.tsx,
    tests/phase-05.2/stepper-states.spec.tsx,
    apps/planner/src/lib/sections.ts
  </read_first>
  <action>
    **tests/phase-02/navigation-copy.spec.ts (line 24):**
    Change the label array from `['Raza', 'Alineamiento', 'Deidad', 'Atributos']` to `['Raza', 'Alineamiento', 'Atributos']`. Remove 'Deidad' from the for-of loop.

    **tests/phase-02/shell-routes.spec.ts (line 23-25):**
    Change the expected originSteps from `['race', 'alignment', 'deity', 'attributes']` to `['race', 'alignment', 'attributes']`. Remove 'deity' from the toEqual array.

    **tests/phase-05.2/stepper-navigation.spec.tsx (lines 46-53):**
    The test "renders 4 origin step buttons with correct labels" must become "renders 3 origin step buttons with correct labels". Remove the line `expect(screen.getByRole('button', { name: /Deidad/ })).toBeInTheDocument();`. Update the test name from "4" to "3".

    **tests/phase-05.2/stepper-states.spec.tsx (lines 62-72):**
    Delete the entire test "shows deity step as pending when alignment is not set" (lines 62-72). This test asserts on a Deidad button that no longer exists.
  </action>
  <verify>
    <automated>cd apps/planner && npx vitest run tests/phase-02/navigation-copy.spec.ts tests/phase-02/shell-routes.spec.ts tests/phase-05.2/stepper-navigation.spec.tsx tests/phase-05.2/stepper-states.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-02/navigation-copy.spec.ts contains "['Raza', 'Alineamiento', 'Atributos']" and does NOT contain 'Deidad'
    - tests/phase-02/shell-routes.spec.ts contains "['race', 'alignment', 'attributes']" and does NOT contain "'deity'"
    - tests/phase-05.2/stepper-navigation.spec.tsx contains "3 origin step" in a test name and does NOT contain "Deidad"
    - tests/phase-05.2/stepper-states.spec.tsx does NOT contain "deity" or "Deidad" anywhere
    - All 4 test files pass with 0 failures
  </acceptance_criteria>
  <done>Phase 02 and 05.2 stepper/shell tests pass without deity references</done>
</task>

<task type="auto">
  <name>Task 2: Remove setDeity from Phase 03 test setup helpers and update origin flow tests</name>
  <files>
    tests/phase-03/foundation-validation.spec.ts,
    tests/phase-03/origin-flow.spec.tsx,
    tests/phase-03/summary-status.spec.tsx,
    tests/phase-03/attribute-budget.spec.tsx
  </files>
  <read_first>
    tests/phase-03/foundation-validation.spec.ts,
    tests/phase-03/origin-flow.spec.tsx,
    tests/phase-03/summary-status.spec.tsx,
    tests/phase-03/attribute-budget.spec.tsx,
    apps/planner/src/features/character-foundation/store.ts,
    packages/rules-engine/src/foundation/origin-rules.ts
  </read_first>
  <action>
    **tests/phase-03/foundation-validation.spec.ts:**
    This file calls `evaluateOriginSelection()` directly, NOT the store. The `EvaluateOriginSelectionInput` interface still expects `deityId` and `deities` fields. The `createOriginSelection` helper at lines 6-19 builds the input with `deityId: null` and `deities: phase03FoundationFixture.deities`. This is CORRECT -- the rules engine interface is unchanged. However, check whether `phase03FoundationFixture.deities` still exists. If it does not, pass `deities: [{ id: 'deity:none', allowedAlignmentIds: [] }]` as a hardcoded fallback. The 3 tests pass `deityId: 'deity:none'` in overrides -- this is fine for the rules engine. Run the tests to confirm -- these may already pass since they don't call `setDeity()` on the store.

    **tests/phase-03/origin-flow.spec.tsx:**
    - Lines 15-20: The `beforeEach` sets old shell store fields (`activeSection: 'build'`, `summaryPanelOpen: true`). These fields no longer exist on the shell store. Replace with the current shape: `{ activeOriginStep: 'race', activeLevelSubStep: null, characterSheetTab: 'stats', expandedLevel: null, mobileNavOpen: false }`.
    - Test "renders the ordered origin steps and explicit deity option" (lines 24-43): This test expects to find 'Deidad' text and 'Sin deidad' text in the rendered output. The shell no longer has a deity step. Update this test to verify the 3 origin steps exist in order (Raza < Alineamiento < Atributos) and remove the Deidad/Sin deidad assertions. Also remove the assertion for 'Confirmar origen' button if it no longer exists. Rename test to "renders the ordered origin steps without deity".
    - Test "keeps Atributos blocked" (lines 45-61): Uses `createPlannerRouter(['/abilities'])` but routes no longer exist (router was simplified to single root route). This test needs a full rewrite to use the stepper-driven navigation: render PlannerShellFrame, set activeOriginStep to 'attributes' without completing race first, and check for the locked state. HOWEVER, if this rewrite is too complex, simply update the shell store setState and remove the router-based rendering. The key assertion is the heading "El origen del personaje sigue incompleto" and the body text. Update body text to remove "deidad" if it appears: change "Selecciona raza, subraza, alineamiento y deidad en Construccion para desbloquear Atributos." to match whatever the current LockedAbilitiesPanel shows. Read the actual text from `apps/planner/src/lib/copy/es.ts` to get the exact current string.

    **tests/phase-03/summary-status.spec.tsx:**
    - Lines 15-20: Same old shell store shape issue. Replace with current shape: `{ activeOriginStep: 'race', activeLevelSubStep: null, characterSheetTab: 'stats', expandedLevel: null, mobileNavOpen: false }`.
    - Line 44: `foundationStore.setDeity('deity:none')` -- DELETE this line. The store no longer has setDeity.
    - Line 53: Expects text `'Elfo . Elfo lunar . Neutral bueno . Sin deidad'`. Remove ` . Sin deidad` from the expected string so it becomes `'Elfo . Elfo lunar . Neutral bueno'`. Also check if this text is actually rendered by reading the selector that produces it.
    - Line 56: `expect(screen.getAllByText('Sin deidad').length).toBeGreaterThan(0)` -- DELETE this line entirely.
    - Line 63: `foundationStore.setDeity('deity:none')` -- DELETE this line.

    **tests/phase-03/attribute-budget.spec.tsx:**
    - Lines 11-17: The `primeOrigin` function calls `foundationStore.setDeity('deity:none')` at line 16. DELETE this line. The function should only call `setRace` and `setAlignment`.
    - Lines 38-45: The `beforeEach` sets old shell store shape. Replace with current shape: `{ activeOriginStep: 'attributes', activeLevelSubStep: null, characterSheetTab: 'stats', expandedLevel: null, mobileNavOpen: false }`.
  </action>
  <verify>
    <automated>cd apps/planner && npx vitest run tests/phase-03/foundation-validation.spec.ts tests/phase-03/summary-status.spec.tsx tests/phase-03/attribute-budget.spec.tsx tests/phase-03/origin-flow.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No file in tests/phase-03/ contains the string "setDeity"
    - tests/phase-03/origin-flow.spec.tsx does NOT contain "Deidad" or "Sin deidad"
    - tests/phase-03/summary-status.spec.tsx does NOT contain "setDeity" or "Sin deidad"
    - tests/phase-03/attribute-budget.spec.tsx does NOT contain "setDeity"
    - All 4 phase-03 test files pass with 0 failures
  </acceptance_criteria>
  <done>Phase 03 tests pass without deity store calls or deity UI assertions</done>
</task>

<task type="auto">
  <name>Task 3: Remove setDeity from Phase 04 and Phase 05 test setup helpers</name>
  <files>
    tests/phase-04/build-progression-shell.spec.tsx,
    tests/phase-04/level-sheet-gains.spec.tsx,
    tests/phase-04/level-timeline.spec.tsx,
    tests/phase-04/progression-revalidation.spec.tsx,
    tests/phase-05/skill-allocation-flow.spec.tsx,
    tests/phase-05/skill-stats-sync.spec.tsx
  </files>
  <read_first>
    tests/phase-04/build-progression-shell.spec.tsx,
    tests/phase-04/level-sheet-gains.spec.tsx,
    tests/phase-04/level-timeline.spec.tsx,
    tests/phase-04/progression-revalidation.spec.tsx,
    tests/phase-05/skill-allocation-flow.spec.tsx,
    tests/phase-05/skill-stats-sync.spec.tsx,
    apps/planner/src/features/character-foundation/store.ts
  </read_first>
  <action>
    All 6 files follow the same pattern: a `primeOrigin()` or `primeFoundation()` helper function that calls `foundationStore.setDeity('deity:none')`. Delete that one line from each helper. The store no longer exposes `setDeity`.

    Additionally, all 6 files set old shell store shape in `beforeEach` (e.g., `activeSection: 'build'`, `summaryPanelOpen: true`). Replace with the current shape: `{ activeOriginStep: 'race', activeLevelSubStep: null, characterSheetTab: 'stats', expandedLevel: null, mobileNavOpen: false }`. For files that set `activeSection: 'skills'`, use `activeOriginStep: 'race'` as well (the stepper-based shell does not have an activeSection concept).

    **Specific changes per file:**

    **tests/phase-04/build-progression-shell.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 25-30: Replace shell store setState with `{ activeOriginStep: 'race', activeLevelSubStep: null, characterSheetTab: 'stats', expandedLevel: null, mobileNavOpen: false }`

    **tests/phase-04/level-sheet-gains.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 27-32: Replace shell store setState with current shape

    **tests/phase-04/level-timeline.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 25-30: Replace shell store setState with current shape

    **tests/phase-04/progression-revalidation.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 26-31: Replace shell store setState with current shape

    **tests/phase-05/skill-allocation-flow.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 28-33: Replace shell store setState with current shape

    **tests/phase-05/skill-stats-sync.spec.tsx:**
    - Line 17: Delete `foundationStore.setDeity('deity:none');`
    - Lines 35-40: Replace shell store setState with current shape

    After all changes, run the full test suite to confirm 0 failures and 0 errors.
  </action>
  <verify>
    <automated>cd apps/planner && npx vitest run tests/phase-04/ tests/phase-05/</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "setDeity" tests/phase-04/ tests/phase-05/ returns 0 matches
    - grep -r "activeSection" tests/phase-04/ tests/phase-05/ returns 0 matches
    - grep -r "summaryPanelOpen" tests/phase-04/ tests/phase-05/ returns 0 matches
    - All phase-04 and phase-05 test files pass with 0 failures
    - Full suite: `npx vitest run tests/` shows 0 failures and 0 errors
  </acceptance_criteria>
  <done>All 14 test files pass, full suite green with 0 failures and 0 errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan modifies only test files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-07-01 | n/a | test files only | accept | Test-only changes carry no runtime security risk |
</threat_model>

<verification>
After all 3 tasks complete:
1. `npx vitest run tests/` must show 196/196 tests passing, 0 failures, 0 errors
2. `grep -r "setDeity" tests/` must return 0 matches
3. `grep -r "'Deidad'" tests/` must return 0 matches
4. `npx tsc --noEmit --project apps/planner/tsconfig.json` exits 0
</verification>

<success_criteria>
- All 24 previously-failing tests now pass
- No test file references setDeity(), deity:none, or 'Deidad' label
- Full suite: 196/196 pass, 0 fail, 0 errors
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ux-overhaul-inserted/05.2-07-SUMMARY.md`
</output>
