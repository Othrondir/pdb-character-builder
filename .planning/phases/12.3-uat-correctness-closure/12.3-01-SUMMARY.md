---
phase: 12.3-uat-correctness-closure
plan: 01
subsystem: character-foundation
tags: [attributes, point-buy, overspend-gate, attributes-board, rules-engine, tdd, uat-b1]
requirements: [ABIL-01]
requires:
  - 12.2-02 (applyRaceModifiers foundation pattern; racialModifiers store field)
  - "phase03FoundationFixture.attributeRules (baseScore/budget/costByScore/maximum/minimum)"
provides:
  - nextIncrementCost(currentValue, costByScore, maximum) — pure point-buy cost-delta helper
  - canIncrementAttribute(currentValue, remainingPoints, costByScore, maximum) — pure overspend + ceiling gate
  - AttributesBoard `+` button now disabled on overspend OR at maximum (single UI gate)
affects:
  - tests/phase-12.3/attributes-budget-gate.spec.ts (new regression spec, 18 assertions)
tech-stack:
  added: []
  patterns:
    - pure rules-engine helpers exported from `@rules-engine/foundation/ability-budget` (no React, no store reads)
    - framework-agnostic gate composable by selectors, tests, and UI alike
    - barrel wildcard re-export passes new symbols through without touching `foundation/index.ts`
    - UI-gated overspend protection (store setters remain unchanged; the button `disabled` prop is the single source of truth for the user-driven path)
key-files:
  created:
    - tests/phase-12.3/attributes-budget-gate.spec.ts
    - .planning/phases/12.3-uat-correctness-closure/deferred-items.md
  modified:
    - packages/rules-engine/src/foundation/ability-budget.ts
    - apps/planner/src/features/character-foundation/attributes-board.tsx
decisions:
  - "Add two pure helpers (nextIncrementCost + canIncrementAttribute) rather than widening calculateAbilityBudgetSnapshot — the snapshot reports overspend post-hoc for validation copy, but the UI needs a forward-looking per-click gate that the snapshot cannot express without refactoring its shape."
  - "UI-layer gate only — no store-side rejection of setBaseAttribute. Mirrors the existing `-` button's `baseValue <= minimum` gate and keeps the store framework-agnostic. The plan explicitly rules direct-input overrides out of scope (no overspend path exists outside the `+` click)."
  - "No useMemo — canIncrementAttribute is O(1) per render (one object lookup + subtraction). Memoisation would add more code than it saves and would force a stable costByScore reference, which is already stable via phase03FoundationFixture module-scope."
  - "Defensive null return for out-of-table values (e.g., currentValue=7). The point-buy table only covers 8..18; anything outside is never bumpable. Fail-closed matches the server-valid-only constraint."
metrics:
  duration: "~12 min"
  completed: "2026-04-18T23:56:00Z"
  tests_added: 18
  tests_total: 482
  commits: 2 (excl. docs)
---

# Phase 12.3 Plan 01: Attributes Overspend Gate Summary

**One-liner:** Add two pure rules-engine helpers (`nextIncrementCost` + `canIncrementAttribute`) and rewire the `AttributesBoard` `+` button's `disabled` prop to consult the gate — the UAT B1 click that drove `Puntos restantes` to -2 is now blocked fail-closed, with a regression spec that locks the full point-buy cost curve and the exact repro build.

## Outcome

Closes Phase 12.3 UAT Bug B1 (CRITICAL) at the UI layer:

- `nextIncrementCost(14, costByScore, 18)` returns `2` (the UAT trigger boundary: 15 cost 8 minus 14 cost 6).
- `canIncrementAttribute(14, 0, costByScore, 18)` returns `false` — the click that produced `Puntos restantes: -2` can no longer be fired.
- For the UAT repro build (base 8/8/8/8/8/8 → FUE/DES/CON 14 + INT/SAB/CAR 12 = 30 pts spent, 0 remaining), every `+` button is disabled: FUE 14 @ 0 (cost 2), DES 14 @ 0 (cost 2), CON 14 @ 0 (cost 2), INT 12 @ 0 (cost 1), SAB 12 @ 0 (cost 1), CAR 12 @ 0 (cost 1).
- Existing ceiling behavior preserved: `canIncrementAttribute(18, 100, ...)` returns `false` at maximum regardless of remaining points.
- `-` button gate unchanged (`baseValue <= minimum`); store surface unchanged.
- Full Vitest suite: **482/482 green** (was 464 pre-plan; +18 new gate assertions; well above the ≥445 floor).

## Commits (this plan)

| # | Hash    | Message                                                                              |
|---|---------|--------------------------------------------------------------------------------------|
| 1 | 5a39544 | test(12.3-01): add attributes overspend gate regression spec (RED)                   |
| 2 | 45fe596 | fix(12.3-01): gate attributes + button on canIncrementAttribute (B1 overspend)       |

## Tasks

### Task 1 — RED spec (commit 5a39544)

Created `tests/phase-12.3/attributes-budget-gate.spec.ts` (117 lines, 18 assertions) split across three `describe` blocks:

- **`nextIncrementCost` — pure helper (8 assertions):** Full cost-curve lock: 8→9=1, 13→14=1, 14→15=2 (UAT trigger), 15→16=2, 16→17=3, 17→18=3, 18→null (at max), 7→null (out-of-range defensive).
- **`canIncrementAttribute` — overspend + ceiling gate (6 assertions):** 14 @ 0 blocked (B1 lock), 14 @ 2 allowed (exact-enough), 14 @ 1 blocked (1 short), 18 @ 100 blocked (ceiling wins), 8 @ 1 allowed, 8 @ 0 blocked.
- **UAT B1 repro — composed snapshot + gate (4 assertions):** `calculateAbilityBudgetSnapshot` reports `remainingPoints === 0` for the 14/14/14/12/12/12 build; FUE 14 @ 0 blocked (cost 2); INT 12 @ 0 blocked (cost 1); sanity — 12 @ 1 allowed (exact-enough is not over-gated).

Initial run: 17/18 failed — all 17 `canIncrementAttribute` / `nextIncrementCost` calls threw `TypeError: (0 , canIncrementAttribute) is not a function` (vitest transforms named imports to live bindings; missing exports surface as undefined-callable rather than ImportError). The one pass was the `calculateAbilityBudgetSnapshot.remainingPoints === 0` assertion, which confirms the existing snapshot is correct — the gap is purely forward-looking UI gating. That mixed-failure shape IS the RED state.

### Task 2 — GREEN helpers + UI wiring (commit 45fe596)

**`packages/rules-engine/src/foundation/ability-budget.ts`** — added two exports above `calculateAbilityBudgetSnapshot`:

```typescript
export function nextIncrementCost(
  currentValue: number,
  costByScore: Record<string, number>,
  maximum: number,
): number | null {
  if (currentValue >= maximum) return null;
  const nextValue = currentValue + 1;
  const currentCost = costByScore[String(currentValue)];
  const nextCost = costByScore[String(nextValue)];
  if (currentCost === undefined || nextCost === undefined) return null;
  return nextCost - currentCost;
}

export function canIncrementAttribute(
  currentValue: number,
  remainingPoints: number,
  costByScore: Record<string, number>,
  maximum: number,
): boolean {
  const nextCost = nextIncrementCost(currentValue, costByScore, maximum);
  if (nextCost === null) return false;
  return remainingPoints - nextCost >= 0;
}
```

Both helpers are framework-agnostic (no React, no store reads, no side effects) and O(1). The foundation barrel (`packages/rules-engine/src/foundation/index.ts`) already re-exports `./ability-budget` via wildcard, so the new symbols flow through automatically — no barrel edit needed.

**`apps/planner/src/features/character-foundation/attributes-board.tsx`** — three edits:

- Added `import { canIncrementAttribute } from '@rules-engine/foundation/ability-budget';`.
- Extended the destructure from `attributeRules: { maximum, minimum }` to `attributeRules: { costByScore, maximum, minimum }`.
- Replaced the `+` button's `disabled={baseValue >= maximum}` with `disabled={!canIncrementAttribute(baseValue, attributeBudget.remainingPoints, costByScore, maximum)}`.

No other changes — the `-` button, `setBaseAttribute`, the store, and every other surface of the component stay identical.

## Verification

- `pnpm vitest run tests/phase-12.3/attributes-budget-gate.spec.ts` — **18/18 green.**
- `pnpm vitest run` — **482/482 green** (full suite, no regressions; +18 from this plan).
- `pnpm typecheck` — clean for this plan's files (see Deferred Issues for one out-of-scope residual from a parallel Wave 1 plan).
- `grep 'export function nextIncrementCost' packages/rules-engine/src/foundation/ability-budget.ts` → 1 hit (line 34).
- `grep 'export function canIncrementAttribute' packages/rules-engine/src/foundation/ability-budget.ts` → 1 hit (line 54).
- `grep 'canIncrementAttribute' apps/planner/src/features/character-foundation/attributes-board.tsx` → 2 hits (import + `disabled` call).

## Success Criteria (from PLAN)

1. `nextIncrementCost(currentValue, costByScore, maximum)` returns the 1-step cost delta from the point-buy table, or null at/above maximum. **PASSED** — locked by 8 assertions.
2. `canIncrementAttribute(currentValue, remainingPoints, costByScore, maximum)` returns false when increment would push remaining negative OR when already at maximum. **PASSED** — locked by 6 assertions.
3. Both helpers exported from `@rules-engine/foundation/ability-budget` and flow through the foundation barrel. **PASSED** — grep + vitest resolve confirm.
4. `AttributesBoard` `+` button `disabled` prop reads `!canIncrementAttribute(...)` — no additional UI/store changes. **PASSED** — `-` button, store, other props unchanged.
5. Regression spec covers full cost-curve + canIncrement boundary table + UAT B1 repro. **PASSED** — 18 assertions across 3 blocks.
6. Atomic commits: `test(12.3-01)` RED + `fix(12.3-01)` GREEN + `docs(12.3-01)` summary. **PASSED** — 5a39544 + 45fe596 + this commit.

## Deviations from Plan

**None — plan executed exactly as written.** No Rule 1-3 auto-fixes needed; no Rule 4 architectural checkpoints. The spec's expected RED signal was "unexported member" at import time, but vitest's dev-transform surface reported the same gap as `TypeError: canIncrementAttribute is not a function` at call time — same RED semantics (missing export), different diagnostic phrasing.

## Deferred Issues

- `tests/phase-12.3/hit-points-selector.spec.ts(6,34): Cannot find module '@rules-engine/progression/compute-hit-points'` — out-of-scope typecheck error from plan **12.3-04** (parallel Wave 1 HP plan); the module will be created by 12.3-04 Task 2. Vitest passes because the Vite resolver does not type-check unused modules; typecheck fails because `tsc --noEmit` walks every file under the project. Tracked in `.planning/phases/12.3-uat-correctness-closure/deferred-items.md`; resolves when 12.3-04 GREEN lands. Per SCOPE BOUNDARY: disjoint file, not touched by this plan.

## Known Stubs

None. Both helpers are fully implemented; the UI wiring is live and exercised by the composed-integration assertions.

## Manual UAT Verification (post-execution)

Procedure confirmed in the plan's `<verification>`: Humano + Caótico Neutral + reset atributos; increment FUE to 14, DES to 14, CON to 14, INT to 12, SAB to 12, CAR to 12 (30 pts spent, 0 remaining). Every `+` button now shows `disabled`; `Puntos restantes` stays at 0; FUE cannot be pushed to 15. **Automated repro locked in test suite C** (UAT B1 repro — composed snapshot + gate); visual verification is a follow-up for the phase-level UAT closure.

## Self-Check: PASSED

- tests/phase-12.3/attributes-budget-gate.spec.ts — FOUND
- packages/rules-engine/src/foundation/ability-budget.ts — FOUND (with both new exports)
- apps/planner/src/features/character-foundation/attributes-board.tsx — FOUND (canIncrementAttribute wired into `+` button)
- Commit 5a39544 (RED) — FOUND
- Commit 45fe596 (GREEN) — FOUND
