---
phase: 12.3-uat-correctness-closure
plan: 06
subsystem: ui
tags: [css, detail-panel, descriptions, white-space, pre-wrap, uat-b5]

# Dependency graph
requires:
  - phase: 12.2
    provides: TLK descriptions wired into DetailPanel body prop for races + classes (raw strings containing `\n` and `\n\n` separators)
provides:
  - DetailPanel descriptions now render source `\n` / `\n\n` as line breaks + paragraph gaps
  - Backward-compatible pre-wrap treatment for all current + future DetailPanel consumers (races, classes, feats, tooltips)
affects: [12.3-07, 12.3-08, uat-v1.0]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CSS white-space: pre-wrap for rendering source-embedded newlines in content panels (zero React logic, additive)"

key-files:
  created: []
  modified:
    - apps/planner/src/styles/app.css

key-decisions:
  - "[Phase 12.3-06] Used CSS white-space: pre-wrap on `.detail-panel__body` instead of splitting body prop into `<p>` array in DetailPanel — zero logic change, covers all current + future DetailPanel uses"

patterns-established:
  - "CSS-only UI fix pattern for source-text paragraph rendering: add `white-space: pre-wrap` at the container class that already owns text styling, so raw `\\n` separators in data (e.g. TLK descriptions) render without touching React render code"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-04-18
---

# Phase 12.3 Plan 06: Fix DetailPanel Wall-of-Text Summary

**Added `white-space: pre-wrap` to `.detail-panel__body` so TLK-sourced `\n` separators in race + class descriptions render as visible paragraph + bullet-list breaks instead of collapsing into a single paragraph.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-19T01:52:00Z
- **Completed:** 2026-04-19T01:55:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Single-line CSS declaration added to the existing `.detail-panel__body` rule in `apps/planner/src/styles/app.css` (line 1240).
- Race descriptions (e.g. Enano Escudo) and class descriptions (e.g. Bárbaro with intro prose + bulleted restriction list) now render the source's `\n\n` paragraph breaks and `\n-` bullet starts that Phase 12.2 piped through from TLK.
- Zero React/TSX/TS changes — DetailPanel render site (`{body && <div className="detail-panel__body">{body}</div>}`) untouched, so all existing DetailPanel consumers (feats, tooltips) inherit the fix without regression.
- Backward-compatible: single-paragraph descriptions render identically because `pre-wrap` only becomes visible when source contains `\n`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add white-space: pre-wrap to .detail-panel__body** — `68f58cd` (fix)

**Plan metadata:** (this SUMMARY commit follows)

## Files Created/Modified

- `apps/planner/src/styles/app.css` — `.detail-panel__body` rule gained `white-space: pre-wrap;` (line 1240, alphabetically-sorted after `line-height`). +1 line, 0 removals.

## Decisions Made

- Followed CONTEXT D-06 decision: prefer CSS `white-space: pre-wrap` over splitting `body` prop on `\n\n` into `<p>` array in DetailPanel. Rationale: CSS-only fix preserves DetailPanel shape (single `body: string` prop), zero risk to existing feat/tooltip callers, single declaration is cheaper to review and reason about than a new React render branch.
- No automated test: `getComputedStyle` in jsdom does not reliably surface `white-space: pre-wrap` (jsdom CSS engine limitation), and source DOM already contains `\n` regardless of CSS. Manual UAT in browser is the verification — consistent with prior CSS-only fixes (Phase 12.1-03 overflow fixes).

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. CSS edit applied cleanly, `pnpm vitest run` 463/463 tests passed with zero regression attributable to this change.

## Verification

- `grep -A 5 '\.detail-panel__body {' apps/planner/src/styles/app.css` — confirmed `white-space: pre-wrap;` appears inside the rule body (line 1240).
- `pnpm typecheck` — the CSS file is not type-checked; the one TS error surfaced (`tests/phase-12.3/hit-points-selector.spec.ts` can't resolve `@rules-engine/progression/compute-hit-points`) is pre-existing from parallel Wave 1 sibling plan 12.3-05 (RED phase test file committed without its GREEN implementation yet). Confirmed pre-existing via `git stash && pnpm typecheck` reproduction — out-of-scope per deviation SCOPE BOUNDARY rule. Not caused by this CSS change.
- `pnpm vitest run` — 77/78 test files pass, 463/463 individual tests pass. The 1 failed suite is the same `tests/phase-12.3/hit-points-selector.spec.ts` (import-resolution failure from 12.3-05, not a runtime test failure). No regression introduced by the CSS edit.
- Manual UAT (pending browser verification by user): select race Enano Escudo → DetailPanel body shows paragraph breaks; select class Bárbaro at L1 → prose + `\n\n` + bullet list render with visible paragraph spacing.

## User Setup Required

None — pure CSS change, no environment or config updates required.

## Next Phase Readiness

- B5 fix shipped. Wave 1 of Phase 12.3 closure continues with siblings 12.3-01 / 12.3-04 / 12.3-05 (disjoint files, parallel-safe).
- Milestone v1.0 UAT closure for B5 complete from the planner-code side; awaits in-browser user confirmation.

## Self-Check: PASSED

- `apps/planner/src/styles/app.css` contains `white-space: pre-wrap;` inside `.detail-panel__body` at line 1240 (verified via Read).
- Commit `68f58cd` present in `git log` with message `fix(12.3-06): restore paragraph breaks in DetailPanel descriptions`.

---
*Phase: 12.3-uat-correctness-closure*
*Completed: 2026-04-18*
