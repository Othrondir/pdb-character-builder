---
phase: 01-canonical-puerta-dataset
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/data-extractor/src/contracts/dataset-manifest.ts
  - packages/data-extractor/src/contracts/dataset-catalog.ts
  - packages/data-extractor/src/contracts/override-registry.ts
  - packages/overrides/registry.json
  - packages/overrides/text/custom-domain-labels.json
  - tests/phase-01/manifest-contract.spec.ts
autonomous: true
requirements: [VALI-04]
user_setup: []
must_haves:
  truths:
    - "Every compiled dataset snapshot carries schemaVersion, datasetId, datasetHash, locale metadata, and provenance."
    - "Publicly committed manifest data never leaks local Windows paths or raw asset payload references."
    - "Dataset activation is manual and explicit instead of silently replacing the active rules snapshot."
    - "Override entries are versioned artifacts with evidence and payload-file pointers."
  artifacts:
    - path: "packages/data-extractor/src/contracts/dataset-manifest.ts"
      provides: "Dataset manifest schema and public-safe provenance rules"
      contains: "datasetManifestSchema"
    - path: "packages/data-extractor/src/contracts/dataset-catalog.ts"
      provides: "Manual dataset activation contract"
      contains: "activationMode"
    - path: "packages/data-extractor/src/contracts/override-registry.ts"
      provides: "Versioned override registry schema"
      contains: "overrideRegistryEntrySchema"
    - path: "packages/overrides/registry.json"
      provides: "Committed override registry seed"
      contains: "\"registryVersion\": \"1\""
    - path: "tests/phase-01/manifest-contract.spec.ts"
      provides: "Executable manifest and provenance validation"
      contains: "C:\\\\Users\\\\"
  key_links:
    - from: "packages/data-extractor/src/contracts/dataset-manifest.ts"
      to: "packages/data-extractor/src/contracts/dataset-catalog.ts"
      via: "datasetId and manual activation contract"
      pattern: "activeDatasetId"
    - from: "packages/data-extractor/src/contracts/override-registry.ts"
      to: "packages/overrides/registry.json"
      via: "payloadFile and reviewStatus validation"
      pattern: "payloadFile"
    - from: "tests/phase-01/manifest-contract.spec.ts"
      to: "packages/data-extractor/src/contracts/dataset-manifest.ts"
      via: "Vitest schema assertions"
      pattern: "datasetManifestSchema"
---

<objective>
Define the public-safe dataset manifest, manual activation contract, and override registry structure for versioned Puerta snapshots.

Purpose: Encode D-05 through D-08 as executable schemas so future extraction work cannot leak raw assets, auto-promote a dataset, or hide provenance gaps.
Output: Manifest and dataset-catalog contracts, override-registry contract, committed registry seed files, and manifest-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-puerta-dataset/01-CONTEXT.md
@.planning/phases/01-canonical-puerta-dataset/01-RESEARCH.md
@.planning/phases/01-canonical-puerta-dataset/01-VALIDATION.md
@packages/data-extractor/src/contracts/canonical-record.ts
@packages/data-extractor/src/contracts/source-precedence.ts
@packages/rules-engine/src/contracts/canonical-id.ts
@tests/phase-01/schema-contract.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the dataset manifest schema and manual activation contract</name>
  <files>packages/data-extractor/src/contracts/dataset-manifest.ts, packages/data-extractor/src/contracts/dataset-catalog.ts, tests/phase-01/manifest-contract.spec.ts</files>
  <read_first>packages/data-extractor/src/contracts/dataset-manifest.ts, packages/data-extractor/src/contracts/dataset-catalog.ts, tests/phase-01/manifest-contract.spec.ts, packages/data-extractor/src/contracts/source-precedence.ts, .planning/phases/01-canonical-puerta-dataset/01-CONTEXT.md, .planning/phases/01-canonical-puerta-dataset/01-RESEARCH.md</read_first>
  <action>Implement the manifest and activation contract per D-05, D-07, and D-08. In `packages/data-extractor/src/contracts/dataset-manifest.ts`, export `datasetManifestSchema` with exact fields `schemaVersion: z.literal('1')`, `datasetId`, `datasetHash`, `generatedAt`, `defaultLocale: z.literal('es')`, `supportedLocales`, `levelCap: z.literal(16)`, `precedencePolicy.mechanics = ['manual-override', 'puerta-snapshot', 'base-game']`, `precedencePolicy.evidence = ['override-evidence', 'forum-doc', 'stale-doc']`, `ambiguityPolicy: z.literal('blocked')`, `sourceSummary.baseGame.keyFile = 'data/nwn_base.key'`, `sourceSummary.baseGame.mechanicalBif = 'data/base_2da.bif'`, `sourceSummary.puertaSnapshot.originUrl = 'http://nwsync.puertadebaldur.com'`, `sourceSummary.puertaSnapshot.includesClientContents = true`, `sourceSummary.puertaSnapshot.includesModuleContents = false`, `sourceSummary.manualOverrides.registryVersion`, `sourceSummary.manualOverrides.entryCount`, `sourceSummary.manualOverrides.registryHash`, and `artifactHashes`. Reject absolute Windows paths such as `C:\Users\...` anywhere inside the public manifest. In `packages/data-extractor/src/contracts/dataset-catalog.ts`, export `datasetCatalogSchema` with `activationMode: z.literal('manual')`, `activeDatasetId`, `availableDatasetIds`, `lastPromotedAt`, and `lastPromotedBy: z.literal('manual')` so a newly generated manifest cannot become active without an explicit catalog update. In `tests/phase-01/manifest-contract.spec.ts`, add tests that `datasetId` must match `puerta-ee-YYYY-MM-DD+<hash>`, that manifest payloads fail when given absolute local paths, and that a catalog entry without an explicit `activeDatasetId` update is rejected.</action>
  <verify>
    <automated>pnpm vitest run tests/phase-01/manifest-contract.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/data-extractor/src/contracts/dataset-manifest.ts` contains `schemaVersion`
    - `packages/data-extractor/src/contracts/dataset-manifest.ts` contains `defaultLocale`
    - `packages/data-extractor/src/contracts/dataset-manifest.ts` contains `http://nwsync.puertadebaldur.com`
    - `packages/data-extractor/src/contracts/dataset-manifest.ts` contains `ambiguityPolicy`
    - `packages/data-extractor/src/contracts/dataset-catalog.ts` contains `activationMode`
    - `packages/data-extractor/src/contracts/dataset-catalog.ts` contains `lastPromotedBy`
    - `tests/phase-01/manifest-contract.spec.ts` contains `puerta-ee-`
    - `tests/phase-01/manifest-contract.spec.ts` contains `C:\\Users\\`
  </acceptance_criteria>
  <done>The dataset manifest is versioned, provenance-aware, public-safe, and impossible to auto-promote without a manual catalog change.</done>
</task>

<task type="auto">
  <name>Task 2: Define the override registry schema and commit a text-override seed artifact</name>
  <files>packages/data-extractor/src/contracts/override-registry.ts, packages/overrides/registry.json, packages/overrides/text/custom-domain-labels.json, tests/phase-01/manifest-contract.spec.ts</files>
  <read_first>packages/data-extractor/src/contracts/override-registry.ts, packages/overrides/registry.json, packages/overrides/text/custom-domain-labels.json, tests/phase-01/manifest-contract.spec.ts, .planning/phases/01-canonical-puerta-dataset/01-CONTEXT.md, .planning/phases/01-canonical-puerta-dataset/01-RESEARCH.md</read_first>
  <action>Implement the versioned override registry per D-05 and D-06. In `packages/data-extractor/src/contracts/override-registry.ts`, export `overrideRegistryEntrySchema` and `overrideRegistrySchema` with exact enums `kind: 'rule' | 'text' | 'conflict-resolution' | 'blocked-marker'`, `operation: 'replace-fields' | 'append-values' | 'suppress-record' | 'add-synthetic-rule' | 'mark-blocked'`, evidence `type: 'forum-post' | 'admin-note' | 'script-review' | 'manual-reconciliation'`, confidence `high | medium | low`, and reviewStatus `verified | needs-review | blocked`. Require `payloadFile` to start with `packages/overrides/` so no raw install or `nwsync` files can be committed as override payloads. In `packages/overrides/registry.json`, create `{"registryVersion":"1","entries":[...]}` with one text-only seed entry `id: "text:custom-domain-labels"`, `kind: "text"`, `targetIds: []`, `operation: "append-values"`, `confidence: "medium"`, `reviewStatus: "needs-review"`, and `payloadFile: "packages/overrides/text/custom-domain-labels.json"`. In `packages/overrides/text/custom-domain-labels.json`, create a public-safe seed payload `{ "locale": "es", "records": [] }` that demonstrates the file shape without asserting any mechanical rule. Extend `tests/phase-01/manifest-contract.spec.ts` to verify the registry file passes schema validation, the seed payload is repo-relative, and any `payloadFile` pointing at `C:\` or raw sqlite paths is rejected.</action>
  <verify>
    <automated>pnpm vitest run tests/phase-01/manifest-contract.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/data-extractor/src/contracts/override-registry.ts` contains `blocked-marker`
    - `packages/data-extractor/src/contracts/override-registry.ts` contains `manual-reconciliation`
    - `packages/overrides/registry.json` contains `"registryVersion": "1"`
    - `packages/overrides/registry.json` contains `"id": "text:custom-domain-labels"`
    - `packages/overrides/registry.json` contains `"payloadFile": "packages/overrides/text/custom-domain-labels.json"`
    - `packages/overrides/text/custom-domain-labels.json` contains `"locale": "es"`
    - `tests/phase-01/manifest-contract.spec.ts` contains `packages/overrides/`
  </acceptance_criteria>
  <done>The repo has a public-safe override registry contract and a committed seed payload that future extraction work can extend without mixing raw assets into source control.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `pnpm vitest run tests/phase-01/manifest-contract.spec.ts --reporter=dot`
- [ ] `pnpm vitest run tests/phase-01/schema-contract.spec.ts --reporter=dot`
</verification>

<success_criteria>

- All tasks completed
- Dataset manifest and catalog contracts enforce explicit versioning and manual activation
- Override registry entries are reviewable JSON artifacts with evidence metadata
- Public artifacts reject local absolute paths and raw-source payload references
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-puerta-dataset/01-02-SUMMARY.md`
</output>
