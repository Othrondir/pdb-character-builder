---
phase: 01-canonical-puerta-dataset
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - packages/rules-engine/src/contracts/validation-outcome.ts
  - packages/data-extractor/src/contracts/conflict-record.ts
  - packages/data-extractor/fixtures/phase1-conflict-fixtures.ts
  - packages/overrides/blocked/phase-01-not-verifiable-domain.json
  - tests/phase-01/conflict-policy.spec.ts
autonomous: true
requirements: [VALI-04]
user_setup: []
must_haves:
  truths:
    - "Missing or conflicting legality-critical data produces blocked outcomes instead of legal outcomes."
    - "The rules contract distinguishes illegal states from blocked states."
    - "Blocked outcomes preserve evidence, affected IDs, and machine-readable error codes."
    - "Text-only conflicts can be downgraded without letting mechanical ambiguity pass as legal."
  artifacts:
    - path: "packages/rules-engine/src/contracts/validation-outcome.ts"
      provides: "Legal, illegal, and blocked outcome contract"
      contains: "ValidationStatus"
    - path: "packages/data-extractor/src/contracts/conflict-record.ts"
      provides: "Conflict severity and resolution contract"
      contains: "conflictRecordSchema"
    - path: "packages/overrides/blocked/phase-01-not-verifiable-domain.json"
      provides: "Example blocked marker for unverifiable rules"
      contains: "\"status\": \"blocked\""
    - path: "tests/phase-01/conflict-policy.spec.ts"
      provides: "Executable fail-closed policy tests"
      contains: "RULE_NOT_VERIFIABLE"
  key_links:
    - from: "packages/rules-engine/src/contracts/validation-outcome.ts"
      to: "packages/data-extractor/src/contracts/conflict-record.ts"
      via: "shared conflict severity and block-kind semantics"
      pattern: "conflict"
    - from: "tests/phase-01/conflict-policy.spec.ts"
      to: "packages/rules-engine/src/contracts/validation-outcome.ts"
      via: "Vitest fail-closed assertions"
      pattern: "blocked"
    - from: "tests/phase-01/conflict-policy.spec.ts"
      to: "packages/overrides/blocked/phase-01-not-verifiable-domain.json"
      via: "Fixture-backed blocked-marker coverage"
      pattern: "phase-01-not-verifiable-domain"
---

<objective>
Define the fail-closed outcome model for unsupported or conflicting Puerta rules so ambiguity can never be misreported as a valid build state.

Purpose: Encode D-03 and D-04 as executable contracts and fixtures before the later rules engine or UI can accidentally collapse blocked states into legal ones.
Output: Validation-outcome contracts, conflict-record contracts, blocked-state fixtures, and fail-closed policy tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-puerta-dataset/01-CONTEXT.md
@.planning/phases/01-canonical-puerta-dataset/01-RESEARCH.md
@.planning/phases/01-canonical-puerta-dataset/01-VALIDATION.md
@packages/data-extractor/src/contracts/source-precedence.ts
@packages/data-extractor/src/contracts/dataset-manifest.ts
@packages/data-extractor/src/contracts/override-registry.ts
@tests/phase-01/manifest-contract.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the blocked, illegal, and conflict outcome contracts</name>
  <files>packages/rules-engine/src/contracts/validation-outcome.ts, packages/data-extractor/src/contracts/conflict-record.ts</files>
  <read_first>packages/rules-engine/src/contracts/validation-outcome.ts, packages/data-extractor/src/contracts/conflict-record.ts, packages/data-extractor/src/contracts/source-precedence.ts, packages/data-extractor/src/contracts/dataset-manifest.ts, .planning/phases/01-canonical-puerta-dataset/01-CONTEXT.md, .planning/phases/01-canonical-puerta-dataset/01-RESEARCH.md</read_first>
  <action>Implement the fail-closed legality contract per D-03 and D-04. In `packages/rules-engine/src/contracts/validation-outcome.ts`, export `ValidationStatus = 'legal' | 'illegal' | 'blocked'`, `BlockKind = 'unsupported' | 'conflict' | 'missing-source' | 'not-verifiable'`, `validationOutcomeSchema`, and a `resolveValidationOutcome(...)` helper that returns `blocked` with `code: 'RULE_CONFLICT'` and `messageKey: 'validation.blocked.conflict'` when `hasConflict` is true, returns `blocked` with `code: 'RULE_NOT_VERIFIABLE'` and `messageKey: 'validation.blocked.notVerifiable'` when rule evidence is missing or the rule is unknown, returns `illegal` with `code: 'RULE_FAILED'` when a known rule fails, and returns `legal` with `code: 'RULE_OK'` only when the rule is known and passes. Include `evidence` and `affectedIds` arrays on every outcome object. In `packages/data-extractor/src/contracts/conflict-record.ts`, export `conflictSeverity = 'mechanical' | 'text'`, `conflictResolution = 'override-required' | 'blocked' | 'warning-only'`, and `conflictRecordSchema` with `id`, `severity`, `affectedIds`, `evidence`, `resolution`, and `notes`. Mechanical conflicts must default to `blocked`; text-only conflicts may use `warning-only` when all legality-critical fields agree.</action>
  <verify>
    <automated>pnpm vitest run tests/phase-01/conflict-policy.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rules-engine/src/contracts/validation-outcome.ts` contains `ValidationStatus = 'legal' | 'illegal' | 'blocked'`
    - `packages/rules-engine/src/contracts/validation-outcome.ts` contains `RULE_NOT_VERIFIABLE`
    - `packages/rules-engine/src/contracts/validation-outcome.ts` contains `validation.blocked.conflict`
    - `packages/data-extractor/src/contracts/conflict-record.ts` contains `mechanical`
    - `packages/data-extractor/src/contracts/conflict-record.ts` contains `warning-only`
  </acceptance_criteria>
  <done>The legality contract can represent unknown and conflicting rules without ever defaulting them to legal.</done>
</task>

<task type="auto">
  <name>Task 2: Add blocked-rule fixtures and fail-closed policy tests</name>
  <files>packages/data-extractor/fixtures/phase1-conflict-fixtures.ts, packages/overrides/blocked/phase-01-not-verifiable-domain.json, tests/phase-01/conflict-policy.spec.ts</files>
  <read_first>packages/data-extractor/fixtures/phase1-conflict-fixtures.ts, packages/overrides/blocked/phase-01-not-verifiable-domain.json, tests/phase-01/conflict-policy.spec.ts, packages/rules-engine/src/contracts/validation-outcome.ts, packages/data-extractor/src/contracts/conflict-record.ts, .planning/phases/01-canonical-puerta-dataset/01-VALIDATION.md</read_first>
  <action>Create executable fixtures for the ambiguity policy. In `packages/data-extractor/fixtures/phase1-conflict-fixtures.ts`, export three concrete cases: `mechanicalConflictFixture` with competing legality-critical values and unresolved provenance, `missingSourceFixture` for an unsupported rule path, and `textOnlyConflictFixture` where presentation strings disagree but mechanics are identical. In `packages/overrides/blocked/phase-01-not-verifiable-domain.json`, store a blocked marker fixture with `id: "blocked:phase-01-not-verifiable-domain"`, `status: "blocked"`, `blockKind: "not-verifiable"`, `code: "RULE_NOT_VERIFIABLE"`, `canonicalId: "domain:phase-01-not-verifiable"`, and `reason: "Missing authoritative Puerta source"`. In `tests/phase-01/conflict-policy.spec.ts`, assert that unresolved mechanical conflicts produce `blocked`, missing-source cases produce `blocked`, known failed rules produce `illegal`, known passing rules produce `legal`, and `textOnlyConflictFixture` can map to `warning-only` without changing the legality result.</action>
  <verify>
    <automated>pnpm vitest run tests/phase-01/conflict-policy.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `packages/data-extractor/fixtures/phase1-conflict-fixtures.ts` contains `mechanicalConflictFixture`
    - `packages/data-extractor/fixtures/phase1-conflict-fixtures.ts` contains `textOnlyConflictFixture`
    - `packages/overrides/blocked/phase-01-not-verifiable-domain.json` contains `"status": "blocked"`
    - `packages/overrides/blocked/phase-01-not-verifiable-domain.json` contains `"code": "RULE_NOT_VERIFIABLE"`
    - `tests/phase-01/conflict-policy.spec.ts` contains `warning-only`
    - `tests/phase-01/conflict-policy.spec.ts` contains `RULE_CONFLICT`
    - `tests/phase-01/conflict-policy.spec.ts` contains `phase-01-not-verifiable-domain`
  </acceptance_criteria>
  <done>The phase has a concrete blocked-marker example and tests that keep unsupported or ambiguous rules out of the legal state forever.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `pnpm vitest run tests/phase-01/conflict-policy.spec.ts --reporter=dot`
- [ ] `pnpm vitest run tests/phase-01 --reporter=dot`
</verification>

<success_criteria>

- All tasks completed
- The legality contract distinguishes legal, illegal, and blocked states
- Missing-source and conflict cases are fail-closed by tests, not left to later UI decisions
- The phase ships an explicit blocked-rule fixture proving how ambiguous content must be represented
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-puerta-dataset/01-03-SUMMARY.md`
</output>
