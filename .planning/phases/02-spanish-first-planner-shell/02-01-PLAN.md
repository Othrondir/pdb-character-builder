---
phase: 02-spanish-first-planner-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - apps/planner/package.json
  - apps/planner/index.html
  - apps/planner/tsconfig.json
  - apps/planner/vite.config.ts
  - apps/planner/src/main.tsx
  - apps/planner/src/router.tsx
  - apps/planner/src/lib/sections.ts
  - apps/planner/src/state/planner-shell.ts
  - apps/planner/src/components/shell/planner-shell-frame.tsx
  - apps/planner/src/routes/root.tsx
  - apps/planner/src/routes/skills.tsx
  - apps/planner/src/routes/spells.tsx
  - apps/planner/src/routes/abilities.tsx
  - apps/planner/src/routes/stats.tsx
  - apps/planner/src/routes/summary.tsx
  - apps/planner/src/routes/utilities.tsx
  - tests/phase-02/shell-routes.spec.ts
autonomous: true
requirements: [FLOW-01]
user_setup: []
must_haves:
  truths:
    - "Phase 2 introduces a real `apps/planner` SPA instead of planning around missing frontend scaffolding."
    - "Every primary planner area is reachable through a stable route entry in one shared shell."
    - "Shell state stays limited to navigation and chrome concerns, leaving gameplay logic to later phases."
  artifacts:
    - path: "apps/planner/package.json"
      provides: "Planner workspace and frontend dependency boundary"
      contains: "\"@tanstack/react-router\""
    - path: "apps/planner/src/router.tsx"
      provides: "Route tree for the planner shell"
      contains: "createRouter"
    - path: "apps/planner/src/state/planner-shell.ts"
      provides: "Shared shell-state store"
      contains: "activeSection"
    - path: "tests/phase-02/shell-routes.spec.ts"
      provides: "Smoke coverage for section routing and shell frame"
      contains: "utilities"
  key_links:
    - from: "apps/planner/src/lib/sections.ts"
      to: "apps/planner/src/router.tsx"
      via: "shared section ids and paths"
      pattern: "skills"
    - from: "apps/planner/src/state/planner-shell.ts"
      to: "apps/planner/src/components/shell/planner-shell-frame.tsx"
      via: "navigation and chrome state wiring"
      pattern: "mobileNavOpen"
---

<objective>
Create the `apps/planner` frontend workspace, static build pipeline, route tree, and shell-state skeleton that later Phase 2 work can refine without rewriting the app entrypoint.

Purpose: land the actual planner SPA and stable section routes before Spanish framing and visual-system work build on top of it.
Output: planner app package, Vite/React entrypoint, TanStack Router shell, Zustand shell store, and smoke tests for the section skeleton.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spanish-first-planner-shell/02-CONTEXT.md
@.planning/phases/02-spanish-first-planner-shell/02-RESEARCH.md
@.planning/phases/02-spanish-first-planner-shell/02-VALIDATION.md
@.planning/phases/02-spanish-first-planner-shell/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the planner workspace and static build pipeline</name>
  <files>package.json, tsconfig.base.json, apps/planner/package.json, apps/planner/index.html, apps/planner/tsconfig.json, apps/planner/vite.config.ts, apps/planner/src/main.tsx</files>
  <read_first>package.json, tsconfig.base.json, pnpm-workspace.yaml, .planning/phases/02-spanish-first-planner-shell/02-CONTEXT.md, .planning/phases/02-spanish-first-planner-shell/02-RESEARCH.md, .planning/phases/02-spanish-first-planner-shell/02-VALIDATION.md, .planning/phases/02-spanish-first-planner-shell/02-UI-SPEC.md, AGENTS.md</read_first>
  <action>Create `apps/planner` as the Phase 2 frontend package. In root `package.json`, add scripts `"dev:planner": "pnpm --dir apps/planner dev"`, `"build:planner": "pnpm --dir apps/planner build"`, and `"test:phase-02": "vitest run tests/phase-02 --reporter=dot"`. In `tsconfig.base.json`, add `apps/**/*.ts` and `apps/**/*.tsx` to `include`, plus the path alias `"@planner/*": ["apps/planner/src/*"]`. Create `apps/planner/package.json` with `"name": "@nwn1-character-builder/planner"`, `"private": true`, scripts `"dev": "vite"`, `"build": "vite build"`, and `"preview": "vite preview"`, dependencies `react`, `react-dom`, `@tanstack/react-router`, `@tanstack/zod-adapter`, `zustand`, `lucide-react`, `@fontsource/cormorant-garamond`, and `@fontsource/spectral`, plus devDependencies `vite`, `@vitejs/plugin-react`, and `jsdom`. Create `apps/planner/index.html` with `<div id="root"></div>`, `apps/planner/tsconfig.json` extending `../../tsconfig.base.json`, `apps/planner/vite.config.ts` using `react()` and `base: './'`, and `apps/planner/src/main.tsx` that mounts React with `RouterProvider`.</action>
  <verify>
    <automated>corepack pnpm build:planner</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"dev:planner": "pnpm --dir apps/planner dev"`
    - `package.json` contains `"build:planner": "pnpm --dir apps/planner build"`
    - `package.json` contains `"test:phase-02": "vitest run tests/phase-02 --reporter=dot"`
    - `tsconfig.base.json` contains `"@planner/*"`
    - `tsconfig.base.json` contains `"apps/**/*.tsx"`
    - `apps/planner/package.json` contains `"@tanstack/react-router"`
    - `apps/planner/package.json` contains `"zustand"`
    - `apps/planner/vite.config.ts` contains `base: './'`
    - `apps/planner/src/main.tsx` contains `RouterProvider`
  </acceptance_criteria>
  <done>The repo has a real planner app package that can build as static assets and mount a routed React shell.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the route tree, section registry, and shell-state baseline</name>
  <files>apps/planner/src/router.tsx, apps/planner/src/lib/sections.ts, apps/planner/src/state/planner-shell.ts, apps/planner/src/components/shell/planner-shell-frame.tsx, apps/planner/src/routes/root.tsx, apps/planner/src/routes/skills.tsx, apps/planner/src/routes/spells.tsx, apps/planner/src/routes/abilities.tsx, apps/planner/src/routes/stats.tsx, apps/planner/src/routes/summary.tsx, apps/planner/src/routes/utilities.tsx, tests/phase-02/shell-routes.spec.ts</files>
  <read_first>apps/planner/src/router.tsx, apps/planner/src/lib/sections.ts, apps/planner/src/state/planner-shell.ts, .planning/phases/02-spanish-first-planner-shell/02-CONTEXT.md, .planning/phases/02-spanish-first-planner-shell/02-RESEARCH.md, .planning/phases/02-spanish-first-planner-shell/02-VALIDATION.md</read_first>
  <action>Create `apps/planner/src/lib/sections.ts` exporting one section registry with ids `build`, `skills`, `spells`, `abilities`, `stats`, `summary`, and `utilities`, each with a concrete path (`/`, `/skills`, `/spells`, `/abilities`, `/stats`, `/summary`, `/utilities`). Create `apps/planner/src/state/planner-shell.ts` with a Zustand store exposing `activeSection`, `mobileNavOpen`, `summaryPanelOpen`, `setActiveSection`, `toggleMobileNav`, and `setSummaryPanelOpen`. In `apps/planner/src/router.tsx`, build a TanStack Router tree that renders a shared `PlannerShellFrame` component and child routes for every section. Create `apps/planner/src/components/shell/planner-shell-frame.tsx` with a semantic `nav`, `main`, and `aside` structure plus an `Outlet`. Seed route files with deterministic placeholder headings keyed by the section registry rather than freeform strings. Add `tests/phase-02/shell-routes.spec.ts` that asserts the route tree registers all seven sections and the shell frame renders `nav`, `main`, and `aside` landmarks.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-02/shell-routes.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/lib/sections.ts` contains `build`, `skills`, `spells`, `abilities`, `stats`, `summary`, and `utilities`
    - `apps/planner/src/lib/sections.ts` contains `/utilities`
    - `apps/planner/src/state/planner-shell.ts` contains `toggleMobileNav`
    - `apps/planner/src/state/planner-shell.ts` contains `summaryPanelOpen`
    - `apps/planner/src/router.tsx` contains `/skills`
    - `apps/planner/src/components/shell/planner-shell-frame.tsx` contains `<nav`
    - `apps/planner/src/components/shell/planner-shell-frame.tsx` contains `<aside`
    - `tests/phase-02/shell-routes.spec.ts` contains `utilities`
  </acceptance_criteria>
  <done>The planner shell has stable routes, a shared frame, and shell-level state that later phases can reuse.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm build:planner`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm vitest run tests/phase-02/shell-routes.spec.ts --reporter=dot`
</verification>

<success_criteria>

- All tasks completed
- `apps/planner` builds as a static frontend package
- Every primary planner area has a stable route entry in one shared shell
- Shell state is limited to navigation/chrome concerns and does not invent gameplay logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-spanish-first-planner-shell/02-01-SUMMARY.md`
</output>
