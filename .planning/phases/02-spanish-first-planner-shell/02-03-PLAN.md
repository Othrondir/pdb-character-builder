---
phase: 02-spanish-first-planner-shell
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - apps/planner/src/styles/fonts.css
  - apps/planner/src/styles/tokens.css
  - apps/planner/src/styles/app.css
  - apps/planner/src/components/shell/planner-shell-frame.tsx
  - apps/planner/src/components/shell/section-nav.tsx
  - apps/planner/src/components/shell/summary-panel.tsx
  - apps/planner/src/routes/root.tsx
  - tests/phase-02/theme-contract.spec.ts
autonomous: true
requirements: [FLOW-02]
user_setup: []
must_haves:
  truths:
    - "The shell reads as NWN1-inspired through tokens, type, framing, and restrained accent usage."
    - "The visual system is codified in reusable CSS tokens rather than inline one-off styling."
    - "Responsive shell polish keeps the UI usable without sliding back to generic web-tool visuals."
  artifacts:
    - path: "apps/planner/src/styles/tokens.css"
      provides: "Shared NWN1 shell design tokens"
      contains: "--color-parchment"
    - path: "apps/planner/src/styles/fonts.css"
      provides: "Approved font imports for headings and body copy"
      contains: "@fontsource/cormorant-garamond"
    - path: "apps/planner/src/styles/app.css"
      provides: "Shell surface, border, motion, and layout styling"
      contains: ".planner-shell"
    - path: "tests/phase-02/theme-contract.spec.ts"
      provides: "Assertions for token and font contract compliance"
      contains: "--color-bronze"
  key_links:
    - from: "apps/planner/src/styles/tokens.css"
      to: "apps/planner/src/styles/app.css"
      via: "token-driven shell styling"
      pattern: "--color-parchment"
    - from: "apps/planner/src/styles/fonts.css"
      to: "apps/planner/src/components/shell/planner-shell-frame.tsx"
      via: "display/body font application"
      pattern: "Cormorant"
---

<objective>
Codify and apply the NWN1 visual system so the planner shell feels intentional, thematic, and distinct from generic web tooling or a straight NWN2DB clone.

Purpose: finish Phase 2 by locking the shell's fonts, color tokens, panel framing, and restrained motion around the already-localized shell.
Output: UI-SPEC-backed theme token files, font imports, shell styling, and contract tests for the NWN1 visual baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spanish-first-planner-shell/02-CONTEXT.md
@.planning/phases/02-spanish-first-planner-shell/02-RESEARCH.md
@.planning/phases/02-spanish-first-planner-shell/02-VALIDATION.md
@.planning/phases/02-spanish-first-planner-shell/02-UI-SPEC.md
@.planning/phases/02-spanish-first-planner-shell/02-01-PLAN.md
@.planning/phases/02-spanish-first-planner-shell/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the shell token layer and font contract from UI-SPEC</name>
  <files>apps/planner/src/styles/fonts.css, apps/planner/src/styles/tokens.css, apps/planner/src/styles/app.css, tests/phase-02/theme-contract.spec.ts</files>
  <read_first>apps/planner/src/styles/fonts.css, apps/planner/src/styles/tokens.css, apps/planner/src/styles/app.css, .planning/phases/02-spanish-first-planner-shell/02-UI-SPEC.md, .planning/phases/02-spanish-first-planner-shell/02-CONTEXT.md, .planning/phases/02-spanish-first-planner-shell/02-RESEARCH.md</read_first>
  <action>Create `apps/planner/src/styles/fonts.css` importing `@fontsource/cormorant-garamond` and `@fontsource/spectral`. Create `apps/planner/src/styles/tokens.css` with explicit custom properties `--color-parchment: #e3d8c1`, `--color-ink: #2f241d`, `--color-bronze: #9f7a31`, `--color-destructive: #7a1f1f`, `--font-display: "Cormorant Garamond"`, `--font-body: "Spectral"`, and spacing tokens `--space-xs` through `--space-3xl`. Create or update `apps/planner/src/styles/app.css` so `.planner-shell`, `.planner-panel`, `.planner-nav`, and `.planner-summary` consume those tokens instead of inline colors or spacing. Add `tests/phase-02/theme-contract.spec.ts` that asserts the token names and font imports exist exactly as defined.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-02/theme-contract.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/styles/fonts.css` contains `@fontsource/cormorant-garamond`
    - `apps/planner/src/styles/fonts.css` contains `@fontsource/spectral`
    - `apps/planner/src/styles/tokens.css` contains `--color-parchment: #e3d8c1`
    - `apps/planner/src/styles/tokens.css` contains `--color-ink: #2f241d`
    - `apps/planner/src/styles/tokens.css` contains `--color-bronze: #9f7a31`
    - `apps/planner/src/styles/app.css` contains `.planner-shell`
    - `tests/phase-02/theme-contract.spec.ts` contains `--color-bronze`
  </acceptance_criteria>
  <done>The planner has a reusable token and font layer that encodes the approved shell design contract.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the NWN1 shell chrome, accent rules, and responsive polish</name>
  <files>apps/planner/src/components/shell/planner-shell-frame.tsx, apps/planner/src/components/shell/section-nav.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/routes/root.tsx, apps/planner/src/styles/app.css</files>
  <read_first>apps/planner/src/components/shell/planner-shell-frame.tsx, apps/planner/src/components/shell/section-nav.tsx, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/styles/app.css, .planning/phases/02-spanish-first-planner-shell/02-UI-SPEC.md, AGENTS.md</read_first>
  <action>Update the shell components so the planner frame uses parchment-backed panels, dark frame chrome, bronze accent separators, and serif-forward headings from the token layer. Apply active-nav treatment only to the current section, keep destructive red isolated to reset/destructive affordances, and add limited reveal classes `shell-reveal` and `section-fade` for initial frame and section transitions. Ensure the desktop rail, summary panel, and mobile drawer share the same framed visual language and remain readable on narrow widths. Keep the styling concentrated in the shared shell components rather than scattering route-specific overrides.</action>
  <verify>
    <automated>corepack pnpm build:planner</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/styles/app.css` contains `.shell-reveal`
    - `apps/planner/src/styles/app.css` contains `.section-fade`
    - `apps/planner/src/styles/app.css` contains `.planner-panel`
    - `apps/planner/src/components/shell/section-nav.tsx` contains `aria-current`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `planner-summary`
    - `apps/planner/src/components/shell/planner-shell-frame.tsx` contains `planner-shell`
  </acceptance_criteria>
  <done>The shell now presents a consistent NWN1-inspired visual identity rather than a generic frontend scaffold.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm vitest run tests/phase-02/theme-contract.spec.ts --reporter=dot`
- [ ] `corepack pnpm build:planner`
- [ ] Manually inspect desktop and mobile shell layouts for NWN1 identity and readable Spanish copy
</verification>

<success_criteria>

- All tasks completed
- The shell's look is controlled by reusable tokens and approved fonts
- Visual treatment reads as NWN1-inspired without copying NWN2DB
- Responsive shell polish preserves the established navigation and summary framing
</success_criteria>

<output>
After completion, create `.planning/phases/02-spanish-first-planner-shell/02-03-SUMMARY.md`
</output>
