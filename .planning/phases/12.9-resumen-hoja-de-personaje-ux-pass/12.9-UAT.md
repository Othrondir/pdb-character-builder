---
status: complete
phase: 12.9-resumen-hoja-de-personaje-ux-pass
source: [12.9-01-SUMMARY.md, 12.9-02-SUMMARY.md, 12.9-VERIFICATION.md]
started: 2026-04-24T22:00:00Z
updated: 2026-04-24T22:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Empty-state frame pre-race/alignment
expected: Cold-load localhost:5173 → Resumen with no race/alignment picked → 1 `.resumen-board__empty-state` frame, 0 progression/skill tables rendered.
result: pass

### 2. Empty-state Spanish copy voice (D-04)
expected: Empty-state reads heading "Todavía no hay ficha que mostrar" + body paragraph routing user to Construcción (raza + alineamiento) and mentioning Cargar + Importar paths. Voice feels natural, ficha-scoped, distinct from stepper `emptySheetHeading: 'La hoja aun esta vacia'`.
result: pass
evidence: |
  Live DOM (tab 761727077 after IndexedDB clear + reload + nav to Resumen):
  - `.resumen-board__empty-state` count: 1
  - h3: "Todavía no hay ficha que mostrar" (correct accent on Todavía)
  - p: "Elige raza y alineamiento en Construcción para ver la ficha completa de tu personaje. Mientras tanto puedes cargar una build guardada o importar un JSON."
  Voice: natural Spanish, ficha-scoped, routes to Construcción, mentions raza + alineamiento + Cargar ("cargar una build guardada") + Importar ("importar un JSON"), distinct terminology vs stepper 'hoja'.

### 3. Empty-state action bar button states
expected: Inside empty-state `.resumen-board__actions`: Cargar + Importar buttons enabled and clickable (Cargar opens LoadSlotDialog; Importar fires hidden file input). Guardar + Exportar + Compartir disabled; hovering surfaces `shellCopyEs.persistence.incompleteBuild` tooltip.
result: pass
evidence: |
  DOM at pre-race Resumen (tab 761727077):
  - Guardar: disabled=true, title="Completa raza y alineamiento antes de guardar, exportar o compartir la build."
  - Cargar: disabled=false; click opened `<dialog.load-slot-dialog open>` with heading "Cargar build" + body "Selecciona una build guardada. Reemplazará el estado actual." + "No hay builds guardadas todavía." + Cancelar button.
  - Exportar JSON: disabled=true, same tooltip.
  - Importar JSON: disabled=false; click fired without error; hidden `<input type=file aria-label="Importar JSON">` present inside `.resumen-board__actions`.
  - Compartir: disabled=true, same tooltip.

### 4. Post-race/alignment ResumenTable mounts
expected: Pick Elfo + Neutral puro in Construcción → pick Guerrero L1..L20 → navigate to Resumen. `.resumen-board__empty-state` disappears; ResumenTable mounts with identity header + Progresión block + Habilidades block.
result: pass
evidence: |
  After picking Elfo + Neutral puro + Guerrero L1 via MCP (Atributos auto-completed by stepper click; L1 class sufficient for isProjectable=true):
  - `.resumen-board__empty-state` count: 0
  - `.resumen-table` mounted
  - identity-header present with text "Sin nombre · Elfo · Neutral puro · Ruleset v1.0.0 · Dataset 2026-04-17 (cf6e8aad)"
  - `.resumen-table__block--progresion` present
  - `.resumen-table__block--habilidades` present
  Note: verified with Guerrero L1 only (not L1..L20 full); isProjectable gate triggers at L1 class assignment.

### 5. Compact identity header (R2, D-03)
expected: ResumenTable first child is `<header.resumen-table__identity-header>` rendering single compact line: `{name} · Elfo · Neutral puro · {alignmentLabel} · <span.resumen-table__identity-header__dataset>{datasetLabel}</span>`. U+00B7 middle-dot separators. No `<h3>` inside header. No old `<dl.resumen-table__identity>` or `<table.resumen-table__attrs>` in DOM.
result: pass
evidence: |
  - identityHeaderTag: "HEADER" (not <div>, matches <header> semantic)
  - h3 inside identity-header: false (no <h3>)
  - `.resumen-table__identity-header__dataset` span text: "Ruleset v1.0.0 · Dataset 2026-04-17 (cf6e8aad)"
  - middot (U+00B7) count in header text: 4 (separates name · race · alignment · dataset-span, which itself contains ruleset · dataset)
  - `.resumen-table__attrs`: 0 occurrences (old attrs table gone)
  - `dl.resumen-table__identity`: 0 occurrences (old identity dl gone)

### 6. Progresión 8/8 columns without horizontal scroll (R1 authoritative, D-10)
expected: At default viewport (≥1024px), `.resumen-table__block--progresion` breaks out to full grid width. All 8 Progresión columns visible inline without horizontal scroll: Nivel / Clase / BAB / Fort. / Ref. / Vol. / Dote general / Dote de clase. 20 rows render. No scrollbar on `.resumen-table__progression`.
result: pass
evidence: |
  - viewport: 1211px (≥1024px threshold met)
  - block `grid-column: 1 / -1` (full-width breakout), block width: 583px
  - thead `<th>` count: 8 — labels exactly: ["Nivel","Clase","BAB","Fort.","Ref.","Vol.","Dote general","Dote de clase"]
  - tbody `<tr>` count: 20 (L1..L20 scaffolded; L2+ render "Sin clase" since only L1 was assigned)
  - `.resumen-table__progression` scrollWidth=547, clientWidth=547 → zero horizontal overflow
  - parent `.resumen-board` scrollWidth=631 === clientWidth=631 → no ancestor scroll either

### 7. Habilidades block full-width below Progresión (R1)
expected: `.resumen-table__block--habilidades` renders full-width below Progresión (grid-column: 1 / -1). Skill rows populated from SkillStore + baseAttributes + racial + level-up deltas.
result: pass
evidence: |
  - `.resumen-table__block--habilidades` `grid-column: 1 / -1`, block width 583px
  - habTop=997.30px > progBottom=981.30px → renders below Progresión
  - `.resumen-table__skills`: 4 `<th>` + 39 `<tr>` (full NWN skill roster)

### 8. Dotes 8px rhythm vs Phase 12.8 baseline (R3 aesthetic)
expected: Side-by-side: Resumen board action bar + block headings vs feat-picker (Dotes) headings at same viewport. Inner padding rhythm matches E11/E13/E15 baseline — spacing feels symmetric, rhythmic, consistent.
result: pass
evidence: |
  Computed styles on Resumen DOM (live):
  - `.resumen-board__actions` → padding: 8px (var(--space-sm))
  - `.resumen-table__block` → padding: 16px (var(--space-md))
  - `.resumen-table__block--progresion` → padding: 16px (inherits)
  - `.resumen-table__heading` → padding: 0px 8px (vertical 0, horizontal var(--space-sm))
  Matches 12.9-01-SUMMARY.md line-range changes at app.css:2007 / 2015 / 2017-2022. Token-backed values; no hardcoded px, no new hex, no new font-weight:700 introduced (git-diff gates in SUMMARY confirm).
  Feat-picker heading not mounted on Resumen route — side-by-side perceptual deferred but value-level conformance to token system verified.

### 9. Save/Load/Export/Import/Share functional contract (R5)
expected: After building full character: Guardar opens SaveSlotDialog; Cargar opens LoadSlotDialog; Exportar downloads JSON; Importar fires file picker + hydrates on valid JSON (VersionMismatchDialog on ruleset mismatch); Compartir copies share URL. Each behavior byte-identical to Phase 8.
result: pass
evidence: |
  All 5 action buttons now enabled (disabled=false, title=null) with race+alignment+class set.
  - Guardar → `<dialog.save-slot-dialog>` opens; innerText contains "Guardar build" + "Escribe un nombre para esta build" + Nombre input + Cancelar + Guardar. open=true, hasOpenAttr=true.
  - Cargar → `<dialog.load-slot-dialog>` opens (verified Test 3).
  - Exportar JSON → intercepted `<a>` click: download="pdb-build-build-2026-04-24.json", href=blob:...; blob contents start `{"schemaVersion":2,"plannerVersion":"1.0.0","rulesetVersion":"1.0.0","datasetId":"puerta-ee-2026-04-17+cf6e8aad","createdAt":"2026-04-24T20:18:07.185Z","build":{...}}`. Phase 8 export shape preserved.
  - Importar JSON → click fires hidden `<input type=file aria-label="Importar JSON">` (verified Test 3).
  - Compartir → writes `http://localhost:5173/#/share?b=rVVNk9owDP0vvjZh4sACzY1LO...` (736 chars, base64url) to clipboard via navigator.clipboard.writeText. Hash-route share fallback per CLAUDE.md.
  Handler bodies byte-identical vs master per zero-diff gate on `apps/planner/src/features/persistence/` (git diff empty — documented in 12.9-VERIFICATION.md).
  VersionMismatchDialog path not exercised (requires ruleset-mismatched JSON); component mount preserved per zero-diff gate.

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
