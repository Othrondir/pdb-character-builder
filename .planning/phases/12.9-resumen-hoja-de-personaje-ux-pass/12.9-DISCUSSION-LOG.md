# Phase 12.9: Resumen (Hoja de personaje) UX Pass - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 12.9-resumen-hoja-de-personaje-ux-pass
**Areas discussed:** Layout mechanism (GA-1), Habilidades block layout (GA-2), Identity header template (GA-3), Empty-state surface (GA-4)

**Mode:** User invoked `/gsd-discuss-phase 12.9` interactively, then answered "auto" to the gray-area prompt. Claude auto-selected option 1 for every gray area (explicit recommended defaults). No per-question AskUserQuestion flow — single-batch auto-resolution. SPEC.md pre-locked Goal / Boundaries / Constraints / Acceptance Criteria, so discussion focused exclusively on HOW to implement.

---

## GA-1: Layout mechanism for full-width Progresión

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | `grid-column: 1 / -1` on `.resumen-table__block--progresion` modifier (min change, reuses grid) | ✓ |
| 2 | Rewrite `.resumen-table` to vertical flex stack — Identidad / Progresión / Habilidades | |
| 3 | Two-row grid: row 1 identity-header full, row 2 progresión full, row 3 habilidades full | |
| 4 | Keep auto-fit but raise minmax to `minmax(44rem, 1fr)` to force single column | |

**User's choice:** Option 1 (auto default).
**Notes:** Minimal blast radius. Keeps existing `grid-template-columns: repeat(auto-fit, minmax(18rem, 1fr))` intact; only the modifier class overrides column span.

---

## GA-2: Habilidades block layout

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | Full-width (stack vertical with Progresión — simple, consistent) | ✓ |
| 2 | Share auto-fit column with identity-header | |
| 3 | Full-width but internal multi-column via CSS `columns: 2` (39 skills split) | |

**User's choice:** Option 1 (auto default).
**Notes:** Option 3 rejected for accessibility regression (TH/TD row associations break across CSS column breaks). Option 2 rejected because identity-header D-03 resolves to a thin single-line header that does not want a side-by-side table companion.

---

## GA-3: Identity header template

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | Compact single line `{name} · {race} · {alignment} · {dataset}` with middot separators | ✓ |
| 2 | Compact with small-caps labels: `Nombre: X · Raza: Y · Alin.: Z` in `--color-gold-faded` | |
| 3 | `<dl>` horizontal flex with `<dt>` label + `<dd>` value preserved | |

**User's choice:** Option 1 (auto default).
**Notes:** Stepper summary bar already provides labeled identity; Resumen header is the ficha surface, not a dup. Dataset span wrapped in `color: var(--color-gold-faded)` so it visually recedes vs name/race/alignment (the "identifying" pieces).

---

## GA-4: Empty-state surface content

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | Título + cuerpo solamente (custom ficha voice) | ✓ |
| 2 | Título + cuerpo + NwnButton CTA "Volver a Construcción" | |
| 3 | Reutilizar `stepper.emptySheetHeading` / `emptySheetBody` (cero copy nuevo) | |

**User's choice:** Option 1 (auto default).
**Notes:** Option 2 rejected — stepper left-rail already provides navigation back to Construcción; button wiring would need `setActiveView` + `activeLevel` reset plumbing for no user benefit. Option 3 rejected — stepper voice is wizard/onboarding-oriented, ficha voice is handoff/completion-oriented; different surface deserves different copy.

---

## Claude's Discretion

Items where the user said "auto" → Claude defaults apply, but downstream planner still owns concrete implementation details:

- Exact BEM modifier naming (`--progresion` vs `--progresion-block` etc.) — BEM-consistent, planner chooses.
- Placement of `<header>.resumen-table__identity-header` (inside first block frame or as plain header sibling) — planner decides.
- Exact Spanish copy strings for `shellCopyEs.resumen.emptyState.heading` / `.body` — planner/executor propose, user approves at UAT.
- Whether R3 (spacing) ships bundled with R1+R2 or splits into a two-plan wave — planner per complexity budget.

## Deferred Ideas

- CTA button in empty state (GA-4 option 2) — reopen as 999.x backlog if post-launch UAT confusion surfaces.
- Habilidades 2-column CSS trick (GA-2 option 3) — reopen as a grouped-layout polish phase if full-width stack feels too long.
- Mobile <1024px Resumen layout — out of SPEC 12.9 scope; pending future mobile pass.
- Right-rail aside `<CharacterSheet>` polish — separate component, separate phase if needed.
