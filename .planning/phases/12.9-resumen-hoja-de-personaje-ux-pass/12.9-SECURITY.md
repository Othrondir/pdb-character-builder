---
phase: 12.9
slug: resumen-hoja-de-personaje-ux-pass
status: verified
threats_open: 0
asvs_level: 1
created: 2026-04-24
---

# Phase 12.9 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> UX pass over an existing read-only Resumen surface. No new authentication paths,
> network endpoints, file-access patterns, or trust-boundary schema changes were
> introduced. All threats are refactoring-hygiene scoped (type drift, selector
> drift, token discipline, zero-diff gates, test-env integrity).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| `resumen-selectors.ts` → `resumen-table.tsx` | `ResumenViewModel` shape shrank (attributes[] removed); typecheck breadcrumb bridges the transition window between Plans 01 and 02 | character-foundation state → read-only view-model (non-sensitive) |
| `shellCopyEs.resumen.emptyState.*` → JSX + tests | Added `emptyState.{heading,body}` keys in Plan 01; consumed by `resumen-board.tsx` + `tests/phase-12.9/resumen-empty-state.spec.tsx` | Spanish UI copy strings (non-sensitive) |
| CSS selector namespace `.resumen-table__*` → JSX className | New BEM modifiers `--progresion` / `--habilidades` + `__identity-header` + `.resumen-board__empty-state`; any drift = silent visual regression | none (presentation) |
| `vitest.config.ts` `environmentMatchGlobs` → phase-12.9 specs | phase-12.9 jsdom glob added; absence would silently resolve to node env and break DOM asserts | test env resolution (dev-time only) |
| `<ResumenBoard>` JSX tree → persistence handlers | `isProjectable ? <ResumenTable/> : <NwnFrame.empty-state>` ternary gates mount; handler closures (`onExport` / `onShare` / `onImportFile`) must remain byte-identical (R5 / Phase 8) | local IndexedDB slots, URL share payloads, file download blobs |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-12.9-01-01 | Tampering | `es.ts` orphan keys (identityBlockHeading, columnLabels.{attribute,total,modifier}, attributeLabels.*) | accept → later mitigated | LANG-01 permitted orphans; Plan 02 Task 3 global grep confirmed zero external consumers → orphans deleted (commit `0230a23`). Global `grep -rn identityBlockHeading`/`attributeLabels` return empty. | closed |
| T-12.9-01-02 | Tampering | CSS selector name drift (`--progresion` vs `--progression`) | mitigate | Both plans read UI-SPEC §L-1; Plan 02 acceptance `grep -c resumen-table__block--progresion resumen-table.tsx = 1` + `resumen-table__block--habilidades = 1`. | closed |
| T-12.9-01-03 | Repudiation | vitest env-glob missing ⇒ tests fall back to node ⇒ DOM asserts silently fail | mitigate | `vitest.config.ts` line 20: `['tests/phase-12.9/**/*.spec.tsx', 'jsdom']`. Each spec carries `// @vitest-environment jsdom` belt-and-braces header (grep = 3 across 3 spec files). | closed |
| T-12.9-01-04 | Information Disclosure | `app.css` silently gains new hex colors | mitigate | `git diff HEAD~2 -- apps/planner/src/styles/app.css \| grep -cE "^\+.*#[0-9a-fA-F]{3,6}"` = 0. All new declarations route through `var(--color-*)` tokens. | closed |
| T-12.9-01-05 | Denial of Service | Selector trim breaks Phase 8 slot-load/share-URL regression suite | accept (risk materialized + mitigated) | Threat disposition was incomplete — the trim regressed 7 phase-08 specs at runtime (`model.attributes.map` on undefined). Auto-fixed: added `(model.attributes ?? [])` runtime guard in resumen-table.tsx and deleted 2 obsolete spec cases. Phase 8 back to baseline 97 passed / 1 pre-existing. Logged in 12.9-01-SUMMARY.md "Deviations". | closed |
| T-12.9-01-06 | Elevation of Privilege | Planner edits out-of-scope files | mitigate | `git diff origin/master` empty on all six zero-diff paths: `apps/planner/src/features/persistence/`, `save-slot-dialog.tsx`, `version-mismatch-dialog.tsx`, `packages/rules-engine/`, `apps/planner/src/data/compiled-*`, `apps/planner/src/styles/tokens.css`. | closed |
| T-12.9-01-07 | Tampering | Stale `@ts-expect-error` lingers after Plan 02 rewrite ⇒ unused-suppression typecheck regression | mitigate | `grep -c "@ts-expect-error Plan 12.9-02-01" resumen-table.tsx` = 0 after commit `cdaeba8`. Full `npx tsc -p tsconfig.base.json --noEmit` reports 4 pre-existing errors, 0 new. | closed |
| T-12.9-02-01 | Tampering | `resumen-board.tsx` handler-body drift under "while I'm editing this file" temptation | mitigate | `git diff origin/master -- apps/planner/src/features/persistence/` empty; 5 NwnButton mounts + onClick/disabled/title/variant preserved (confirmed live DOM in 12.9-UAT Test 9). | closed |
| T-12.9-02-02 | Spoofing | Spec fixtures use wrong canonical IDs ⇒ isProjectable stays false ⇒ R4 spec green by accident | mitigate | Pre-execution grep verified `race:elf` (compiled-races.ts:36), `alignment:true-neutral` (foundation-fixture.ts:176), `class:fighter` (compiled-classes.ts:121). 10/10 it blocks green on real store subscriptions. | closed |
| T-12.9-02-03 | Repudiation | R1 spec ships vacuous scrollWidth assertion ⇒ visual regression lands masked as green | mitigate | `grep -c scrollWidth tests/phase-12.9/resumen-progresion-full-width.spec.tsx` = 0 + `clientWidth` = 0. Third `it` block asserts only jsdom-deterministic invariants (BEM + 8 `<th>` + 20 `<tr>`). Live MCP Chrome UAT (12.9-UAT Test 6) verified real scroll-free rendering: table scrollWidth=547 == clientWidth=547 at 1211px viewport. | closed |
| T-12.9-02-04 | Information Disclosure | Spanish copy drifts vs CONTEXT-D-04 locked voice | mitigate | Empty-state spec case 3 asserts `emptyState.heading !== stepper.emptySheetHeading`. Live DOM (12.9-UAT Test 2): h3 = "Todavía no hay ficha que mostrar", p = "Elige raza y alineamiento en Construcción…". Voice approved. | closed |
| T-12.9-02-05 | Denial of Service | Orphan key deletion accidentally removes a key still consumed elsewhere | mitigate | Task 3 gate: repo-wide `grep -rn identityBlockHeading apps/planner/src tests/ packages/` = empty; same for `attributeLabels`. Deletion landed in commit `0230a23`. | closed |
| T-12.9-02-06 | Elevation of Privilege | Commit touches `packages/rules-engine/*`, `apps/planner/src/data/compiled-*`, `apps/planner/src/features/persistence/**`, or `save-slot-dialog.tsx` | mitigate | All six zero-diff gates empty (see T-12.9-01-06 evidence block). | closed |
| T-12.9-02-07 | Tampering | New hex color or `font-weight: 700` slipped into app.css via inline `style={{}}` or new declaration | mitigate | `git diff 62428f1 -- app.css \| grep -cE "^\+.*#[0-9a-fA-F]{3,6}"` = 0; `grep -cE "^\+.*font-weight:\s*700"` = 0. app.css actually untouched by Plan 02 (deltas landed in Plan 01). Dataset tint class `.resumen-table__identity-header__dataset` uses `var(--color-gold-faded)`. | closed |
| T-12.9-02-08 | Tampering | Plan 01 `// @ts-expect-error Plan 12.9-02-01` breadcrumb survives Task 1 rewrite ⇒ unused-directive TS regression | mitigate | Duplicate of T-12.9-01-07 from the Plan 02 side. Post-rewrite grep = 0; `tsc --noEmit` clean (0 new errors vs 4 pre-existing baseline). | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| R-12.9-01 | T-12.9-01-01 | LANG-01 permits orphan copy keys; deletion deferred to Plan 02 with grep-confirm gate. Cleanup landed (commit `0230a23`) after global grep verified zero consumers. Accepted risk window lasted between commits `5a803be` (orphans introduced by selector trim) and `0230a23` (orphans deleted). | plan author + executor | 2026-04-24 |
| R-12.9-02 | T-12.9-01-05 | Phase 8 regression suite initially broke (7 specs) when `ResumenViewModel.attributes` was removed — the disposition underestimated runtime coupling via `<ResumenTable>` renders inside share/import specs. Auto-fixed via Rule 3 deviation (`(model.attributes ?? [])` runtime guard) + Rule 1 obsolete-case cleanup. Phase 8 returned to baseline 97 passed / 1 pre-existing. Logged as accepted because the mitigation path was transparent and fully documented in 12.9-01-SUMMARY.md. | executor | 2026-04-24 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-04-24 | 15 | 15 | 0 | Claude (gsd-secure-phase orchestrator, inline classification; auditor spawn skipped per workflow §3 shortcut when threats_open: 0) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-04-24
