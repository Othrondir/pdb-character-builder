---
phase: 12.9
slug: resumen-hoja-de-personaje-ux-pass
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-24
derived_from: "12.9-SPEC.md + 12.9-01-PLAN.md + 12.9-02-PLAN.md (research skipped this run; validation derived directly from plans)"
---

# Phase 12.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Research was skipped for Phase 12.9 (CONTEXT.md + SPEC.md already locked 10 decisions + 6 requirements with grep-verifiable acceptance). This VALIDATION.md is derived directly from PLANs + SPEC acceptance checklist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.16 + React Testing Library (jsdom environment, per D-10) |
| **Config file** | `vitest.config.ts` (root) — Plan 01 Task 2 adds `tests/phase-12.9/**/*.spec.tsx` to the include glob |
| **Quick run command** | `pnpm --filter planner test tests/phase-12.9/` |
| **Full suite command** | `pnpm --filter planner test` (all phases, run at wave close) |
| **TypeScript gate** | `pnpm --filter planner exec tsc --noEmit` (runs after each plan commit) |
| **Estimated runtime** | ~4–8 seconds for phase-12.9 scope; ~40 seconds for full suite |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter planner test tests/phase-12.9/` + `pnpm --filter planner exec tsc --noEmit`
- **After every plan wave:** Run `pnpm --filter planner test` (full suite — regression guard against R5 byte-identical contract)
- **Before live MCP Chrome UAT (phase close):** Full suite must be green on master
- **Max feedback latency:** < 10 seconds (phase-12.9 scope quick command)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.9-01-01 | 01 | 1 | R6, R4 | T-12.9-01-01 | ResumenViewModel.attributes removed; 2 new copy keys present | grep + tsc | `grep -c "^\s*attributes: AttributeRow\[\];" apps/planner/src/features/summary/resumen-selectors.ts` → 0 AND `grep -c "emptyState:" apps/planner/src/lib/copy/es.ts` → 1 | ✅ existing files | ⬜ pending |
| 12.9-01-02 | 01 | 1 | R3, R4 | T-12.9-01-02 | Dotes 8px padding adopted; empty-state + BEM modifier selectors defined; zero new tokens/hex/font-700 | grep + git diff | `grep -c "\.resumen-table__block--progresion" apps/planner/src/styles/app.css` → ≥ 1 AND `grep -c "\.resumen-board__empty-state" apps/planner/src/styles/app.css` → ≥ 1 AND `git diff origin/master -- apps/planner/src/styles/app.css \| grep -cE "^\+.*#[0-9a-fA-F]{3,6}"` → 0 AND `git diff origin/master -- apps/planner/src/styles/app.css \| grep -cE "^\+.*font-weight:\s*700"` → 0 | ✅ existing file | ⬜ pending |
| 12.9-02-01 | 02 | 2 | R1, R2, R6 | T-12.9-02-01 | `<dl>.resumen-table__identity` + `<table>.resumen-table__attrs` removed; `<header>.resumen-table__identity-header` added; Progresión + Habilidades block get BEM `--progresion` + `--habilidades` modifiers | grep + tsc | `grep -c "resumen-table__attrs" apps/planner/src/features/summary/resumen-table.tsx` → 0 AND `grep -c "resumen-table__identity-header" apps/planner/src/features/summary/resumen-table.tsx` → ≥ 1 AND `pnpm --filter planner exec tsc --noEmit` → 0 errors | ✅ existing file | ⬜ pending |
| 12.9-02-02 | 02 | 2 | R4, R5 | T-12.9-02-02 | Empty-state conditional wraps `<ResumenTable>` via `isProjectable`; handler bodies + 5 action button contracts unchanged | grep + git diff | `grep -c "resumen-board__empty-state" apps/planner/src/features/summary/resumen-board.tsx` → ≥ 1 AND `git diff origin/master -- apps/planner/src/features/persistence/ apps/planner/src/features/summary/save-slot-dialog.tsx apps/planner/src/components/ui/version-mismatch-dialog.tsx` → empty (no output) | ✅ existing file | ⬜ pending |
| 12.9-02-03 | 02 | 2 | R1, R2, R4 | T-12.9-02-03 | 3 new Vitest RTL specs under `tests/phase-12.9/` pass; structural invariants (BEM modifier present, 8 `<th>` in Progresión, identity-header count = 1, empty-state count = 1 when not projectable) all assert | unit (vitest) | `pnpm --filter planner test tests/phase-12.9/` exits 0 AND spec files exist at the 3 canonical paths | ❌ W0 new files | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `vitest.config.ts` include glob covers `tests/phase-12.9/**/*.spec.tsx` (Plan 01 Task 2 handles this)
- [x] `tests/phase-12.9/` directory will be created by Plan 02 Task 3 (first-commit auto-create)
- [x] Vitest + @testing-library/react already installed (Phase 12.8 infrastructure)
- [x] `vitest.config.ts` excludes Playwright specs (existing config)

*Wave 0 fully satisfied by Plan 01 Task 2 (vitest config) + existing infrastructure.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Progresión 8/8 columns visible without horizontal scroll at 1280px viewport across all 20 rows | R1 | jsdom has no layout engine; `scrollWidth`/`clientWidth` return 0. Real verification needs live Chromium + NWN1 fixture (Elfo + Neutral puro + Guerrero L1..L20) | `pnpm --filter planner dev` → open `localhost:5173` → create fixture → navigate Resumen → inspect Progresión row count 20 + col count 8; no h-scroll. Agent uses MCP Chrome tools per `feedback_delegate_agent_uat.md` |
| Empty-state copy reads cleanly in Spanish to a human reviewer | R4 | Copywriting quality + voice are subjective; grep confirms keys exist but not that the prose is natural | Navigate to Resumen pre-race/pre-alignment → inspect heading + body paragraph text; user approves or proposes revised strings |
| Dotes 8px inner padding matches Phase 12.8 visual rhythm to the eye | R3 | Selector grep + value grep confirm tokens, but aesthetic consistency with `.feat-board__section-header` is a perceptual check | Side-by-side compare Resumen board action bar + heading spacing against the Dotes picker (same viewport, dev tools off); confirm rhythm matches |
| Cargar + Importar clickable and Save / Export / Share disabled with tooltip in empty state | R4 + R5 | Button disabled state + tooltip hover behavior is interactive; automated assertions cover attribute presence but not hover UX | Hover each of the 5 action buttons in empty state; confirm tooltip shows on disabled ones; click Cargar + Importar to confirm they fire the dialog flow |

---

## Validation Sign-Off

- [x] All 5 tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has at least grep or test command)
- [x] Wave 0 covers all MISSING references (vitest config include glob + new test dir)
- [x] No watch-mode flags (all commands use `vitest run` via `pnpm test`)
- [x] Feedback latency < 10s (phase-12.9 scope is a single directory of ~3 specs)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-24 — derived from PLANs + SPEC (research skipped this phase per user choice; CONTEXT.md + SPEC.md provided full acceptance derivation).
