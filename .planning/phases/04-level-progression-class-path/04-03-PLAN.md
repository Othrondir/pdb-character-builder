---
phase: 04-level-progression-class-path
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/components/shell/summary-panel.tsx
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/level-progression/class-fixture.ts
  - apps/planner/src/features/level-progression/store.ts
  - apps/planner/src/features/level-progression/selectors.ts
  - apps/planner/src/features/level-progression/level-rail.tsx
  - apps/planner/src/features/level-progression/level-sheet.tsx
  - packages/rules-engine/src/progression/multiclass-rules.ts
  - packages/rules-engine/src/progression/progression-revalidation.ts
  - tests/phase-04/multiclass-rules.spec.ts
  - tests/phase-04/progression-revalidation.spec.tsx
autonomous: true
requirements: [FLOW-03, PROG-02, CLAS-03]
user_setup: []
must_haves:
  truths:
    - "Earlier-level edits preserve later level records and revalidate them instead of deleting downstream work."
    - "Multiclass restrictions, minimum-class commitments, and exception seams resolve through pure helpers rather than route-local conditionals."
    - "The level rail, active level sheet, and persistent summary panel share one progression severity model and one repair vocabulary."
  artifacts:
    - path: "packages/rules-engine/src/progression/multiclass-rules.ts"
      provides: "Pure multiclass and commitment legality resolver"
      contains: "evaluateMulticlassLegality"
    - path: "packages/rules-engine/src/progression/progression-revalidation.ts"
      provides: "Preserve-first downstream revalidation helper"
      contains: "revalidateProgressionAfterLevelChange"
    - path: "apps/planner/src/features/level-progression/level-rail.tsx"
      provides: "Rail status rendering for blocked and invalid downstream levels"
      contains: "Bloqueada"
    - path: "tests/phase-04/progression-revalidation.spec.tsx"
      provides: "Integration proof that downstream levels survive earlier edits and show repair feedback"
      contains: "Este nivel se conserva, pero depende de corregir decisiones anteriores."
  key_links:
    - from: "apps/planner/src/features/level-progression/class-fixture.ts"
      to: "packages/rules-engine/src/progression/multiclass-rules.ts"
      via: "minimum commitment, deferred rules, and exception overrides"
      pattern: "exceptionOverrides"
    - from: "packages/rules-engine/src/progression/progression-revalidation.ts"
      to: "apps/planner/src/features/level-progression/selectors.ts"
      via: "per-level status and inherited break metadata"
      pattern: "inheritedFromLevel"
    - from: "apps/planner/src/features/level-progression/selectors.ts"
      to: "apps/planner/src/components/shell/summary-panel.tsx"
      via: "shared summary severity for Build and shell status"
      pattern: "summaryStatus"
---

<objective>
Finish the Phase 4 legality backbone by enforcing multiclass and progression-specific restrictions, preserving downstream levels after upstream edits, and exposing repair state consistently across the rail, sheet, and summary panel.

Purpose: make the progression editor coherent under change so users can revisit earlier decisions without silent data loss or drift.
Output: multiclass rules, preserve-first revalidation, rail and sheet repair states, summary integration, and final Phase 4 legality tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-level-progression-class-path/04-CONTEXT.md
@.planning/phases/04-level-progression-class-path/04-RESEARCH.md
@.planning/phases/04-level-progression-class-path/04-VALIDATION.md
@.planning/phases/04-level-progression-class-path/04-UI-SPEC.md
@.planning/phases/04-level-progression-class-path/04-01-PLAN.md
@.planning/phases/04-level-progression-class-path/04-02-PLAN.md
@apps/planner/src/features/level-progression/class-fixture.ts
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/features/level-progression/selectors.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
@packages/rules-engine/src/progression/class-entry-rules.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure multiclass and preserve-first revalidation helpers</name>
  <files>apps/planner/src/features/level-progression/class-fixture.ts, packages/rules-engine/src/progression/multiclass-rules.ts, packages/rules-engine/src/progression/progression-revalidation.ts, tests/phase-04/multiclass-rules.spec.ts</files>
  <read_first>apps/planner/src/features/level-progression/class-fixture.ts, apps/planner/src/features/level-progression/progression-fixture.ts, packages/rules-engine/src/contracts/validation-outcome.ts, packages/rules-engine/src/progression/class-entry-rules.ts, .planning/phases/04-level-progression-class-path/04-CONTEXT.md, .planning/phases/04-level-progression-class-path/04-RESEARCH.md, .planning/phases/04-level-progression-class-path/04-VALIDATION.md, .planning/phases/04-level-progression-class-path/04-02-PLAN.md</read_first>
  <action>Extend `apps/planner/src/features/level-progression/class-fixture.ts` so relevant class records include the exact fields `minimumClassCommitment`, `exclusiveClassIds`, and `exceptionOverrides`. Add the exact `minimumClassCommitment` value `2` to `class:paladin` and `class:rogue`. Add at least one exact override record `{ code: 'puerta.shadowdancer-rogue-bridge', sourceClassId: 'class:rogue', targetClassId: 'class:shadowdancer' }` inside `class:shadowdancer.exceptionOverrides` so the rules layer has a concrete server-exception seam. Create `packages/rules-engine/src/progression/multiclass-rules.ts` exporting `evaluateMulticlassLegality(input)` and `applyExceptionOverrides(input)`, both using `resolveValidationOutcome`. The resolver must return `illegal` when a class change breaks a minimum commitment or an explicit exclusive-class rule, and `blocked` when a later phase is still required to verify a deferred class restriction. Create `packages/rules-engine/src/progression/progression-revalidation.ts` exporting `revalidateProgressionAfterLevelChange(input)` with exact per-level output fields `level`, `status`, `issues`, and `inheritedFromLevel`. The helper must preserve all level records passed in, re-check the edited level and every later level, and never delete entries from the array. Add `tests/phase-04/multiclass-rules.spec.ts` that asserts a minimum commitment violation resolves non-legally, the exact `puerta.shadowdancer-rogue-bridge` override can bypass the default commitment rule in the pure resolver, and a prestige class with deferred requirements still returns `blocked` rather than `legal`.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-04/multiclass-rules.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `minimumClassCommitment`
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `exceptionOverrides`
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `puerta.shadowdancer-rogue-bridge`
    - `packages/rules-engine/src/progression/multiclass-rules.ts` contains `evaluateMulticlassLegality`
    - `packages/rules-engine/src/progression/multiclass-rules.ts` contains `applyExceptionOverrides`
    - `packages/rules-engine/src/progression/multiclass-rules.ts` contains `resolveValidationOutcome`
    - `packages/rules-engine/src/progression/progression-revalidation.ts` contains `revalidateProgressionAfterLevelChange`
    - `packages/rules-engine/src/progression/progression-revalidation.ts` contains `inheritedFromLevel`
    - `tests/phase-04/multiclass-rules.spec.ts` contains `puerta.shadowdancer-rogue-bridge`
    - `tests/phase-04/multiclass-rules.spec.ts` contains `blocked`
  </acceptance_criteria>
  <done>Phase 4 has a pure legality layer for multiclass and preserve-first downstream revalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Surface downstream repair state in the rail, active sheet, and summary panel</name>
  <files>apps/planner/src/lib/copy/es.ts, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/styles/app.css, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/features/level-progression/level-rail.tsx, apps/planner/src/features/level-progression/level-sheet.tsx, tests/phase-04/progression-revalidation.spec.tsx</files>
  <read_first>apps/planner/src/lib/copy/es.ts, apps/planner/src/components/shell/summary-panel.tsx, apps/planner/src/styles/app.css, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/features/level-progression/level-rail.tsx, apps/planner/src/features/level-progression/level-sheet.tsx, packages/rules-engine/src/progression/multiclass-rules.ts, packages/rules-engine/src/progression/progression-revalidation.ts, .planning/phases/04-level-progression-class-path/04-UI-SPEC.md</read_first>
  <action>Update `apps/planner/src/features/level-progression/store.ts` so `setLevelClassId` and `setLevelAbilityIncrease` keep the full 16-level array intact and trigger a selector-friendly revalidation path rather than truncating later levels. Update `apps/planner/src/features/level-progression/selectors.ts` so `selectLevelRail`, `selectActiveLevelSheetView`, and `selectProgressionSummary` consume `revalidateProgressionAfterLevelChange`, emit the exact status labels `Bloqueada` and `Inválida`, and expose the exact repair message `Este nivel se conserva, pero depende de corregir decisiones anteriores.` whenever a later level inherits a break from an earlier edit. Update `apps/planner/src/features/level-progression/level-rail.tsx` to render status badges and issue count or severity markers for blocked and illegal levels, and update `apps/planner/src/features/level-progression/level-sheet.tsx` to show the repair callout inline when the current level was preserved but invalidated by an earlier change. Update `apps/planner/src/components/shell/summary-panel.tsx` so progression severity overrides the shell fallback and the exact plan-state copy becomes `Progresión en reparación` for blocked paths, `Ruta inválida` for illegal paths, and `Lista para habilidades` once the Phase 4 class path is coherent. Extend `apps/planner/src/lib/copy/es.ts` with the exact strings `Progresión en reparación`, `Ruta inválida`, `Lista para habilidades`, and `Este nivel se conserva, pero depende de corregir decisiones anteriores.`. Extend `apps/planner/src/styles/app.css` with the exact class names `.level-rail__entry.is-blocked`, `.level-rail__entry.is-illegal`, `.level-sheet__repair-callout`, and `.progression-summary-strip__status`, keeping blocked states bronze-muted and illegal states dark red per the UI contract. Add `tests/phase-04/progression-revalidation.spec.tsx` asserting that after changing an earlier level, later levels still exist in the DOM, their rail entries show `Bloqueada` or `Inválida`, the exact repair message renders in the active sheet, and the summary panel reflects the most severe current progression state.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-04/progression-revalidation.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/lib/copy/es.ts` contains `Progresión en reparación`
    - `apps/planner/src/lib/copy/es.ts` contains `Ruta inválida`
    - `apps/planner/src/lib/copy/es.ts` contains `Lista para habilidades`
    - `apps/planner/src/lib/copy/es.ts` contains `Este nivel se conserva, pero depende de corregir decisiones anteriores.`
    - `apps/planner/src/components/shell/summary-panel.tsx` contains `Lista para habilidades`
    - `apps/planner/src/features/level-progression/level-rail.tsx` contains `Bloqueada`
    - `apps/planner/src/features/level-progression/level-rail.tsx` contains `Inválida`
    - `apps/planner/src/features/level-progression/level-sheet.tsx` contains `Este nivel se conserva, pero depende de corregir decisiones anteriores.`
    - `apps/planner/src/styles/app.css` contains `.level-rail__entry.is-blocked`
    - `apps/planner/src/styles/app.css` contains `.level-sheet__repair-callout`
    - `tests/phase-04/progression-revalidation.spec.tsx` contains `Bloqueada`
    - `tests/phase-04/progression-revalidation.spec.tsx` contains `Este nivel se conserva, pero depende de corregir decisiones anteriores.`
  </acceptance_criteria>
  <done>The planner now preserves downstream levels under change and explains exactly how to repair broken class paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm vitest run tests/phase-04 --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- Earlier-level edits preserve later levels instead of deleting them
- Broken downstream levels become visibly blocked or invalid with explicit repair messaging
- The summary panel, rail, and active sheet stay synchronized on the most severe progression state
</success_criteria>

<output>
After completion, create `.planning/phases/04-level-progression-class-path/04-03-SUMMARY.md`
</output>
