---
phase: 04-level-progression-class-path
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/styles/app.css
  - apps/planner/src/features/level-progression/class-fixture.ts
  - apps/planner/src/features/level-progression/selectors.ts
  - apps/planner/src/features/level-progression/build-progression-board.tsx
  - apps/planner/src/features/level-progression/level-sheet.tsx
  - apps/planner/src/features/level-progression/ability-increase-control.tsx
  - packages/rules-engine/src/progression/class-entry-rules.ts
  - packages/rules-engine/src/progression/level-gains.ts
  - tests/phase-04/class-prerequisites.spec.ts
  - tests/phase-04/level-sheet-gains.spec.tsx
autonomous: true
requirements: [ABIL-02, CLAS-01, CLAS-02, CLAS-04]
user_setup: []
must_haves:
  truths:
    - "Phase 4 consumes a compiled class catalog keyed by canonical `class:*` ids rather than inferring class choices from UI labels."
    - "The active level sheet owns class selection, prerequisite feedback, level gains, and level-based ability increases exactly where the user edits that level."
    - "Class-entry legality and gain summaries resolve through pure helpers shared by selectors, tests, and the routed Build UI."
  artifacts:
    - path: "apps/planner/src/features/level-progression/class-fixture.ts"
      provides: "Planner-facing Phase 4 class catalog with base and prestige entries"
      contains: "class:shadowdancer"
    - path: "packages/rules-engine/src/progression/class-entry-rules.ts"
      provides: "Pure prerequisite evaluator for class entry"
      contains: "evaluateClassEntry"
    - path: "packages/rules-engine/src/progression/level-gains.ts"
      provides: "Pure level-gain summarizer for the active level sheet"
      contains: "abilityIncreaseAvailable"
    - path: "apps/planner/src/features/level-progression/level-sheet.tsx"
      provides: "Editable class sheet with prerequisite and gains sections"
      contains: "Aumento de característica"
    - path: "tests/phase-04/class-prerequisites.spec.ts"
      provides: "Contract coverage for base-class legality and prestige blocked states"
      contains: "class:paladin"
  key_links:
    - from: "apps/planner/src/features/level-progression/class-fixture.ts"
      to: "packages/rules-engine/src/progression/class-entry-rules.ts"
      via: "implemented requirements and deferred requirement labels"
      pattern: "implementedRequirements"
    - from: "packages/rules-engine/src/progression/level-gains.ts"
      to: "apps/planner/src/features/level-progression/level-sheet.tsx"
      via: "level-specific gain projection"
      pattern: "choicePrompts"
    - from: "apps/planner/src/features/level-progression/selectors.ts"
      to: "apps/planner/src/features/level-progression/build-progression-board.tsx"
      via: "active sheet view model for available classes and prerequisite rows"
      pattern: "selectActiveLevelSheetView"
---

<objective>
Add the class-path mechanics that make the Phase 4 editor meaningful: a compiled class catalog, pure class-entry and gain resolvers, and a level sheet that shows classes, prerequisites, gains, and ability increases in one place.

Purpose: turn the scaffolded progression board into an actual class-planning surface before multiclass and downstream repair rules land.
Output: class fixture, prerequisite resolver, level-gain resolver, editable level sheet, ability-increase UI, and targeted Phase 4 tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-level-progression-class-path/04-CONTEXT.md
@.planning/phases/04-level-progression-class-path/04-RESEARCH.md
@.planning/phases/04-level-progression-class-path/04-VALIDATION.md
@.planning/phases/04-level-progression-class-path/04-UI-SPEC.md
@.planning/phases/04-level-progression-class-path/04-01-PLAN.md
@apps/planner/src/features/character-foundation/foundation-fixture.ts
@apps/planner/src/features/character-foundation/store.ts
@apps/planner/src/features/character-foundation/selectors.ts
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/features/level-progression/selectors.ts
@packages/rules-engine/src/contracts/canonical-id.ts
@packages/rules-engine/src/contracts/validation-outcome.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the compiled class catalog plus pure prerequisite and gain resolvers</name>
  <files>apps/planner/src/features/level-progression/class-fixture.ts, packages/rules-engine/src/progression/class-entry-rules.ts, packages/rules-engine/src/progression/level-gains.ts, tests/phase-04/class-prerequisites.spec.ts</files>
  <read_first>apps/planner/src/features/character-foundation/foundation-fixture.ts, apps/planner/src/features/level-progression/progression-fixture.ts, packages/rules-engine/src/contracts/canonical-id.ts, packages/rules-engine/src/contracts/validation-outcome.ts, .planning/phases/04-level-progression-class-path/04-CONTEXT.md, .planning/phases/04-level-progression-class-path/04-RESEARCH.md, .planning/phases/04-level-progression-class-path/04-VALIDATION.md, .planning/phases/04-level-progression-class-path/04-01-PLAN.md</read_first>
  <action>Create `apps/planner/src/features/level-progression/class-fixture.ts` exporting `phase04ClassFixture` with the exact top-level fields `classes` and `abilityIncreaseLevels`. Each class record must use the exact fields `id`, `label`, `kind`, `hitDie`, `implementedRequirements`, `deferredRequirementLabels`, `gainTable`, and `tags`. Include the exact class ids `class:fighter`, `class:rogue`, `class:wizard`, `class:cleric`, `class:paladin`, `class:shadowdancer`, and `class:weapon-master`. Set the exact `abilityIncreaseLevels` array to `[4, 8, 12, 16]`. In `implementedRequirements`, encode the exact rules `class:wizard` requires `{ key: 'int', score: 11 }`, `class:cleric` requires `requiresDeity: true`, and `class:paladin` only allows `['alignment:lawful-good']`. For `class:shadowdancer` and `class:weapon-master`, populate `deferredRequirementLabels` with exact Spanish strings explaining that feat or skill prerequisites are still pending later phases so the resolver must fail closed with `blocked`. Create `packages/rules-engine/src/progression/class-entry-rules.ts` exporting `evaluateClassEntry(input)` and `collectVisibleClassOptions(input)`; both must use `resolveValidationOutcome` so blocked and illegal class choices stay on the project-standard status vocabulary. Create `packages/rules-engine/src/progression/level-gains.ts` exporting `getLevelGainsForSelection(input)` with the exact output fields `features`, `choicePrompts`, and `abilityIncreaseAvailable`. Add `tests/phase-04/class-prerequisites.spec.ts` asserting the exact behaviors: `class:paladin` is non-legal outside lawful good alignment, `class:cleric` is non-legal when `deity:none` or missing deity is present, `class:wizard` is non-legal below `INT 11`, and `class:shadowdancer` is `blocked` because its deferred prerequisites are not yet implemented.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-04/class-prerequisites.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `class:fighter`
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `class:shadowdancer`
    - `apps/planner/src/features/level-progression/class-fixture.ts` contains `abilityIncreaseLevels`
    - `packages/rules-engine/src/progression/class-entry-rules.ts` contains `evaluateClassEntry`
    - `packages/rules-engine/src/progression/class-entry-rules.ts` contains `collectVisibleClassOptions`
    - `packages/rules-engine/src/progression/class-entry-rules.ts` contains `resolveValidationOutcome`
    - `packages/rules-engine/src/progression/level-gains.ts` contains `getLevelGainsForSelection`
    - `packages/rules-engine/src/progression/level-gains.ts` contains `abilityIncreaseAvailable`
    - `tests/phase-04/class-prerequisites.spec.ts` contains `class:paladin`
    - `tests/phase-04/class-prerequisites.spec.ts` contains `class:shadowdancer`
    - `tests/phase-04/class-prerequisites.spec.ts` contains `blocked`
  </acceptance_criteria>
  <done>Phase 4 has a concrete class catalog and pure rule helpers for class entry and per-level gains.</done>
</task>

<task type="auto">
  <name>Task 2: Render class choice, prerequisites, gains, and ability increases in the active level sheet</name>
  <files>apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/features/level-progression/build-progression-board.tsx, apps/planner/src/features/level-progression/level-sheet.tsx, apps/planner/src/features/level-progression/ability-increase-control.tsx, tests/phase-04/level-sheet-gains.spec.tsx</files>
  <read_first>apps/planner/src/lib/copy/es.ts, apps/planner/src/styles/app.css, apps/planner/src/features/level-progression/build-progression-board.tsx, apps/planner/src/features/level-progression/level-sheet.tsx, apps/planner/src/features/level-progression/store.ts, apps/planner/src/features/level-progression/selectors.ts, apps/planner/src/features/level-progression/class-fixture.ts, packages/rules-engine/src/progression/class-entry-rules.ts, packages/rules-engine/src/progression/level-gains.ts, .planning/phases/04-level-progression-class-path/04-UI-SPEC.md</read_first>
  <action>Create `apps/planner/src/features/level-progression/ability-increase-control.tsx` as the level-owned selector for milestone levels, using the exact helper text `Este nivel concede un aumento de característica que se reflejará en Atributos.`. Update `apps/planner/src/features/level-progression/selectors.ts` to export the exact selectors `selectClassOptionsForLevel` and `selectActiveLevelSheetView`, combining the foundation store, progression store, `evaluateClassEntry`, and `getLevelGainsForSelection` into one active-sheet projection. Update `apps/planner/src/features/level-progression/level-sheet.tsx` to render the exact section headings `Clase del nivel`, `Requisitos de entrada`, `Ganancias del nivel`, and `Aumento de característica`, plus visible class options, inline prerequisite rows, a gains ledger, and the ability-increase control only on levels 4, 8, 12, and 16. Update `apps/planner/src/features/level-progression/build-progression-board.tsx` so the active level sheet consumes `selectActiveLevelSheetView` and the rail keeps focus on the selected level while the right side updates. Extend `apps/planner/src/lib/copy/es.ts` with the exact strings `Clase del nivel`, `Requisitos de entrada`, `Ganancias del nivel`, `Aumento de característica`, `Legal`, `Bloqueada`, `Inválida`, and `Este nivel concede un aumento de característica que se reflejará en Atributos.`. Extend `apps/planner/src/styles/app.css` with the exact class names `.level-sheet__requirements`, `.level-sheet__gains`, `.level-sheet__ability`, and `.class-option.is-selected`, preserving the existing NWN1 dark-and-gold board treatment. Add `tests/phase-04/level-sheet-gains.spec.tsx` asserting a selectable base class renders gains in the active sheet, a blocked prestige class renders its deferred-prerequisite copy inline, and level 4 shows the exact ability-increase helper text.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/phase-04/level-sheet-gains.spec.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `apps/planner/src/lib/copy/es.ts` contains `Clase del nivel`
    - `apps/planner/src/lib/copy/es.ts` contains `Aumento de característica`
    - `apps/planner/src/features/level-progression/ability-increase-control.tsx` contains `Este nivel concede un aumento de característica que se reflejará en Atributos.`
    - `apps/planner/src/features/level-progression/selectors.ts` contains `selectActiveLevelSheetView`
    - `apps/planner/src/features/level-progression/selectors.ts` contains `selectClassOptionsForLevel`
    - `apps/planner/src/features/level-progression/level-sheet.tsx` contains `Ganancias del nivel`
    - `apps/planner/src/features/level-progression/level-sheet.tsx` contains `Requisitos de entrada`
    - `apps/planner/src/styles/app.css` contains `.level-sheet__gains`
    - `apps/planner/src/styles/app.css` contains `.class-option.is-selected`
    - `tests/phase-04/level-sheet-gains.spec.tsx` contains `Aumento de característica`
    - `tests/phase-04/level-sheet-gains.spec.tsx` contains `class:shadowdancer`
  </acceptance_criteria>
  <done>The active level sheet now behaves like a real class-planning panel with gains and level-based ability milestones.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm vitest run tests/phase-04/class-prerequisites.spec.ts tests/phase-04/level-sheet-gains.spec.tsx --reporter=dot`
- [ ] `corepack pnpm exec tsc -p tsconfig.base.json --noEmit`
- [ ] `corepack pnpm build:planner`
</verification>

<success_criteria>

- All tasks completed
- Users can choose visible base and prestige classes from a canonical class catalog while seeing prerequisite status before confirmation
- The active level sheet shows gains and milestone-based ability increases at the relevant levels
- Unsupported or deferred prestige prerequisites fail closed as blocked instead of appearing legal
</success_criteria>

<output>
After completion, create `.planning/phases/04-level-progression-class-path/04-02-SUMMARY.md`
</output>
