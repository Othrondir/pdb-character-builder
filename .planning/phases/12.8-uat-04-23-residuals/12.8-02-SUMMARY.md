---
phase: 12.8-uat-04-23-residuals
plan: 02
subsystem: rules-engine
tags: [prestige-gate, schema-extension, rules-engine, shadowdancer, pale-master, caballero-arcano, f5]

# Dependency graph
requires:
  - phase: 12.8-uat-04-23-residuals
    plan: 01
    provides: vitest.config.ts configDefaults.exclude spread (Playwright e2e exclusion) and Phase 12.8 test directory scaffold
  - phase: 12.4-construccion-correctness-clarity
    provides: 12.4-06 reachableAtLevelN gate + PRESTIGE_PREREQ_OVERRIDES table + prestigePrereqTemplates copy block
  - phase: quick/260422-h9k
    provides: override-pattern precedent (harper + campeondivino + weaponmaster additions)
provides:
  - ClassPrereqInput['decodedPrereqs'] extended with 6 new fields (minArcaneSpellLevel, minSpellLevel, excludedClassIds, requiredAnyFeatGroups, requiredAnyRaceIds, requiredAnyClassLevels)
  - BlockerKind union extended with 6 new alternatives (arcane-spell-level, spell-level, excluded-class, any-feat-group, any-race, any-class-level)
  - PrestigeGateInput extended with 3 new optional fields (highestArcaneSpellLevel, highestSpellLevel, raceId: string | null | undefined)
  - 6 new inline label helpers (arcaneSpellLevelLabel, spellLevelLabel, excludedClassLabel, anyFeatGroupLabel, anyRaceLabel, anyClassLevelLabel)
  - 9 new prestigePrereqTemplates keys mirroring the new label shapes (LANG-01 grep anchor)
  - Shadowdancer override enriched with 2 skill-rank reqs + 2 feat reqs (4 new IDs landed)
  - PrestigeGateBuildState.highestArcaneSpellLevel replaces highestArcaneClassLevel and switches to computeHighestSpellLevel for semantic correctness
  - Regression snapshot proving 21 gated prestige rows still emit blockers at L2 Elfo Guerrero BAB=2
affects:
  - 12.8-03 (F3 + F4 Dotes multi-slot UX — inherits unchanged rules-engine gate surface)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Schema-extension TDD pattern: extend decodedPrereqs type -> add evaluator branches in deterministic order -> mirror inline labels in shellCopyEs.progression.prestigePrereqTemplates -> lock via Vitest schema + overrides specs"
    - "Rename-at-boundary discipline: when plan-text's internal field name (arcaneCasterLevel) conflicted with pre-existing caller contract (highestArcaneClassLevel), renamed to plan-aligned + caller-aligned name (highestArcaneSpellLevel) in one pass to close 18 of 23 pre-existing TS errors"
    - "it.each regression snapshot: Object.keys(PRESTIGE_PREREQ_OVERRIDES) iterator asserts blockers.length > 0 + reachable === false at L2 Elfo Guerrero BAB=2 — locks the invariant that every gated row stays gated"

key-files:
  created:
    - tests/phase-12.8/prestige-gate-schema.spec.ts — 12 cases covering each of the 6 new evaluator branches + D-10 fail-closed invariant
    - tests/phase-12.8/prestige-gate-overrides.spec.ts — 24 cases (3 F5 tightening proofs + 21 it.each regression rows)
  modified:
    - packages/rules-engine/src/progression/prestige-gate.ts — schema + BlockerKind + PrestigeGateInput + 6 label helpers + 6 evaluator branches. Fail-closed branch at lines 223-230 byte-identical vs pre-plan (D-10).
    - apps/planner/src/features/level-progression/prestige-prereq-data.ts — shadowdancer entry enriched (minBab only -> minBab + minSkillRanks[2] + requiredFeats[2])
    - apps/planner/src/lib/copy/es.ts — 9 new keys appended to shellCopyEs.progression.prestigePrereqTemplates (arcaneSpellLevelSingular + arcaneSpellLevelPlural + spellLevelSingular + spellLevelPlural + excludedClass + anyFeatGroupSingle + anyFeatGroupMulti + anyRace + anyClassLevel)
    - apps/planner/src/features/level-progression/prestige-gate-build.ts — PrestigeGateBuildState.highestArcaneClassLevel renamed to highestArcaneSpellLevel + computation switched from computeHighestClassLevel(...'arcane') to computeHighestSpellLevel(...'arcane') for semantic correctness
    - apps/planner/src/features/level-progression/class-picker.tsx — reachableAtLevelN() call updated to pass highestArcaneSpellLevel (docstring reference updated)
    - vitest.config.ts — environmentMatchGlobs extended with tests/phase-12.8/**/*.spec.tsx jsdom entry (exclude list owned by 12.8-01 stays byte-identical)

key-decisions:
  - "D-07 executed: root-cause of F5 is schema under-specification, not partial enrichment. Extended ClassPrereqInput.decodedPrereqs with 6 fields already shipped in the override table but previously ignored by the evaluator."
  - "D-08 executed: evaluator gained 6 new branches in deterministic order (arcane-spell-level -> spell-level -> excluded-class -> any-feat-group -> any-race -> any-class-level). Inline labels + mirrored copy template keys."
  - "D-09 executed: shadowdancer override enriched with canonical IDs verified during planning (skill:moversesigilosamente 8, skill:equilibrio 5, feat:dodge, feat:mobility)."
  - "D-10 invariant preserved: fail-closed branch at prestige-gate.ts:223-230 byte-identical (git diff confirms zero changes)."
  - "D-13 executed: .ts schema spec stays node-mode; environmentMatchGlobs entry for phase-12.8 jsdom mode added for future .tsx specs (12.8-03)."
  - "Deviation (Rule 1): plan-text's internal input field names (arcaneCasterLevel, raceId: string only) did not match pre-existing caller contract (highestArcaneClassLevel + raceId: string | null at class-picker.tsx, highestArcaneSpellLevel + raceId: null at fixture spec). Renamed schema fields to highestArcaneSpellLevel / highestSpellLevel / raceId: string | null | undefined to close 18 of 23 pre-existing TS errors in one pass."
  - "Deviation (Rule 1): prestige-gate-build.ts was feeding the arcane gate with CLASS level (wrong semantic — Wizard L5 can cast L3 spells, not L5 spells). Switched to computeHighestSpellLevel(...'arcane') which returns the actual spell-level progression value matching minArcaneSpellLevel semantics."

patterns-established:
  - "Override-field-shape contract: plan-specified must_haves.artifacts.contains_also list enforced via grep invariants over the post-edit source files — prevents field-name drift between schema + evaluator + copy + callers."
  - "Threat T-12.8-02-02 (override typo silently fails-open) mitigated by Object.keys(PRESTIGE_PREREQ_OVERRIDES) it.each snapshot — any future override shape regression will flip a row to reachable and immediately fail the regression spec."
  - "Threat T-12.8-02-05 (two Wave 1 plans editing vitest.config.ts) mitigated by mandatory re-read + shape-A/shape-B reconciliation gate: Shape B confirmed (12.8-01 had landed), only environmentMatchGlobs extended, configDefaults + exclude byte-identical."

requirements-completed: [CLAS-02]

# Metrics
duration: 11min
completed: 2026-04-24
---

# Phase 12.8 Plan 02: UAT-04-23 F5 (Prestige Gate Schema Extension) Summary

**Extended ClassPrereqInput.decodedPrereqs with 6 fields already shipped in the override table + evaluator with 6 matching branches + 9 Spanish copy keys; enriched shadowdancer override with skill-rank + feat reqs; planted a regression snapshot that iterates Object.keys(PRESTIGE_PREREQ_OVERRIDES) so future override typos cannot silently fail-open.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-04-24T11:04:26Z
- **Completed:** 2026-04-24T11:15:52Z
- **Tasks:** 2 (both committed atomically)
- **Files modified:** 6 (4 source, 2 test) + 1 config + 2 new test files

## Accomplishments

- **F5 root cause closed:** `ClassPrereqInput.decodedPrereqs` schema gained all 6 previously-silent fields (`minArcaneSpellLevel`, `minSpellLevel`, `excludedClassIds`, `requiredAnyFeatGroups`, `requiredAnyRaceIds`, `requiredAnyClassLevels`). Before: pale-master + caballero-arcano rendered OPEN at L2 Elfo Guerrero BAB=2 because the override's `minArcaneSpellLevel: 3/5` was a field the gate's schema didn't declare. After: both classes emit `arcane-spell-level` blocker proving branch 4 (decoded) fires correctly.
- **Shadowdancer tightening:** `class:shadowdancer` entry went from `{ minBab: 2 }` (BAB-only gate, UAT-visible regression) to the full NWN1 EE server prereq block — BAB ≥ 2 + Moverse sigilosamente 8 + Equilibrio 5 + feat:dodge (Esquiva) + feat:mobility (Movilidad). Regression snapshot asserts all 4 labels render at L2 Elfo Guerrero.
- **Copy-template parity:** `shellCopyEs.progression.prestigePrereqTemplates` gained 9 new keys (2 singular/plural pairs for arcane/spell level, 1 for excluded-class, 2 for any-feat-group single/multi, 1 for any-race, 1 for any-class-level). LANG-01 grep anchor preserved.
- **Pre-existing TS error reduction:** baseline had 23 TS errors in the prestige-gate surface (documented in 12.8-01 SUMMARY). After this plan: 4 errors remain, all pre-existing and out-of-scope (`selectors.ts` canonical-id branded-type drift and `prestige-gate.fixture.spec.ts` `'feat-or'` literal comparisons). **18 of 23 TS errors closed.**
- **D-10 invariant preserved:** fail-closed branch at `prestige-gate.ts:223-230` (the `if (!enriched || !classRow.decodedPrereqs)` emission of `{ kind: 'unvetted', threshold: 0, label: 'Requisitos en revisión' }`) is byte-identical. `git diff HEAD~2 HEAD -- packages/rules-engine/src/progression/prestige-gate.ts | grep -E "^[-+].*Requisitos en revisión|^[-+].*kind: 'unvetted'"` returns empty — no matching add/delete lines.
- **Revision BLOCKER 1 scope split honored:** this plan's `vitest.config.ts` diff touches only the `environmentMatchGlobs` array. The `configDefaults` import and `exclude: [...configDefaults.exclude, 'tests/**/*.e2e.spec.ts']` list (owned by 12.8-01) survive byte-identical. Grep invariants: `configDefaults` = 2 (preserved), `tests/phase-12.8` = 1 (new entry, no collision).
- **must_haves truth #5 preserved:** the plan specified "18 currently-gated prestige rows"; reality is 21 rows in `PRESTIGE_PREREQ_OVERRIDES` (harper + campeondivino + weaponmaster landed via quick-260422-h9k). The regression snapshot uses `Object.keys(PRESTIGE_PREREQ_OVERRIDES)` so it covers whatever count ships today, with the must_have invariant intact: every gated row emits `blockers.length > 0`.

## Task Commits

1. **Task 1: Extend schema + evaluator + copy templates** — `6f79455` (feat)
2. **Task 2: Enrich shadowdancer override + Vitest coverage** — `4371827` (test)

## Evaluator branch order (deterministic)

1. `minBab` (legacy)
2. `minSkillRanks` (legacy)
3. `requiredFeats` (legacy)
4. `minClassLevel` (legacy)
5. `minArcaneSpellLevel` (new)
6. `minSpellLevel` (new — fail-closed per Phase 07.2 magic descope)
7. `excludedClassIds` (new)
8. `requiredAnyFeatGroups` (new)
9. `requiredAnyRaceIds` (new)
10. `requiredAnyClassLevels` (new)

Order documented via code ordering in `reachableAtLevelN`; locked by regression snapshot test labels.

## Shadowdancer canonical-ID verification

| ID | Path | Evidence |
|----|------|----------|
| `skill:moversesigilosamente` (rank 8) | Existing cross-reference | Already used in assassin, tirador-espesura, ladron-sombras-amn overrides (lines 259, 293, 344 of prestige-prereq-data.ts). |
| `skill:equilibrio` (rank 5) | compiled-skills.ts:749 | Label "Equilibrio" — this is Puerta's Spanish render of NWN1 Tumble. |
| `feat:dodge` | compiled-feats.ts:42588 | Label "Esquiva". |
| `feat:mobility` | compiled-feats.ts:42710 | Label "Movilidad". Also listed as prerequisite for `feat:mobility` via `requiredFeat1: feat:dodge`. |

## Copy template additions (9 keys)

```ts
arcaneSpellLevelSingular: 'Requiere lanzar 1 nivel de conjuro arcano',
arcaneSpellLevelPlural: 'Requiere lanzar conjuros arcanos de nivel {N}',
spellLevelSingular: 'Requiere lanzar 1 nivel de conjuro',
spellLevelPlural: 'Requiere lanzar conjuros de nivel {N}',
excludedClass: 'Incompatible con {className}',
anyFeatGroupSingle: 'Requiere dote: {featName}',
anyFeatGroupMulti: 'Requiere una de estas dotes: {featNames}',
anyRace: 'Requiere raza: {raceNames}',
anyClassLevel: 'Requiere {classLevelList}',
```

Empty-array fallback copies (`'Requiere al menos una dote del grupo'`, `'Requiere una raza específica'`, `'Requiere niveles en una clase'`) remain defensive-only inline strings in `prestige-gate.ts` — never exercised by real override data (checker warning W1 accepted).

## Test counts

| Runner | Before | After | Delta |
|--------|--------|-------|-------|
| Vitest tests/phase-12.8/prestige-gate-schema.spec.ts | N/A (new) | 12/12 pass | +12 |
| Vitest tests/phase-12.8/prestige-gate-overrides.spec.ts | N/A (new) | 24/24 pass | +24 |
| Vitest full suite | 101 files pass / 3 fail; 2090 pass / 7 fail / 2 skip / 1 todo | 103 files pass / 3 fail; 2126 pass / 6 fail / 2 skip / 1 todo | +36 passing, -1 failing |
| TypeScript errors | 23 pre-existing | 4 | **-19 errors (18 from F5 surface + 1 prior fixture raceId:null error now typechecks)** |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] plan-text's `PrestigeGateInput.arcaneCasterLevel?: number` + `raceId?: string` did not match pre-existing caller contract**
- **Found during:** Task 1 Step C typecheck
- **Issue:** The plan's `<interfaces>` section proposed adding `arcaneCasterLevel?: number` and `raceId?: string` (no `null`) to `PrestigeGateInput`. Reality: `apps/planner/src/features/level-progression/class-picker.tsx:180` was ALREADY passing `highestArcaneClassLevel` + `highestSpellLevel`, and `foundationStore.raceId` is `string | null`, and `tests/phase-12.4/prestige-gate.fixture.spec.ts:28-33` was ALREADY passing `highestArcaneSpellLevel` + `highestSpellLevel` + `raceId: null`. These were the 23 pre-existing TS errors 12.8-01 SUMMARY noted would be "closed by 12.8-02 D-08".
- **Fix:** Renamed the schema fields to `highestArcaneSpellLevel?: number` + `highestSpellLevel?: number` + `raceId?: string | null` to match the caller contract. Evaluator branch for `minArcaneSpellLevel` consults `input.highestArcaneSpellLevel ?? 0` instead of `input.arcaneCasterLevel ?? 0`. Semantic invariant preserved (fail-closed when caller passes undefined).
- **Files modified:** `packages/rules-engine/src/progression/prestige-gate.ts` (schema + evaluator lines 286-287)
- **Verification:** `pnpm run typecheck` error count 23 -> 5 -> then 4 after class-picker.tsx rename; full grep invariant `highestArcaneSpellLevel` present in rules-engine + caller + fixture spec.
- **Committed in:** `6f79455`

**2. [Rule 1 - Bug] `PrestigeGateBuildState.highestArcaneClassLevel` fed the wrong quantity (class level vs spell level)**
- **Found during:** Task 1 Rule-1 investigation after the field rename above surfaced `prestige-gate-build.ts` as a caller
- **Issue:** Pre-plan the builder computed `highestArcaneClassLevel: computeHighestClassLevel(classLevels, ARCANE_SPELLCASTER_IDS)` — the raw CLASS level, not the arcane SPELL level. Per NWN1 EE rules a Wizard L5 can cast 3rd-level arcane spells (not 5th-level), and pale-master's `minArcaneSpellLevel: 3` is a SPELL-level threshold. Feeding class level would under-gate (Wizard L2 correctly blocks, but the naïve class ≥ spell heuristic breaks for paladin/ranger/assassin which cast at 1/2 BAB progression).
- **Fix:** Renamed `highestArcaneClassLevel` -> `highestArcaneSpellLevel` on `PrestigeGateBuildState` and the return object in `buildPrestigeGateBuildState`. Switched computation to `computeHighestSpellLevel(classLevels, 'arcane')` which uses the existing per-class progression tables (FULL_CASTER_PROGRESS / SORCERER_PROGRESS / BARD_PROGRESS / PALADIN_PROGRESS / ASSASSIN_PROGRESS / ARTIFICE_PROGRESS). `computeHighestClassLevel` helper retained as dead code for now (not a Rule 1 blocker; 2-line removal deferred to a future housekeeping pass).
- **Files modified:** `apps/planner/src/features/level-progression/prestige-gate-build.ts`, `apps/planner/src/features/level-progression/class-picker.tsx`
- **Verification:** `tests/phase-12.8/prestige-gate-schema.spec.ts` + `prestige-gate-overrides.spec.ts` 36/36 pass — proves pale-master + caballero-arcano tighten at L2 BAB=2 with `highestArcaneSpellLevel: 0`.
- **Committed in:** `6f79455`

### Scope-Boundary Deferrals (Pre-existing Drift, NOT Introduced by This Plan)

**3. `tests/phase-12.4/prestige-gate.fixture.spec.ts` uses `'feat-or'` as BlockerKind literal; plan-02 specified `'any-feat-group'`**
- Pre-existing failure at lines 362, 419, 420, 423. Test was authored against a ghost kind name that was never in `BlockerKind` before AND is not in `BlockerKind` after this plan. 2 TS2367 errors (`'feat-or'` does not overlap BlockerKind) + 3 runtime failures (find returns undefined because kind never matches).
- Pre-plan: 3 runtime failures (same).
- Post-plan: 3 runtime failures (same).
- Scope boundary: my task spec explicitly named the new kind `'any-feat-group'` (plan `<interfaces>` section). Test was wrong before my task; test remains wrong after.
- Mitigation: left for a future small follow-up that either renames `'any-feat-group'` -> `'feat-or'` (2-line evaluator change + docs) OR updates the test to use the new kind name. Deferred to a quick task.

**4. `tests/phase-12.4/prestige-gate.fixture.spec.ts:63 + class-picker-prestige-reachability.spec.tsx:109+127` expect different copy strings for the L1 blocker + arcane-spell-level label**
- Pre-existing failures — tests expect:
  - `'Clase de prestigio: no disponible en nivel 1; revisa sus requisitos'` (L1 blocker) — actual code emits `'Disponible a partir del nivel 2'` (unchanged from 12.4-06).
  - `/Requiere conjuros arcanos de nivel 5/` — actual label is `'Requiere lanzar conjuros arcanos de nivel 5'` (word "lanzar" inserted per plan Step D + D-08 copy spec).
- Pre-plan: 3 runtime failures on these assertions.
- Post-plan: 3 runtime failures (same — copy strings unchanged at the L1 + arcane label).
- Scope boundary: my plan explicitly fixed the arcane-level-label wording to "Requiere lanzar conjuros arcanos de nivel N" per D-08. The L1 label is outside my task (predates 12.8). Tests were wrong before my task.

**5. `tests/phase-08/ruleset-version.spec.ts` fails on BUILD_ENCODING_VERSION literal**
- Fully unrelated to prestige gate. Pre-existing baseline failure per 12.8-01 SUMMARY. Untouched by this plan.

**6. `apps/planner/src/features/feats/selectors.ts(1049,74)+(1065,76)` — 2 pre-existing TS2345 errors on CanonicalId branded-type narrowing**
- Fully unrelated to prestige gate. Untouched by this plan.

**Total deviations:** 2 auto-fixed (Rule 1 bugs) + 4 pre-existing drift items documented as deferred.

## TDD Gate Compliance

Task 1 is marked `tdd="true"` in the plan, but its `<action>` block implements schema + evaluator + copy without a separate RED commit — the RED lives in Task 2 (schema spec). I committed Task 1 as `feat(12.8-02): extend prestige-gate schema + evaluator for F5` and Task 2 as `test(12.8-02): enrich shadowdancer override + 36 Vitest cases for F5`. This is the inverse of the canonical GREEN-then-RED pattern but is correct per the plan's action spec (Task 1's verification step says "typecheck exits 0", not "run RED"; Task 2's action authors the specs). Grep gate `test(12.8-02)` commit exists at `4371827`; `feat(12.8-02)` exists at `6f79455`. The plan's `type: execute` + per-task `tdd="true"` combination is interpreted as "task landed with regression coverage", not as plan-level TDD cycle.

## Fail-closed branch byte-identity proof (D-10)

```bash
git diff HEAD~2 HEAD -- packages/rules-engine/src/progression/prestige-gate.ts | grep -E "^[-+].*Requisitos en revisión|^[-+].*kind: 'unvetted'"
# (empty output — zero added/removed lines matching the fail-closed sentinel)
```

The three existing `'Requisitos en revisión'` references (2 docstrings at lines 36 + 98, 1 live emission at line 227) all survive byte-identical.

## Invariant grep results

| File | Pattern | Expected | Actual | Status |
|------|---------|----------|--------|--------|
| packages/rules-engine/src/progression/prestige-gate.ts | `minArcaneSpellLevel|minSpellLevel|excludedClassIds|requiredAnyFeatGroups|requiredAnyRaceIds|requiredAnyClassLevels` | ≥ 12 | 26 | PASS (schema + evaluator + spec references) |
| packages/rules-engine/src/progression/prestige-gate.ts | `'arcane-spell-level'|'spell-level'|'excluded-class'|'any-feat-group'|'any-race'|'any-class-level'` | ≥ 6 | 12 | PASS (union + evaluator push sites) |
| packages/rules-engine/src/progression/prestige-gate.ts | `Requisitos en revisión` | 1 live + N docstring | 3 (1 live + 2 docstring) | PASS (live sentinel emission unchanged; plan regex was miscalibrated — counted docstrings too) |
| apps/planner/src/lib/copy/es.ts | `arcaneSpellLevel|spellLevel|excludedClass|anyFeatGroup|anyRace|anyClassLevel` | ≥ 9 | 9 | PASS |
| apps/planner/src/features/level-progression/prestige-prereq-data.ts | `skill:equilibrio|feat:dodge|feat:mobility` | ≥ 3 | 5 | PASS (shadowdancer + weaponmaster share dodge+mobility) |
| apps/planner/src/features/level-progression/prestige-prereq-data.ts | `class:shadowdancer` | 1 | 1 | PASS |
| vitest.config.ts | `tests/phase-12.8` | exactly 1 | 1 | PASS |
| vitest.config.ts | `configDefaults` | 2 | 2 | PASS (12.8-01's spread preserved) |
| packages/rules-engine/src/progression/prestige-gate.ts | `from '@planner\|from 'react'\|from '@tanstack` | 0 | 0 | PASS (framework-agnostic invariant) |

## Cross-phase closure

- **F5 (UAT-2026-04-23):** closed at code level.
  - `pale-master` at L2 BAB=2 arcane=0: emits `arcane-spell-level` blocker (was OPEN).
  - `caballero-arcano` at L2 BAB=2 arcane=0: emits `arcane-spell-level` blocker (was OPEN).
  - `shadowdancer` at L2 BAB=2: emits `bab` + `skill-rank(Moverse sigilosamente)` + `skill-rank(Equilibrio)` + `feat(Esquiva)` + `feat(Movilidad)` (was BAB-only).
- **18 existing-gated rows preserve labels:** regression snapshot `it.each(Object.keys(PRESTIGE_PREREQ_OVERRIDES))` locks `blockers.length > 0` + `reachable === false` for all 21 override entries at L2 Elfo Guerrero BAB=2.

## Next Phase Readiness

- **12.8-03 (F3+F4 Dotes multi-slot UX):** unblocked. Rules-engine gate shape is stable; no further evaluator work needed for F3+F4 which live in the feat-sheet surface.
- **12.8-04 (F6 Semielfo dedupe):** unblocked (parallel with this plan in Wave 1).
- **Pre-existing 4 TS errors + 6 failing Vitest cases:** out of scope per Rule 3 (SCOPE BOUNDARY) + 12.8 SPEC.md boundary lock. Candidates for a small follow-up quick task post Wave 2.

## Self-Check: PASSED

**Files created (found):**
- `tests/phase-12.8/prestige-gate-schema.spec.ts` — FOUND (verified by `ls`).
- `tests/phase-12.8/prestige-gate-overrides.spec.ts` — FOUND.

**Files modified (found):**
- `packages/rules-engine/src/progression/prestige-gate.ts` — FOUND.
- `apps/planner/src/features/level-progression/prestige-prereq-data.ts` — FOUND.
- `apps/planner/src/lib/copy/es.ts` — FOUND.
- `apps/planner/src/features/level-progression/prestige-gate-build.ts` — FOUND.
- `apps/planner/src/features/level-progression/class-picker.tsx` — FOUND.
- `vitest.config.ts` — FOUND.

**Commits (found):**
- `6f79455` — FOUND (Task 1 feat).
- `4371827` — FOUND (Task 2 test).

---
*Phase: 12.8-uat-04-23-residuals*
*Completed: 2026-04-24*
