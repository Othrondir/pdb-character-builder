# Phase 12.8: UAT-2026-04-23 Residuals — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 12.8-uat-04-23-residuals
**Mode:** `[auto]` — invoked under running user delegation ("hazlo tu todo"). No interactive AskUserQuestion rounds. Claude auto-selected the recommended option for each gray area and recorded the tradeoffs considered below.
**Areas discussed:** Habilidades scroll-snap (F1+F2), Dotes multi-slot UX (F3+F4), Prestige gate tightening (F5), Race roster dedupe (F6), Test tooling split, Plan-wave shape.

---

## Habilidades scroll-snap (F1 + F2)

### Q1: Fix scope — CSS removal vs snap-anchor retune?

| Option | Description | Selected |
|--------|-------------|----------|
| Remove `scroll-snap-type: y proximity` + row-level `scroll-snap-align: start` | Surgical root-cause fix; list scans continuously without per-row snap. | ✓ |
| Retune snap to `scroll-snap-type: y mandatory` with a different anchor (e.g., section headings) | Keeps snap semantics on the Habilidades list. | |
| Keep snap + wrap programmatic scroll with `scroll-snap-type: none` toggle | Preserves snap but adds per-mount complexity. | |

**User's choice:** (auto) Option 1.
**Notes:** Habilidades rows have no per-row dominance; users scan continuously. Retuning re-opens jitter class of regression. Toggle is a band-aid.

### Q2: scrollerRef retarget — BuildProgressionBoard ref pass-down vs DOM querySelector?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `useRef` in skill-sheet.tsx, retarget via `document.querySelector('.skill-board .selection-screen__content')` | Minimal diff; no new component contract. | ✓ |
| Lift the ref to `<BuildProgressionBoard>` and pass it down as a prop | Clean React idiom but cross-component contract change. | |
| Use `findDOMNode` on a stable React-rendered wrapper | Deprecated React pattern; avoid. | |

**User's choice:** (auto) Option 1.
**Notes:** Option 2 is cleaner long-term but 12.8-01 scope is a bug fix, not a refactor. Planner can escalate if an existing ref plumbing is already present.

### Q3: Regression test runtime — Playwright vs Vitest?

| Option | Description | Selected |
|--------|-------------|----------|
| Playwright e2e (real Chromium + layout engine) | Observes actual scroll-snap behavior. | ✓ |
| Vitest + jsdom | jsdom has no layout engine — missed 12.7-03 R3 regression entirely. | |
| Vitest + happy-dom | Partial layout support; unreliable for snap semantics. | |

**User's choice:** (auto) Option 1.
**Notes:** SPEC.md constraint locks Playwright for F1/F2. This question confirms there is no cheaper alternative that works.

---

## Dotes multi-slot UX (F3 + F4)

### Q1: F3 resolution approach — auto-scroll, collapse-on-complete, or viewport-nudge banner?

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-scroll first general row into viewport (`scrollIntoView({ block: 'nearest', behavior: 'smooth' })`) | Keeps both sections visible; composes with F4 per-chip deselect. | ✓ |
| Collapse-completed-section — hide class section once filled | Strong signal but hides class pick from review; conflicts with F4. | |
| Viewport-nudge banner ("Ahora elige la dote general") | Explicit copy but duplicates existing pill + current-section highlight. | |
| Fixed-pane split — show both sections in a vertical two-pane layout | Layout overhaul; out of proportion to the bug. | |

**User's choice:** (auto) Option 1.
**Notes:** Collapse-on-complete for the full panel is the shipped Phase 12.4-07 D-04 behavior; F3 is about cross-section visibility WITHIN the not-yet-collapsed sheet. Auto-scroll keeps class chip reviewable and makes F4 deselect ergonomic.

### Q2: F4 deselect control shape — per-chip × button, whole-chip toggle, or no-collapse-on-same-substep?

| Option | Description | Selected |
|--------|-------------|----------|
| Per-chip × button (glyph rendered inside `.feat-summary-card__item`) | Clear affordance; preserves "Modificar selección" button. | ✓ |
| Whole-chip toggle — tap any part of chip to clear | Lower affordance, risk of accidental tap. | |
| Don't collapse while user is on Dotes sub-step | Reverses Phase 12.4-07 D-04 design decision. | |
| Long-press to clear | Mobile-first gesture; inconsistent with click-primary UI. | |

**User's choice:** (auto) Option 1.
**Notes:** × button pattern matches `<NwnButton>` + existing close glyph conventions. Accessibility label `Quitar selección` (confirmed during planning).

### Q3: Chip entry schema — string[] vs structured FeatSummaryChosenEntry?

| Option | Description | Selected |
|--------|-------------|----------|
| Extend `FeatSummaryChosenEntry` with `slotKind` + `slotIndex` fields | Chip knows which clear action to invoke. | ✓ |
| Pass a second parallel array `slotKinds[]` alongside labels | Brittle; two arrays must stay in sync. | |
| Infer slot-kind from label prefix | Magic-string coupling; rejected on sight. | |

**User's choice:** (auto) Option 1.
**Notes:** Existing `FeatSummaryChosenEntry` is already a structured object (featId + label); adding two fields is trivial.

---

## Prestige gate tightening (F5)

### Q1: Root cause framing — partial enrichment (UAT hypothesis) vs gate-schema under-specification (code-scout observation)?

| Option | Description | Selected |
|--------|-------------|----------|
| Gate schema missing fields — `ClassPrereqInput.decodedPrereqs` only declares 4 of 10 override fields | Explains pale-master/caballero-arcano OPEN; matches actual code state. | ✓ |
| Enrichment data is partial — overrides lack feat/skill-rank rows | UAT-FINDINGS hypothesis; refuted by reading the override file (pale-master ships full data). | |
| Runtime routing bug — gate not called for these classes | Ruled out; all prestige rows flow through `reachableAtLevelN`. | |

**User's choice:** (auto) Option 1.
**Notes:** Code scout in discuss-phase revealed the schema mismatch. UAT author didn't inspect `prestige-gate.ts`; the hypothesis was plausible but wrong. CONTEXT.md D-07 captures the correction — planner must not re-derive.

### Q2: Fix approach — extend gate schema vs trim override data?

| Option | Description | Selected |
|--------|-------------|----------|
| Extend gate schema + evaluator to handle `minArcaneSpellLevel`, `minSpellLevel`, `excludedClassIds`, `requiredAnyFeatGroups`, `requiredAnyRaceIds`, `requiredAnyClassLevels` | Preserves richer override data; unblocks future enrichment. | ✓ |
| Trim overrides to only use the 4 existing schema fields | Loses real NWN1 EE prereq data; band-aid. | |
| Build a second evaluator for the extended fields and compose | Double-indirection with no clear win. | |

**User's choice:** (auto) Option 1.
**Notes:** Override data encodes canon prereqs. Schema extension is additive, backward-compatible, and fixes multiple classes simultaneously (not just the 3 UAT-called-out ones).

### Q3: Shadowdancer enrichment — what prereqs to add?

| Option | Description | Selected |
|--------|-------------|----------|
| Move Silent 8, Tumble 5 (verify skill ID), Dodge, Mobility feats | Matches NWN1 EE canon; pattern follows quick-260422-h9k. | ✓ |
| Only BAB + Move Silent 8 (minimum viable tightening) | Undercovers; leaves gate too permissive. | |
| Defer shadowdancer to extractor pass | Out of scope per SPEC.md; 12.8 owns the override. | |

**User's choice:** (auto) Option 1.
**Notes:** Skill ID for Tumble (Interpretar? Equilibrio?) not self-evident — researcher confirms against Puerta snapshot before locking in plan-phase.

---

## Race roster dedupe (F6)

### Q1: Fix approach — remove duplicate at source, extend dedupe key, or whitelist?

| Option | Description | Selected |
|--------|-------------|----------|
| Delete `race:halfelf2` from `compiled-races.ts` | Single-line fix; duplicate adds no data. | ✓ |
| Extend `dedupeByCanonicalId` to include label in key | Fragile — label collisions for localized names. | |
| Add `race:halfelf2` to a skip-list in foundation-fixture | Side-door workaround; obscures the artifact. | |

**User's choice:** (auto) Option 1.
**Notes:** D-12 gates the delete on a grep sweep — if `race:halfelf2` is referenced elsewhere, plan adjusts.

---

## Test tooling split

### Q: Which findings to cover in Playwright vs Vitest?

| Finding | Playwright | Vitest | Selected |
|---------|------------|--------|----------|
| F1 (scroll on entry) | ✓ | — | Playwright |
| F2 (jitter on +/-) | ✓ | — | Playwright |
| F3 (active-section surfacing) | ✓ | — | Playwright |
| F4 (chip deselect) | smoke test | ✓ | Vitest primary + Playwright smoke |
| F5 (prestige gate) | — | ✓ | Vitest |
| F6 (Semielfo dedupe) | — | ✓ | Vitest |

**User's choice:** (auto) As tabled.
**Notes:** Layout-dependent findings → Playwright (real engine). Pure-function / component-unit findings → Vitest (fast feedback).

---

## Plan-wave shape

### Q: How many plans, wave split?

| Option | Description | Selected |
|--------|-------------|----------|
| 3 plans / 2 waves — F1+F2 + F5 + F6 parallel, then F3+F4 | Matches file-touch boundaries; Wave 2 can reuse Wave 1's Playwright fixture. | ✓ |
| 6 plans / 1 wave — one plan per finding | Too granular; scroll-snap + chip-deselect share no files. | |
| 1 plan / 1 wave — bundle everything | Too large; parallel execution value lost. | |
| 4 plans / 2 waves — add F6 as quick task not plan | STATE.md resume hint suggests this; rejected because F6 has same test+review discipline as the others. | |

**User's choice:** (auto) Option 1.
**Notes:** D-14 table in CONTEXT.md locks the wave shape; `/gsd-plan-phase 12.8` materializes 3 plan files.

---

## Claude's Discretion

- Shadowdancer Tumble skill ID + Mobility feat canonical ID (D-09) — researcher verifies against Puerta snapshot.
- F3 auto-scroll hook shape (useLayoutEffect vs imperative call-site) — planner picks on code-style fit.
- Playwright fixture sharing topology — planner owns during plan-12.8-01.
- CSS cleanup scope beyond the two targeted lines (`scroll-snap-type` + `scroll-snap-align`) — only if tests fail.

## Deferred Ideas

- Broader prestige-class audit after D-08 schema extension lands.
- Skill-point carryover (12.7 F5) — data-blocked.
- Cross-surface scroll-snap audit (feat-board, class-picker).
- `<FeatSummaryCard>` transition polish — respects Phase 12.4-07 D-04 "instant swap" rule.
- Extractor-side normalization of `race:halfelfN` artifacts — tracked in 12.1-02 deferred-items.md.
