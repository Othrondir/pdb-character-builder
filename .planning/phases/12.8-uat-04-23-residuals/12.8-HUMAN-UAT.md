---
status: resolved
phase: 12.8-uat-04-23-residuals
source: [12.8-VERIFICATION.md]
started: 2026-04-24T14:10:00Z
updated: 2026-04-24T15:15:00Z
fixture: "Elfo + Neutral puro + Atributos default + Guerrero L1 — clean localStorage + IndexedDB, reload localhost:5173"
executor: agent-driven UAT (MCP Chrome, tab 761727061)
---

## Current Test

Complete — 5 of 6 pass, 1 gap on F3.

## Tests

### 1. F1 Habilidades scroll reset — live browser
expected: "`.skill-board .selection-screen__content` scrollTop === 0 en primera entrada; `scroll-snap-type: none`."
result: pass
evidence: "scrollTop=0; snapType='none'; scroller clientHeight=744 / scrollHeight=1990 (real overflow owner)."

### 2. F2 Habilidades +/- sin jitter — live browser
expected: "Click + y - sobre la primera fila de habilidad: fila no se desplaza; scrollTop se mantiene."
result: pass
evidence: "deltaScrollPlus=0, deltaRectPlus=0, deltaScrollMinus=0, deltaRectMinus=0 (all 0px)."

### 3. F3 Dotes auto-scroll — live browser
expected: "Click class-bonus legal (Carrera). Primera fila de `[data-slot-section=\"general\"]` entra en viewport (smooth)."
result: pass (post-fix 14ee309)
evidence_initial: "Primera pasada falló: RAF + `scrollIntoView(smooth)` no-op en Chrome. Scroll 0 durante 2s polling, first general row rect.top=20584 (fuera de viewport)."
fix: "Commit `14ee309` dropped the RAF wrapper — useEffect already runs post-commit so the `feat-sheet__group--current` swap is live in the DOM; the frame defer was redundant and broke the nested scrollIntoView."
evidence_post_fix: "Re-run con fixture limpio: scrollBefore=0 → samples[0..14]=19860 (scroll aplicado en primer 100ms), genFirstAfter_top=724 (∈ viewport vh=856), counter 0/2 → 1/2. Playwright `feat-auto-scroll.e2e.spec.ts` 4/4 pass. Vitest phase-12.8 46/46 pass. Vitest full 2133 pass / 6 pre-existing fail / 0 nuevas regresiones."

### 4. F4 Chip × deselect — live browser
expected: "Ambos slots llenos → `<FeatSummaryCard>` con `×` por chip; click class-bonus × desmonta card, vacía slot class-bonus, preserva general."
result: pass
evidence: "2 chips. Chip 1: aria-label='Quitar selección: Carrera', data-testid='deselect-chip-class-bonus-0', class='feat-summary-card__deselect'. Chip 2: aria-label='Quitar selección: Abstención de materiales', testid='deselect-chip-general-0'. `Modificar selección` presente. Click class-bonus × → summaryStillPresent=false, counter=1/2, chosenCb=0, chosenGen=1."

### 5. F5 Prestige prereq labels en L2 — live browser
expected: "En L2 con Guerrero L1 (BAB=1 al elegir): Pale Master/Caballero arcano muestran blocker de nivel arcano; Shadowdancer muestra BAB blocker (y lista completa de blockers en evaluator)."
result: pass
evidence: "Pale Master: `em.class-picker__reason`='Requiere lanzar conjuros arcanos de nivel 3' (antes OPEN). Caballero arcano: 'Requiere lanzar conjuros arcanos de nivel 5' (antes OPEN). Shadowdancer: 'Requiere BAB ≥ 2'. UI renderiza `gateResult.blockers[0]?.label` por diseño (`class-picker.tsx:191`); el evaluator devuelve la lista completa según Vitest 24/24 (`tests/phase-12.8/prestige-gate-overrides.spec.ts`). 39 filas prestige totales, todas renderizan sin regresión."
note: "La must_have 'shadowdancer reports BAB + skill-rank + feat blockers' describe la LISTA del evaluator, no la UI. El DOM compacto sólo muestra el blocker[0]. Sin cambio visible vs 12.7 — el fix real es que pale-master y caballero-arcano ya no aparecen OPEN."

### 6. F6 Race picker Semielfo singleton — live browser
expected: "Exactamente 1 fila Semielfo, total ≤ 44."
result: pass
evidence: "`ul.option-list` count=44 (antes 45). `Semielfo` text leaf count=1."

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

### F3-AUTO-SCROLL-RAF — resolved
severity: major
status: resolved (commit 14ee309)
finding: "F3 auto-scroll no disparaba en Chrome real. `feat-sheet.tsx:283-300` envolvía `scrollIntoView({block:'nearest', behavior:'smooth'})` en `requestAnimationFrame`; RAF + scrollIntoView no scrollea el `aside.feat-sheet` scrollable ancestor."
fix: "Commit 14ee309 — dropped RAF wrapper. useEffect already runs post-commit, DOM swap already applied when effect runs, frame defer was redundant. Net diff: 12 ins / 11 del (comment rewrite + body unwrap)."
verification: "Live Chrome MCP re-run 0→19860 scroll @ 100ms, general first row in viewport. Playwright 4/4 green. Vitest phase-12.8 46/46 + full suite 2133 pass / 6 pre-existing fail / 0 new regressions."

