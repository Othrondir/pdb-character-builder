---
status: partial
phase: 12.8-uat-04-23-residuals
source: [12.8-VERIFICATION.md]
started: 2026-04-24T14:10:00Z
updated: 2026-04-24T14:10:00Z
fixture: "Elfo + Neutral puro + Atributos default (FUE 8 DES 10 CON 6 INT 8 SAB 8 CAR 8) + Guerrero L1 — clean localStorage + IndexedDB, reload localhost:5173"
---

## Current Test

[awaiting human testing]

## Tests

### 1. F1 Habilidades scroll reset — live browser
expected: "Abrir sub-paso Habilidades. `document.querySelector('.skill-board .selection-screen__content').scrollTop === 0` en primera entrada y al volver de L1→L2. No hay jitter/snap al desplazar."
result: [pending]

### 2. F2 Habilidades +/- sin jitter — live browser
expected: "Mismo fixture. Click + y - sobre la primera fila de habilidad. La posición visual de la fila no se desplaza; scrollTop se mantiene."
result: [pending]

### 3. F3 Dotes auto-scroll — live browser
expected: "Sub-paso Dotes. Click en la primera fila class-bonus (ej. `Carrera`). La primera fila de `[data-slot-section=\"general\"]` entra al viewport en ~300ms (smooth). Segundo click sobre fila general rellena el slot general (contador 2/2); el chip class-bonus se mantiene; `<FeatSummaryCard>` se renderiza."
result: [pending]

### 4. F4 Chip × deselect — live browser
expected: "Con `<FeatSummaryCard>` montado (ambos slots llenos), cada chip muestra botón `×` con tooltip español `Quitar selección: {nombre del dote}`. Click en `×` del class-bonus: la tarjeta se desmonta y la hoja re-renderiza con slot class-bonus vacío y general conservado."
result: [pending]

### 5. F5 Prestige prereq labels en L2 — live browser
expected: "Avanzar a L2 con mismo fixture (BAB=2). En class picker: `Pale Master` muestra blocker de nivel de conjuro arcano (NO `OPEN`). `Caballero arcano` muestra blocker de nivel de conjuro arcano (NO `OPEN`). `Shadowdancer` muestra BAB + Moverse sigilosamente + Equilibrio + Esquiva + Movilidad (NO sólo BAB). Las 18+ filas de prestigio restantes conservan sus etiquetas (sin regresión)."
result: [pending]

### 6. F6 Race picker Semielfo singleton — live browser
expected: "Carga fresca. Abrir selector de razas. Exactamente una fila `Semielfo`. Conteo total ≤ 44 filas."
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps
