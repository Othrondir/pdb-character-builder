---
phase: 12.8-uat-04-23-residuals
verified: 2026-04-24T14:05:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "F1 Habilidades scroll reset — live browser"
    expected: "Elfo + Neutral puro + Guerrero L1 fixture; open Habilidades sub-step. `document.querySelector('.skill-board .selection-screen__content').scrollTop === 0` on first entry AND on L1→L2 re-entry. No jitter/snap feel while scrolling."
    why_human: "Scroll-snap + layout effects are viewport/engine dependent. Playwright regression exists but user UAT request explicitly asked for live re-sweep against the 2026-04-23 fixture."
  - test: "F2 Habilidades +/- no jitter — live browser"
    expected: "Same fixture. Click + then - on the first skill row. Row visual position does not shift. Scroll position stays put."
    why_human: "Jitter is a perceptual defect; UAT observer confirms absence in real layout engine at user's typical viewport."
  - test: "F3 Dotes auto-scroll — live browser"
    expected: "Same fixture. In Dotes sub-step click first class-bonus row (e.g., `Carrera`). The first row of `[data-slot-section=\"general\"]` visibly enters viewport within ~300ms (smooth scroll). Second click on a general row fills the general slot (counter 2/2), the class-bonus chip remains intact, and `<FeatSummaryCard>` renders."
    why_human: "F3 is a viewport/UX behavior — observer confirms smoothness + correct landing target."
  - test: "F4 Chip × deselect — live browser"
    expected: "With `<FeatSummaryCard>` rendered (both slots filled), each chip shows a `×` button with Spanish tooltip `Quitar selección: {feat name}`. Click the class-bonus `×`: card unmounts, sheet re-renders with class slot empty and general slot preserved."
    why_human: "Screen-reader aria label + NWN1-inspired visual feel need human confirmation."
  - test: "F5 Prestige prereq labels at L2 — live browser"
    expected: "Advance to L2 with same fixture (BAB=2). In class picker: `Pale Master` shows an arcane-spell-level blocker (NOT `OPEN`). `Caballero arcano` shows an arcane-spell-level blocker (NOT `OPEN`). `Shadowdancer` shows BAB + Moverse sigilosamente + Equilibrio + Esquiva + Movilidad blockers (NOT BAB-only). 18+ other gated prestige rows still render their existing labels (no regression)."
    why_human: "Spanish copy rendering + positional label correctness need visual sweep; Vitest locks shape but not final DOM composition."
  - test: "F6 Race picker Semielfo singleton — live browser"
    expected: "On fresh app load open the race picker. Exactly one `Semielfo` row. Total row count ≤ 44."
    why_human: "Simple visual confirmation; Vitest snapshot already locks projection but UAT mandate covers live picker rendering."
---

# Phase 12.8: UAT-2026-04-23 Residuals Verification Report

**Phase Goal:** Close 6 UAT findings F1..F6 from `.planning/UAT-FINDINGS-2026-04-23.md` against master 2026-04-23 (post Phase 12.7); cross-phase close 12.7 T3 gap via D-15 marker.
**Verified:** 2026-04-24T14:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (F1) | Habilidades scroll container at `scrollTop === 0` on first entry + level re-entry | ✓ VERIFIED | `app.css` zero hits for `scroll-snap-type|scroll-snap-align`; `skill-sheet.tsx:150-154` uses `useLayoutEffect` + `document.querySelector('.skill-board .selection-screen__content')`; Playwright spec `skill-scroll-snap.e2e.spec.ts` exists with 5 cases |
| 2 (F2) | +/- click preserves row rect + scrollTop within 1px | ✓ VERIFIED | Snap CSS deleted (root cause); Playwright spec asserts rect stability; computed-style assertions for `scrollSnapType === 'none'` |
| 3 (F3) | First `[data-slot-section="general"]` row enters viewport after class-slot fill | ✓ VERIFIED | `feat-sheet.tsx:280-301` implements `useRef` null→non-null transition + `requestAnimationFrame` + `scrollIntoView({block:'nearest', behavior:'smooth'})`; Playwright `feat-auto-scroll.e2e.spec.ts` asserts boundingBox containment |
| 4 (F4) | FeatSummaryCard chips expose × deselect with Spanish aria + wired `clearClassFeat`/`clearGeneralFeat` | ✓ VERIFIED | `feat-summary-card.tsx:28-51` has `onDeselect` prop + per-chip button + `deselect-chip-{kind}-{idx}` testid; `feat-board.tsx:48-49,64,105-115` threads `onDeselect` with reused `activeLevel`; `deselectChipAriaLabel: 'Quitar selección'` in `es.ts`; Vitest RTL 5/5 green |
| 5 (F5) | Prestige gate schema extended; pale-master/caballero-arcano emit arcane-spell-level; shadowdancer emits BAB+skill+feat; 18+ existing rows preserved | ✓ VERIFIED | `prestige-gate.ts` grep 38 matches across the 6 new field names + kinds; `prestige-prereq-data.ts:228-237` shadowdancer has `skill:moversesigilosamente(8)`, `skill:equilibrio(5)`, `feat:dodge`, `feat:mobility`; Vitest 36/36 green (schema 12 + overrides 24) |
| 6 (F6) | Race picker renders exactly one `Semielfo`, ≤ 44 rows | ✓ VERIFIED | `compiled-races.ts` zero hits for `race:halfelf2`; exactly one `"label": "Semielfo"` at line 85 under `"id": "race:halfelf"`; propagation applied to `puerta-point-buy.json` + `.md`; Vitest 5/5 green |
| 7 (D-15) | 12.7 T3 gap marked resolved in `12.7-UAT.md` | ✓ VERIFIED | `12.7-UAT.md` grep: 1 hit `Closed by Phase 12.8`, 1 hit `T3 gap status: resolved` |

**Score:** 7/7 truths verified (6 UAT findings + cross-phase closure)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/planner/src/styles/app.css` | No snap rules under `.skill-board` | ✓ VERIFIED | 0 hits for `scroll-snap-type`/`scroll-snap-align` |
| `apps/planner/src/features/skills/skill-sheet.tsx` | `useLayoutEffect` targets `.skill-board .selection-screen__content` | ✓ VERIFIED | Line 150-154; `useRef`/`scrollerRef` removed |
| `apps/planner/src/features/feats/feat-sheet.tsx` | `scrollIntoView` on null→non-null class slot | ✓ VERIFIED | Lines 280-301 with rAF defer + `[data-slot-section="general"]` query |
| `apps/planner/src/features/feats/feat-summary-card.tsx` | × button + aria + onDeselect | ✓ VERIFIED | Full rewrite; preserves `modifySelectionLabel` button |
| `apps/planner/src/features/feats/feat-board.tsx` | `clearClassFeat`/`clearGeneralFeat` wired via `onDeselect` using existing `activeLevel` | ✓ VERIFIED | Lines 48-49 subscriptions; line 64 activeLevel; lines 105-115 dispatch |
| `apps/planner/src/features/feats/selectors.ts` | `FeatSummaryChosenEntry` with `slotKind`+`slotIndex`; per-slot projection | ✓ VERIFIED | 9 grep hits for `slotKind` |
| `apps/planner/src/lib/copy/es.ts` | `deselectChipAriaLabel: 'Quitar selección'` + 9 prestige template keys | ✓ VERIFIED | 1 hit for `Quitar selección`; template keys present |
| `packages/rules-engine/src/progression/prestige-gate.ts` | 6 new decodedPrereqs fields + 6 BlockerKinds + evaluator branches + fail-closed unchanged | ✓ VERIFIED | 38 matches for new fields/kinds; `Requisitos en revisión` unchanged (3 hits — 2 docstrings + 1 live emit) |
| `apps/planner/src/features/level-progression/prestige-prereq-data.ts` | Shadowdancer enriched | ✓ VERIFIED | Lines 228-237 |
| `apps/planner/src/data/compiled-races.ts` | `race:halfelf2` deleted | ✓ VERIFIED | 0 hits; primary `race:halfelf` preserved at line 84 |
| `tests/phase-12.8/skill-scroll-snap.e2e.spec.ts` | Playwright F1+F2 spec | ✓ VERIFIED | File exists |
| `tests/phase-12.8/feat-auto-scroll.e2e.spec.ts` | Playwright F3+F4 spec | ✓ VERIFIED | File exists |
| `tests/phase-12.8/feat-summary-card-deselect.spec.tsx` | Vitest RTL F4 unit | ✓ VERIFIED | 5/5 green |
| `tests/phase-12.8/prestige-gate-schema.spec.ts` | Vitest F5 new branches | ✓ VERIFIED | 12/12 green |
| `tests/phase-12.8/prestige-gate-overrides.spec.ts` | Vitest F5 regression snapshot | ✓ VERIFIED | 24/24 green |
| `tests/phase-12.8/race-roster-dedupe.spec.ts` | Vitest F6 | ✓ VERIFIED | 5/5 green |
| `playwright.config.ts` | Repo-root harness | ✓ VERIFIED | File exists |
| `.planning/phases/12.7-uat-04-20-post-12.6-residuals/12.7-UAT.md` | D-15 closure marker | ✓ VERIFIED | Lines 89+102 contain both phrases |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `skill-sheet.tsx` `useLayoutEffect` | `.skill-board .selection-screen__content` | `document.querySelector` | ✓ WIRED |
| `feat-sheet.tsx` scroll effect | `[data-slot-section="general"]` first row | `document.querySelector` + rAF + `scrollIntoView` | ✓ WIRED |
| `feat-summary-card.tsx` × button | `feat-board.tsx onDeselect` | `onDeselect` prop | ✓ WIRED |
| `feat-board.tsx onDeselect` | `useFeatStore.clearClassFeat`/`clearGeneralFeat` | narrow subscriptions + `activeLevel` closure | ✓ WIRED |
| `selectors.ts chosenFeats` | `FeatSummaryCard` | `FeatSummaryChosenEntry.slotKind/slotIndex` projection | ✓ WIRED |
| `prestige-prereq-data.ts` shadowdancer override | `prestige-gate.ts` evaluator | `DecodedPrereqs` type + 6 new branches | ✓ WIRED |
| `prestige-gate-build.ts` | `prestige-gate.ts` evaluator | `highestArcaneSpellLevel` (renamed from `...ClassLevel`, semantic fix) | ✓ WIRED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SKIL-01 | 12.8-01 | Rangos de habilidad por nivel | ✓ SATISFIED | Regression on shipped req; CSS snap removed + scroll retarget; Playwright guards |
| CLAS-02 | 12.8-02 | Prereq visibility for prestige classes | ✓ SATISFIED | 6 new decodedPrereqs + 36 Vitest cases; REQUIREMENTS.md:129 explicitly cites 12.8-02 closure |
| FEAT-02 | 12.8-03 | Ver prereqs incumplidos + razones exactas | ✓ SATISFIED | Per-chip × deselect + auto-scroll surface reasons inline; RTL+Playwright green |
| CHAR-01 | 12.8-04 | Elegir raza soportada por el servidor | ✓ SATISFIED | Semielfo singleton; Vitest 5/5 |

Note: REQUIREMENTS.md:136 (FEAT-02) and :119 (CHAR-01) still reference older phases — 12.8 closed regressions on shipped requirements. Traceability update for 12.8-03/04 closure could be appended in a housekeeping pass (not a gap).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `feat-sheet.tsx` | 288-299 | Unscoped `document.querySelector` for `[data-slot-section]` | ℹ Info (REVIEW WR-01) | Low — attribute currently unique; future dotes surfaces could collide |
| `skill-sheet.tsx` | 150-154 | Unscoped `document.querySelector` for `.skill-board .selection-screen__content` | ℹ Info (REVIEW WR-02) | Low — same class of concern |
| `prestige-gate-build.ts` | 188, 346-361 | Dead code `computeHighestClassLevel` / `ARCANE_SPELLCASTER_IDS` | ℹ Info (REVIEW IN-01) | None at runtime; housekeeping candidate |
| `es.ts` | 182 | `anyFeatGroupSingle` duplicates `feat` template | ℹ Info (REVIEW IN-03) | None; cosmetic |
| `app.css` | 465-474 | Orphan `padding-bottom` on `.skill-board .selection-screen__content` post-snap removal | ℹ Info (REVIEW IN-04) | None; harmless |

No blocker anti-patterns. All findings are polish items documented in 12.8-REVIEW.md.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase-12.8 Vitest suite | `pnpm run test -- tests/phase-12.8/` | 4 files / 46 tests pass | ✓ PASS |
| Full Vitest baseline (regression) | same run | 105/108 files pass; 2133/2142 tests pass; 6 failures all pre-existing (phase-08 `ruleset-version`, phase-12.4 `prestige-gate.fixture` `'feat-or'` literal + L1 copy, `class-picker-prestige-reachability` arcane copy) | ✓ PASS (no new regressions) |
| Playwright harness installed | `ls playwright.config.ts` + e2e spec files | Both present | ✓ PASS |
| Playwright e2e run | `pnpm run test:e2e:phase-12.8` | Not executed during verification (requires dev server) — routed to human UAT | ? SKIP |

### Gaps Summary

No blocking gaps. All 6 UAT findings (F1..F6) have code-level closure plus automated regression coverage (46 Vitest + 9 Playwright cases). D-15 cross-phase closure marker correctly appended to 12.7-UAT.md. Pre-existing Vitest failures (6 tests across phase-08 / phase-12.4) are fully documented in 12.8-01 + 12.8-02 summaries as out-of-scope; 12.8-02 closed 18 of the 23 pre-existing TS errors previously tracked.

### Human Verification Required

See frontmatter `human_verification:` list. The phase's own SPEC + the original UAT request mandates a live re-sweep against the Elfo + Neutral puro + Guerrero L1 fixture at `localhost:5173` to confirm F1..F6 in a real browser (Playwright automates but user-driven UAT is the acceptance contract). The Playwright e2e suite exists and should be executed against the running dev server (`bfl04rdu4` per STATE.md) or dispatched via agent-driven Chrome MCP before phase close.

---

*Verified: 2026-04-24T14:05:00Z*
*Verifier: Claude (gsd-verifier)*
