---
phase: 12.6-uat-04-20-residuals
plan: 02
subsystem: rules-engine + character-foundation
tags: [attr-01, a1a, snapshot-loader, fail-closed, zod, selector, attributes-board]
requires:
  - phase-12.6-jsdom-glob
  - wave-0-test-stubs
  - puerta-point-buy-empty-snapshot
  - shellCopyEs-attributes-block
provides:
  - canonicalRaceIdSchema
  - puerta-point-buy-snapshot-loader
  - ability-budget-null-branch
  - selectAbilityBudgetRulesForRace
  - attributes-board-fail-closed-callout
  - attributes-board-disabled-increment-decrement
  - attributes-board-hidden-budget-dl-when-blocked
  - foundation-fixture-attributeRules-test-only
affects:
  - packages/rules-engine/src/contracts/canonical-id.ts
  - packages/rules-engine/src/foundation/ability-budget.ts
  - packages/rules-engine/src/foundation/index.ts
  - packages/rules-engine/src/foundation/point-buy-snapshot.ts
  - apps/planner/src/features/character-foundation/selectors.ts
  - apps/planner/src/features/character-foundation/attributes-board.tsx
  - apps/planner/src/features/character-foundation/foundation-fixture.ts
  - tests/phase-12.6/point-buy-snapshot-coverage.spec.ts
  - tests/phase-12.6/ability-budget-per-race.spec.ts
  - tests/phase-12.6/attributes-board-fail-closed.spec.tsx
  - tests/phase-03/summary-status.spec.tsx
  - tests/phase-03/attribute-budget.spec.tsx
  - tests/phase-10/attributes-advance.spec.tsx
tech-stack:
  added: []
  patterns:
    - module-load-Zod-parse-for-fail-closed-bundle-init
    - null-branch-rule-emission-via-resolveValidationOutcome
    - planner-selector-as-rules-engine-boundary
    - isBlockedForMissingCurve-predicate-OR-into-button-disabled
    - beforeEach-mutate-PUERTA_POINT_BUY_SNAPSHOT-for-legacy-legal-path-tests
key-files:
  created:
    - packages/rules-engine/src/foundation/point-buy-snapshot.ts
  modified:
    - packages/rules-engine/src/contracts/canonical-id.ts
    - packages/rules-engine/src/foundation/ability-budget.ts
    - packages/rules-engine/src/foundation/index.ts
    - apps/planner/src/features/character-foundation/selectors.ts
    - apps/planner/src/features/character-foundation/attributes-board.tsx
    - apps/planner/src/features/character-foundation/foundation-fixture.ts
    - tests/phase-12.6/point-buy-snapshot-coverage.spec.ts
    - tests/phase-12.6/ability-budget-per-race.spec.ts
    - tests/phase-12.6/attributes-board-fail-closed.spec.tsx
    - tests/phase-03/summary-status.spec.tsx
    - tests/phase-03/attribute-budget.spec.tsx
    - tests/phase-10/attributes-advance.spec.tsx
decisions:
  - Removed `baseScore` from `AbilityBudgetRules` in ability-budget.ts since it was never consumed by any function in that module; keeping the interface tighter lets `PointBuyCurve` satisfy the structural contract so the per-race selector threads cleanly without widening/optional gymnastics
  - Seeded `PUERTA_POINT_BUY_SNAPSHOT['race:human']` (and 'race:elf' in one suite) via `beforeEach` mutation in three pre-12.6 legal-path specs (phase-03 summary-status + attribute-budget, phase-10 attributes-advance) so their behavioral contract survives the runtime path swap; afterEach cleans up so snapshot stays empty for neighbouring suites that rely on fail-closed
  - Local non-null binding inside `calculateAbilityBudgetSnapshot` after the early return so TypeScript narrows through the closure (the previous `input.attributeRules.*` dereferences hit TS18047 once the type widened)
metrics:
  duration_seconds: 888
  duration_human: "14m 48s"
  tasks_completed: 2
  files_created: 1
  files_modified: 12
  tests_added: 11
  tests_passing: 643
  tests_skipped: 7
  tests_todo: 4
  completed_at: "2026-04-20T13:05:44Z"
---

# Phase 12.6 Plan 02: ATTR-01 A1a (Fail-Closed Point-Buy Path) Summary

Per-race point-buy resolution end-to-end against an intentionally empty snapshot: module-load Zod parse of `puerta-point-buy.json`, `calculateAbilityBudgetSnapshot` null branch emitting `rule:point-buy-missing`, planner-side selector `selectAbilityBudgetRulesForRace`, and AttributesBoard fail-closed UI (Spanish callout, disabled +/-, hidden budget dl, disabled Aceptar). Because the snapshot is empty, the fail-closed branch is now exercised 45 times at runtime; Plan 06 lights up the nominal path by populating JSON — zero code changes needed downstream.

## Execution Overview

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | canonicalRaceIdSchema + point-buy-snapshot.ts loader + ability-budget.ts null branch + barrel | `8004e3e` | `canonical-id.ts`, `point-buy-snapshot.ts` (new), `ability-budget.ts`, `foundation/index.ts` |
| 2 | selectAbilityBudgetRulesForRace + AttributesBoard fail-closed branch + foundation-fixture comment + Wave 0 spec activation + pre-12.6 test seed | `c60158f` | `selectors.ts`, `attributes-board.tsx`, `foundation-fixture.ts`, 3 phase-12.6 specs, 3 pre-12.6 specs |

Total: 2 commits, 1 file created, 12 files modified.

## What This Delivers

### Engineering Contract (Task 1 — 8004e3e)

- **`canonicalRaceIdSchema`** exported from `packages/rules-engine/src/contracts/canonical-id.ts` — `z.string().regex(/^race:[A-Za-z0-9._-]+$/)`. Resolves RESEARCH Pitfall 4 (logical symbol referenced by CONTEXT D-02 had no prior export); the extractor-side race catalog inline regex at `packages/data-extractor/src/contracts/race-catalog.ts:13` is now mirrored in the shared rules-engine contracts.
- **`packages/rules-engine/src/foundation/point-buy-snapshot.ts`** — Zod schema (`pointBuyCurveSchema` with two refinements: `minimum ≤ maximum` + `costByScore keys in [minimum, maximum]`) plus `pointBuySnapshotSchema = z.record(canonicalRaceIdSchema, pointBuyCurveSchema)`. `PUERTA_POINT_BUY_SNAPSHOT = pointBuySnapshotSchema.parse(snapshotJson)` runs at module-load — D-03 fail-closed-at-bundle-init semantic; malformed JSON throws before the UI mounts, preventing a partially valid snapshot from reaching render.
- **`calculateAbilityBudgetSnapshot`** widened: `attributeRules: AbilityBudgetRules | null`. Null branch at the top of the function body returns `{ status:'blocked', remainingPoints:0, spentPoints:0, issues:[resolveValidationOutcome({ affectedIds:['rule:point-buy-missing'], blockKind:'missing-source', hasMissingEvidence:true, passesRule:false, ruleKnown:true })] }`. Existing non-null branch unchanged — phase-03 foundation-validation + phase-12.3 attributes-budget-gate suites continue to green with no modification.
- **`AbilityBudgetRules.baseScore` removed** (Rule 2 auto-fix): the field was declared in the interface but never referenced inside `ability-budget.ts`; `createBaseAttributes` in `store.ts` reads `phase03FoundationFixture.attributeRules.baseScore` which is typed via the planner-side `AttributeRules` interface, not this one. Removing the dead field lets `PointBuyCurve` (which has no `baseScore`) satisfy the contract structurally — no Optional acrobatics, no type assertions at the call site.
- **Barrel export** in `packages/rules-engine/src/foundation/index.ts` appends `export * from './point-buy-snapshot'`.
- **Framework-agnostic guard** green: `grep -rE "from 'react'|from '@planner'" packages/rules-engine/src/foundation/` returns zero matches.

### UI Contract (Task 2 — c60158f)

- **`selectAbilityBudgetRulesForRace(raceId: CanonicalId | null): PointBuyCurve | null`** in `apps/planner/src/features/character-foundation/selectors.ts` (D-06). Returns `PUERTA_POINT_BUY_SNAPSHOT[raceId] ?? null`. Sole source of `attributeRules` for `selectAttributeBudgetSnapshot` — the uniform fixture curve is no longer on the runtime path.
- **`selectAttributeBudgetSnapshot`** swap: `attributeRules: phase03FoundationFixture.attributeRules` → `attributeRules: selectAbilityBudgetRulesForRace(state.raceId)`. Severs the runtime path from the fixture; only pre-12.6 unit tests still read the fixture's `attributeRules` (preserved per D-09).
- **`AttributesBoard`** branch thread (D-07, UI-SPEC A1):
  - `isBlockedForMissingCurve = attributeBudget.status === 'blocked' && attributeBudget.issues.some((i) => i.affectedIds.includes('rule:point-buy-missing'))`
  - Root `.attributes-editor` gets `data-testid="attributes-board"`.
  - Fail-closed callout at top of editor (conditional): `<p className="attributes-editor__point-buy-missing foundation-step__issue is-illegal" data-testid="point-buy-missing-callout">{shellCopyEs.attributes.pointBuyMissing(raceLabel)}</p>` + body `<p className="...point-buy-missing-body detail-panel__body">{shellCopyEs.attributes.pointBuyMissingBody}</p>`.
  - `raceLabel` resolved through `compiledRaceCatalog.races.find((r) => r.id === state.raceId)?.label ?? '—'` — falls back to em-dash when `raceId` is null.
  - Every `+` button `disabled`: `isBlockedForMissingCurve || !canIncrementAttribute(...)`.
  - Every `-` button `disabled`: `isBlockedForMissingCurve || baseValue <= minimum` (RESEARCH Pitfall 7 — OR with the curve-missing predicate, don't rely on `baseValue <= minimum` which would leave `-` clickable if the user edited the base up before the race swap nulled the curve).
  - Budget `<dl className="attributes-editor__summary">` wrapped in `!isBlockedForMissingCurve ? (...) : null` — dl is absent from DOM in fail-closed state.
  - ActionBar `acceptDisabled={!canAdvance || isBlockedForMissingCurve}`.
  - Destructure falls back to `FAIL_CLOSED_CURVE_FALLBACK = { costByScore: {}, maximum: 8, minimum: 8 }` when the selector returns null; these values never participate in a live calculation because `isBlockedForMissingCurve` gates every consumer.
- **`foundation-fixture.ts attributeRules`** marked `TEST-ONLY FIXTURE` per D-09. Runtime no longer reads it; preserved for phase-03 foundation-validation + phase-12.3 attributes-budget-gate which continue to pass it directly into `calculateAbilityBudgetSnapshot`.

### Test Surfaces

- **`tests/phase-12.6/point-buy-snapshot-coverage.spec.ts`** (node env): 2 real assertions GREEN (empty snapshot parses; malformed curve rejected) + 2 `it.todo` (per-race coverage + dedupe count) for Plan 06.
- **`tests/phase-12.6/ability-budget-per-race.spec.ts`** (node env): 3 real assertions GREEN (null branch is blocked + issues contain `rule:point-buy-missing`; null branch short-circuits BEFORE originReady check → single issue dominates; issue carries `blockKind: 'missing-source'` discriminant) + 2 `it.todo` for Plan 06.
- **`tests/phase-12.6/attributes-board-fail-closed.spec.tsx`** (jsdom): 6 RTL assertions GREEN — callout rendered with Spanish copy; 6 `+` buttons disabled; 6 `-` buttons disabled (Pitfall 7); budget dl absent; callout interpolates race label; null-race em-dash fallback.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — blocking] Worktree-path Edit routing**

- **Found during:** Task 1 Step A.
- **Issue:** Initial Edit calls used Windows-style absolute paths (`C:\Users\...`) and routed to the MAIN REPO instead of the worktree. The Read tool appeared to succeed (cached session state), but `grep` and typecheck against the worktree filesystem showed none of the edits persisted there. Running `git status` in the main repo revealed phantom changes to `canonical-id.ts`, `ability-budget.ts`, `index.ts`, and a new `point-buy-snapshot.ts` — none of which belonged on the main branch.
- **Fix:** Reverted main repo (`git checkout` + `rm`). Reapplied all edits using the worktree's forward-slash path form (`/c/Users/pzhly/RiderProjects/pdb-character-builder/.claude/worktrees/agent-ac7c1c53/...`). Verified via `wc -l` and `grep -c` on the worktree path that each edit persisted before proceeding.
- **Files modified:** n/a (process correction, no runtime impact).
- **Commit:** N/A (pre-commit hygiene).

**2. [Rule 3 — blocking] Missing `node_modules` in worktree broke typecheck**

- **Found during:** Task 1 verification (`pnpm typecheck`).
- **Issue:** Fresh worktree had no installed dependencies, so `tsc` reported pre-existing module-resolution errors for `dexie` and `better-sqlite3` (which are pnpm workspace deps under `apps/planner/node_modules` and `packages/data-extractor/node_modules` respectively in the main repo).
- **Fix:** Removed stray symlink; ran `corepack pnpm install --frozen-lockfile --prefer-offline` in the worktree; `pnpm typecheck` then green.
- **Files modified:** n/a (environmental setup).
- **Commit:** N/A.

**3. [Rule 1 — bug] TS18047 after widening `attributeRules: AbilityBudgetRules | null`**

- **Found during:** Task 1 typecheck after the early-return null branch.
- **Issue:** TypeScript doesn't narrow `input.attributeRules` from null across closure boundaries (`.reduce` callback + multiple property accesses); four `'input.attributeRules' is possibly 'null'` errors at the call sites that survived the early-return.
- **Fix:** Introduced a local `const attributeRules = input.attributeRules;` binding right after the fail-closed check. The closure captures the non-null reference directly; no runtime change.
- **Files modified:** `packages/rules-engine/src/foundation/ability-budget.ts`.
- **Commit:** `8004e3e` (included in Task 1 commit).

**4. [Rule 1 — bug] `PointBuyCurve` vs `AbilityBudgetRules` structural mismatch — missing `baseScore`**

- **Found during:** Task 2 typecheck after swapping `selectAttributeBudgetSnapshot` to the per-race selector.
- **Issue:** `PointBuyCurve` (from the point-buy Zod schema at D-02) has no `baseScore` field, but `AbilityBudgetRules` declared one. TS error: `Property 'baseScore' is missing in type '{ budget: number; minimum: number; maximum: number; costByScore: ... }' but required in type 'AbilityBudgetRules'`.
- **Fix:** Removed `baseScore` from the `AbilityBudgetRules` interface in ability-budget.ts. `baseScore` was never referenced inside the rules-engine module (grep-verified); it only lived on the planner-side fixture's `AttributeRules` interface (foundation-fixture.ts:45) and was read once by `createBaseAttributes` in the store. Dropping the dead field is the minimal, type-consistent fix — no widening/optional gymnastics — and keeps the rules-engine contract tighter. SPEC §79's "Rules-engine shape preserved... no breaking change" invariant still holds at the API boundary: consumers that pass a `{ budget, minimum, maximum, costByScore }` shape (with or without `baseScore`) still satisfy the input type, and the existing non-null-branch tests continue to green.
- **Files modified:** `packages/rules-engine/src/foundation/ability-budget.ts`.
- **Commit:** `c60158f` (included in Task 2 commit).

**5. [Rule 3 — blocking] Pre-12.6 legal-path tests broke after runtime-path swap**

- **Found during:** Task 2 full-suite run.
- **Issue:** Three pre-existing suites (`tests/phase-03/summary-status.spec.tsx`, `tests/phase-03/attribute-budget.spec.tsx`, `tests/phase-10/attributes-advance.spec.tsx`) test legal-path behavior by setting `race:human` and asserting budget status `legal` / spent/remaining totals / Aceptar enabled. After the runtime-path swap, `race:human` has no snapshot entry → null curve → fail-closed → NEVER legal. 5 tests failed.
- **Fix:** In each failing suite's `beforeEach`, mutate `PUERTA_POINT_BUY_SNAPSHOT['race:human']` (and `'race:elf'` in summary-status.spec.tsx which tests `race:elf`) with the pre-12.6 uniform curve. `afterEach` deletes those keys so neighbouring suites (including phase-12.6 fail-closed specs) see an untouched empty snapshot. This preserves the pre-12.6 contract those tests were written for without reverting the runtime-path severance.
- **Files modified:** `tests/phase-03/summary-status.spec.tsx`, `tests/phase-03/attribute-budget.spec.tsx`, `tests/phase-10/attributes-advance.spec.tsx`.
- **Commit:** `c60158f` (included in Task 2 commit).

### Architectural Decisions Avoided

None — no Rule 4 checkpoints hit. Every deviation was a scoped bug-fix / blocker-clearing step, fully inside the plan's engineering contract.

## Verification Gates

| Gate | Command | Result |
|------|---------|--------|
| canonicalRaceIdSchema export | `grep -c "export const canonicalRaceIdSchema" packages/rules-engine/src/contracts/canonical-id.ts` | `1` |
| point-buy-snapshot.ts exists | `test -f packages/rules-engine/src/foundation/point-buy-snapshot.ts` | `0` (present) |
| PUERTA_POINT_BUY_SNAPSHOT grep | `grep -c "PUERTA_POINT_BUY_SNAPSHOT" packages/rules-engine/src/foundation/point-buy-snapshot.ts` | `1` |
| Module-load parse | `grep -c "pointBuySnapshotSchema.parse" packages/rules-engine/src/foundation/point-buy-snapshot.ts` | `1` |
| Null-type widening | `grep -c "AbilityBudgetRules \| null" packages/rules-engine/src/foundation/ability-budget.ts` | `1` |
| rule:point-buy-missing emission | `grep -c "rule:point-buy-missing" packages/rules-engine/src/foundation/ability-budget.ts` | `1` |
| Barrel export | `grep -c "point-buy-snapshot" packages/rules-engine/src/foundation/index.ts` | `1` |
| Framework-agnostic guard | `grep -rE "from 'react'\|from '@planner'" packages/rules-engine/src/foundation/` | `0` matches |
| Selector declaration + call site | `grep -c "selectAbilityBudgetRulesForRace" apps/planner/src/features/character-foundation/selectors.ts` | `3` (import + decl + call) |
| Runtime path severed | `grep "phase03FoundationFixture.attributeRules" apps/planner/src/features/character-foundation/selectors.ts` | zero matches |
| Callout testid | `grep -c "data-testid=\"point-buy-missing-callout\"" apps/planner/src/features/character-foundation/attributes-board.tsx` | `1` |
| shellCopyEs.attributes.pointBuyMissing used | `grep -c "shellCopyEs.attributes.pointBuyMissing" apps/planner/src/features/character-foundation/attributes-board.tsx` | `2` |
| isBlockedForMissingCurve usage | `grep -c "isBlockedForMissingCurve" apps/planner/src/features/character-foundation/attributes-board.tsx` | `9` |
| Test-only fixture marker | `grep -c "TEST-ONLY FIXTURE" apps/planner/src/features/character-foundation/foundation-fixture.ts` | `1` |
| phase-12.6 suite | `corepack pnpm test -- tests/phase-12.6 --reporter=dot` | 0 failures (ATTR-01 R1 + R3 GREEN; PROG-04 R5/R6 stay skipped for Plan 03) |
| Full suite | `corepack pnpm test -- --reporter=dot` | `643 passed / 7 skipped / 4 todo / 0 failed` (92 files passed + 1 skipped + level-progression-scan.spec.tsx stays skipped for Plan 03) |
| Typecheck | `corepack pnpm typecheck` | green (tsc --noEmit) |
| SHAR-05 invariant | `git diff 64d32b4..HEAD -- packages/rules-engine/src/share-url/` | empty (no schemaVersion touch) |

## Known Stubs

- **`puerta-point-buy.json`** ships as `{}`. This is INTENTIONAL per CONTEXT D-03 and UI-SPEC: the empty snapshot proves the fail-closed path for every race at runtime (45 races × null curve). Plan 06 A1b will populate it with Puerta-vetted per-race curves once the user delivers them; no additional code changes required at that point — the nominal-path branch in `calculateAbilityBudgetSnapshot` + `selectAbilityBudgetRulesForRace` already exists and is exercised by the seed entries in the three pre-12.6 legal-path specs.

## Threat Surface Scan

No new threat flags. The plan's `<threat_model>` covers every change:

- **T-12.6-02-01 (Tampering of snapshot JSON)** — mitigated by `pointBuySnapshotSchema.parse(snapshotJson)` at module load.
- **T-12.6-02-02 (callout leaks internal state)** — accepted; callout shows only `raceLabel` (already user-visible in Origin step) + static Spanish copy.
- **T-12.6-02-03 (raceLabel injection)** — mitigated; Zod-validated upstream via compiledRaceCatalog; React renders as text node.
- **T-12.6-02-04 (user unaware of cause)** — accepted; body copy directs user to change race or contact DM.
- **T-12.6-02-05 (malformed JSON DoS)** — mitigated by design (fail-closed bundle init).
- **T-12.6-02-06 (non-null bypasses gate)** — mitigated; existing non-null branch unchanged; pre-12.6 suites continue to validate nominal path.

## Downstream Impact

| Downstream plan | Consumes from 12.6-02 |
|-----------------|----------------------|
| 12.6-06 (Plan 06 A1b) | Populates `puerta-point-buy.json` with real per-race curves. `pointBuySnapshotSchema` + `canonicalRaceIdSchema` enforce shape at module load; `PUERTA_POINT_BUY_SNAPSHOT[raceId]` resolves to the new curve automatically via `selectAbilityBudgetRulesForRace`; AttributesBoard fail-closed path unmounts for any race with a snapshot entry. Flips the `it.todo` entries in `point-buy-snapshot-coverage.spec.ts` + `ability-budget-per-race.spec.ts` from pending-todo to active-GREEN. |
| 12.6-03 (Wave 1 Plan 03 — PROG-04) | Independent; no shared files. Can run in parallel (verified zero file overlap per orchestrator). |

## Self-Check: PASSED

- [x] `packages/rules-engine/src/contracts/canonical-id.ts` — MODIFIED (`canonicalRaceIdSchema` exported)
- [x] `packages/rules-engine/src/foundation/point-buy-snapshot.ts` — FOUND (created)
- [x] `packages/rules-engine/src/foundation/ability-budget.ts` — MODIFIED (null branch + widened input + baseScore removed)
- [x] `packages/rules-engine/src/foundation/index.ts` — MODIFIED (barrel export)
- [x] `apps/planner/src/features/character-foundation/selectors.ts` — MODIFIED (selector + runtime swap)
- [x] `apps/planner/src/features/character-foundation/attributes-board.tsx` — MODIFIED (fail-closed branch)
- [x] `apps/planner/src/features/character-foundation/foundation-fixture.ts` — MODIFIED (test-only comment)
- [x] `tests/phase-12.6/point-buy-snapshot-coverage.spec.ts` — MODIFIED (activated + real asserts)
- [x] `tests/phase-12.6/ability-budget-per-race.spec.ts` — MODIFIED (activated + real asserts)
- [x] `tests/phase-12.6/attributes-board-fail-closed.spec.tsx` — MODIFIED (activated + 6 real asserts)
- [x] `tests/phase-03/summary-status.spec.tsx` — MODIFIED (seed + cleanup)
- [x] `tests/phase-03/attribute-budget.spec.tsx` — MODIFIED (seed + cleanup)
- [x] `tests/phase-10/attributes-advance.spec.tsx` — MODIFIED (seed + cleanup)
- [x] commit `8004e3e` — FOUND (Task 1)
- [x] commit `c60158f` — FOUND (Task 2)
- [x] `pnpm test` full suite — 643 passed / 7 skipped / 4 todo / 0 failed
- [x] `pnpm typecheck` — green
- [x] framework-agnostic guard — zero react/@planner imports in packages/rules-engine/src/foundation/
- [x] SHAR-05 invariant — share-url/ untouched
