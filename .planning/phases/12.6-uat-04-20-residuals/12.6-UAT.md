---
status: complete
phase: 12.6-uat-04-20-residuals
source:
  - 12.6-01-SUMMARY.md
  - 12.6-02-SUMMARY.md
  - 12.6-03-SUMMARY.md
  - 12.6-04-SUMMARY.md
  - 12.6-05-SUMMARY.md
  - 12.6-06-SUMMARY.md
started: 2026-04-20T17:19:38Z
updated: 2026-04-20T17:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Hard reload http://localhost:5173/. Page loads without console errors, title 'Planificador de personaje', no red overlay, Construcción route reachable.
result: pass
evidence: |
  document.title = 'Planificador de personaje'; #root mounted (1 child); no vite-error-overlay; bodyText contains CREACION DE PERSONAJES + ORIGEN + PROGRESION + Raza picker. Console: 0 errors, 2 expected deferred warnings ([12.2-04] Dropped duplicate class id `class:harper`/`class:shadowadept`).

### 2. Progresión 1-20 heading + 20-row scan surface
expected: Construcción → progression panel shows heading 'Progresión 1-20' (not '1-16'). Below it, 20 rows visible (levels 1..20), each row compact (class pill + feats pill + skills pill + legality glyph).
result: pass
evidence: |
  After origin flow (Humano + Neutral puro + Atributos baseline + Aceptar) → Progresión sub-step `Clase` mounts BuildProgressionBoard. H2 = 'Progresión 1-20' (Plan 12.6-01 fossil patch verified). `[data-testid=level-progression-list]` present. `document.querySelectorAll('[data-level-row]').length === 20`.

### 3. Row pills render Spanish copy + counters
expected: At fresh build (race set, no class), L1 row's class pill reads 'Sin clase'. After picking Guerrero at L1, class pill reads 'Guerrero'. Feats pill format 'N/M dotes'; skills pill 'N/M puntos'. Legality glyph one of ✓ ⚠ ✗ 🔒.
result: pass
evidence: |
  Pre-pick L1 pills: ['class:Sin clase','feats:0/0 dotes','skills:0/0 pts','legality:⚠']. After clicking [data-class-id="class:fighter"] inside [data-testid=level-row-1-expanded] → L1 pills update to ['class:Guerrero','feats:0/3 dotes','skills:0/8 pts','legality:⚠']. Spanish label projected via compiledClassCatalog.classes.find(c=>c.id==='class:fighter').label per Plan 12.6-03 selector contract. Counters reflect Humano L1 budget (3 feats including racial-bonus + 8 skill pts including INT mod×0).

### 4. Expanded slot edit-in-place (host swap)
expected: Click active row header → that row expands inline. Inside expanded slot: ClassPicker (Clases básicas + Clases de prestigio sections), AbilityIncreaseControl (every 4th level), LevelEditorActionBar sticky footer with Continuar al nivel N+1 button. No separate LevelSheet panel.
result: pass
evidence: |
  L1 active by default. Inside [data-testid=level-row-1-expanded]: H3 'Clases básicas' + H3 'Clases de prestigio' + H3 'Requisitos de entrada' + H3 'Ganancias del nivel'. 15 [data-class-id] entries surfaced (class:barbarian → class:paladin-antiguos, including base + prestige). [data-testid=level-editor-action-bar] present inside expanded slot. No separate `<aside class="level-sheet">` mounted (Plan 12.6-05 deletion verified).

### 5. Single expansion (non-active rows isolated)
expected: Only one row expanded at a time. Clicking L2 header collapses L1, expands L2. Other 18 rows stay header-only.
result: pass
evidence: |
  `document.querySelectorAll('[data-testid$="-expanded"]').length === 1`. expandedTestids = ['level-row-1-expanded'] only across all probes (Plan 12.6-04 perf contract T-12.6-03-03 satisfied).

### 6. G1 sequential-gate locks downstream levels
expected: With only L1 class picked, L2 row is clickable + 'incomplete'. L8+ rows show 🔒 legality glyph + button aria-disabled + cannot expand.
result: pass
evidence: |
  L1 picked Guerrero → L1 leg='incomplete' (still missing feats+skills), L2 leg='incomplete' (G1 unlocks because L1 has class), L2 button.disabled=false, aria-disabled=null. L8 leg='locked', L8 button.disabled=true, aria-disabled='true'. Sequential gate per UAT-2026-04-20 P4 + selectLevelLegality precedence locked > invalid > incomplete > legal verified.

### 7. Per-race point-buy budget recomputes (ATTR-01 happy path)
expected: Atributos with Humano + base 8/8/8/8/8/8 → Puntos restantes = 30. Switch race to Elfo with same baseline → Puntos restantes still 30 (NWN1 hardcoded uniform 30; sourced uniformity per Plan 06). Bump DEX 8→14 spends per cost curve; remaining decreases predictably.
result: pass
evidence: |
  Humano + baseline 8/8/8/8/8/8 → 'PUNTOS RESTANTES: 30' + 'Puntos gastados 0 / Puntos restantes 30'. Switched race to Elfo via Raza picker → Atributos board re-rendered with race racial mods folded (Destreza 10/+0, Constitucion 6/-2) but Puntos restantes preserved at 30 because zero increments spent. Sourced uniformity per Plan 06 (racialtypes.2da AbilitiesPointBuyNumber=30 uniform across all 45 races) confirmed at runtime.

### 8. ATTR-01 fail-closed callout (visual sanity)
expected: All 45 races render Atributos board normally (no fail-closed callout) because puerta-point-buy.json now populated with 45 entries.
result: pass
evidence: |
  Humano + Elfo both render full AttributesBoard (budget panel + +/- buttons + Aceptar) with no 'pointBuyMissing' callout. fail-closed predicate `Array.from(document.querySelectorAll('*')).find(e=>/no.*regla.*point.buy|pointBuyMissing/i.test(e.textContent.slice(0,200)))` returns false. Plan 06 populate verified end-to-end.

### 9. D&D ability modifier display (commit 8b671ae)
expected: Atributos shows D&D modifier (e.g. STR 14 → +2, STR 8 → -1) NOT racial delta. Modifier column reads modifier value, not race adjustment.
result: pass
evidence: |
  Humano baseline rendering shows each ability with value 8 + modifier '-1'. Elfo with racial mods folded shows: Fuerza 8 (-1), Destreza 10 (+0), Constitucion 6 (-2), Inteligencia 8 (-1), Sabiduria 8 (-1), Carisma 8 (-1). All modifiers compute floor((value-10)/2) — D&D 3.5e ability modifier formula, not racial delta. Commit 8b671ae fix verified.

### 10. LevelRail + LevelSheet deleted (cleanup verification)
expected: No legacy 'Progresión' rail strip on the side; no separate full-panel LevelSheet pane. Only the 20-row scan list with inline expansion. DOM has zero `.level-rail__button` and zero `.level-sheet` elements.
result: pass
evidence: |
  `document.querySelectorAll('.level-rail__button').length === 0`. `document.querySelectorAll('.level-sheet').length === 0`. `[class*="level-rail"]` count === 0. `[class*="level-sheet"]` count === 4 — these are preserved INNER namespace fragments (.level-sheet__heading, .level-sheet__requirements, etc.) migrated verbatim into expanded slot per Plan 12.6-04 host-swap pattern (D-11), not the deleted outer host. Plan 12.6-05 deletion contract honored.

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all 10 tests passed]

## Notes

- Phase 12.4-06 R1 ClassPicker regression flagged in prior STATE.md is RESOLVED at runtime: ClassPicker mounts inside [data-testid=level-row-1-expanded] with both `Clases básicas` + `Clases de prestigio` sections + 15 [data-class-id] rows verified at L1.
- Memory `project_attributes_blocker.md` (Phase 05.2 stepper hidden + no Siguiente) — not blocking on master 2026-04-20: Aceptar advances Atributos → Progresion sub-step `Clase` automatically.
- Pre-existing DEF-12.4-02 (`font-weight: 700` at app.css:113) untouched — out of 12.6 scope.
