# Phase 12.6: UAT-2026-04-20 Residuals - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-20
**Phase:** 12.6-uat-04-20-residuals
**Areas discussed:** A1 snapshot file shape + loader, P5 row component layout, level-rail.tsx fate, Missing-curve fail-closed routing

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| A1 snapshot file shape + loader | Where does puerta.point-buy.json live? Zod schema shape. Loader module boundary. | ✓ (delegated) |
| P5 row component layout | Compact row internals — pill positions, legality glyph style, expand/collapse mechanism. | ✓ (delegated) |
| level-rail.tsx fate | Delete / keep / fold. | ✓ (delegated) |
| Missing-curve fail-closed routing | Selector vs rules-engine helper. Copy + disabled-state shape. Test strategy. | ✓ (delegated) |

**User's choice:** `tu decide` (full delegation across all 4 areas)
**Notes:** User delegated all implementation decisions. Claude locked decisions per SPEC.md constraints (fail-closed, framework-agnostic, no schemaVersion bump, 1280×800 scan target).

---

## A1 snapshot file shape + loader

**Claude's Discretion — decisions locked:**

- File path: `packages/rules-engine/src/foundation/data/puerta-point-buy.json` (kebab-case)
- Zod schema module: `packages/rules-engine/src/foundation/point-buy-snapshot.ts`
- Loader: pure function + JSON import (Vite/Vitest resolve); startup parse failure = fail-closed at boot
- Provenance: sibling `puerta-point-buy.md` cites Puerta source per race; no runtime `verified` field (considered + rejected)

**Alternatives considered:**
- Extractor-emitted `pointBuyCurve` field on compiled-race-schema — rejected by SPEC.md (deferred to future phase)
- Filesystem-loaded JSON at runtime — rejected (framework-agnostic + browser compatibility)
- `verified: boolean` on snapshot entries — rejected (provenance lives in .md instead; keeps JSON schema tight)

---

## P5 row component layout

**Claude's Discretion — decisions locked:**

- New file: `apps/planner/src/features/level-progression/level-progression-row.tsx`
- `<li>` root, clickable `<button>` header, conditional expanded `<section>`
- Pills: `<span data-pill="class|feats|skills|legality">`; Unicode glyphs for legality (✓ ⚠ ✗ 🔒)
- CSS namespace `.level-progression-row__*` + `.level-row__pill--*`
- Container: `<ol className="level-progression__list">`, grid single-column, `grid-template-rows: repeat(20, auto)`

**Alternatives considered:**
- Modal for active-level edit — rejected by SPEC.md (edit-in-place locked)
- New icon library (lucide-react, etc.) — rejected (scope creep; Unicode sufficient)
- Local expand state per row — rejected (derives from `progressionState.activeLevel` for single source of truth)

---

## Expansion mechanism

**Claude's Discretion — decisions locked:**

- Click dispatches `setActiveLevel(N)` + `setExpandedLevel(N)` atomically (12.4-09 pattern)
- `aria-expanded` on button, `.level-progression-row--expanded` on row
- LevelEditorActionBar dispatch order (sub-step FIRST, then active level, then expanded level) preserved inside expanded row

---

## level-rail.tsx fate

**Claude's Discretion — decisions locked:**

- DELETE `apps/planner/src/components/shell/level-rail.tsx` entirely
- Scan surface = new row list; rail was scan proxy, becomes redundant
- Migrate test fixtures from `level-rail__button` → `[data-level-row]`
- `[data-testid="advance-to-level-{N+1}"]` (12.4-09) stays on the advance button inside expanded row

**Alternatives considered:**
- Keep rail as sidebar + scan list coexist — rejected (duplication + 1280×800 horizontal real estate)
- Fold rail internals into new row component — rejected (no shared logic worth preserving; atomic delete cleaner)

---

## Missing-curve fail-closed routing

**Claude's Discretion — decisions locked:**

- `calculateAbilityBudgetSnapshot` input signature extends: `attributeRules: AbilityBudgetRules | null`
- Null → `status: 'blocked'`, `remainingPoints: 0`, issues includes `resolveValidationOutcome({ affectedIds: ['rule:point-buy-missing'], ... })`
- Selector `selectAbilityBudgetRulesForRace(raceId)` returns `AbilityBudgetRules | null`
- Copy: `shellCopyEs.attributes.pointBuyMissing(raceLabel)` → `"Curva punto-compra no disponible para ${raceLabel}"`
- AttributesBoard threads selector output directly into the helper — no UI-layer short-circuit

**Alternatives considered:**
- UI short-circuit before calling helper — rejected (duplicates branching logic; helper is single source of truth)
- Sentinel `AbilityBudgetRules` with marker field — rejected (hacky, less explicit than null branch)

---

## Test strategy

**Claude's Discretion — decisions locked:**

- `tests/phase-12.6/point-buy-snapshot-coverage.spec.ts` — build-time Zod + coverage assertion
- `tests/phase-12.6/ability-budget-per-race.spec.ts` — table-driven per-race regression
- `tests/phase-12.6/level-progression-scan.spec.tsx` — RTL-jsdom at 1280×800; 20 rows + scroll equality + pills + expansion + legality
- Pre-Phase-12.6 suites kept green via additive null branch (no breaking change to `ability-budget.ts` input shape)

---

## Claude's Discretion

All 4 gray areas fully delegated — user typed `tu decides` on the selection question. Decisions above reflect Claude's lock per SPEC.md constraints.

## Deferred Ideas

- Extractor enrichment for point-buy curves (future phase when extractor decodes Puerta-specific 2DA)
- Responsive behavior below 1280px width (separate phase)
- Font-weight 700 hygiene fold at `app.css:113` (DEF-12.4-02 deferred until milestone close)
- `verified: boolean` snapshot field (provenance in sibling .md instead)
