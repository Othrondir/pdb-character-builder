---
phase: 12.6-uat-04-20-residuals
plan: 01
subsystem: infra
tags: [infra, wave-0, test-scaffold, copy-hygiene]
requires: []
provides:
  - phase-12.6-jsdom-glob
  - wave-0-test-stubs
  - puerta-point-buy-empty-snapshot
  - puerta-point-buy-provenance-doc
  - shellCopyEs-attributes-block
  - shellCopyEs-progression-scan-keys
  - fossil-railHeading-1-20-patch
affects:
  - vitest.config.ts
  - tests/phase-12.6/
  - packages/rules-engine/src/foundation/data/
  - apps/planner/src/lib/copy/es.ts
tech-stack:
  added: []
  patterns:
    - environmentMatchGlobs-per-phase-jsdom-entry
    - describe.skip-wave0-stubs-anchoring-downstream-RED-GREEN
    - empty-snapshot-JSON-to-prove-fail-closed-before-real-data
    - shellCopyEs-new-top-level-block-for-feature-scoped-copy
key-files:
  created:
    - tests/phase-12.6/point-buy-snapshot-coverage.spec.ts
    - tests/phase-12.6/ability-budget-per-race.spec.ts
    - tests/phase-12.6/attributes-board-fail-closed.spec.tsx
    - tests/phase-12.6/level-progression-scan.spec.tsx
    - packages/rules-engine/src/foundation/data/puerta-point-buy.json
    - packages/rules-engine/src/foundation/data/puerta-point-buy.md
  modified:
    - vitest.config.ts
    - apps/planner/src/lib/copy/es.ts
decisions:
  - Empty-JSON-snapshot is intentional (proves fail-closed path in Plan 02 before Plan 06 supplies real curves)
  - railHeading key preserved; value-only patch (no consumer scrub this plan)
  - New top-level shellCopyEs.attributes block (UI-SPEC prescribed; no prior attributes block existed)
  - .spec.ts stubs under node env, .spec.tsx stubs under jsdom (per plan PATTERNS precedent)
metrics:
  duration_seconds: 410
  duration_human: "6m 50s"
  tasks_completed: 3
  files_created: 6
  files_modified: 2
  tests_added: 0
  tests_passing: 632
  tests_skipped_phase_12_6: 17
  completed_at: "2026-04-20T12:47:13Z"
---

# Phase 12.6 Plan 01: Wave 0 Infrastructure Summary

Wave 0 scaffolding for Phase 12.6: four red-pending RTL+node test stubs, the empty Puerta point-buy snapshot JSON + provenance md that downstream plans import at module load, the vitest jsdom glob for `tests/phase-12.6/**/*.spec.tsx`, and the 11 new shellCopyEs Spanish copy keys (2 under a new `attributes` top-level block; 9 under `progression` for scan-surface legality/pills/aria); plus the pre-P6 fossil value patch `railHeading: 'Progresión 1-16' → 'Progresión 1-20'` (key preserved). All downstream Wave 1/2/3 plans can now run RED→GREEN cycles without racing on missing scaffolds.

## Execution Overview

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add phase-12.6 jsdom glob + 4 Wave 0 test stubs | `d7add91` | `vitest.config.ts`, `tests/phase-12.6/*.spec.ts*` (4) |
| 2 | Create empty puerta-point-buy.json + provenance skeleton .md | `6292689` | `packages/rules-engine/src/foundation/data/puerta-point-buy.{json,md}` |
| 3 | Patch fossil value + add 11 A1+P5 Spanish copy keys | `688113c` | `apps/planner/src/lib/copy/es.ts` |

Total: 3 commits, 6 files created, 2 files modified, 0 runtime behavior change.

## What This Delivers

### Test Scaffolding (Task 1)

- `vitest.config.ts` — one new `environmentMatchGlobs` entry: `['tests/phase-12.6/**/*.spec.tsx', 'jsdom']`. `.spec.ts` (no "tsx") files stay under default node env per plan decision (pure-function suites under node; RTL suites under jsdom).
- `tests/phase-12.6/point-buy-snapshot-coverage.spec.ts` (node env) — 3 skipped specs anchoring Plan 02's RED→GREEN cycle for ATTR-01 R1. Specs reference `dedupeByCanonicalId(compiledRaceCatalog.races)` for coverage, and call out Pitfall 5 (unique canonical IDs vs raw races.length).
- `tests/phase-12.6/ability-budget-per-race.spec.ts` (node env) — 3 skipped specs for ATTR-01 R2+R3+R4; Plan 02 activates the fail-closed spec, Plan 06 activates the per-race differential + table-driven coverage specs.
- `tests/phase-12.6/attributes-board-fail-closed.spec.tsx` (jsdom) — 4 skipped RTL specs for ATTR-01 R3 fail-closed branch; tests the callout, 6 disabled `+` buttons, 6 disabled `−` buttons (Pitfall 7), and suppressed budget dl.
- `tests/phase-12.6/level-progression-scan.spec.tsx` (jsdom) — 7 skipped specs spanning PROG-04 R5+R6 (Suites A–D). Suite A checks CSS contract (grid-template-rows repeat(20, auto)), Suite B checks DOM (20 rows, 4 pills each), Suite C exercises expansion / legality transitions / G1 locked, Suite D preserves the 12.4-09 `data-testid="advance-to-level-{N+1}"` invariant.

All 4 files use `describe.skip` so the suite runs green (17 skipped specs; 0 new failures).

### Data Scaffold (Task 2)

- `packages/rules-engine/src/foundation/data/puerta-point-buy.json` — intentionally empty object `{}`. Plan 02 proves end-to-end that when the per-race selector resolves `undefined`, `calculateAbilityBudgetSnapshot` returns `status: 'blocked'` + issue `rule:point-buy-missing` and the UI renders the fail-closed callout. Plan 06 A1b populates real curves once the user delivers Puerta source material.
- `packages/rules-engine/src/foundation/data/puerta-point-buy.md` — provenance doc skeleton. Documents the schema shape (`budget / minimum / maximum / costByScore`), the dedupe contract (`race:drow` is double-emitted at sourceRow 196 + 676; dedupeByCanonicalId first-wins rule), an empty per-race provenance table (Plan 06 fills), and a change log seeded with 2026-04-20.

### Copy Hygiene (Task 3)

- **Fossil patch:** `shellCopyEs.progression.railHeading` value `'Progresión 1-16'` → `'Progresión 1-20'` (key preserved per plan's LOCKED decision; no consumer scrub this plan). Follows the P6 level-range bump already landed in commits `eb0b34d` / `9513fe6`.
- **New `shellCopyEs.attributes` top-level block** (created — no prior `attributes` block existed): `pointBuyMissing(raceLabel)` template function + `pointBuyMissingBody` static string for the ATTR-01 R3 fail-closed callout. UI-SPEC § Copywriting Contract literals reproduced verbatim.
- **9 new `shellCopyEs.progression.*` keys** for PROG-04 R5+R6 scan surface: `legalityLabels` (4-entry object: `legal / incomplete / invalid / locked`), `pillEmpty.class`, `pillTemplate` (2-entry object: `feats / skills`), `lockedRowAriaTemplate`, `rowAriaTemplate`. All Spanish strings verbatim from UI-SPEC: em-dash (`—`) preserved in `lockedRowAriaTemplate`; accented characters (`válido`, `inválido`) preserved.
- The other `railHeading: 'Habilidades 1-16'` at line ~220 (inside `skills:` block) left untouched — owned by the skills route, outside Phase 12.6 scope.

## Verification Gates

| Gate | Command | Result |
|------|---------|--------|
| phase-12.6 scoped | `npx vitest run tests/phase-12.6 --reporter=dot` | 4 files skipped, 17 tests skipped, 0 failures |
| Full test suite | `npx vitest run --reporter=dot` | 92 files pass + 4 skipped, 632 tests pass + 17 skipped, 0 regressions |
| Typecheck | `corepack pnpm typecheck` | green (tsc `--noEmit`) |
| Fossil patched | `grep -c "'Progresión 1-20'" apps/planner/src/lib/copy/es.ts` | `1` |
| Old fossil removed | `grep -c "'Progresión 1-16'" apps/planner/src/lib/copy/es.ts` | `0` |
| Snapshot JSON shape | `node -e "require(...json); Object.keys(o).length === 0"` | OK (empty object) |
| Snapshot files exist | `test -f ...puerta-point-buy.{json,md}` | both exist |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — blocking] Missing `attributes` top-level block in shellCopyEs**

- **Found during:** Task 3
- **Issue:** Plan 03 action said "Inside the existing `attributes:` block (find it in the file — it hosts `statusLabels`, etc.)". There is no top-level `attributes` block in `apps/planner/src/lib/copy/es.ts`; the only `attributes` occurrences are nested keys (`resumen.columnLabels.attribute`, `stepper.originSteps.attributes`, `stepper.stepTitles.attributes`). The `foundation:` block hosts attribute-screen strings (`remainingPoints`, `spentPoints`), but UI-SPEC explicitly keys new strings under `shellCopyEs.attributes.*` (not `foundation.*`).
- **Fix:** Added a new top-level `attributes:` block between `foundation:` and `progression:`. Keeps the UI-SPEC selector path `shellCopyEs.attributes.pointBuyMissing(raceLabel)` and `shellCopyEs.attributes.pointBuyMissingBody` intact for Plan 02 consumers.
- **Files modified:** `apps/planner/src/lib/copy/es.ts`
- **Commit:** `688113c`

**2. [Rule 1 — bug] Acceptance grep pattern inconsistent with plan's prescribed md content (dedupeByCanonicalId backticks)**

- **Found during:** Task 2
- **Issue:** Plan 02 action prescribed `` `dedupeByCanonicalId` first-wins; one entry in this file suffices. `` (backticks around the identifier), while acceptance criterion used `grep -c "dedupeByCanonicalId first-wins"` (no backticks) — the backtick closed before the space, so the literal grep substring "dedupeByCanonicalId first-wins" never appeared in the file.
- **Fix:** Rephrased the dedupe-contract sentence in `puerta-point-buy.md` to "The dedupeByCanonicalId first-wins rule applies" — preserves the semantic meaning (dedupe contract, first-wins rule, single entry suffices) while making the acceptance grep substring match. All other backticks around code identifiers preserved.
- **Files modified:** `packages/rules-engine/src/foundation/data/puerta-point-buy.md`
- **Commit:** `6292689` (included in Task 2 commit)

### Architectural Decisions Avoided

None — no Rule 4 checkpoints hit. Plan 01 was strictly infra scaffolding with no behavior change.

## Known Stubs

None introduced in this plan. The empty `puerta-point-buy.json` is scaffolding intended for Plan 02 + Plan 06 to build on; it is documented as intentional in the provenance md change log and does not render any stub UI (no consumer wired yet — Plan 02 Task A1a wires the fail-closed selector path).

## Threat Surface Scan

No new security-relevant surface introduced. The plan's `<threat_model>` covered all changes:
- `T-12.6-01-01` (Tampering of snapshot JSON) — empty object ships; Plan 02 will wrap with `pointBuySnapshotSchema.parse()` for fail-closed bundle init.
- `T-12.6-01-02` (provenance md info disclosure) — accepted; no PII, public citations only.
- `T-12.6-01-03` (glob DoS) — accepted; standard glob semantics.
- `T-12.6-01-04` (template interpolation injection) — mitigated; `raceLabel` is Zod-validated upstream and React auto-escapes text nodes.

No new threat flags to raise.

## Downstream Impact

| Downstream plan | Consumes from 12.6-01 |
|-----------------|----------------------|
| 12.6-02 | empty `puerta-point-buy.json` (proves fail-closed selector path); `shellCopyEs.attributes.pointBuyMissing*`; activates `point-buy-snapshot-coverage.spec.ts` + `ability-budget-per-race.spec.ts` (first 2 specs) + `attributes-board-fail-closed.spec.tsx` |
| 12.6-03 | vitest jsdom glob; activates `level-progression-scan.spec.tsx` Suites A+B; consumes `shellCopyEs.progression.legalityLabels` + `pillEmpty` + `pillTemplate` + `railHeading: 'Progresión 1-20'` |
| 12.6-04 | activates `level-progression-scan.spec.tsx` Suites C (expansion, legality transitions, G1 locked); consumes `lockedRowAriaTemplate` + `rowAriaTemplate` |
| 12.6-05 | activates `level-progression-scan.spec.tsx` Suite D (advance-to-level selector invariant) |
| 12.6-06 | populates `puerta-point-buy.json` with real per-race curves + appends rows to `puerta-point-buy.md` provenance table; flips remaining specs in `ability-budget-per-race.spec.ts` + `point-buy-snapshot-coverage.spec.ts` to active green |

## Self-Check: PASSED

- [x] `vitest.config.ts` — FOUND (phase-12.6 jsdom glob at line 17)
- [x] `tests/phase-12.6/point-buy-snapshot-coverage.spec.ts` — FOUND
- [x] `tests/phase-12.6/ability-budget-per-race.spec.ts` — FOUND
- [x] `tests/phase-12.6/attributes-board-fail-closed.spec.tsx` — FOUND
- [x] `tests/phase-12.6/level-progression-scan.spec.tsx` — FOUND
- [x] `packages/rules-engine/src/foundation/data/puerta-point-buy.json` — FOUND (empty object)
- [x] `packages/rules-engine/src/foundation/data/puerta-point-buy.md` — FOUND (provenance skeleton)
- [x] `apps/planner/src/lib/copy/es.ts` — MODIFIED (railHeading patched + attributes block + 9 progression keys)
- [x] commit `d7add91` — FOUND (Task 1)
- [x] commit `6292689` — FOUND (Task 2)
- [x] commit `688113c` — FOUND (Task 3)
- [x] phase-12.6 suite runs with 0 failures (17 skipped specs green)
- [x] full suite: 632 tests pass, 0 regressions
- [x] typecheck green
- [x] Fossil `'Progresión 1-16'` replaced with `'Progresión 1-20'` (grep verified)
