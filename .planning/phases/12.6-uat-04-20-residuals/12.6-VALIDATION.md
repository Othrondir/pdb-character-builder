---
phase: 12.6
slug: uat-04-20-residuals
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-20
---

# Phase 12.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Sourced from `12.6-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.16 (jsdom + node environments via `environmentMatchGlobs`) |
| **Config file** | `vitest.config.ts` (add `['tests/phase-12.6/**/*.spec.tsx', 'jsdom']` glob in Wave 0) |
| **Quick run command** | `pnpm test -- tests/phase-12.6 --reporter=dot` |
| **Full suite command** | `pnpm test` |
| **Typecheck gate** | `pnpm typecheck` (must be green before commit) |
| **Estimated runtime** | quick ~5s · full ~25s |

---

## Sampling Rate

- **After every task commit:** `pnpm test -- tests/phase-12.6 --reporter=dot` + `pnpm typecheck`
- **After every plan wave:** `pnpm test` (full suite) + CSS grep gates
- **Before `/gsd-verify-work`:** Full suite + typecheck + all Regression Guards green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Populated by `/gsd-plan-phase` — each plan's tasks get a row with exact automated command.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.6-01-01 | 01 (Wave 0 infra) | 0 | ATTR-01 R1 · PROG-04 R5 · R6 | — | N/A | config | `pnpm test -- tests/phase-12.6 --reporter=dot` | ❌ W0 | ⬜ pending |
| 12.6-02-xx | 02 (A1a schema+loader+selector+UI) | 1 | ATTR-01 R1 · R3 | — | fail-closed on missing curve | unit + RTL | `pnpm test -- tests/phase-12.6/point-buy-snapshot-coverage.spec.ts tests/phase-12.6/attributes-board-fail-closed.spec.tsx` | ❌ W0 | ⬜ pending |
| 12.6-03-xx | 03 (A1b populate snapshot) | 2 | ATTR-01 R2 · R4 | — | correct per-race deltas | unit (table) | `pnpm test -- tests/phase-12.6/ability-budget-per-race.spec.ts` | ❌ W0 | ⬜ pending · **DATA BLOCKER** |
| 12.6-04-xx | 04 (P5 LevelProgressionRow + scan surface) | 1 (parallel with A1a) | PROG-04 R5 | — | 20-row scan fits 1280×800 | RTL (hybrid CSS+DOM) | `pnpm test -- tests/phase-12.6/level-progression-scan.spec.tsx` | ❌ W0 | ⬜ pending |
| 12.6-05-xx | 05 (P5 expanded-row host swap + legality) | 2 (serial on 04) | PROG-04 R6 | — | edit-in-place + legality diff + G1 locked | RTL | `pnpm test -- tests/phase-12.6/level-progression-scan.spec.tsx` | ❌ W0 | ⬜ pending |
| 12.6-06-xx | 06 (level-rail.tsx deletion + consumer scrub) | 2 (serial on 04+05) | PROG-04 R5 | — | zero dangling imports | typecheck + grep + RTL migration | `pnpm test && pnpm typecheck && rg 'level-rail' apps packages tests` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/phase-12.6/point-buy-snapshot-coverage.spec.ts` — stubs for ATTR-01 R1
- [ ] `tests/phase-12.6/ability-budget-per-race.spec.ts` — stubs for ATTR-01 R2 + R3 + R4
- [ ] `tests/phase-12.6/attributes-board-fail-closed.spec.tsx` — stubs for ATTR-01 R3 (RTL)
- [ ] `tests/phase-12.6/level-progression-scan.spec.tsx` — stubs for PROG-04 R5 + R6
- [ ] `vitest.config.ts` — add `['tests/phase-12.6/**/*.spec.tsx', 'jsdom']` to `environmentMatchGlobs`
- [ ] `.planning/phases/12.6-uat-04-20-residuals/12.6-RESEARCH.md` § Validation Architecture — already committed (8a3f41c)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Atributos board renders fail-closed copy + disabled +/- for race lacking snapshot entry (visual match vs UI-SPEC tokens) | ATTR-01 R3 | RTL asserts DOM + copy + disabled, but visual fidelity (border/accent/spacing) needs live browser check | Run `pnpm dev`, choose race `class:harper` or any race absent from snapshot, observe AttributesBoard renders `Curva punto-compra no disponible para {raceLabel}` callout + all 6 `+` + all 6 `-` buttons disabled + budget line hidden. |
| 20-row scan list fits 1280×800 without vertical scroll on list container | PROG-04 R5 | jsdom cannot compute layout (`scrollHeight === clientHeight === 0`); RTL uses hybrid CSS+DOM proxy | Run `pnpm dev`, resize viewport to 1280×800, confirm `.level-progression__list` contains 20 rows visible without scroll. Browser DevTools: `document.querySelector('.level-progression__list').scrollHeight === document.querySelector('.level-progression__list').clientHeight`. |
| Click row → inline expand (no layout jank, no modal) | PROG-04 R6 | Same jsdom limitation on height transitions | Browser: click rows L1..L20 in sequence, confirm each expands in-place without scroll reset, non-active rows stay compact. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
