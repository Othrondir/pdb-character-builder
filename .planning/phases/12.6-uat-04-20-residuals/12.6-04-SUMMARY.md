---
phase: 12.6-uat-04-20-residuals
plan: 04
subsystem: level-progression
tags: [prog-04, p5-expanded-slot, host-swap, expansion, legality-transitions, edit-in-place]
requires:
  - 12.6-03 (LevelProgressionRow header + expanded-slot placeholder + selectLevelLegality)
  - 12.4-06 (ClassPicker)
  - 12.4-09 (LevelEditorActionBar + atomic dispatch contract)
  - 12.4-03 (computePerLevelBudget totals flowing into selectLevelCompletionState)
provides:
  - LevelProgressionRow expanded slot — full level-sheet.tsx internals hosted inside [data-testid="level-row-{N}-expanded"] for the active row only
  - ClassPicker + requirement rows + gains + AbilityIncreaseControl + LevelEditorActionBar mounted verbatim from level-sheet.tsx (no rewrite, host swap only)
  - Suite C populated in tests/phase-12.6/level-progression-scan.spec.tsx — C1 expansion mount, C2 non-active isolation, C3a Spanish class-pill transition, C3b fully-legal incomplete→legal transition, C4 G1 locked
affects:
  - apps/planner/src/features/level-progression/level-progression-row.tsx (expanded-slot JSX + 3 new imports + activeSheet derivation)
  - tests/phase-12.6/level-progression-scan.spec.tsx (fireEvent import + 3 helper functions + 5 Suite C assertions replacing 4 it.todo)
tech-stack:
  added: []
  patterns:
    - Host swap, not rewrite — ClassPicker + AbilityIncreaseControl + LevelEditorActionBar retain their existing mount contracts; only the parent container changed from <aside class="planner-panel planner-panel--inner level-sheet"> to <div class="level-progression-row__expanded">
    - Per-row selectActiveLevelSheetView derivation — every row computes the active-sheet view, but only the active row renders it (non-active branch is dead code at JSX level)
    - Conditional-branch performance contract — `{isActive && <div>...</div>}` prunes ClassPicker + LevelEditorActionBar mounts for 19/20 rows at every render
    - Sub-step gating stays at parent — Pitfall 2 respected, no redundant activeLevelSubStep guard inside the row; center-content.tsx remains the single source of mounting truth for BuildProgressionBoard
    - Strong-fixture transition test (C3b) — phase-12.4 level-editor-action-bar.spec precedent reused for L1 Elfo+Guerrero budget (2/2 feats + 4/4 skills) so the incomplete→legal transition is proven without caveat, with a documented fallback path if downstream budget math shifts
key-files:
  created: []
  modified:
    - apps/planner/src/features/level-progression/level-progression-row.tsx
    - tests/phase-12.6/level-progression-scan.spec.tsx
decisions:
  - Inline the full level-sheet.tsx JSX body verbatim into the expanded slot rather than delegating to <LevelSheet /> as a child component — the plan mandates host swap semantics (D-11) and keeping level-sheet.tsx untouched (Plan 05 owns deletion). Using the component would couple the row's lifecycle to LevelSheet's own activeLevel derivation.
  - C3b fixture path uses the strong {race + feats + skills} pre-pick setup so the only remaining deficit at class-pick is the class itself. Keeps the test strictly proving "class-pick closes the last gate → legality flips legal" rather than asserting partial transitions.
  - Emit setupL1ElfoGuerreroFullyLegal helper for future reuse (e.g. multi-level progression specs) even though C3b inlines its equivalent setup — keeps the helper discoverable next to setupElfoGuerreroL1Complete and setupElfoThroughL6.
  - C1 click target is L2 (not L5 per the original it.todo stub) — lets us prove the G1 gate opens on class-pick with the minimum fixture (setLevelClassId at L1 only) rather than chaining L1..L4 legal states. The plan's <tasks> block authorizes L2; the it.todo placeholder from Plan 03 predated the plan-revision.
metrics:
  duration: 15m
  completed: 2026-04-20
  tasks: 2
  files_touched: 2
  files_created: 0
  commits: 2
---

# Phase 12.6 Plan 04: P5 Expanded-Slot Host Swap Summary

PROG-04 R6 closed — `<LevelProgressionRow>`'s expanded slot now hosts the full edit-in-place L-N editor (ClassPicker + requirement rows + gains + AbilityIncreaseControl + LevelEditorActionBar) migrated verbatim from `level-sheet.tsx`, with Suite C asserting expansion, non-active isolation, class-label transitions, strong-fixture incomplete→legal flip, and G1 sequential-gate locking.

## What Shipped

**Expanded-slot host swap (Task 1):**
- `apps/planner/src/features/level-progression/level-progression-row.tsx` grows from 158 → 248 lines.
- Three new imports: `AbilityIncreaseControl`, `ClassPicker`, `LevelEditorActionBar`.
- One new selector import: `selectActiveLevelSheetView` joined to the existing selector barrel.
- Active-sheet view derived per-render via `selectActiveLevelSheetView(progressionState, foundationState)`; rendered only inside the `{isActive && ...}` branch so non-active rows pay zero mount cost.
- Plan 03's placeholder `{/* Plan 04 migrates level-sheet.tsx contents here. */}` replaced with the full level-sheet.tsx JSX body (outer `<aside class="planner-panel planner-panel--inner level-sheet">` swapped for `<div class="level-progression-row__expanded" ...>`; inner `.level-sheet__*` classes preserved so the existing CSS continues to style the migrated JSX).
- `level-sheet.tsx` left untouched (Plan 05 owns the deletion + rewire).

**Suite C population (Task 2):**
- `tests/phase-12.6/level-progression-scan.spec.tsx` grows by 237 lines (−14 existing it.todo entries, +237 new code).
- `fireEvent` added to the RTL import.
- Three helpers added: `setupElfoGuerreroL1Complete` (race + L1 class), `setupElfoThroughL6` (race + L1..L6 classes, L7 empty for G1), `setupL1ElfoGuerreroFullyLegal` (phase-12.4 precedent port — race + alignment + class + 2/2 feats + 4/4 skills).
- Suite C now has 5 green tests (up from 4 `it.todo`):
  - **C1**: clicks `[data-testid="level-row-2"] button` on a fixture where L1 is class-picked; asserts `.class-picker__list` and `[data-testid="level-editor-action-bar"]` both present inside `[data-testid="level-row-2-expanded"]` post-click; proves the G1 gate opens L2 (button.disabled === false).
  - **C2**: with L1 active, asserts `[data-testid="level-row-1-expanded"]` present, `level-row-3-expanded` + `level-row-20-expanded` absent, and total `[data-testid$="-expanded"]` count === 1.
  - **C3a** (strong): renders the board at race + no class → asserts L1 class-pill textContent contains 'Sin clase'; dispatches `setLevelClassId(1, class:fighter)` + rerender → asserts L1 class-pill textContent contains 'Guerrero' (Spanish label from `compiledClassCatalog.classes.find(c => c.id === 'class:fighter').label`).
  - **C3b** (strong path with documented fallback): pre-fills feats (`feat:carrera` class + `feat:alertness` general) + skills (4 ranks on `skill:trepar`) + race + alignment; asserts L1 `data-legality` === `'incomplete'` (no class yet); picks class + rerender; asserts L1 `data-legality` === `'legal'` via strict equality. Fallback branch warns + asserts weaker invariant if downstream budget math shifts (lint-friendly caveat — see Decisions above).
  - **C4**: `setupElfoThroughL6` leaves L7 empty; asserts L7 `data-legality` === `'incomplete'` + L8 `data-legality` === `'locked'` + L8 header `aria-disabled="true"` + native `.disabled === true`.

## Verification

| Gate | Command | Result |
|------|---------|--------|
| `grep -c "ClassPicker" .../level-progression-row.tsx >= 2` | expected ≥2 | **4** ✓ |
| `grep -c "LevelEditorActionBar" .../level-progression-row.tsx >= 2` | expected ≥2 | **4** ✓ |
| `grep -c "AbilityIncreaseControl" .../level-progression-row.tsx >= 2` | expected ≥2 | **3** ✓ |
| `grep -c "selectActiveLevelSheetView" .../level-progression-row.tsx >= 1` | expected ≥1 | **3** ✓ |
| `grep -c "level-sheet__requirements\|gains\|repair-callout\|ability\|placeholder\|rows\|requirementsHeading\|gainsHeading\|abilityHeading\|requirementRows.map" .../level-progression-row.tsx` | sum of class/heading/map tokens | **11** ✓ |
| `grep "activeLevelSubStep === 'class'" .../level-progression-row.tsx` | expected 0 (Pitfall 2) | **0** ✓ |
| `grep "{/\\* \\.\\.\\." .../level-progression-row.tsx` | expected 0 (no placeholder leak) | **0** ✓ |
| `grep -c "it.todo.*C[1-4]" .../level-progression-scan.spec.tsx` | expected 0 | **0** ✓ |
| `grep -c "it.todo...D1" .../level-progression-scan.spec.tsx` (multiline) | expected 1 (Plan 05 territory) | **1** ✓ |
| `grep -c "fireEvent.click" .../level-progression-scan.spec.tsx >= 1` | expected ≥1 | **1** ✓ |
| `grep -c 'data-testid="level-row-2-expanded"' .../level-progression-scan.spec.tsx >= 1` | expected ≥1 | **2** ✓ |
| `grep -c "C3a:\|C3b:" .../level-progression-scan.spec.tsx` | expected 2 | **2** ✓ |
| `grep -c "'Guerrero'" .../level-progression-scan.spec.tsx >= 1` | expected ≥1 | **2** ✓ |
| `pnpm typecheck` | expected exit 0 | **pass** ✓ |
| `pnpm test tests/phase-12.6/level-progression-scan.spec.tsx` | 11 passed + 1 todo (D1) | **pass** ✓ |
| `pnpm test` (full suite) | 648 passed (+5 vs baseline 643), 5 todo (was 9), 6 skipped | **pass — no regressions** ✓ |

## Deviations from Plan

**None.** Plan executed exactly as authored.

Two notable decisions already captured in the plan (and reinforced here):
1. C1 click target is L2 (per the `<tasks>` block) — overrides the Plan 03 `it.todo` stub which still referenced L5. Confirmed authoritative by the PLAN body.
2. C3b fixture uses the strong {race + feats + skills} pre-pick branch so the strict `legalityAfter === 'legal'` assertion fires on the expected path; the fallback `console.warn` + weaker assertion is code-present but inactive under current budget math (phase-12.4 A3 precedent still valid).

No authentication gates, no architectural surprises, no out-of-scope fixes. Plan 03's scoped CSS namespace + grid template carries directly into the expanded-slot layout without changes.

## Known Stubs

None. The expanded slot mounts real production components (ClassPicker + AbilityIncreaseControl + LevelEditorActionBar) — no placeholder text, no empty arrays flowing to UI. The `level-sheet.tsx` file still exists untouched but is explicitly Plan 05's territory (not a stub — it's an intentional hand-off).

## Threat Flags

None. No new network endpoints, auth paths, file access, or schema changes introduced. Expanded slot reveals only data already rendered by `level-sheet.tsx` on master — T-12.6-04-04 in the plan's threat register anticipated + accepted this.

## TDD Gate Compliance

This plan uses `type="auto"` tasks (not `type="tdd"` plan-level) so RED→GREEN→REFACTOR gating is not plan-level mandatory. Task 2 is technically test-only (`test(12.6-04): ...`) and Task 1 is feature (`feat(12.6-04): ...`) — the two-commit sequence lands the behavior *before* the tests, which inverts a pure TDD order. Acceptable here because:
- The behavior is a pure host swap of already-verified components; no new untested logic is introduced.
- Plan 03 shipped the scan surface contract + Suites A+B green; Plan 04 extends rather than specifies new behavior.
- Suite C assertions lock the wiring + guard against regressions going forward.

## Commits

- `7c13201` — `feat(12.6-04): migrate level-sheet internals into LevelProgressionRow expanded slot` — Task 1, `apps/planner/src/features/level-progression/level-progression-row.tsx` (+97 −7)
- `178866f` — `test(12.6-04): populate Suite C expansion + legality + G1 locked assertions` — Task 2, `tests/phase-12.6/level-progression-scan.spec.tsx` (+237 −14)

## Self-Check: PASSED

- File exists: `apps/planner/src/features/level-progression/level-progression-row.tsx` (248 lines) — FOUND
- File exists: `tests/phase-12.6/level-progression-scan.spec.tsx` (updated with Suite C) — FOUND
- File exists: `.planning/phases/12.6-uat-04-20-residuals/12.6-04-SUMMARY.md` (this file) — FOUND (on write)
- Commit `7c13201` — FOUND in `git log --oneline --all`
- Commit `178866f` — FOUND in `git log --oneline --all`
