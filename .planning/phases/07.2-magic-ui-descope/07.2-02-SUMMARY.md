---
phase: 07.2-magic-ui-descope
plan: 02
subsystem: descope
tags: [descope, magic, feats, catalog, extractor, uat]
requires:
  - phase: 07.2-magic-ui-descope
    provides: magic runtime purge, extractor-magic-flag-gated, 3-tab-character-sheet, 3-substep-level-chips
provides:
  - feat-catalog-no-min-spell-level
  - phase-07-deferred-items-archived
  - phase-07.2-human-verified-cold-load
  - magic-free-production-bundle
affects:
  - packages/data-extractor/src/contracts/feat-catalog.ts
  - packages/data-extractor/src/assemblers/feat-assembler.ts
  - apps/planner/src/data/compiled-feats.ts
  - .planning/phases/07-magic-full-legality-engine/deferred-items.md
tech-stack:
  added: []
  patterns:
    - "Magic-free bundle verification: tight symbol + copy-key greps (aggregateMagicLegality|MagicBoard|MagicSheet|classHasCastingAtLevel|computeCasterLevelByClass|getMaxSpellLevelFromBuildState and stepper.levelSubSteps.spells|stepper.sheetTabs.spells|stepper.stepTitles.spells|sections.spells) instead of broad Spanish-text greps — Spanish prose 'magia'/'conjuros' inside legitimate feat/class descriptions must ship."
key-files:
  created:
    - .planning/phases/07.2-magic-ui-descope/07.2-02-SUMMARY.md
  modified:
    - packages/data-extractor/src/contracts/feat-catalog.ts
    - packages/data-extractor/src/assemblers/feat-assembler.ts
    - apps/planner/src/data/compiled-feats.ts
    - .planning/phases/07-magic-full-legality-engine/deferred-items.md
key-decisions:
  - "Phase 07.2 Check 3 grep (Magia|Conjuros|aggregateMagicLegality|MagicBoard|MagicSheet) is too broad — it matches legitimate Spanish feat/class description prose (Paladin, Ranger, Bard, Wizard 'Libro de conjuros', etc.). Magic-free verification uses tighter symbol + copy-key greps instead; Spanish description text ships unchanged."
  - "Hechicero (Sorcerer) caster class listed in the Plan 07.2-02 UAT script is absent from the current compiled-classes catalog; the UAT substituted Mago (Wizard) as the full-caster path. Both exercise the same code branches (chips filter + Gains panel render) and both confirm zero Magia chip."
requirements-completed: [FLOW-01, FLOW-02, LANG-01]
metrics:
  duration: ~15m (Task 1 ~3m data cleanup + Task 2 ~2m archival + Task 3 ~10m automated checks + human UAT)
  tasks_completed: 3
  files_modified: 4
  completed: "2026-04-17T18:30:00Z"
---

# Phase 07.2 Plan 02: Catalog Cleanup + Human-Verified UAT Summary

**Dropped dangling `minSpellLevel` field from feat catalog contract/assembler/compiled data, archived Phase 07 `deferred-items.md` with a DESCOPED trailer, and closed the phase on a human-verified cold-load UAT (build + vitest + bundle magic-free + 3 class paths walked in Chrome) — planner ships with zero magic UI surface.**

## Performance

- **Duration:** ~15 min total (Task 1 data cleanup, Task 2 archival doc edit, Task 3 automated checks + orchestrator-driven browser UAT)
- **Completed:** 2026-04-17
- **Tasks:** 3 (2 auto + 1 human-verify checkpoint)
- **Files modified:** 4 (3 source/data + 1 archival doc)

## Accomplishments

- `minSpellLevel` removed atomically from the feat catalog Zod schema, the assembler writer, and 33 entries in the compiled catalog — one data surface, one commit, parse still green.
- Phase 07 `deferred-items.md` preserved with a clear DESCOPED trailer pointing forward at Phase 07.2 SUMMARY files — archival history intact, future reader knows the open item (empty spell descriptions) is no longer relevant to the shipping planner.
- Full verification stack (production build, vitest 259/259, dev-server boot, human cold-load UAT across 3 class paths) confirms the Phase 07.2 goal is met end-to-end.
- Magic-free bundle verification strengthened: 0 matches for magic UI symbols (`aggregateMagicLegality`, `MagicBoard`, `MagicSheet`, `classHasCastingAtLevel`, `computeCasterLevelByClass`, `getMaxSpellLevelFromBuildState`) and 0 matches for magic copy keys (`stepper.levelSubSteps.spells`, `stepper.sheetTabs.spells`, `stepper.stepTitles.spells`, `sections.spells`) in the production bundle.

## Task Commits

Each task was committed atomically:

1. **Task 1: Drop `minSpellLevel` from feat catalog schema, assembler, and compiled data** — `a23f6d9` (data)
   - `data(07.2-02): drop minSpellLevel from feat catalog schema, assembler, and compiled catalog`
   - Deleted: 1 schema line + 2 assembler lines + 33 compiled-feats entries (36 LOC net removal, 1 field surface gone)
2. **Task 2: Archive Phase 07 deferred-items.md with DESCOPED trailer** — `ca2533b` (docs)
   - `docs(07.2-02): archive Phase 07 deferred-items with DESCOPED trailer`
   - Appended `## DESCOPED — 2026-04-17 (Phase 07.2: magic-ui-descope)` section, preserved prior 07-01 and 07-05 discovery sections.
3. **Task 3: Final verification — build + vitest + human UAT** — checkpoint (no source commit)
   - Build, vitest, bundle grep, and human cold-load walk all verified green. See "Verification Output" below.

**Plan metadata commit:** this SUMMARY.md + STATE.md + ROADMAP.md landed together as `docs(07.2-02): complete catalog cleanup + human-verified UAT for magic UI descope`.

## Files Created/Modified

- `packages/data-extractor/src/contracts/feat-catalog.ts` — Dropped `minSpellLevel: z.number().int().nonnegative().nullable().optional(),` from the prerequisites schema.
- `packages/data-extractor/src/assemblers/feat-assembler.ts` — Dropped the `parseIntOrNull(row.MINSPELLLVL)` read + the `if (minSpellLevel != null && minSpellLevel > 0) prerequisites.minSpellLevel = ...` writer pair (lines 272-273).
- `apps/planner/src/data/compiled-feats.ts` — `sed`-deleted 33 `"minSpellLevel":` entries from the hand-compiled catalog (before: 33 occurrences; after: 0).
- `.planning/phases/07-magic-full-legality-engine/deferred-items.md` — Appended a DESCOPED trailer section so the Phase 07 archival record points forward at Phase 07.2 SUMMARY files.

## Decisions Made

- **Tightened Check 3 grep as a deviation:** The plan's original grep (`Magia|Conjuros|aggregateMagicLegality|MagicBoard|MagicSheet`) returned 48 hits on the production bundle, but every hit was inside legitimate Spanish prose on feat or class descriptions (e.g. Paladin/Ranger/Bard flavor, Wizard's "Libro de conjuros" gain line, "Conjuros penetrantes" feat name, "Magia Urdimbre Sombria"). These are shipping product content — Puerta de Baldur server feats and classes name-check spell concepts even when the planner doesn't let you pick spells. The tighter magic-UI symbol grep + copy-key grep both return 0 on the bundle, proving magic UI chrome is provably gone. Decision: ship the broad-grep non-match as a deviation, document the substitution, and add the tight-grep pattern to the Summary for future phases.
- **Hechicero substitution in UAT:** The plan's Check 4 Step 5 called for exercising the caster filter with Hechicero (Sorcerer). Current `compiled-classes.ts` does not include Hechicero (likely because the extractor snapshot the planner runs against doesn't ship that class tag). The UAT substituted Mago (Wizard) as the full-caster path — same code branches (D-02 filter, Gains panel render), same assertion. This is a catalog/test-coverage note for later, not a Phase 07.2 regression.

## Deviations from Plan

### Documented Deviations

**1. [Plan text vs. actual data] Overly-broad Check 3 grep**
- **Found during:** Task 3, Check 3 on bundle `apps/planner/dist/assets/index-UYYjZCBH.js`.
- **Issue:** Plan prescribed `grep -rn "Magia\|Conjuros\|aggregateMagicLegality\|MagicBoard\|MagicSheet" apps/planner/dist/` expecting 0. Actual count: 48 — but every hit was legitimate Spanish prose on feat/class content ("Conjuros penetrantes", "Libro de conjuros", "Magia Urdimbre Sombria", Paladin/Ranger/Bard flavor). These ship as valid content.
- **Fix:** Executed tighter greps for magic UI *symbols* (`aggregateMagicLegality|MagicBoard|MagicSheet|classHasCastingAtLevel|computeCasterLevelByClass|getMaxSpellLevelFromBuildState`) and magic *copy keys* (`stepper.levelSubSteps.spells|stepper.sheetTabs.spells|stepper.stepTitles.spells|sections.spells`). Both return 0 on the bundle. Magic UI chrome is provably gone.
- **Files modified:** None — this was a verification-step deviation, not a source fix.
- **Verification:** 0-match count from both tighter greps + human-UAT confirmation that no Magia chip / Conjuros tab / Magia panel renders on any class path.

**2. [Catalog coverage] Hechicero absent from compiled-classes**
- **Found during:** Task 3, Check 4 Step 5 (user-facing class list in the shell).
- **Issue:** Plan UAT script asked to test Hechicero (Sorcerer) as the full-caster path; the current compiled-classes catalog doesn't include Hechicero.
- **Fix:** Substituted Mago (Wizard) as the full-caster path — same code branches (D-02 filter removal, Gains panel render), same assertion scope (no Magia chip on a full-caster).
- **Files modified:** None.
- **Verification:** Mago path verified in the Chrome UAT — chips = `Clase / Habilidades / Dotes`, no Magia chip, Gains panel shows "Dado de golpe d4 / Libro de conjuros" (class flavor, not UI chrome).

---

**Total deviations:** 2 verification-level (grep precision + catalog substitution). Zero scope creep; zero source changes beyond the plan's declared surface.

**Impact on plan:** Neither deviation weakens the Phase 07.2 success criteria. The tighter grep is strictly *more* precise than the plan's broader one (false positives filtered out, true negatives confirmed). The Hechicero→Mago swap exercises the same code paths. Future Phase 06 / catalog work may want to re-surface Hechicero; logged under "Next Phase Readiness" below.

## Issues Encountered

None. The two deviations above are verification-step precision notes, not execution blockers.

## Verification Output

### Task 1 verify: minSpellLevel fully removed

```
grep -c 'minSpellLevel' apps/planner/src/data/compiled-feats.ts                         → 0
grep -c 'minSpellLevel' packages/data-extractor/src/contracts/feat-catalog.ts           → 0
grep -c 'minSpellLevel' packages/data-extractor/src/assemblers/feat-assembler.ts        → 0
```

Before: 33 + 1 + 2 = 36 occurrences. After: 0. Commit `a23f6d9` shows 37-line deletion (includes surrounding whitespace).

### Task 2 verify: DESCOPED trailer present

```
grep -q "^## DESCOPED — 2026-04-17 (Phase 07.2: magic-ui-descope)$" .planning/phases/07-magic-full-legality-engine/deferred-items.md   → exit 0
```

Archival record preserved: prior 07-01 and 07-05 sections unchanged; new trailer appended.

### Task 3 Check 1: Production build

```
pnpm --filter @nwn1-character-builder/planner build
→ Exit 0
→ Built in 454ms
→ apps/planner/dist/assets/index-UYYjZCBH.js      2,095,959 bytes ( 2095.95 kB / 285.57 kB gzip)
→ apps/planner/dist/assets/index-CUUin9_o.css        35,376 bytes (   35.37 kB /   5.55 kB gzip)
→ 0 compiled-spells-*.js chunks emitted
→ 0 compiled-domains-*.js chunks emitted
```

Bundle contains exactly one JS chunk and one CSS chunk + font assets. Phase 07 compiled catalogs are gone at build time.

### Task 3 Check 2: Full vitest

```
pnpm vitest run --reporter=dot
→ Exit 0
→ Test Files: 42
→ Tests: 259 passed
→ Duration: 6.80s
```

All 259 tests pass. This is the main-branch run (worktree-hoist node_modules issue from Plan 07.2-01 no longer applies — this plan executes on main working tree).

### Task 3 Check 3: Magic-free bundle (tightened)

Plan-prescribed broad grep:
```
grep -rn "Magia\|Conjuros\|aggregateMagicLegality\|MagicBoard\|MagicSheet" apps/planner/dist/ | wc -l   → 48
```
All 48 hits are legitimate Spanish feat/class description prose (Puerta de Baldur server content). See "Decisions Made" for rationale.

Tightened magic-UI symbol grep:
```
grep -rn "aggregateMagicLegality\|MagicBoard\|MagicSheet\|classHasCastingAtLevel\|computeCasterLevelByClass\|getMaxSpellLevelFromBuildState" apps/planner/dist/ | wc -l   → 0
```

Tightened magic copy-key grep:
```
grep -rn "stepper\.levelSubSteps\.spells\|stepper\.sheetTabs\.spells\|stepper\.stepTitles\.spells\|sections\.spells" apps/planner/dist/ | wc -l   → 0
```

Magic UI chrome is provably gone from the production bundle.

### Task 3 Check 4: Human cold-load UAT (orchestrator-driven, Claude-in-Chrome @ http://localhost:5174/)

1. **Cold-load hard-refresh:** No console errors (only `[vite] connected` + React DevTools hint).
2. **Origen sub-steps:** `Raza / Alineamiento / Atributos` — no Magia step.
3. **Origen walk:** Humano race + Legal neutral alignment + default attributes — legal build state.
4. **Level 1 chips (no class picked yet):** `Clase / Habilidades / Dotes` — no Magia chip.
5. **Class paths tested at Level 1:**
   - **Guerrero (non-caster):** chips `Clase/Habilidades/Dotes`, no Magia, Gains panel shows "Dado de golpe d10 / Competencias marciales".
   - **Mago (full caster, substituted for absent Hechicero):** chips `Clase/Habilidades/Dotes`, no Magia, Gains panel shows "Dado de golpe d4 / Libro de conjuros" (legitimate class flavor, not UI chrome).
   - **Clérigo (originally had domain tiles):** chips `Clase/Habilidades/Dotes`, no Magia, no domain panel, Gains panel shows "Dado de golpe d8 / Canalización divina".
6. **Page text counts:** `Magia` = 0, `Conjuros` = 0 (after Clérigo path) — the Spanish UI strings are gone.
7. **Character sheet tabs:** `Estadisticas / Habilidades / Dotes` — no Conjuros tab.
8. **No Magia panel renders anywhere across the 3 class paths.**

**UAT result:** All 5 checks PASSED.

## Mapping to Phase 07.2 ROADMAP Success Criteria

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | `apps/planner/src/features/magic/` deleted | `test ! -d` → OK (confirmed by Plan 07.2-01 + re-verified this plan) |
| 2 | Conjuros tab gone; sheet shows Estadísticas/Habilidades/Dotes | `character-sheet.tsx` renders 3 tab panels; UAT Step 7 confirmed in Chrome |
| 3 | No Magia sub-step for any class | `sections.ts` `LevelSubStep = 'class' \| 'skills' \| 'feats'`; UAT Step 4-5 confirmed in Chrome across Guerrero/Mago/Clérigo |
| 4 | `packages/rules-engine/src/magic/` deleted | `test ! -d` → OK (confirmed by Plan 07.2-01 + re-verified this plan) |
| 5 | compiled-spells.ts + compiled-domains.ts deleted | `test ! -f` → OK (confirmed by Plan 07.2-01 + re-verified this plan) |
| 6 | tests/phase-07 deleted | `test ! -d` → OK (confirmed by Plan 07.2-01 + re-verified this plan) |
| 7 | Copy keys + verifier script gone | `scripts/verify-phase-07-copy.cjs` absent (Plan 07.2-01); tightened copy-key grep (Check 3) returns 0 on the bundle |
| 8 | `PlannerValidationStatus.repair_needed` removed | Plan 07.2-01 Task 2 trimmed the union to 4 members; no non-magic consumer remained |
| 9 | Extractor spell/domain source preserved but disconnected | `packages/data-extractor/src/cli.ts:232` `EMIT_MAGIC_CATALOGS` env-flag gate; default `pnpm extract` emits 5 catalogs |
| 10 | Full vitest + build + dev server + UAT all green | Task 3 Checks 1-4 all PASSED (see Verification Output) |

All 10 success criteria verifiable as true.

## Known Stubs

None. This plan removes data and archives docs — no placeholders introduced.

## Threat Flags

None. Threat register (T-07.2-08 / T-07.2-09 / T-07.2-10) mitigations all confirmed:

- **T-07.2-08 (Tampering, compiled-feats.ts JSON integrity):** Mitigated — vitest imports and Zod-parses the compiled catalog as part of the 259-test run; zero failures → structural integrity preserved after the `sed` deletion.
- **T-07.2-09 (Information Disclosure, bundle magic strings):** Mitigated — tightened symbol + copy-key greps on `apps/planner/dist/` both return 0. Spanish-prose hits are legitimate shipping product content.
- **T-07.2-10 (DoS, dev-server boot failure):** Mitigated — Task 3 Check 4 boots dev server cleanly, console zero errors on cold hard-refresh.

## Next Phase Readiness

- **Phase 07.2 complete** — both plans landed, success criteria green, human UAT approved.
- **Phase 07.1 (narrow-viewport nav fix) can now proceed** — the phase execution order intentionally ran 07.2 before 07.1 so the drawer feature lands over a slimmer, magic-free shell.
- **Phase 6 / catalog debt note:** Hechicero (Sorcerer) is not present in the current compiled-classes catalog. If/when a future phase re-enables caster-class coverage or revisits the class list, extractor class-catalog extraction should confirm whether Hechicero is omitted by design (server config) or by extraction gap. Logged here rather than in `deferred-items.md` because it does not affect shipping planner correctness today.

## Self-Check: PASSED

- SUMMARY.md created at `.planning/phases/07.2-magic-ui-descope/07.2-02-SUMMARY.md` ✓
- Commit `a23f6d9` (Task 1, data cleanup) found in `git log` ✓
- Commit `ca2533b` (Task 2, archival trailer) found in `git log` ✓
- `apps/planner/src/data/compiled-feats.ts` contains zero `minSpellLevel` occurrences ✓
- `packages/data-extractor/src/contracts/feat-catalog.ts` contains zero `minSpellLevel` occurrences ✓
- `packages/data-extractor/src/assemblers/feat-assembler.ts` contains zero `minSpellLevel` occurrences ✓
- `.planning/phases/07-magic-full-legality-engine/deferred-items.md` ends with `## DESCOPED — 2026-04-17 (Phase 07.2: magic-ui-descope)` ✓
- Production build output exists at `apps/planner/dist/assets/index-UYYjZCBH.js` (2,095,959 bytes) ✓
- All 10 Phase 07.2 ROADMAP success criteria mapped to concrete evidence ✓
- Human UAT report acknowledged with "approved" (all 5 checks PASSED) ✓
