---
phase: 07.2-magic-ui-descope
plan: 01
subsystem: descope
tags: [descope, magic, shell, extractor, feats]
requires: []
provides:
  - 3-tab-character-sheet
  - 3-substep-level-chips
  - feat-prereq-no-spell-level
  - confirm-dialog-5-props
  - planner-validation-status-4-variants
  - extractor-magic-flag-gated
affects:
  - apps/planner/src/components/shell/character-sheet.tsx
  - apps/planner/src/components/shell/center-content.tsx
  - apps/planner/src/components/shell/level-sub-steps.tsx
  - apps/planner/src/lib/sections.ts
  - apps/planner/src/lib/copy/es.ts
  - apps/planner/src/components/ui/confirm-dialog.tsx
  - apps/planner/src/state/planner-shell.ts
  - apps/planner/src/features/feats/selectors.ts
  - packages/rules-engine/src/feats/feat-prerequisite.ts
  - packages/data-extractor/src/cli.ts
tech-stack:
  added: []
  patterns:
    - "EMIT_MAGIC_CATALOGS env-var gate preserves extractor assemblers behind an off-by-default flag"
key-files:
  deleted:
    - apps/planner/src/features/magic/ (11 files, ~1,840 LOC)
    - packages/rules-engine/src/magic/ (8 files, ~769 LOC)
    - apps/planner/src/data/compiled-spells.ts (13,604 LOC)
    - apps/planner/src/data/compiled-domains.ts (898 LOC)
    - tests/phase-07/ (14 files, ~1,526 LOC)
    - scripts/verify-phase-07-copy.cjs (91 LOC)
  modified:
    - apps/planner/src/components/shell/character-sheet.tsx
    - apps/planner/src/components/shell/center-content.tsx
    - apps/planner/src/components/shell/level-sub-steps.tsx
    - apps/planner/src/lib/sections.ts
    - apps/planner/src/lib/copy/es.ts
    - apps/planner/src/components/ui/confirm-dialog.tsx
    - apps/planner/src/state/planner-shell.ts
    - apps/planner/src/features/feats/selectors.ts
    - packages/rules-engine/src/feats/feat-prerequisite.ts
    - packages/data-extractor/src/cli.ts
    - tests/phase-06/feat-prerequisite.spec.ts
    - tests/phase-06/feat-revalidation.spec.ts
    - tests/phase-06/feat-eligibility.spec.ts
    - tests/phase-06/feat-proficiency.spec.ts
    - tests/phase-05.2/character-sheet.spec.tsx
    - tests/phase-02/shell-routes.spec.ts
decisions:
  - "PlannerValidationStatus reverts to pre-Phase-07 4-variant union (blocked/illegal/legal/pending); repair_needed was magic-only"
  - "ConfirmDialog reverts to 5-prop pre-07 shape; no children slot, no confirmDisabled, no disabled attribute on Aceptar"
  - "Extractor preserves assembleSpellCatalog/assembleDomainCatalog code behind EMIT_MAGIC_CATALOGS=1 env flag; default run emits 5 catalogs (classes/races/skills/feats/deities)"
  - "Feat prerequisite minSpellLevel branch + getMaxSpellLevelFromBuildState helper + 'spell-level' PrerequisiteCheck variant all deleted; caster-level map removed from BuildStateAtLevel"
  - "level prop retained on LevelSubSteps component despite no longer being used inside (tsconfig noUnusedParameters is off, caller contract unchanged)"
metrics:
  duration: 6m37s
  tasks_completed: 2
  files_deleted: 47
  files_modified: 16
  completed: "2026-04-17T17:44:34Z"
---

# Phase 07.2 Plan 01: Magic UI Descope Purge Summary

Removed the entire Phase 07 magic runtime surface (feature dir, rules-engine module, compiled spell/domain catalogs, phase-07 test suite) from the planner, then surgically reverted the 3 non-magic consumers that Phase 07 had coupled to the magic module (feat prerequisite spell-level check, PlannerValidationStatus.repair_needed, ConfirmDialog confirmDisabled+children) so the planner compiles and vitest is green with 223 tests passing.

## Deletion Log

| Path | Kind | Size |
|------|------|------|
| `apps/planner/src/features/magic/` | directory | 11 files (~1,840 LOC) — compiled-magic-catalog, domain-tile-grid, magic-board, magic-detail-panel, magic-sheet-tab, magic-sheet, selectors, spell-level-tabs, spell-row, store, swap-spell-dialog |
| `packages/rules-engine/src/magic/` | directory | 8 files (~769 LOC) — caster-level, catalog-fail-closed, domain-rules, index, magic-legality-aggregator, magic-revalidation, spell-eligibility, spell-prerequisite |
| `apps/planner/src/data/compiled-spells.ts` | file | 13,604 LOC |
| `apps/planner/src/data/compiled-domains.ts` | file | 898 LOC |
| `tests/phase-07/` | directory | 14 files (~1,526 LOC) — caster-level, catalog-fail-closed, center-content, domain-rules, magic-board, magic-legality-aggregator, magic-revalidation, magic-sheet-tab-validation, magic-sheet-tab, magic-store, paradigm-dispatch, sorcerer-catalog-coverage, spell-eligibility, spell-prerequisite specs |
| `scripts/verify-phase-07-copy.cjs` | file | 91 LOC |

**Total deleted:** 47 files, ~18,728 LOC removed from the codebase.

## Edit Log

### Task 1 — Purge magic runtime + copy namespace

| File | Delta |
|------|-------|
| `apps/planner/src/components/shell/character-sheet.tsx` | Dropped `MagicSheetTab` import + `{activeTab === 'spells' && <MagicSheetTab />}` JSX. Sheet renders 3 tab panels (stats/skills/feats) only. |
| `apps/planner/src/components/shell/center-content.tsx` | Dropped `MagicBoard` import + `case 'spells': return <MagicBoard />` branch. Sub-step switch now has exactly 3 cases. |
| `apps/planner/src/components/shell/level-sub-steps.tsx` | Removed `classHasCastingAtLevel` import, `useLevelProgressionStore` import + `progressionState` call, and the D-02 filter block. JSDoc rewritten. Now renders `levelSubSteps` directly (3 chips). |
| `apps/planner/src/lib/sections.ts` | `LevelSubStep` = `'class' \| 'skills' \| 'feats'` (3 members). `SheetTab` = `'stats' \| 'skills' \| 'feats'` (3 members). `{ id: 'spells', ... }` entries removed from both arrays. |
| `apps/planner/src/lib/copy/es.ts` | Deleted the entire top-level `magic: { ... }` object (52 lines). Deleted `spells: 'Magia'` from `stepper.levelSubSteps`, `spells: 'Conjuros'` from `stepper.sheetTabs`, `spells: 'Selecciona los conjuros del nivel'` from `stepper.stepTitles`, and the entire `sections.spells` block. |

### Task 2 — Detach feats + revert primitives + disconnect extractor

| File | Delta |
|------|-------|
| `packages/rules-engine/src/feats/feat-prerequisite.ts` | `BuildStateAtLevel.casterLevelByClass` field + JSDoc deleted. `'spell-level'` removed from `PrerequisiteCheck.type` string-literal union. `getMaxSpellLevelFromBuildState` function + JSDoc deleted. `if (prereqs.minSpellLevel != null && prereqs.minSpellLevel > 0) { ... }` branch deleted from `evaluateFeatPrerequisites`. |
| `apps/planner/src/features/feats/selectors.ts` | Deleted `import { computeCasterLevelByClass } from '@rules-engine/magic/caster-level';`. Deleted "Phase 7 Pitfall 4" comment block + the `const casterLevelByClass = computeCasterLevelByClass(...)` call. Removed `casterLevelByClass,` from the `computeBuildStateAtLevel` return literal. |
| `apps/planner/src/components/ui/confirm-dialog.tsx` | Reverted to pre-07 5-prop shape. Dropped `children?: ReactNode` prop + JSDoc, `confirmDisabled?: boolean` prop + JSDoc, destructured `children` and `confirmDisabled` params, `{children}` JSX expression, `disabled={confirmDisabled}` attribute on Aceptar button, and `ReactNode` import. |
| `apps/planner/src/state/planner-shell.ts` | `PlannerValidationStatus` union trimmed from 5 members to 4: `'blocked' \| 'illegal' \| 'legal' \| 'pending'`. |
| `tests/phase-06/feat-prerequisite.spec.ts` | Removed `casterLevelByClass: {}` from `createBuildState` factory. |
| `tests/phase-06/feat-revalidation.spec.ts` | Same. |
| `tests/phase-06/feat-eligibility.spec.ts` | Same. |
| `tests/phase-06/feat-proficiency.spec.ts` | Same. |
| `tests/phase-05.2/character-sheet.spec.tsx` | `renders 4 tabs` → `renders 3 tabs`; `toHaveLength(4)` → `toHaveLength(3)`; removed `expect(tabs[3]).toHaveTextContent('Conjuros');`. Removed the trailing Conjuros-tab click + `/\d+ conjuros/` assertion from the tab-switching test. |
| `tests/phase-02/shell-routes.spec.ts` | **[Rule 1 auto-fix]** Updated `levelSubSteps.map(s => s.id)` and `sheetTabs.map(s => s.id)` assertions to 3-member arrays (`['class', 'skills', 'feats']`, `['stats', 'skills', 'feats']`) — test was asserting on the trimmed union members. |
| `packages/data-extractor/src/cli.ts` | Added `const EMIT_MAGIC_CATALOGS = process.env.EMIT_MAGIC_CATALOGS === '1';` guard near the top of `main()`. Wrapped the spell assembler try/catch (step 5/7) and the domain assembler try/catch (step 6/7) in `if (EMIT_MAGIC_CATALOGS)` blocks. Deleted the `spells` and `domains` entries from `CATALOG_CONFIGS`. Default run emits 5 catalogs; assemblers + helpers remain imported behind the flag. |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed phase-02 shell-routes.spec.ts 4-member array assertions**
- **Found during:** Task 2 Step 8 (`pnpm vitest run` red)
- **Issue:** `tests/phase-02/shell-routes.spec.ts:26-31` asserts `levelSubSteps.map(s => s.id).toEqual(['class', 'skills', 'feats', 'spells'])` and `sheetTabs.map(s => s.id).toEqual(['stats', 'skills', 'feats', 'spells'])`. Plan enumeration of phase-06 + phase-05.2 test edits did not include this phase-02 test, but the sections.ts array trim (Task 1 Step 5) broke the assertion.
- **Fix:** Trimmed both `toEqual` arrays to 3 members matching the new unions.
- **Files modified:** `tests/phase-02/shell-routes.spec.ts`
- **Commit:** `0fcb185`

## Verification Output

### Cross-cutting grep (runtime dirs only — `.planning/`, `.claude/`, `node_modules/` excluded)

```
# Pattern: casterLevelByClass|computeCasterLevelByClass|getMaxSpellLevelFromBuildState|repair_needed|
#          confirmDisabled|aggregateMagicLegality|from '@rules-engine/magic|from '@planner/features/magic
# Target:  apps/ packages/ tests/ scripts/
# Result:  No matches found
```

### Magic-specific grep

```
# Pattern: shellCopyEs\.magic|stepper\.levelSubSteps\.spells|stepper\.sheetTabs\.spells|
#          stepper\.stepTitles\.spells|sections\.spells
# Target:  apps/ packages/ tests/
# Result:  No runtime matches (only hits are in .planning/ archival documentation)
```

### Filesystem deletion checks

```
test ! -d apps/planner/src/features/magic                   → OK
test ! -d packages/rules-engine/src/magic                   → OK
test ! -f apps/planner/src/data/compiled-spells.ts          → OK
test ! -f apps/planner/src/data/compiled-domains.ts         → OK
test ! -d tests/phase-07                                    → OK
test ! -f scripts/verify-phase-07-copy.cjs                  → OK
```

### Test Suite Tally

`node_modules/.bin/vitest run --reporter=dot` (invoked via main repo binary from worktree cwd):

```
Test Files  3 failed | 39 passed (42)
     Tests  223 passed (223)
  Duration  6.06s
```

**All 223 tests pass.** The 3 "failed" test files all fail at *file load* (before any test function runs) with identical errors:

```
Error: Cannot find package 'better-sqlite3' imported from
'.../packages/data-extractor/src/readers/nwsync-reader.ts'
- tests/phase-05.1/assemblers-core.spec.ts
- tests/phase-05.1/assemblers-extended.spec.ts
- tests/phase-05.1/nwsync-reader.spec.ts
```

These 3 files depend on `better-sqlite3` (listed in `package.json → pnpm.onlyBuiltDependencies`) which lives in the main repo's hoisted `node_modules` but is NOT reachable from this worktree because pnpm's hoisted layout does not resolve through `.git/worktrees/*/`. Confirmed pre-existing environment issue unrelated to this plan — running the same 3 files from the main repo cwd returns `8 passed (8)` (87 tests green). Do NOT count as regression; these pass once the orchestrator merges the worktree back into the main branch where `node_modules` is reachable.

## Known Stubs

None. This plan removes code and does not introduce placeholders.

## Threat Flags

None. This plan reduces attack surface by deleting modules; no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries introduced.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | `6c2187e` | `feat(07.2-01): purge magic feature dir, rules-engine magic dir, compiled spell/domain catalogs, phase-07 tests, copy namespace` |
| 2 | `0fcb185` | `feat(07.2-01): detach feats from magic, revert ConfirmDialog + PlannerValidationStatus, disconnect extractor spell/domain emit` |

## Self-Check: PASSED

- SUMMARY.md created at `.planning/phases/07.2-magic-ui-descope/07.2-01-SUMMARY.md` ✓
- Commit `6c2187e` (Task 1) found in `git log` ✓
- Commit `0fcb185` (Task 2) found in `git log` ✓
- All 6 filesystem deletion targets confirmed gone ✓
- All 10 plan-declared edited files confirmed modified + 1 auto-fixed test file ✓
- Cross-cutting grep returns zero runtime hits for magic symbols ✓
- 223 / 223 tests pass (3 file-load failures are pre-existing worktree environment issue unrelated to this plan) ✓
