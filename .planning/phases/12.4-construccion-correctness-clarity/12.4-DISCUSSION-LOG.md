# Phase 12.4: Construcción Correctness & Clarity — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 12.4-construccion-correctness-clarity
**Mode:** default `discuss` (USER-PROFILE absent → ADVISOR_MODE=false; no `--auto`, no `--all`, no `--chain`)
**SPEC.md loaded:** yes (9 requirements locked, ambiguity 0.17)
**Pending todos matched:** 0
**Areas discussed:** Class picker layout + prereq, Dotes selectability + collapse, Feat-family secondary picker, Advance button shape (all four via "tú decides" → Claude defaults)

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Class picker layout + prereq evidence | R1 HOW: section vs tabs vs accordion; inline vs tooltip vs badge; threshold vs progress | ✓ (via "tú decides") |
| Dotes selectability + collapse-on-complete UX | R5 HOW: visual state treatment; collapse shape; re-expand mechanism | ✓ (via "tú decides") |
| Feat-family secondary picker mechanism | R7 HOW: modal vs inline expander vs nested column | ✓ (via "tú decides") |
| Advance button placement + action bar shape | R2 HOW: sticky footer vs inline vs floating; disabled feedback; rail coexistence | ✓ (via "tú decides") |

**User's choice:** `tú decides` — freeform note confirming Claude should apply recommended defaults across all 4 areas.

**Notes:** Pattern matches SPEC.md interview log (user also chose "tú decides" on all 6 SPEC rounds). Consistent delegation posture for this phase — user locked WHAT in SPEC and delegates HOW to Claude, respecting prior-phase canon (Phase 1 Spanish-first + manual-override chain, Phase 12.3 TDD atomic cadence, Phase 12.2-03 `BASE_CLASS_ALLOWLIST` fail-closed gate).

---

## D-01 — Class picker layout (R1)

| Option | Description | Selected |
|--------|-------------|----------|
| Single picker, two `<section>` blocks | Matches SPEC acceptance (a); single-scroll idiom | ✓ |
| Tabs (Básicas / Prestigio) | Hides half the roster behind a click | |
| Accordion (collapsible sections) | Adds state machinery for zero clarity win | |

**Rationale locked:** SPEC acceptance (a) already requires two `<section>` containers with `aria-labelledby`. Tabs + accordion conflict with that contract.

---

## D-02 — Class prestige prereq evidence (R1)

| Option | Description | Selected |
|--------|-------------|----------|
| Inline italic muted copy below row | Accessible to mobile + keyboard; self-describing | ✓ |
| Tooltip on hover | Hidden from non-pointer users | |
| Badge next to row | Low info density; SPEC wants full prereq copy visible | |
| Show progress (`BAB 3/5`) | Needs live BAB selector; polish work beyond 12.4 correctness | deferred |

**Rationale locked:** SPEC R1 acceptance demands inline prereq copy. Progress meter deferred to a future polish phase.

---

## D-03 — Dotes selectability visual treatment (R5)

| Option | Description | Selected |
|--------|-------------|----------|
| Opacity + cursor + inline copy + pill badge | Redundant accessibility: glance (opacity) + keyboard (badge) + SR (inline copy) | ✓ |
| Tooltip only | Hidden from non-pointer users | |
| Icon only | Low information density | |

**Rationale locked:** SPEC R5 forbids `display: none` (`blocked-*` rows must remain in DOM). Accessibility redundancy is the natural fit.

---

## D-04 — Dotes collapse-on-complete interaction (R5)

| Option | Description | Selected |
|--------|-------------|----------|
| Summary card + `Modificar selección` TextButton | Reduces visual load at completion; re-expand via named action | ✓ |
| Filtered list (chosen only) with expand button | Preserves list shape but loses "completion" signal | |
| Inline row per chosen + expand icon | Most compact; discoverability risk on expand affordance | |
| Animated height transition | Rejected — NWN1 UI feels static; instant swap matches aesthetic | deferred |

**Rationale locked:** SPEC R5 requires collapse with `Modificar selección` re-expand. No animation decision in SPEC → defer to polish if users flag.

---

## D-05 — Feat-family secondary picker (R7)

| Option | Description | Selected |
|--------|-------------|----------|
| Inline expander (fieldset + radio list) | Keeps context; reuses `.feat-picker__list` scroll idiom; Esc collapses, Enter confirms | ✓ |
| Modal overlay with focus trap | Overkill for 2-3-click scoped pick; breaks context | |
| Nested side column | Conflicts with R9 scroll relocation (list column already owns scroll) | |

**Rationale locked:** SPEC R7 allows "modal/expander" — Claude chose inline expander as least-intrusive. User can revisit in plan pass if desired.

---

## D-06 — `LevelEditorActionBar` shape (R2)

| Option | Description | Selected |
|--------|-------------|----------|
| Sticky footer inside `.level-sheet` | Mirrors Atributos `ActionBar` pattern; always visible | ✓ |
| Inline at end of sub-step list | Hidden on long sub-steps (no sticky) | |
| Floating action button | Covers rail content; mobile-first idiom feels off-brand for desktop planner | |
| Tooltip on disabled state | Rejected — button label is already self-describing (`Faltan {N} dotes...`) | |

**Rationale locked:** SPEC R2 dynamic-label contract demands visible affordance. Sticky footer matches existing idiom.

---

## D-07 — `per-level-budget` selector module layout (R3)

| Option | Description | Selected |
|--------|-------------|----------|
| Single module `per-level-budget.ts` | Co-locates feat-slot + skill-point math sharing level context | ✓ |
| Split into `feat-slots.ts` + `skill-points.ts` | Forces callers to import both every time | |
| Inline in `level-progression/selectors.ts` | Breaks rules-engine purity (selectors live in app layer) | |

**Rationale locked:** SPEC R3 locks purity + perf gate; module layout is HOW. Single file minimizes coupling footprint.

---

## D-08 — L1 neutral state + auto-seed removal (R6 + X1)

Not surfaced as a discrete gray area — SPEC R6 + X1 regression already lock behavior. Decision names predicate owners only; no alternatives to weigh.

---

## D-09 — Habilidades class/transclase split + cost copy (R4)

Not surfaced as a discrete gray area — SPEC R4 acceptance locks section + copy contract. Decision names exact cost-copy strings + 12px legibility floor.

---

## D-10 — Plan split + TDD wave strategy (meta)

Not surfaced via AskUserQuestion (exceeded 4-option cap on the gray area selection). Handled as Claude's discretion under the same "tú decides" delegation.

**Options considered:**

| Option | Description | Selected |
|--------|-------------|----------|
| 9 plans / 3 waves (atomic per requirement) | Mirrors 12.3 D-07 pattern; file-disjoint within wave | ✓ |
| 5 plans (bundle correctness into 2, UI into 2, extractor into 1) | Larger plans → higher blast radius per commit | |
| 3 phases-inside-phase | Over-hierarchy for a 9-requirement closure | |

**Rationale locked:** 12.3 shipped 6 atomic plans across 3 waves; 12.4 scales the same pattern to 9 requirements. Predictable, reviewable, revertable.

---

## Claude's Discretion

User selected "tú decides" on gray area selection. All 10 decisions (D-01..D-10) are Claude defaults following:

- Phase 1 canon chain: manual override > Puerta snapshot > NWN1 EE canon.
- Phase 2 Spanish-first copy.
- Phase 12.2-03 `BASE_CLASS_ALLOWLIST` + `PlannerClassKind`.
- Phase 12.3 D-02 atomic `activeLevelIndex` dispatch pattern.
- Phase 12.3 D-07 (plan split) + D-08 (TDD RED→GREEN + atomic commit cadence).
- SPEC.md 9 requirements + acceptance criteria verbatim.

---

## Deferred Ideas

- Extractor prereq enrichment (BAB / skill-rank / feat decoding from `PreReqTable`).
- Live prestige progress meter (`BAB 3/5` beneath disabled rows).
- Animated collapse transitions on Dotes summary card.
- Modal vs inline expander A/B for feat families.
- Custom content surfacing (milestone v1.1).
- Levels 17-20 expansion (milestone v1.1).
- Epic feats + epic bonus feats (milestone v1.1).
- Inventario / equipamiento / objects / armas / armaduras (future-milestone capability phase).
- Build comparador + presets + saved templates (backlog).
- Subrace emission from extractor (still deferred from Phase 12.1-02).

---

*Phase: 12.4-construccion-correctness-clarity*
*Discussion log written: 2026-04-19*
