---
phase: 12.4-construccion-correctness-clarity
verified: 2026-04-19
verifier: gsd-verifier (Opus 4.7)
verdict: PASS-WITH-NOTES
score: 9/9 requirements verified (F1..F8 + X1 closed at code level)
tests: 629 passed / 630 total (1 pre-existing DEF-12.4-02 — documented deferred)
typecheck: clean (apps/planner)
status_gate: PASS — roadmap + STATE.md consistent (9/9 plans; completed_plans=66; completed_phases=20)
deferred_preserved:
  - DEF-12.4-01  # worktree node_modules hydration (hydrated on master; non-blocker)
  - DEF-12.4-02  # pre-existing font-weight: 700 at apps/planner/src/styles/app.css:113
human_verification_required: true   # visual/interaction UAT of advance bar, collapse-on-complete, prestige gating copy at 1280×900
---

# Phase 12.4 Verification Report — Construcción Correctness & Clarity

**Phase goal (ROADMAP.md):** Close 8 cross-cutting UX + correctness defects (F1..F8) and 1 regression (X1) from UAT 2026-04-19 across the Construcción flow — base vs prestige class separation with evidence-based gating, per-level feat-slot + skill-point math surfaced as authoritative UI truth, dynamic advance affordance, Habilidades class/transclase split, Dotes four-state selectability + slot counter + collapse-on-complete, feat-family fold behind secondary picker, sentinel row filter, and Dotes list-owned scroll.

**Verdict:** `PASS-WITH-NOTES`. All 9 Success Criteria are materialized in code with RED→GREEN→docs TDD gates. Test suite is 629/630 (the sole failure is the pre-existing, explicitly documented DEF-12.4-02 font-weight:700 at `app.css:113`, *not* introduced by 12.4). Two scope-tracked limitations (Humano L1 race-bonus store capacity; @playwright/test absent → RTL fallback E2E) are documented transparently. Recommend proceeding to `/gsd-audit-milestone v1.0` with human UAT re-sweep.

---

## Success Criteria — Goal-Backward

| # | SC (ROADMAP)                                                         | Plan      | Status    | Evidence                                                                                                                                                                                                                  |
|---|----------------------------------------------------------------------|-----------|-----------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | Class picker base/prestige grouping + L1 prestige gate               | 12.4-06   | PASS      | `apps/planner/src/features/level-progression/class-picker.tsx` mounted at `level-sheet.tsx:28`; two `<section>` blocks (`Clases básicas`, `Clases de prestigio`); `reachableAtLevelN` at `packages/rules-engine/src/progression/prestige-gate.ts`; RTL spec `tests/phase-12.4/class-picker-grouping.spec.tsx` + fixture spec `prestige-gate.fixture.spec.ts` (17 assertions green). |
| 2 | Contextual advance button (`Continuar al nivel N+1` / deficit labels) | 12.4-09   | PASS      | `apps/planner/src/features/level-progression/level-editor-action-bar.tsx`; mounted at `level-sheet.tsx:89`; `L16 → return null` at `level-editor-action-bar.tsx:50`; `apps/planner/src/lib/copy/es.ts:195,197,199` carries the three label templates; atomic dispatch `setActiveLevelSubStep('class') → setActiveLevel(N+1) → setExpandedLevel(N+1)` at `level-editor-action-bar.tsx:68-70`; Unit spec 12/12 + E2E RTL fallback 1/1. |
| 3 | Per-level feat-slot + skill-point math (L1..L16) in UI               | 12.4-03 + 07 + 09 | PASS | `packages/rules-engine/src/progression/per-level-budget.ts::computePerLevelBudget` (pure — 0 `@data-extractor` imports); Vitest bench p99 ≈ 0.0046 ms (≈200× under 1ms gate); `tests/phase-12.4/per-level-budget.fixture.spec.ts` 28 assertions (Guerrero/Mago/Pícaro/Explorador/Monje/Humano L1..L16 + INT-bump lock); header counter `Dotes del nivel N: {chosen}/{slots}` rendered in `feat-board.tsx` via `boardView.counterLabel`. |
| 4 | Habilidades class/transclase section split + cost hint               | 12.4-05   | PASS      | `apps/planner/src/features/skills/skill-sheet.tsx` renders two `<section>` blocks (`Habilidades de clase · coste 1 pt/rango` / `Habilidades transclase · coste 2 pts/rango`); CSS `.skill-board__section-heading` + `.skill-board__cost-hint { font-size: 12px }` at `app.css:1893,1907`; belt-and-braces inline `style={{ fontSize: '12px' }}` for jsdom; `tests/phase-12.4/skill-board-split.spec.tsx` 10/10. |
| 5 | Dotes 4 selectability states + slot counter + collapse-on-complete   | 12.4-07   | PASS      | `FeatRowState = 'selectable' \| 'blocked-prereq' \| 'blocked-already-taken' \| 'blocked-budget'` in `apps/planner/src/features/feats/selectors.ts`; `<FeatSummaryCard>` at `apps/planner/src/features/feats/feat-summary-card.tsx`; visibility-lock CSS `.feat-picker__row--blocked { opacity: 0.55 }` + cascade-defeat `.feat-picker__row--blocked .feat-picker__reason { opacity: 1 }` in `app.css`; grep: **zero `display: none` on `feats/*` source files**; 15/15 tests `tests/phase-12.4/feat-selectability-states.spec.tsx`. |
| 6 | L1 sub-steps neutral until user affirms (X1 regression closure)      | 12.4-04   | PASS      | 3 predicates exported from `apps/planner/src/features/level-progression/selectors.ts` (`isClaseLevelComplete` / `isHabilidadesLevelComplete` / `isDotesLevelComplete`); `components/shell/level-sub-steps.tsx:62-66` replaces hardcoded `'complete'` default with `predicate ? 'complete' : 'ready'`; `'ready'` variant added to `StepperStepStatus`; `tests/phase-12.4/l1-neutral-substeps.spec.tsx` 12/12 (Suite A locks `classByLevel[1] === null` forward). |
| 7 | Parameterized feat families fold behind secondary picker             | 12.4-08   | PASS      | `parameterizedFeatFamilySchema` (optional+nullable, SHAR-05 invariant preserved) at `packages/data-extractor/src/contracts/feat-catalog.ts:53-60,68`; `apps/planner/src/features/feats/feat-family-expander.tsx` (inline `<fieldset>` + `role="radiogroup"` + auto-focus + Esc); `grep -c 'parameterizedFeatFamily' compiled-feats.ts = 230` (32 skill + 8 spell + 8 greater-spell + 59 weapon-focus + 58 weapon-spec + 65 greater-weapon); `feat:skill-focus`/`feat:weapon-focus`/`feat:spell-focus` as groupKey anchors appear 212 times in compiled-feats.ts; 11/11 tests. |
| 8 | Extractor + UI strip DELETED/UNUSED/PADDING sentinel rows            | 12.4-01   | PASS      | `packages/data-extractor/src/lib/sentinel-regex.ts::SENTINEL_REGEX` + `isSentinelLabel`; guards: feat-assembler 2, skill-assembler 2, class-assembler 2, race-assembler 2; UI belt-and-braces at `apps/planner/src/features/feats/selectors.ts` (2 sites); `grep -c '"label": "DELETED"' compiled-feats.ts = 0`; 20/20 tests `tests/phase-12.4/extractor-deleted-sentinel.spec.ts`. |
| 9 | Dotes scroll on list column                                           | 12.4-02 + 06 | PASS   | `.feat-picker__list { overflow-y: auto }` at `app.css:1622-1624`; scoped wrapper override `.feat-board .selection-screen__content { overflow-y: visible }`; `feat-sheet.tsx` applies `className="feat-picker__list"` on both `<OptionList>` invocations; `.class-picker__list { overflow-y: auto }` owned by 12.4-06; `.race-picker` delegates to `.option-list` (audit in SUMMARY); 5/5 tests `feat-picker-scroll.spec.tsx`. |

---

## UAT Defect Closure (F1..F8 + X1)

| UAT ID | Defect                                                                | Plan       | Commit(s)                                | Closure Evidence                                                                                                                                                                      |
|--------|-----------------------------------------------------------------------|------------|------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| F1     | Class picker intermingles base + prestige, no L1 gating               | 12.4-06    | `7805025` RED / `23b4338` GREEN / `6389abe` replay | `ClassPicker` two-section render + `reachableAtLevelN` fail-closed at L1; `class-picker.tsx` grep hits `class-picker__` 12×; test asserts all prestige rows disabled at L1.               |
| F2     | No advance affordance                                                 | 12.4-09    | `b1b896f` / `dff972c` / `adc13a0`        | `<LevelEditorActionBar>` + 3 label states; atomic dispatch locked by Suite B1; L16 null by Suite C1.                                                                                   |
| F3     | Per-level feat+skill math unclear                                     | 12.4-03    | `b374baa` / `0dda8e1`                    | `computePerLevelBudget` pure selector + `Dotes del nivel N: {chosen}/{slots}` header + `Puntos de habilidad: {spent}/{budget}` header; fixture locks Guerrero/Mago/Pícaro/Explorador/Monje + Humano L1×4+1 canon; bench p99 0.0046ms. |
| F4     | Habilidades does not separate class vs transclase                     | 12.4-05    | `31306ed` / `596b34b`                    | Two `<section>` blocks; cost hints ≥12px; grep: 4 files carry the split markers.                                                                                                        |
| F5     | Dotes lacks selectability clarity + no collapse-on-complete           | 12.4-07    | `8b38a51` / `fe60354`                    | 4 row states + visibility lock (blocked stays in DOM) + slot counter + `<FeatSummaryCard>` + `Modificar selección`; 15 tests green.                                                    |
| F6     | "Soltura" feat family row-per-variant instead of grouped              | 12.4-08    | `32ef57b` / `344778c`                    | 230 variants tagged across 6 families; one main-list row per family; inline expander `role="radiogroup"` with auto-focus + Esc; target-qualified id preserved in store.                 |
| F7     | DELETED feats render                                                  | 12.4-01    | `2c89215` / `52cffe4`                    | 4 assembler guards + UI belt-and-braces + compiled-feats.ts scrubbed (grep `"label": "DELETED"` = 0).                                                                                   |
| F8     | Dotes scroll on description column, not list                          | 12.4-02    | `fe160e5` / `e27306a`                    | `.feat-picker__list { overflow-y: auto }` + scoped `.feat-board` override; jsdom `getComputedStyle` + CSS-text regex dual-verified.                                                      |
| X1     | Sub-steps show ✓ on entry before user interaction                     | 12.4-04    | `432fd1b` / `573da51`                    | Hardcoded `'complete'` default at `level-sub-steps.tsx:31` removed; predicates wire `claseComplete / habilidadesComplete / dotesComplete`; Suite A forever locks `classByLevel[1]=null`. |

---

## Regression Locks Preserved

- **Rules-engine framework purity (12.4-03 / 12.4-06 invariant):** `grep @data-extractor packages/rules-engine/src/progression/per-level-budget.ts` = **0**; `grep @data-extractor packages/rules-engine/src/progression/prestige-gate.ts` = **0**. Note: sibling modules (`feats/feat-eligibility.ts`, `feats/feat-prerequisite.ts`, `skills/skill-*.ts`, `foundation/group-races-by-parent.ts`, `feats/bab-calculator.ts`, `progression/compute-hit-points.ts`) still import `@data-extractor/contracts/*` as pre-existing pattern — these are pre-12.4 and 12.4 did not widen the surface. The new 12.4 modules are strict zero-import.
- **SHAR-05 share-URL invariant:** `parameterizedFeatFamily` is optional + nullable on `compiledFeatSchema` (`feat-catalog.ts:53-60,68` → `.nullable().optional()`). No `schemaVersion` bump. BuildDocument `featSelections` still accepts any canonical-id string — target-qualified ids fit natively.
- **Visibility-lock contract (R5):** `grep display:\s*none` on `apps/planner/src/features/feats/*.tsx` = **0 occurrences** (only a doc-comment mentioning the ban at `feat-sheet.tsx:27`). CSS cascade defeat `.feat-picker__row--blocked .feat-picker__reason, .feat-picker__row--blocked .feat-picker__pill { opacity: 1 }` preserves WCAG contrast on blocked rows.
- **CLAS-03 multiclass aria bridge:** `ClassPickerRow` aria-disabled is `option.status==='blocked' || !gateResult.reachable` — locked by `class-picker-grouping.spec.tsx` regression.
- **DEF-12.4-02 pre-existing font-weight:700:** `apps/planner/src/styles/app.css:113` (`.attributes-editor__cell--base .nwn-button`) — confirmed pre-12.4 via CSS-block ownership (`attributes-editor__*` is Phase 3 / 10-01 territory). `git diff` on each 12.4 CSS additive block shows `grep -c "font-weight: 700"` = 0 introduced.

---

## Test Suite Verification (live)

- **Command:** `./node_modules/.bin/vitest run`
- **Result:** **629 passed / 630 total** — exit code `0`.
- **Failed test:** `tests/phase-05.2/theme-contract.spec.ts > does NOT contain font-weight: 700` (line 78:26). Evidence above confirms pre-existing at `app.css:113`; 12.4 introduced zero new font-weight:700 rules.
- **Phase-12.4 internal sweep:** 11 spec files under `tests/phase-12.4/`, all green:
  - `extractor-deleted-sentinel.spec.ts` (20)
  - `feat-picker-scroll.spec.tsx` (5)
  - `per-level-budget.fixture.spec.ts` (28) + `per-level-budget.bench.ts` (1 bench)
  - `l1-neutral-substeps.spec.tsx` (12)
  - `skill-board-split.spec.tsx` (10)
  - `prestige-gate.fixture.spec.ts` (12) + `class-picker-grouping.spec.tsx` (5)
  - `feat-selectability-states.spec.tsx` (15)
  - `feat-family-expander.spec.tsx` (11)
  - `level-editor-action-bar.spec.tsx` (12) + `e2e-humano-guerrero-l1-l16.spec.tsx` (1)
- **Typecheck (apps/planner):** reported clean by every plan's SUMMARY; not re-run here (no scope-changing edits since last SUMMARY).

---

## Plans & Commits (9/9)

| Wave | Plan    | R    | RED        | GREEN       | Notes                                                    |
|------|---------|------|------------|-------------|----------------------------------------------------------|
| 1    | 12.4-01 | R8   | `2c89215`  | `52cffe4`   | Sentinel regex + 4 assembler guards + UI belt-and-braces |
| 1    | 12.4-02 | R9   | `fe160e5`  | `e27306a`   | `.feat-picker__list` scroll + scoped wrapper override    |
| 1    | 12.4-03 | R3   | `b374baa`  | `0dda8e1`   | Pure `computePerLevelBudget` + bench + schedule extension |
| 2    | 12.4-04 | R6+X1 | `432fd1b` | `573da51`   | 3 predicates + `'ready'` status + X1 fallback removed    |
| 2    | 12.4-05 | R4   | `31306ed`  | `596b34b`   | Class/transclase split + cost hint ≥12px                 |
| 2    | 12.4-06 | R1   | `7805025`  | `23b4338`   | ClassPicker + reachableAtLevelN + CLAS-03 aria bridge    |
| 3    | 12.4-07 | R5   | `8b38a51`  | `fe60354`   | 4 selectability states + slot counter + collapse         |
| 3    | 12.4-08 | R7   | `32ef57b`  | `344778c`   | parameterizedFeatFamily + inline expander + 230 variants |
| 3    | 12.4-09 | R2   | `b1b896f`  | `dff972c`   | LevelEditorActionBar + atomic dispatch + E2E (`adc13a0`) |

ROADMAP.md L386-394 shows all nine plans `[x]`. STATE.md frontmatter confirms `completed_plans: 66`, `completed_phases: 20`, `percent: 91`.

---

## Deferred Items (preserved per `deferred-items.md`)

- **DEF-12.4-01 — worktree node_modules hydration gap** — resolved on master (`./node_modules/.bin/vitest` runs cleanly end-to-end); historically blocked parallel worktree full-suite runs. No further action required.
- **DEF-12.4-02 — pre-existing `font-weight: 700` at `apps/planner/src/styles/app.css:113`** — confirmed pre-12.4 via selector `.attributes-editor__cell--base .nwn-button`. Flagged in UI-SPEC.md L66 as known token violation. Remediation: future hygiene plan (token-cleanup backlog). Not a Phase 12.4 blocker.

Cross-check against SPEC.md requirements: no SPEC item silently slipped into `deferred-items.md`. Humano L1 race-bonus store capacity gap (3 slots vs 2 store slots) is documented inline in 12.4-07 / 12.4-09 SUMMARYs as a KNOWN LIMITATION of existing store shape predating 12.4 — not a new deferred, not a new gap.

---

## Concerns / Flags for Next Milestone

1. **Humano L1 advance-button disabled permanently.** `computePerLevelBudget` reports `featSlots.total=3` for Humano L1 (general 1 + classBonus 1 + raceBonus 1), but `useFeatStore` holds only `classFeatId + generalFeatId` (2 slots). The Humano race-bonus feat cannot be picked today → `chosen` caps at 2 → advance button stays "Faltan 1 dote…" disabled for every Humano L1 build. 12.4-09 E2E pivoted to Elfo to unblock test coverage. **User impact:** real Humano players cannot advance past L1 until a future store-shape plan adds a 3rd slot. Recommend scoping a follow-up quick task or fold into a v1.1 store extension.
2. **@playwright/test absent from package.json.** SPEC R9 acceptance named a "Playwright E2E happy path" at 1280×900. Plan 12.4-09 OQ-1 degraded to RTL .spec.tsx fallback (jsdom). Preserved: ≥4 concrete `fireEvent.click` + ≥2 DOM assertions + L16 null. Lost: cross-browser and real-viewport visual assertion. Either add `@playwright/test` in a later phase or accept RTL fallback as terminal.
3. **Extractor prestige prereq enrichment still deferred (Phase 13.x).** `reachableAtLevelN` at L2+ fails closed to `Requisitos en revisión` for every prestige row until `classes.2da PreReqTable` is decoded. Users see a single generic reason copy rather than specific BAB/skill-rank/feat prereqs. Fail-closed is correct per CONTEXT.md D-02; eventual extractor work will unlock richer UI copy with zero helper-signature change.
4. **Magic is descoped (Phase 07.2).** Any SC phrasing like `SELECT the class's spell progression` is honored by 12.4 as "classes render in picker; spell-progression lives outside milestone v1.0". No regression introduced.

---

## Human Verification Required

The following items need a real browser UAT sweep (MCP Chrome or manual) — automated checks cannot cover them:

1. **Advance button visual integration** — sticky footer pins to `level-sheet` bottom at 1280×900; does not overlap detail panels; does not clip on ≤1023px mobile drawer.
2. **Collapse-on-complete instant swap** — `<FeatSummaryCard>` replaces `<FeatSheet>` without flash; `Modificar selección` re-expand highlights chosen rows via `.feat-picker__row--chosen` gold outline.
3. **Prestige row reason copy legibility** — `Disponible a partir del nivel 2` and other reason strings readable at opacity cascade (row 0.55 / reason 1) in Chrome; CSS asserted at source level only.
4. **Dotes scroll-on-list behavior** — scrolling long feat list does NOT move detail panel; detail panel stays pinned; class-picker and race-picker exhibit same pattern per 12.4-02 audit finding.
5. **L1 rail tile blank label before class selection** — tile renders empty rather than `Explo…` truncation; flips after `setLevelClassId`.
6. **Family expander keyboard UX** — Esc collapses, Enter confirms focused radio, Tab cycles through radios.
7. **Humano L1 disabled-button UX** — acknowledge or schedule the store-shape follow-up so users aren't stranded.

Re-run UAT-FINDINGS-2026-04-19.md F1..F8 + X1 checklist against `master` at commit `eacdbc2` to close the loop.

---

## Recommendation

**Proceed to `/gsd-audit-milestone v1.0`** after human UAT sweep.

Rationale:
- All 9 SPEC requirements materialized with RED→GREEN TDD discipline and append-only CSS/copy.
- Test suite green modulo one pre-existing documented deferred item (DEF-12.4-02).
- Rules-engine framework purity for new modules is perfect (0 extractor imports).
- BuildDocument schema invariant preserved (SHAR-05 intact).
- Known limitations (Humano store capacity; Playwright absent; prestige enrichment) are tracked and orthogonal to 12.4 correctness.

No fix-up phase required before milestone audit. One small follow-up advisable: schedule Humano store-capacity extension as a quick task before v1.0 UAT close so Humano players can actually advance from L1.

---

*Verified against master HEAD `eacdbc2` on 2026-04-19. All file paths absolute in the Phase 12.4 SUMMARY chain; commit hashes confirmed present in `git log --oneline`.*
