---
phase: 11-uat-openwork-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md
  - .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md
  - .planning/phases/07-magic-full-legality-engine/07-UAT.md
autonomous: false
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - SKIL-01
  - SKIL-02
  - SKIL-03

must_haves:
  truths:
    - "Phase 06 5-scenario human UAT is signed off: visual feat flow, accent-insensitive search + blocked reasons, Dotes tab grouping, revalidation markers, proficiency feats."
    - "Phase 05 HUMAN-UAT.md carries a re-verification note confirming 12/12 programmatic + layout pass, status resolved."
    - "Phase 07 UAT.md frontmatter status is descoped and its descope marker points to Phase 07.2."
    - "No Phase 06 UAT scenarios remain marked `result: [pending]` after sign-off."
  artifacts:
    - path: ".planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md"
      provides: "Signed-off 5-scenario UAT record for Phase 06"
      contains: "status: complete"
    - path: ".planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md"
      provides: "Re-verification note confirming 12/12 programmatic + layout pass"
      contains: "Re-Verification Note"
    - path: ".planning/phases/07-magic-full-legality-engine/07-UAT.md"
      provides: "Descoped marker tying UAT obsolescence to Phase 07.2"
      contains: "descoped_by: 07.2-magic-ui-descope"
  key_links:
    - from: ".planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md"
      to: ".planning/ROADMAP.md Phase 11 SC1"
      via: "5 scenario results flipped from [pending] to [PASS|FAIL + note]"
      pattern: "result: \\[(PASS|FAIL"
    - from: ".planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md"
      to: ".planning/ROADMAP.md Phase 11 SC2"
      via: "Re-verification note referencing 12/12 programmatic + layout pass"
      pattern: "12/12"
    - from: ".planning/phases/07-magic-full-legality-engine/07-UAT.md"
      to: ".planning/phases/07.2-magic-ui-descope"
      via: "descoped_by frontmatter + descoped trailer body"
      pattern: "descoped_by: 07.2-magic-ui-descope"
---

<objective>
Close all three outstanding UAT artifacts for milestone v1.0.

Purpose: Phase 11 success criteria 1, 2, and 3 require the Phase 05/06/07 UAT files to reach a terminal state (complete / re-verified / descoped) so the milestone audit shows no unresolved UAT debt against FEAT-01..04 and SKIL-01..03.

Output:
- 06-HUMAN-UAT.md: 5 scenarios signed off by the human tester, frontmatter status flipped to `complete`.
- 05-HUMAN-UAT.md: re-verification note appended, frontmatter status advanced from `diagnosed` to `complete` (the documented skill scroll/layout issue was resolved in Phase 05.2 overhaul — 12/12 programmatic + layout pass was verified in Phase 09 audit; this closes the UAT file).
- 07-UAT.md: already carries `status: descoped` in frontmatter; verify the descoped marker is intact and the trailer correctly references Phase 07.2 — no edits required beyond confirmation unless drift exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md

@.planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md
@.planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md
@.planning/phases/07-magic-full-legality-engine/07-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive P05 HUMAN-UAT with re-verification note and mark P07 UAT descoped (autonomous)</name>
  <files>
    .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md
    .planning/phases/07-magic-full-legality-engine/07-UAT.md
  </files>

  <read_first>
    - .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md (current status: `diagnosed`, 1 pass + 1 issue resolved via Phase 05.2 UX overhaul)
    - .planning/phases/07-magic-full-legality-engine/07-UAT.md (already has `status: descoped` + trailer — verify no drift)
    - .planning/ROADMAP.md Phase 11 Success Criteria 2 (12/12 programmatic + layout pass) and 3 (P07 descoped per 07.2)
    - .planning/phases/07.2-magic-ui-descope/ (existence confirms Phase 07.2 delivered)
  </read_first>

  <action>
    **Sub-task 1A: 05-HUMAN-UAT.md re-verification note**

    Update frontmatter:
    ```
    status: complete
    updated: 2026-04-18T00:00:00Z
    closed_by: 11-uat-openwork-closure
    ```

    Append a new section at the end of the file (after the existing `## Gaps` block), before any trailer:

    ```markdown
    ## Re-Verification Note — 2026-04-18 (Phase 11)

    The skill-scroll / two-panel layout issue reported in Test 1 (severity: major) was
    resolved by the Phase 05.2 UX overhaul: `skill-board.tsx` and `skill-sheet.tsx` now
    render a single unified scrollable panel with compact skill rows per the NWN1 original
    layout. Phase 09 audit re-ran the Phase 05 verification scope and confirmed **12/12
    programmatic checks + layout pass** — all SKIL-01..03 behaviours and the reported
    major gap are now green in the shipped build.

    UAT closed as part of Phase 11 (UAT + Open-Work Closure).

    Requirements closed: SKIL-01, SKIL-02, SKIL-03.
    ```

    **Sub-task 1B: 07-UAT.md descoped marker verification**

    The file already has `status: descoped` + `descoped_by: 07.2-magic-ui-descope` in
    frontmatter and a `## DESCOPED — 2026-04-17 (Phase 07.2)` trailer. Do NOT rewrite.
    Only touch the file if verification below fails:

    - If `status: descoped` missing from frontmatter → add it.
    - If `descoped_by: 07.2-magic-ui-descope` missing → add it.
    - If `## DESCOPED` heading missing from body → append the original trailer text
      (see existing body from line 87 onward as reference).
    - If all three present → leave file as-is; task 1B is a no-op.

    Do NOT edit 06-HUMAN-UAT.md in this task — Task 2 (human checkpoint) owns it.
  </action>

  <verify>
    <automated>grep -q "^status: complete$" .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md &amp;&amp; grep -q "Re-Verification Note" .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md &amp;&amp; grep -q "12/12" .planning/phases/05-skills-derived-statistics/05-HUMAN-UAT.md &amp;&amp; grep -q "^status: descoped$" .planning/phases/07-magic-full-legality-engine/07-UAT.md &amp;&amp; grep -q "descoped_by: 07.2-magic-ui-descope" .planning/phases/07-magic-full-legality-engine/07-UAT.md</automated>
  </verify>

  <done>
    05-HUMAN-UAT.md has `status: complete` + re-verification note citing 12/12 programmatic + layout pass + Phase 05.2/09 evidence. 07-UAT.md confirmed `status: descoped` + trailer intact. Requirements SKIL-01..03 closed; Phase 11 SC2 + SC3 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run Phase 06 5-scenario human UAT and sign off (FEAT-01..04)</name>

  <what-built>
    Phase 06 (Dotes + Competencias) shipped FeatBoard with visual feat selection flow,
    accent-insensitive search with blocked-feat reasons, Dotes tab grouping in the
    character sheet, revalidation markers on upstream class changes, and proficiency
    feats exposed like normal feats. Programmatic tests already green. This checkpoint
    is the final human sign-off on the 5 scenarios listed in 06-HUMAN-UAT.md.
  </what-built>

  <how-to-verify>
    **Before you start**: open a second terminal window for the dev server so this
    chat can resume after you sign off.

    1. Start the planner dev server from repo root:
       ```
       pnpm --filter @nwn1-character-builder/planner dev
       ```
       Wait for Vite to print a localhost URL (typically http://localhost:5173 or :5178).

    2. Open that URL in a browser. Confirm the Spanish shell renders and no console errors.

    3. Run the 5 scenarios from `.planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md`:

       **Scenario 1 — Visual feat selection flow (class bonus + general)**
       - Build any class that grants a level-1 bonus feat (e.g., Fighter).
       - Navigate Construcción → nivel 1 → sub-step Dotes.
       - FeatBoard MUST render two sections: **Dotes de clase** and **Dotes generales**.
       - Class-bonus slot is filled first, then general slot — sequential flow.
       - Each feat row shows prerequisite summary text inline.

       **Scenario 2 — Search accent-insensitive + blocked reasons**
       - In the feat search field, type `Poder` (no accent) → matches feats with and
         without accents.
       - Type `arma` → feats with `Armas` / `arma` variants match regardless of case/accent.
       - Blocked feats (prerequisite not met) MUST show red/amber failure reasons
         inline under the feat name (Spanish reason text).

       **Scenario 3 — Character sheet Dotes tab grouping**
       - Navigate to Resumen or the character-sheet tab (wherever `Dotes` lives).
       - Dotes tab lists auto-granted + selected feats **grouped by level**.
       - Each row shows the slot label (e.g., "Dote de clase", "Dote general") and
         invalid-state marker when applicable.

       **Scenario 4 — Revalidation markers on upstream class change**
       - Build any class + pick at least one prerequisite-gated feat at level 1.
       - Change the level-1 class to a class that invalidates the picked feat.
       - Previously valid feats MUST be marked invalid/red with Spanish reason;
         rail shows error state.

       **Scenario 5 — Proficiency feats visible and selectable**
       - In the feat catalog/search, proficiency feats (weapon/armor/shield) appear
         as normal feats grouped by category — selectable where legal, blocked where not.

    4. For each scenario, open `.planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md`
       and replace `result: [pending]` with one of:
       - `result: [PASS]` — scenario works as expected.
       - `result: [FAIL] <reason>` — scenario shows defect; capture screenshot path +
         reason; leave a gap in `## Gaps` section with severity.

    5. When all 5 scenarios are either PASS or FAIL, update frontmatter:
       - If 5/5 PASS → `status: complete`, update `updated:` to today's ISO timestamp.
       - If any FAIL → `status: partial`, add a gap entry per failed scenario, then
         return `failed: <count>` in the resume signal; a follow-up gap-closure phase
         will be required.

    6. Update `## Summary` block counts (`passed`, `issues`, `pending` must reflect reality).

    **Expected outcome**: 5/5 PASS, `status: complete`, no `result: [pending]` lines remain.
  </how-to-verify>

  <resume-signal>
    Reply with one of:
    - `approved` — all 5 scenarios PASS and file marked complete (Phase 11 SC1 satisfied).
    - `failed: <N>` + brief description — N scenarios failed; list which ones. Orchestrator will open gap closure.
    - `blocked: <reason>` — environment problem prevented running UAT (e.g., dev server won't start).
  </resume-signal>

  <acceptance_criteria>
    After `approved`:
    - `grep -c "result: \[pending\]" .planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md` returns `0`.
    - `grep -q "^status: complete$" .planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md` exits 0.
    - `grep -c "result: \[PASS\]" .planning/phases/06-feats-proficiencies/06-HUMAN-UAT.md` returns `5`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:
- 06-HUMAN-UAT.md: `status: complete`, 5/5 PASS, 0 pending lines.
- 05-HUMAN-UAT.md: `status: complete`, re-verification note with `12/12` marker present.
- 07-UAT.md: `status: descoped`, `descoped_by: 07.2-magic-ui-descope` present.
- No source code changes; only `.planning/` edits.
</verification>

<success_criteria>
- [ ] Phase 11 SC1 satisfied (5/5 Phase 06 scenarios signed off).
- [ ] Phase 11 SC2 satisfied (05-HUMAN-UAT.md closed with re-verification note).
- [ ] Phase 11 SC3 satisfied (07-UAT.md descoped per 07.2).
- [ ] Requirements FEAT-01..04 + SKIL-01..03 confirmed closed in verification evidence.
- [ ] No `result: [pending]` lines anywhere in 06-HUMAN-UAT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/11-uat-openwork-closure/11-01-SUMMARY.md` with:
- What was signed off (5 scenarios + archive actions)
- Any FAIL/issue notes from Task 2 if present
- Requirement IDs closed
</output>
