---
phase: 11-uat-openwork-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/guardar-slot-zoderror.md
  - .planning/debug/resolved/guardar-slot-zoderror.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The guardar-slot-zoderror debug session is marked resolved, carries commit 4f03865 as evidence, and lives in .planning/debug/resolved/."
    - "The original .planning/debug/guardar-slot-zoderror.md path no longer exists (file was moved, not duplicated)."
    - "Future audits can find the fix evidence by reading the resolved debug doc without re-running diagnostics."
  artifacts:
    - path: ".planning/debug/resolved/guardar-slot-zoderror.md"
      provides: "Closed debug session with verified fix evidence"
      contains: "status: resolved"
    - path: ".planning/debug/resolved/"
      provides: "Archive directory for resolved debug sessions (created if absent)"
      exists: true
  key_links:
    - from: ".planning/debug/resolved/guardar-slot-zoderror.md"
      to: "commit 4f03865 (fix(08): guard projectBuildDocument against null race/alignment)"
      via: "closure note citing commit hash + files changed"
      pattern: "4f03865"
    - from: ".planning/debug/resolved/guardar-slot-zoderror.md"
      to: "apps/planner/src/features/persistence/project-build-document.ts"
      via: "IncompleteBuildError + isBuildProjectable() evidence reference"
      pattern: "IncompleteBuildError"
---

<objective>
Close the `guardar-slot-zoderror` debug session by verifying that commit 4f03865 implemented the planned fix, stamping the debug doc with a resolution note, and moving it to `.planning/debug/resolved/` per Phase 11 SC4.

Purpose: The debug doc currently sits at `status: awaiting_human_verify` in the active debug directory. Commit 4f03865 already shipped the two-layer fix (IncompleteBuildError + UI gating); Phase 08 UAT confirmed persistence works. Phase 11 requires the doc to be closed out so the audit trail is clean. This plan has no direct roadmap requirement IDs — it is a closure-meta task satisfying Phase 11 SC4 only.

Output:
- `.planning/debug/resolved/guardar-slot-zoderror.md` — debug doc moved, frontmatter `status: resolved`, closure note appended citing commit 4f03865 + three runtime files touched.
- `.planning/debug/guardar-slot-zoderror.md` — gone (moved, not copied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@.planning/debug/guardar-slot-zoderror.md

<!-- Source files touched by commit 4f03865 (for spot-check during closure) -->
@apps/planner/src/features/persistence/project-build-document.ts
@apps/planner/src/features/summary/resumen-board.tsx
@apps/planner/src/features/summary/save-slot-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify fix evidence in commit 4f03865 and in the live runtime files</name>
  <files>
    (read-only verification — no files modified in this task)
  </files>

  <read_first>
    - .planning/debug/guardar-slot-zoderror.md (full doc — fix rationale + files_changed list)
    - apps/planner/src/features/persistence/project-build-document.ts (must export `IncompleteBuildError` and `isBuildProjectable`)
    - apps/planner/src/features/summary/resumen-board.tsx (must disable Guardar/Exportar/Compartir when raceId or alignmentId is null)
    - apps/planner/src/features/summary/save-slot-dialog.tsx (must catch IncompleteBuildError in doSave)
  </read_first>

  <action>
    Run three verification checks against the live workspace. All three MUST succeed
    before Task 2 moves the file; if any fails, stop and report the discrepancy
    (do NOT close a debug session on an unverified fix).

    1. **Commit 4f03865 exists and touches the expected files**. Run:
       ```
       git show --stat 4f03865
       ```
       Expected files in the diff:
       - `apps/planner/src/features/persistence/index.ts`
       - `apps/planner/src/features/persistence/project-build-document.ts`
       - `apps/planner/src/features/summary/resumen-board.tsx`
       - `apps/planner/src/features/summary/save-slot-dialog.tsx`
       - `apps/planner/src/lib/copy/es.ts`
       - `tests/phase-08/project-build-document-incomplete.spec.ts`

    2. **`IncompleteBuildError` and `isBuildProjectable` exist in project-build-document.ts**.
       Use Grep to confirm both symbols are exported from
       `apps/planner/src/features/persistence/project-build-document.ts`.

    3. **`resumen-board.tsx` gates action buttons on raceId/alignmentId null state**.
       Use Grep to confirm `resumen-board.tsx` references `isBuildProjectable` OR
       reads `raceId`/`alignmentId` with a disabled-state check.

    **`save-slot-dialog.tsx` catches IncompleteBuildError**. Use Grep to confirm
    `save-slot-dialog.tsx` has a `catch` block mentioning `IncompleteBuildError`.

    If all 4 grep checks pass, the fix evidence is verified. Proceed to Task 2.
  </action>

  <verify>
    <automated>git show --name-only 4f03865 | grep -q "project-build-document.ts" &amp;&amp; git show --name-only 4f03865 | grep -q "resumen-board.tsx" &amp;&amp; git show --name-only 4f03865 | grep -q "save-slot-dialog.tsx"</automated>
  </verify>

  <done>
    Commit 4f03865 confirmed present with expected files; `IncompleteBuildError` + `isBuildProjectable` found in project-build-document.ts; resumen-board.tsx + save-slot-dialog.tsx carry the UI guards. Fix verification passed — debug session is safe to close.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stamp closure note on debug doc and move to .planning/debug/resolved/</name>
  <files>
    .planning/debug/guardar-slot-zoderror.md (source — will be removed)
    .planning/debug/resolved/guardar-slot-zoderror.md (destination — will be created)
  </files>

  <read_first>
    - .planning/debug/guardar-slot-zoderror.md (full current contents; preserve ALL evidence blocks; only append closure + update frontmatter)
    - Verify `.planning/debug/resolved/` — if missing, create the directory.
  </read_first>

  <action>
    1. **Create `.planning/debug/resolved/` if absent**:
       ```
       mkdir -p .planning/debug/resolved
       ```

    2. **Update frontmatter of the debug doc** (at top of file). Change:
       ```
       status: awaiting_human_verify
       updated: 2026-04-17T00:00:00Z
       ```
       to:
       ```
       status: resolved
       updated: 2026-04-18T00:00:00Z
       resolved_by: commit 4f03865
       resolved_in: Phase 11 (UAT + Open-Work Closure)
       ```

    3. **Append a closure note** at the end of the file (after the existing `## Resolution`
       block and any trailing content):

       ```markdown
       ## Closure — 2026-04-18 (Phase 11)

       Fix shipped in commit **4f03865** — `fix(08): guard projectBuildDocument against
       null race/alignment`. Phase 08 UAT confirmed: Guardar / Exportar / Compartir now
       disabled when raceId or alignmentId are null; when both are set, the slot persists
       in Dexie and a Spanish toast surfaces. Full test suite 376/376 at the time of the
       fix; no regression since through Phases 09 and 10.

       **Verification evidence** (checked 2026-04-18 against HEAD):
       - `apps/planner/src/features/persistence/project-build-document.ts` exports
         `IncompleteBuildError` class and `isBuildProjectable()` predicate.
       - `apps/planner/src/features/summary/resumen-board.tsx` gates Guardar / Exportar /
         Compartir on raceId + alignmentId being non-null and catches IncompleteBuildError
         on the action paths.
       - `apps/planner/src/features/summary/save-slot-dialog.tsx` catches
         IncompleteBuildError in `doSave` and pushes a Spanish warn-toast instead of
         bubbling a raw ZodError.

       Debug session closed and moved to `.planning/debug/resolved/`.
       ```

    4. **Move the file** (Git-tracked move preferred so history follows):
       ```
       git mv .planning/debug/guardar-slot-zoderror.md .planning/debug/resolved/guardar-slot-zoderror.md
       ```
       If `git mv` fails because the file was not yet committed, fall back to:
       ```
       mv .planning/debug/guardar-slot-zoderror.md .planning/debug/resolved/guardar-slot-zoderror.md
       ```

    5. **Do NOT modify the evidence blocks** (hypothesis / confirming_evidence / symptoms /
       Eliminated / Evidence / Resolution). Those are historical record. Only the
       frontmatter and the appended closure are new.
  </action>

  <verify>
    <automated>test -f .planning/debug/resolved/guardar-slot-zoderror.md &amp;&amp; ! test -f .planning/debug/guardar-slot-zoderror.md &amp;&amp; grep -q "^status: resolved$" .planning/debug/resolved/guardar-slot-zoderror.md &amp;&amp; grep -q "4f03865" .planning/debug/resolved/guardar-slot-zoderror.md &amp;&amp; grep -q "IncompleteBuildError" .planning/debug/resolved/guardar-slot-zoderror.md</automated>
  </verify>

  <done>
    File is at `.planning/debug/resolved/guardar-slot-zoderror.md`, original path gone, frontmatter `status: resolved` + `resolved_by: commit 4f03865`, closure note cites commit + three runtime files. Phase 11 SC4 satisfied.
  </done>
</task>

</tasks>

<verification>
- `.planning/debug/guardar-slot-zoderror.md` no longer exists.
- `.planning/debug/resolved/guardar-slot-zoderror.md` exists, `status: resolved`, closure note present citing 4f03865.
- No source code files touched — edits confined to `.planning/debug/`.
</verification>

<success_criteria>
- [ ] Fix evidence in commit 4f03865 re-verified (project-build-document.ts + resumen-board.tsx + save-slot-dialog.tsx).
- [ ] Debug doc frontmatter flipped from `awaiting_human_verify` to `resolved`.
- [ ] Closure note appended referencing commit 4f03865 + IncompleteBuildError + isBuildProjectable + UI gating.
- [ ] File moved to `.planning/debug/resolved/` (original path empty).
- [ ] Phase 11 SC4 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/11-uat-openwork-closure/11-02-SUMMARY.md` with:
- Fix evidence summary (3 runtime files + commit hash)
- Old path → new path move record
- Any verification failures (should be none)
</output>
