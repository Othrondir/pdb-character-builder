---
phase: quick-260422-h9k
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/features/level-progression/prestige-prereq-data.ts
  - tests/phase-12.4/prestige-gate.fixture.spec.ts
  - tests/phase-12.4/class-picker-prestige-reachability.spec.tsx
autonomous: true
requirements:
  - quick-260422-h9k-R1
user_setup: []

must_haves:
  truths:
    - "A L9 con Mago 8 niveles, la fila Agente Custodio (Arcano) / class:harper muestra blockers específicos (BAB/skill/feat/arcane-spell-level según corresponda) en lugar de 'Requisitos en revisión'."
    - "A L9 con Guerrero 8 niveles, la fila Campeón divino muestra el blocker exacto 'Requiere BAB ≥ 7' más el grupo feat-or de Weapon Focus per-arma en lugar de 'Requisitos en revisión'."
    - "A L9 con Guerrero 4 niveles + Hechicero 4 niveles, la fila Maestro de armas muestra blockers específicos (BAB 5 si aplica, skill intimidar 4, feats dodge/mobility/expertise/whirlwind) en lugar de 'Requisitos en revisión'."
    - "Brujo y Espadachín quedan FUERA de scope de este quick — siguen con fallback 'Requisitos en revisión' hasta que un plan cross-package introduzca BlockerKind 'server-gate'. Documentado como gap conocido en SUMMARY."
    - "El spec de fixtures rules-engine (prestige-gate.fixture.spec.ts) cubre los 3 nuevos overrides (harper / campeondivino / weaponmaster) emitiendo PrestigeBlocker con kind + threshold + label exactos derivados de los templates existentes."
    - "El spec de integración (class-picker-prestige-reachability.spec.tsx) verifica reachability positiva y negativa para harper / campeondivino / weaponmaster usando DOM + testing-library."
  artifacts:
    - path: "apps/planner/src/features/level-progression/prestige-prereq-data.ts"
      provides: "Overrides para class:harper, class:campeondivino, class:weaponmaster (3 entradas, no warlock/swashbuckler)"
      contains: "'class:harper':, 'class:campeondivino':, 'class:weaponmaster':"
    - path: "tests/phase-12.4/prestige-gate.fixture.spec.ts"
      provides: "Casos de test rules-engine que ejercitan los nuevos overrides a nivel de fixture (mocks ClassPrereqInput.decodedPrereqs)"
    - path: "tests/phase-12.4/class-picker-prestige-reachability.spec.tsx"
      provides: "Casos de test integración DOM para las 3 clases objetivo del quick (harper + campeondivino + weaponmaster)"
  key_links:
    - from: "apps/planner/src/features/level-progression/prestige-prereq-data.ts"
      to: "packages/rules-engine/src/progression/prestige-gate.ts:reachableAtLevelN"
      via: "getPrestigeDecodedPrereqs → ClassPrereqInput.decodedPrereqs"
      pattern: "PRESTIGE_PREREQ_OVERRIDES\\[.*class:(harper|campeondivino|weaponmaster)"
    - from: "tests/phase-12.4/class-picker-prestige-reachability.spec.tsx"
      to: "apps/planner/src/features/level-progression/class-picker.tsx"
      via: "render(ClassPicker) + document.querySelector('[data-class-id=\"class:X\"]')"
      pattern: "class:(harper|campeondivino|weaponmaster)"
---

<objective>
Expandir cobertura de `PRESTIGE_PREREQ_OVERRIDES` en 3 clases prestige decodables (harper, campeondivino, weaponmaster) para que sus filas en `<ClassPicker>` dejen de mostrar "Requisitos en revisión" y pasen al branch 4 (decoded) del gate.

Warlock + Swashbuckler están FUERA de scope de este quick. Ambos sólo tienen ScriptVar como requisito (gate servidor, no reproducible desde planner state). Dejarlos con override vacío `{}` = `enriched=true` + `blockers=[]` = reachable violaría CLAUDE.md "strict validation — illegal server builds blocked, not warned". Quedan con fallback actual "Requisitos en revisión" hasta que un plan futuro cross-package introduzca BlockerKind `server-gate` en rules-engine + copy dedicado. Documentado como follow-up en SUMMARY.

Purpose: Cerrar 3 de las 5 clases prestige que todavía caen a "Requisitos en revisión" al pasar por `reachableAtLevelN`.

Output:
- Override enriquecido para `class:harper` (3 skills + 2 feats; Discipline dropeada D-03).
- Override enriquecido para `class:campeondivino` (BAB 7 + FEATOR expandido a 32 per-weapon Weapon Focus feats + unarmed; D-06).
- Override enriquecido para `class:weaponmaster` (BAB 5 + skill intimidar 4 + 4 feats + FEATOR 32 per-weapon; D-07).
- Specs extendidos en 2 archivos (fixture + integration) que atrapen regresiones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260422-h9k-ampliar-cobertura-prestige-prereq-data-c/260422-h9k-RESEARCH.md
@apps/planner/src/features/level-progression/prestige-prereq-data.ts
@packages/rules-engine/src/progression/prestige-gate.ts
@tests/phase-12.4/prestige-gate.fixture.spec.ts
@tests/phase-12.4/class-picker-prestige-reachability.spec.tsx

<interfaces>
<!-- Contracts que consume este plan. No explorar codebase. -->

Contract `DecodedPrereqs` (alias local derivado de `ClassPrereqInput['decodedPrereqs']`),
declarado en apps/planner/src/features/level-progression/prestige-prereq-data.ts:4
y definido en packages/rules-engine/src/progression/prestige-gate.ts:38-59:

```typescript
type DecodedPrereqs = {
  minBab?: number;
  minArcaneSpellLevel?: number;
  minSpellLevel?: number;
  minSkillRanks?: ReadonlyArray<{ skillId: string; amount: number; skillName: string }>;
  requiredFeats?: ReadonlyArray<{ featId: string; featName: string }>;
  requiredAnyFeatGroups?: ReadonlyArray<ReadonlyArray<{ featId: string; featName: string }>>;
  requiredAnyRaceIds?: ReadonlyArray<{ raceId: string; raceName: string }>;
  minClassLevel?: { classId: string; amount: number; className: string };
  requiredAnyClassLevels?: ReadonlyArray<{ classId: string; amount: number; className: string }>;
  excludedClassIds?: ReadonlyArray<{ classId: string; className: string }>;
};
```

Helpers locales (prestige-prereq-data.ts:6-20) ya existentes — REUSAR, no redefinir:
```typescript
function feat(featId: string, featName: string) { return { featId, featName }; }
function skill(skillId: string, amount: number, skillName: string) { return { skillId, amount, skillName }; }
// race() y classLevel() también existen pero no se usan en los 3 overrides nuevos.
```

Templates de copy (prestige-gate.ts:111-174) que el gate aplica automáticamente — NO replicar en el override, solo suministrar datos:
- `babLabel(n)` → "Requiere BAB ≥ N"
- `skillRankLabel(n, skillName)` → "Requiere N rangos de X" (plural) / "Requiere 1 rango de X" (singular)
- `featLabel(featName)` → "Requiere dote: X"
- `featOrLabel(featNames[])` → "Requiere una de estas dotes: A, B o C"

Export consumido por el planner (prestige-prereq-data.ts:367-373):
```typescript
export function getPrestigeDecodedPrereqs(classId: CanonicalId | null): DecodedPrereqs | undefined;
```
No se modifica la firma. Se mantiene el objeto literal `PRESTIGE_PREREQ_OVERRIDES` como única fuente.
</interfaces>

<data-tables>
<!-- Mappings canónicos extraídos de 260422-h9k-RESEARCH.md §"Canonical ID Mappings".
     NO re-verificar contra compiled-*.ts — RESEARCH ya lo hizo HIGH confidence. -->

class:harper (D-01, D-03, D-04):
- minSkillRanks: [
    skill('skill:engaar', 6, 'Engañar'),
    skill('skill:buscar', 4, 'Buscar'),
    skill('skill:saberotros', 6, 'Saber (otros)'),
  ]  // Discipline (SKILL 3) omitida — no existe skill:disciplina en catálogo Puerta.
- requiredFeats: [
    feat('feat:alertness', 'Alerta'),
    feat('feat:ironwill', 'Voluntad de hierro'),
  ]
// ScriptVar X1_AllowHarper + CLASSNOT 75-78 (Harper_Mage/Priest/Paragon/Master) omitidas.

class:campeondivino (D-01, D-04, D-06):
- minBab: 7
- requiredAnyFeatGroups: [ [ ...31 per-weapon Weapon Focus feats + feat:weapfocunarm ] ]
  // Expansión de los 4 masters missing (2070-2073: 1HConcussion/1HEdged/2Handed/Polearm)
  // a sus constituyentes per-arma. Lista completa de 32 items en RESEARCH.md §class:campeondivino.
// ScriptVar X2_AllowDivcha + CLASSNOT 79 (Commoner) omitidas.

class:weaponmaster (D-01, D-04, D-07):
- minBab: 5
- minSkillRanks: [ skill('skill:intimidar', 4, 'Intimidar') ]
- requiredFeats: [
    feat('feat:dodge', 'Esquiva'),
    feat('feat:mobility', 'Movilidad'),
    feat('feat:periciaencombate', 'Pericia en combate'),
    feat('feat:feat-whirlwind-attack', 'Ataque de torbellino'),
  ]
- requiredAnyFeatGroups: [ [ ...32 per-weapon Weapon Focus feats ] ]
  // 32 items idénticos a los de campeondivino EXCEPTO que weaponmaster NO incluye
  // feat:weapfocunarm (unarmed no está en su FEATOR original). Lista en RESEARCH.md §class:weaponmaster.
  // Category masters 2070-2073 + Lance (2056) dropeados silenciosamente.
// ScriptVar X2_AllowWM + CLASSNOT 79 omitidas.

class:warlock (D-02): OMITIDO en este quick — ScriptVar-only, requiere BlockerKind 'server-gate' cross-package. Queda con fallback 'Requisitos en revisión' (fail-closed) hasta plan futuro.
class:swashbuckler (D-02): OMITIDO en este quick — mismo motivo que warlock.

IMPORTANTE — Diferencia unarmed entre campeondivino y weaponmaster:
- campeondivino FEATOR original: 2070, 2071, 2072, 2073, 100 (Unarmed) → INCLUIR feat:weapfocunarm.
- weaponmaster FEATOR original: 2070-2073 + 40 per-weapon (SIN Unarmed 100) → NO incluir feat:weapfocunarm.
Este matiz está explícito en los 2DA (server-extract/cls_pres_divcha.2da y cls_pres_weaponmaster.2da).
</data-tables>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extender ambos specs con casos que atrapen los overrides faltantes</name>
  <files>tests/phase-12.4/prestige-gate.fixture.spec.ts, tests/phase-12.4/class-picker-prestige-reachability.spec.tsx</files>
  <behavior>
    Los nuevos casos DEBEN fallar antes del Task 2 (porque `PRESTIGE_PREREQ_OVERRIDES` todavía no contiene las 5 entradas) y pasar después.

    En `tests/phase-12.4/prestige-gate.fixture.spec.ts` — añadir al final del `describe` existente (NO duplicar el top-level describe ni reimportar lo ya importado), 5 nuevos `it` que ejercitan los NUEVOS overrides directamente contra `reachableAtLevelN` vía `ClassPrereqInput.decodedPrereqs` inline (mocks, NO leer `PRESTIGE_PREREQ_OVERRIDES` — eso se valida en el spec de integración). Casos:

    1. `it('harper override: emite blockers skill + feat con copy exacto templateado', ...)`:
       - input: classRow `{ id: 'class:harper', isBase: false, decodedPrereqs: { minSkillRanks: [skill engaar 6, skill buscar 4, skill saberotros 6], requiredFeats: [feat alertness, feat ironwill] } }`, enriched:true, level:5, skillRanks `{}`, featIds empty Set.
       - assert: 3 skill-rank blockers + 2 feat blockers; labels exactos: `'Requiere 6 rangos de Engañar'`, `'Requiere 4 rangos de Buscar'`, `'Requiere 6 rangos de Saber (otros)'`, `'Requiere dote: Alerta'`, `'Requiere dote: Voluntad de hierro'`.
       - assert: reachable === false, blockers.length === 5.

    2. `it('campeondivino override: BAB 7 blocker + feat-or con 32 Weapon Focus per-arma (incluye unarmed)', ...)`:
       - input: classRow decodedPrereqs con minBab:7 + requiredAnyFeatGroups con el array de 32 items (usar una constante local en el test o declarar inline reducido a 4 items representativos: weapfocunarm, weapfoclsw, weapfocgsw, weapfochalb — PERO el assert de count debe ser sobre los 32, así que usar el array real. Sugerencia: extraer a un const CAMPEON_WEAP_FOC_GROUP en el spec con los 32 ids, copy-pasted de RESEARCH §class:campeondivino).
       - bab:3, featIds empty, enriched:true, level:5.
       - assert: un blocker `{ kind: 'bab', threshold: 7, label: 'Requiere BAB ≥ 7' }`.
       - assert: un blocker `{ kind: 'feat-or' }` con `label` empezando por `'Requiere una de estas dotes: '` y conteniendo `'Soltura con un arma (impacto sin arma)'` (verifica que unarmed está en la lista).

    3. `it('campeondivino override: con weapfocunarm en featIds, feat-or satisfecho (sólo BAB blocker queda)', ...)`:
       - Mismo input pero `featIds: new Set(['feat:weapfocunarm'])`, `bab:10`.
       - assert: `reachable: true, blockers: []`.

    4. `it('weaponmaster override: BAB 5 + skill intimidar 4 + 4 feats + feat-or 32 per-arma SIN unarmed', ...)`:
       - input: classRow decodedPrereqs con minBab:5, minSkillRanks [intimidar 4], requiredFeats [dodge/mobility/periciaencombate/feat-whirlwind-attack], requiredAnyFeatGroups con array de 32 items (los mismos de campeondivino EXCEPTO weapfocunarm — ver §data-tables de este PLAN).
       - bab:0, featIds empty, skillRanks {}, enriched:true, level:5.
       - assert: blockers.map(b => b.kind).sort() === ['bab','feat','feat','feat','feat','feat-or','skill-rank'] (orden alfabético tras sort).
       - assert: label del feat-or NO contiene `'impacto sin arma'` (confirma que weaponmaster excluye unarmed).

    (Caso 5 warlock/swashbuckler ELIMINADO — fuera de scope de este quick.)

    En `tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` — añadir al final del `describe` existente 3 nuevos `it` que renderizan `<ClassPicker>` y verifican DOM textContent:

    6. `it('L9 con Guerrero 8: fila class:harper NO muestra "Requisitos en revisión"', ...)`:
       - setupL9WithClassProgression('class:fighter').
       - render.
       - `document.querySelector('[data-class-id="class:harper"]')`.
       - assert: row existe y textContent NO contiene 'Requisitos en revisión'.
       - assert: textContent contiene 'Requiere 6 rangos de Engañar' o 'Requiere dote: Alerta' (prueba branch 4 activo).

    7. `it('L9 con Guerrero 8: fila class:campeondivino muestra blocker BAB 7', ...)`:
       - setupL9WithClassProgression('class:fighter').
       - assert: row class:campeondivino existe.
       - assert: textContent contiene 'Requiere BAB ≥ 7'.
       - assert: textContent NO contiene 'Requisitos en revisión'.
       - Nota: Guerrero L8 tiene BAB 8, pero el gate evalúa PRIOR levels (L1..L8) al activar L9, así que bab=8 ≥ 7 → BAB satisfecho y el feat-or blocker es el que surge. Ajustar: el assertion es que textContent contiene 'Requiere una de estas dotes: ' (feat-or blocker, dado que guerrero sin Weapon Focus elegido). Si bab fuera 6 sería el BAB blocker. Verificar comportamiento real en GREEN; si bab prior levels sale 8, el test debe esperar feat-or blocker.
       - REGLA IMPORTANTE PARA EL EJECUTOR: si durante GREEN el texto esperado no aparece (porque el cálculo de bab/featIds en buildPrestigeGateBuildState da otro resultado), NO cambies la lógica del gate ni del override. Ajusta la aserción del spec al blocker que sí se emite (BAB o feat-or), SIEMPRE que el texto contenga un label templateado del gate (no 'Requisitos en revisión').

    8. `it('L9 con Guerrero 4 + Hechicero 4: fila class:weaponmaster muestra blocker específico (feat o skill)', ...)`:
       - Override `setupL9WithClassProgression` con un setup custom que asigne L1..L4 a Guerrero y L5..L8 a Hechicero. Puede ser inline (sin helper nuevo) ya que el spec ya muestra el patrón de `setLevelClassId(level, classId)`.
       - render.
       - assert: row class:weaponmaster existe, textContent NO contiene 'Requisitos en revisión'.
       - assert: textContent contiene alguno de: 'Requiere dote: Esquiva' / 'Requiere dote: Movilidad' / 'Requiere dote: Pericia en combate' / 'Requiere dote: Ataque de torbellino' / 'Requiere 4 rangos de Intimidar' / 'Requiere una de estas dotes: '.

    (Casos 9-10 warlock/swashbuckler ELIMINADOS — fuera de scope de este quick. class:warlock y class:swashbuckler siguen mostrando 'Requisitos en revisión' en el planner hasta que un plan futuro introduzca BlockerKind 'server-gate' cross-package.)

    Running tests en RED (antes de Task 2):
    - REGLA: los casos 1-4 (fixture) son GREEN desde RED (mocks directos a `reachableAtLevelN`, no dependen de PRESTIGE_PREREQ_OVERRIDES). Los casos 6-8 (integration) son los que fallan en RED (dependen del override real). El test runner reportará 3 failing tests en RED → 0 failing tras Task 2.
  </behavior>
  <action>
    1. Leer ambos specs completos para confirmar el patrón del `describe` + `baseInput` + `setupL9WithClassProgression` existentes.
    2. En `prestige-gate.fixture.spec.ts`: añadir los 5 `it` descritos arriba AL FINAL del `describe('Phase 12.4-06 — reachableAtLevelN...')`. Para el caso 2/3/4 con 32 items de Weapon Focus, declarar dos constantes locales al inicio del describe (o justo antes del primer nuevo it) — `CAMPEON_WEAPON_FOCUS_GROUP` (32 items incluyendo weapfocunarm) y `WEAPONMASTER_WEAPON_FOCUS_GROUP` (32 items sin weapfocunarm, con los otros 32 idénticos + weapfocunarm reemplazado por feat:weapfocclub u otro miembro si hace falta mantener 32 — verificar lista exacta en RESEARCH §class:weaponmaster que tiene EXACTAMENTE 32 entries sin unarmed; si son 31 mantener 31). Importar `feat` y `skill` NO es posible (son helpers privados de prestige-prereq-data.ts), así que redefinir localmente en el spec dos helpers `f(featId, featName)` y `s(skillId, amount, skillName)` o usar literales object `{ featId, featName }` directamente — MÁS SIMPLE: literales object inline, no helpers.
    3. En `class-picker-prestige-reachability.spec.tsx`: añadir los 5 `it` descritos (casos 6-10). Reusar `setupL9WithClassProgression` para casos 6/7/9/10 y un setup inline para el caso 8 (multiclase Guerrero/Hechicero).
    4. Ejecutar `pnpm -w test tests/phase-12.4/prestige-gate.fixture.spec.ts tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` (o el comando equivalente del workspace — inspeccionar `package.json` root si `pnpm test` alone no filtra por path). Verificar:
       - Casos 1-5 (fixture): PASS en RED (mocks directos, no dependen de overrides).
       - Casos 6-10 (integration): FAIL en RED con mensaje sobre 'Requisitos en revisión' presente o label específico ausente.
    5. Commit RED: `test(quick-260422-h9k): extend prestige prereq specs with harper/campeondivino/weaponmaster/warlock/swashbuckler cases`.

    NO modificar `prestige-prereq-data.ts` en esta tarea. NO modificar rules-engine. NO modificar copy/es.ts.
  </action>
  <verify>
    <automated>cd /c/Users/pzhly/RiderProjects/pdb-character-builder && pnpm -w test -- tests/phase-12.4/prestige-gate.fixture.spec.ts tests/phase-12.4/class-picker-prestige-reachability.spec.tsx 2>&1 | tail -60</automated>
  </verify>
  <done>
    - Ambos specs contienen los 10 nuevos `it` (5 fixture + 5 integration).
    - Casos 1-5 (fixture) PASS.
    - Casos 6-10 (integration) FAIL con mensaje que referencia 'Requisitos en revisión' o un label templateado ausente.
    - Commit RED creado con mensaje convencional.
    - Cero modificaciones a `prestige-prereq-data.ts` o a cualquier archivo de `packages/rules-engine/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — añadir 5 overrides en PRESTIGE_PREREQ_OVERRIDES para hacer pasar los casos 6-10</name>
  <files>apps/planner/src/features/level-progression/prestige-prereq-data.ts</files>
  <behavior>
    Tras añadir las 5 entradas (harper + campeondivino + weaponmaster poblados; warlock + swashbuckler vacíos), los tests RED del Task 1 (casos 6-10) deben pasar sin modificar los specs ni el rules-engine.
  </behavior>
  <action>
    Editar `apps/planner/src/features/level-progression/prestige-prereq-data.ts` añadiendo las 5 nuevas entradas al objeto literal `PRESTIGE_PREREQ_OVERRIDES` (después de la entrada existente `'class:maestro-formas'` y antes del cierre `}`). Usar los helpers locales `feat` y `skill` ya definidos (prestige-prereq-data.ts:6-12). NO redefinir helpers.

    Considerar extraer las 2 listas grandes de Weapon Focus per-arma a constantes locales `const MELEE_WEAPON_FOCUS_FEATS` (32 items incluyendo unarmed, para campeondivino) y `const WEAPONMASTER_WEAPON_FOCUS_FEATS` (32 items sin unarmed, para weaponmaster) DECLARADAS al inicio del archivo junto a las constantes existentes `MARTIAL_WEAPON_PROFICIENCIES` / `GREATER_SPELL_FOCUS_FEATS` / `WILD_SHAPE_FEATS` (líneas 22-167). Esto mantiene simetría de estilo con las constantes existentes.

    Contenido exacto a añadir al objeto (copiar las 3 entradas pobladas de RESEARCH §"Prereq tables to drop into prestige-prereq-data.ts" — están ya formateadas como TypeScript válido):

    ```typescript
    'class:harper': {
      minSkillRanks: [
        skill('skill:engaar', 6, 'Engañar'),
        skill('skill:buscar', 4, 'Buscar'),
        skill('skill:saberotros', 6, 'Saber (otros)'),
        // TODO(product): SKILL 3 (Discipline/Disciplina) omitida — no existe
        // skill:disciplina en el catálogo player de Puerta (38 skills). Pre-D-03
        // OPEN-QUESTION: reinstaurar en extractor vs dropear permanentemente.
      ],
      requiredFeats: [
        feat('feat:alertness', 'Alerta'),
        feat('feat:ironwill', 'Voluntad de hierro'),
      ],
      // NOTE: ScriptVar X1_AllowHarper (server gate) + CLASSNOT 75-78 (Harper_Mage/
      // Priest/Paragon/Master, NPC-only) omitidas — no reachable desde planner state.
      // KNOWN LIMITATION: compiled-classes.ts emite class:harper dos veces (Arcano
      // sourceRow 28, Divino sourceRow 54); first-wins dedupe en class-fixture.ts
      // mantiene sólo Arcano. Override aplica a Arcano únicamente (D-05).
    },
    'class:campeondivino': {
      minBab: 7,
      requiredAnyFeatGroups: [MELEE_WEAPON_FOCUS_FEATS],
      // NOTE: FEATOR 2070-2073 (category masters 1HConcussion/1HEdged/2Handed/Polearm)
      // expandidos a sus constituyentes per-arma (D-06). Unarmed (FEATOR 100)
      // incluido como weapfocunarm. ScriptVar X2_AllowDivcha + CLASSNOT 79 omitidas.
    },
    'class:weaponmaster': {
      minBab: 5,
      minSkillRanks: [skill('skill:intimidar', 4, 'Intimidar')],
      requiredFeats: [
        feat('feat:dodge', 'Esquiva'),
        feat('feat:mobility', 'Movilidad'),
        feat('feat:periciaencombate', 'Pericia en combate'),
        feat('feat:feat-whirlwind-attack', 'Ataque de torbellino'),
      ],
      requiredAnyFeatGroups: [WEAPONMASTER_WEAPON_FOCUS_FEATS],
      // NOTE: FEATOR 2070-2073 (category masters) + 2056 (Lance) dropeados
      // silenciosamente (D-07) — constituyentes per-arma ya en la lista.
      // ScriptVar X2_AllowWM + CLASSNOT 79 omitidas.
    },
    // class:warlock y class:swashbuckler OMITIDAS intencionalmente — solo tienen
    // ScriptVar (PRC_AllowWarlock / PRC_AllowSwash). Dar override vacío violaría
    // CLAUDE.md "strict validation". Quedan con fallback 'Requisitos en revisión'
    // hasta que un plan cross-package introduzca BlockerKind 'server-gate' en
    // rules-engine + copy 'Requisito de servidor — contacta DM' en es.ts.
    ```

    Las constantes `MELEE_WEAPON_FOCUS_FEATS` y `WEAPONMASTER_WEAPON_FOCUS_FEATS` contienen exactamente los `feat(...)` listados en RESEARCH §"class:campeondivino" (32 items: unarmed + 31 per-arma) y §"class:weaponmaster" (32 items: per-arma sin unarmed). Copiar los literales de RESEARCH.md — todos los canonical IDs ya están verificados HIGH confidence (grep en compiled-feats.ts).

    Tras editar:
    1. `pnpm -w tsc --noEmit` (o el comando de typecheck del workspace) para verificar que los CanonicalId literals validen contra el template literal type.
    2. Ejecutar los 2 specs otra vez — casos 1-5 siguen PASS, casos 6-10 ahora PASS.
    3. Si caso 7 o 8 fallan por un label de blocker distinto al esperado (por ejemplo porque `buildPrestigeGateBuildState` con 8 levels de Guerrero da bab=8 ≥ 7 y el blocker visible es feat-or no BAB), ajustar el spec para esperar el blocker que efectivamente surge (PER instrucción del Task 1 paso 3). NO tocar el override ni el rules-engine.
    4. Ejecutar la suite completa del workspace `pnpm -w test` para verificar que no hay regresiones en otros specs de phase-12.4 (especialmente `class-picker.spec.tsx` y quicks previos 260422-g7s / 260422-h9k-*).
    5. Commit GREEN: `feat(quick-260422-h9k): add prestige prereq overrides for harper + campeondivino + weaponmaster + warlock + swashbuckler`.

    NO editar `copy/es.ts` (los labels salen templated desde prestige-gate.ts).
    NO editar `packages/rules-engine/` (Prescriptive Shape).
    NO tocar `compiled-classes.ts` / `compiled-feats.ts` / `compiled-skills.ts` (datos del extractor).
  </action>
  <verify>
    <automated>cd /c/Users/pzhly/RiderProjects/pdb-character-builder && pnpm -w test -- tests/phase-12.4/prestige-gate.fixture.spec.ts tests/phase-12.4/class-picker-prestige-reachability.spec.tsx 2>&1 | tail -40 && pnpm -w tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `PRESTIGE_PREREQ_OVERRIDES` contiene las 3 nuevas entradas (harper, campeondivino, weaponmaster) con los comentarios TODO/NOTE citados. Warlock + Swashbuckler NO añadidos (fuera de scope).
    - Constantes `MELEE_WEAPON_FOCUS_FEATS` (32 items con unarmed) y `WEAPONMASTER_WEAPON_FOCUS_FEATS` (32 items sin unarmed) declaradas al estilo de las constantes existentes.
    - Los 7 tests del Task 1 pasan (4 fixture + 3 integration).
    - `pnpm -w tsc --noEmit` clean (canonical IDs validan).
    - Suite completa `pnpm -w test` sin regresiones nuevas.
    - Commit GREEN creado.
    - Cero modificaciones en rules-engine / copy/es.ts / compiled-*.ts.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (se ejecutan tras los 2 tasks):

1. `pnpm -w test -- tests/phase-12.4/prestige-gate.fixture.spec.ts` — todos los casos PASS (incluyendo los 5 nuevos).
2. `pnpm -w test -- tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` — todos los casos PASS (incluyendo los 5 nuevos).
3. `pnpm -w test` — suite completa sin regresiones nuevas respecto a HEAD^.
4. `pnpm -w tsc --noEmit` — typecheck clean.
5. Grep sanity: `grep -c "'class:harper'\\|'class:campeondivino'\\|'class:weaponmaster'" apps/planner/src/features/level-progression/prestige-prereq-data.ts` devuelve ≥ 3. Grep NEGATIVO: `grep "'class:warlock'\\|'class:swashbuckler'" apps/planner/src/features/level-progression/prestige-prereq-data.ts` devuelve 0 (confirmar fuera de scope).
6. `git diff --stat packages/rules-engine/` vacío (Prescriptive Shape intacto).
7. `git diff --stat apps/planner/src/lib/copy/es.ts` vacío (templates se reutilizan, no se añaden strings).
</verification>

<success_criteria>
- Las filas de `class:harper`, `class:campeondivino`, `class:weaponmaster` en `<ClassPicker>` a L≥2 con cualquier progresión NO muestran 'Requisitos en revisión'; muestran los blockers templateados por `prestige-gate.ts` según prereqs no cumplidos.
- Las filas de `class:warlock` y `class:swashbuckler` siguen mostrando 'Requisitos en revisión' (fail-closed) — comportamiento intencional hasta plan cross-package.
- Los 7 nuevos tests (4 fixture + 3 integration) pasan y quedan como regresión contra futuros cambios.
- Se honran los 7 locked decisions del `<locked_decisions>` (sin cambios cross-package, sin copy nueva, sin ediciones en rules-engine).
- El TODO de Discipline (D-03) queda como comentario en línea en harper — OPEN-QUESTION para producto posterior.
- El gap de `server-gate` BlockerKind para warlock/swashbuckler queda documentado en SUMMARY como follow-up (plan separado, cross-package).
</success_criteria>

<output>
After completion, create `.planning/quick/260422-h9k-ampliar-cobertura-prestige-prereq-data-c/260422-h9k-SUMMARY.md` siguiendo el template estándar de quick SUMMARY, incluyendo:
- Sección "Gaps conocidos y follow-ups" que mencione:
  - `server-gate` BlockerKind pendiente para warlock/swashbuckler (cross-package, requiere cambios en `packages/rules-engine/src/progression/prestige-gate.ts` BlockerKind union + label function + copy/es.ts). Plan separado.
  - Discipline skill missing en catálogo player Puerta (D-03 OPEN-QUESTION): requiere decisión de producto — reinstaurar en extractor o dropear permanentemente.
  - First-wins dedupe de `class:harper` (Arcano-only) — pre-existing tech-debt fuera de este scope (D-05).
- Referencia cruzada a RESEARCH.md para las tablas canónicas completas.
</output>
