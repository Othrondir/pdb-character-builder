---
quick_id: 260606-l2n
title: Simular equipo en hoja de personaje
status: complete
created: 2026-06-06
---

# Objetivo

Anadir controles sencillos en `Hoja de personaje` para simular equipo sobre la
CA visible:

- `Simular Equipo nivel 12` suma +15 CA.
- `Simular Equipo nivel 16` suma +19 CA.
- `Tipo de Escudo` abre un modal con Escudo pequeno (+1), Escudo Mediano (+2)
  y Escudo Paves (+3).
- `Tipo de Armadura` abre un modal con CA 0 a CA 8.

# Alcance

- Solo afecta a la CA renderizada en la hoja.
- No modifica la build, persistencia, import/export, URL share ni motor de
  reglas.
- Mantener el patron visual existente de `NwnButton` y `<dialog>`.

# Validacion

- Test focalizado de `CharacterSheet`.
- Typecheck.
- Build del planner.

# Resultado

- Anadidos los botones `Simular Equipo nivel 12` y `Simular Equipo nivel 16`
  bajo las salvaciones de la hoja de personaje.
- Anadidos selectores modales para `Tipo de Escudo` y `Tipo de Armadura`.
- La CA visible suma la CA base actual + preset de equipo + escudo + armadura.
- Los cambios son locales a la vista de hoja; no se persisten ni alteran la
  build.

# Evidencia

- `./node_modules/.bin/vitest run tests/phase-05.2/character-sheet.spec.tsx --reporter=dot` -> 1 file / 11 tests passed.
- `./node_modules/.bin/tsc -p tsconfig.base.json --noEmit` -> passed.
- `pnpm --dir apps/planner build` -> passed.
- `curl -I http://127.0.0.1:5173/` -> HTTP 200.
