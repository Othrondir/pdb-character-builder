---
quick_id: 260419-68b
slug: fix-atributos-layout
date: 2026-04-19
status: complete
---

# Quick Task 260419-68b — Summary

## Changed Files

- `apps/planner/src/styles/app.css` (3 commits)

## Commits

- `118a6f6` style(260419-68b-01): switch atributos editor rows + headers to CSS grid so columns align
- `a05e74a` style(260419-68b-02): strengthen +/- button glyphs in atributos cell-base
- `762aa57` style(260419-68b-03): lining + tabular numerals across atributos numeric text

Base commit: `7e983ca` (direct children, no rebase needed).

## What Changed

### Task 1 — Grid-based row alignment (commit 118a6f6)

- `.attributes-editor__row` and `.attributes-editor__column-headers` both now use `display: grid` with identical `grid-template-columns: minmax(0, 1fr) 140px 64px 56px 48px`.
- Removed `flex: 1` from `.attributes-editor__label` and `.attributes-editor__column-headers-label` (grid track sizing replaces it).
- Removed `min-width: 72px` from `.attributes-editor__cell` and `.attributes-editor__column-headers-cell`.
- `.attributes-editor__cell--base` keeps `display: inline-flex` internally so the `[-] value [+]` row stays horizontal inside its 140px grid column.
- Mod cell pinned with `justify-self: end` + `text-align: right` so `(+N)` stays column-aligned across all six rows.

### Task 2 — Button glyph visibility (commit a05e74a)

- Scoped rule `.attributes-editor__cell--base .nwn-button` only — global `.nwn-button--secondary` untouched (Reiniciar/Aceptar unaffected).
- `color: var(--color-text)` + `font-weight: 700` + `font-size: 18px` bring dash/plus to full legibility against dark panel.
- `min-height: 36px` + compact `padding: var(--space-xs) var(--space-sm)` + `line-height: 1` keep the button footprint inside the 140px column.

### Task 3 — Lining + tabular numerals (commit 762aa57)

`font-variant-numeric: lining-nums tabular-nums;` applied to every numeric surface in the attributes editor:

- `.attributes-editor__header` (Puntos restantes: 30)
- `.attributes-editor__value` (base score)
- `.attributes-editor__mod` (modifier in parens)
- `.attributes-editor__cell--racial` (signed racial mod)
- `.attributes-editor__cell--final` (final score)
- `.attributes-editor__summary dd` (spent/remaining panel on right)

Fixes `10 → io` / `0 → o` rendering from `var(--font-display)` oldstyle default; tabular-nums locks digit advance so columns do not jitter on `9 ↔ 10` transitions.

## Verification

- CSS block at `apps/planner/src/styles/app.css:89-138` rewritten exactly per plan.
- Vite HMR dev server (background task `bdugirolg` on port 5173) accepted all CSS saves: 6 clean `hmr update /src/styles/app.css` entries, zero `[vite] internal server error`.
- `attributes-board.tsx` class tree unchanged — all selectors targeted by the CSS changes continue to exist in JSX (`attributes-editor__row`, `__label`, `__cell--base`, `__cell--racial`, `__cell--final`, `__mod`, `__value`, `__column-headers*`, `__header`, `__summary dd`).
- No TS changes; TS suite not re-run (scope: pure CSS visual patch, 499/499 green from STATE.md baseline).
- User will perform in-browser UAT at `http://localhost:5173/` — Construcción → Atributos.

## Expected UAT Outcomes

- FUE/DES/CON/INT/SAB/CAR rows align column-for-column with Base/Racial/Final header at 1280×900.
- CON `+2` no longer shifts FINAL column; INT FINAL fully visible inside left panel (no clip behind DetailPanel).
- `-` and `+` glyphs read at equal weight/contrast to the base value; disabled rows still dim via existing `.nwn-button:disabled { opacity: 0.45 }`.
- `10` reads as `10` (lining digits), not `io`; `0` reads as `0`, not `o`. `Puntos restantes: 30` reads correctly.

## Follow-ups

- None — CSS-only visual patch. No new regression spec (pure layout; test suite scope unchanged).

## Self-Check: PASSED

- [x] `apps/planner/src/styles/app.css` — present, modified in all three commits.
- [x] Commit `118a6f6` — present in `git log`.
- [x] Commit `a05e74a` — present in `git log`.
- [x] Commit `762aa57` — present in `git log`.
- [x] Base commit `7e983ca` — still parent of `118a6f6`.
- [x] Vite HMR accepted all CSS saves without internal server error.
