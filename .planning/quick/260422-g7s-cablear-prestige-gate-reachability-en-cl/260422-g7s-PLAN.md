---
phase: quick-260422-g7s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/planner/src/features/level-progression/class-picker.tsx
  - tests/phase-12.4/class-picker-prestige-reachability.spec.tsx
autonomous: true
requirements:
  - QUICK-260422-G7S-R1
must_haves:
  truths:
    - "En L9 con Mago 8 y un slot libre, la fila de Caballero Arcano en el class-picker queda habilitada (aria-disabled='false', sin texto 'Requisitos en revisión')."
    - "En L9 con Mago 7 (+ cualquier otra clase no-arcana), Caballero Arcano sigue bloqueada y muestra copy 'Requiere conjuros arcanos de nivel 5'."
    - "En L1, toda clase de prestigio sigue bloqueada con copy 'Clase de prestigio: no disponible en nivel 1; revisa sus requisitos' (regresión 12.4-06 intacta)."
    - "Las clases base siguen siendo reachable sin afectar su rama (branch 1 del gate)."
    - "Las clases de prestigio SIN override en PRESTIGE_PREREQ_OVERRIDES siguen bloqueadas con 'Requisitos en revisión' (fail-closed para prestigio no enriquecido)."
    - "Los tests existentes de rules-engine (prestige-gate.fixture.spec.ts) y picker (class-picker-grouping.spec.tsx) siguen verdes."
  artifacts:
    - path: "apps/planner/src/features/level-progression/class-picker.tsx"
      provides: "ClassPicker cableado a buildPrestigeGateBuildState + getPrestigeDecodedPrereqs con enriched=true cuando hay overrides."
      contains: "buildPrestigeGateBuildState"
    - path: "tests/phase-12.4/class-picker-prestige-reachability.spec.tsx"
      provides: "RTL integration spec para Caballero Arcano en L9: reachable=true con Mago 8, bloqueada con Mago 7."
      contains: "caballero-arcano"
  key_links:
    - from: "apps/planner/src/features/level-progression/class-picker.tsx"
      to: "apps/planner/src/features/level-progression/prestige-gate-build.ts"
      via: "buildPrestigeGateBuildState(progressionState, foundationState, featState, skillState, activeLevel) invocado una vez por render del ClassPicker"
      pattern: "buildPrestigeGateBuildState\\("
    - from: "apps/planner/src/features/level-progression/class-picker.tsx"
      to: "apps/planner/src/features/level-progression/prestige-prereq-data.ts"
      via: "getPrestigeDecodedPrereqs(option.id) en toClassPrereqInput cuando option.kind === 'prestige'"
      pattern: "getPrestigeDecodedPrereqs\\("
    - from: "apps/planner/src/features/level-progression/class-picker.tsx"
      to: "packages/rules-engine/src/progression/prestige-gate.ts"
      via: "reachableAtLevelN(...) recibe gateBuildState + enriched=true cuando decodedPrereqs existe"
      pattern: "reachableAtLevelN\\("
---

<objective>
Cablear el gate de reachability de clases de prestigio en `class-picker.tsx` para que clases como Caballero Arcano, Asesino, Danzante de Sombras etc. se habiliten cuando el personaje cumple los requisitos reales (BAB / nivel de conjuro arcano / rangos de habilidad / dotes / raza / niveles de clase).

Purpose: Actualmente el gate ejecuta siempre la rama 3 (`!enriched || !classRow.decodedPrereqs`) porque `toClassPrereqInput` no adjunta `decodedPrereqs` y `ClassPickerRow` pasa contexto vacío + `enriched: false` hardcoded. Los overrides ya existen en `prestige-prereq-data.ts` y el builder ya existe en `prestige-gate-build.ts` — solo falta el cableado en el límite UI. El comentario del header que dice "Phase 13.x deferred" está desactualizado.

Output:
- `class-picker.tsx` modificado para derivar `PrestigeGateBuildState` una vez por render via `buildPrestigeGateBuildState`, propagar a cada `ClassPickerRow`, adjuntar `decodedPrereqs` desde `getPrestigeDecodedPrereqs` en `toClassPrereqInput`, y pasar `enriched: true` cuando hay overrides.
- Nuevo spec en `tests/phase-12.4/` que locks el happy path (Mago 8 en L9 → Caballero Arcano reachable) + negative path (Mago 7 en L9 → arcane-spell blocker).
- Header comment actualizado: elimina "Phase 13.x enrichment deferred" y refleja que el enrichment ya está cableado via planner overrides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@apps/planner/src/features/level-progression/class-picker.tsx
@apps/planner/src/features/level-progression/prestige-prereq-data.ts
@apps/planner/src/features/level-progression/prestige-gate-build.ts
@packages/rules-engine/src/progression/prestige-gate.ts
@apps/planner/src/features/feats/store.ts
@apps/planner/src/features/skills/store.ts
@apps/planner/src/features/level-progression/store.ts
@apps/planner/src/features/character-foundation/store.ts
@tests/phase-12.4/prestige-gate.fixture.spec.ts
@tests/phase-12.4/class-picker-grouping.spec.tsx

<interfaces>
<!-- Contratos clave ya existentes en el codebase que el executor consume. Extraídos directamente de los archivos listados arriba. No hay que explorar. -->

From `apps/planner/src/features/level-progression/prestige-gate-build.ts`:
```typescript
export interface PrestigeGateBuildState {
  abilityScores: Record<string, number>;
  bab: number;
  classLevels: Record<string, number>;
  featIds: Set<string>;
  highestArcaneSpellLevel: number;
  highestSpellLevel: number;
  raceId: string | null;
  skillRanks: Record<string, number>;
}

export function buildPrestigeGateBuildState(
  progressionState: LevelProgressionStoreState,
  foundationState: CharacterFoundationStoreState,
  featState: FeatStoreState,
  skillState: SkillStoreState,
  level: ProgressionLevel,
): PrestigeGateBuildState;
```

From `apps/planner/src/features/level-progression/prestige-prereq-data.ts`:
```typescript
export function getPrestigeDecodedPrereqs(
  classId: CanonicalId | null,
): DecodedPrereqs | undefined;
// Hay 19 overrides incluyendo 'class:caballero-arcano':
//   { minArcaneSpellLevel: 5, requiredFeats: [...MARTIAL_WEAPON_PROFICIENCIES] }
```

From `packages/rules-engine/src/progression/prestige-gate.ts` — gate contract (NO TOCAR):
```typescript
export interface PrestigeGateInput {
  classRow: ClassPrereqInput;       // { id, isBase, decodedPrereqs? }
  level: number;
  abilityScores: Record<string, number>;
  bab: number;
  raceId: string | null;
  skillRanks: Record<string, number>;
  featIds: Set<string>;
  classLevels: Record<string, number>;
  highestArcaneSpellLevel: number;
  highestSpellLevel: number;
  enriched: boolean;
}
export function reachableAtLevelN(input: PrestigeGateInput): PrestigeGateResult;
// Branch 3 (!enriched || !classRow.decodedPrereqs) → { unvetted, 'Requisitos en revisión' }
// Branch 4 (enriched && decodedPrereqs) → decode blockers → reachable si lista vacía
```

From `apps/planner/src/features/feats/store.ts`:
```typescript
export const useFeatStore: () => FeatStoreState;  // zustand hook
```

From `apps/planner/src/features/skills/store.ts`:
```typescript
export const useSkillStore: () => SkillStoreState; // zustand hook
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Spec de reachability para Caballero Arcano (L9 Mago 8 reachable, L9 Mago 7 bloqueada)</name>
  <files>tests/phase-12.4/class-picker-prestige-reachability.spec.tsx</files>
  <behavior>
    Crear spec RTL bajo `tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` (glob jsdom ya configurado para `tests/phase-12.4/**/*.spec.tsx`).

    Casos a cubrir:
    - **Happy path**: Humano + L1..L8 = Mago, L9 activo, 5 niveles de dotes de competencia marcial auto-granted por Mago Ø — en su lugar el test seedeará los feats requeridos por el override de Caballero Arcano directamente via `useFeatStore.getState().setClassFeat(...)` o `setGeneralFeat(...)` para algunos de los `MARTIAL_WEAPON_PROFICIENCIES` (los `requiredFeats` de Caballero Arcano). Como el override declara `requiredFeats: [...MARTIAL_WEAPON_PROFICIENCIES]` (AND, no OR), para mantener el test atomic se usará una variante PRAGMÁTICA: pre-seed del feat set via un harness que use `buildPriorFeatIds`-equivalente directo. **Pero** la ruta UI es lo que queremos locking. Solución: el happy-path asserta ÚNICAMENTE que la rama `arcane-spell` ya no dispara (i.e. el label 'Requiere conjuros arcanos de nivel 5' desaparece de la fila `[data-class-id="class:caballero-arcano"]`) cuando el personaje tiene Mago 8. El test NO requiere reachable=true full-green (porque eso exigiría seedear 25 feats marciales), solo verifica que el blocker de arcane-spell se RESUELVE. Este es el core del fix — el blocker de feats es independiente y ya funciona.
    - **Negative arcane-spell path**: Humano + L1..L8 = Guerrero (no arcano), L9 activo → fila de Caballero Arcano muestra copy 'Requiere conjuros arcanos de nivel 5' (label exacta del override minArcaneSpellLevel=5).
    - **Fail-closed intact**: L9, prestige class SIN override en `PRESTIGE_PREREQ_OVERRIDES` (ej. `class:weapon-master` si existe sin override, o usar otra prestige conocida sin override — verificar cuál del catálogo compilado NO está en PRESTIGE_PREREQ_OVERRIDES). Esa fila sigue mostrando 'Requisitos en revisión'. Si no hay ninguna prestige sin override en el catálogo actual, OMITIR este caso y dejar un comentario `// No prestige class lacks override in current catalog; negative branch already locked by 12.4-06 spec at L1`.
    - **L1 regresión**: En L1 con Humano + Mago L1 seed, toda clase de prestigio (incluyendo Caballero Arcano) muestra 'Clase de prestigio: no disponible en nivel 1; revisa sus requisitos' (rama 2 del gate no fue tocada por este plan).

    Setup pattern: copiar `setupL1Humano` de `class-picker-grouping.spec.tsx` y añadir una variante `setupL9WithClassProgression(classId: CanonicalId)` que loopea `setLevelClassId(n, classId)` for n in 1..8, luego `setActiveLevel(9)`. Reset stores en `beforeEach` incluyendo `useFeatStore.getState().resetFeatSelections()` + `useSkillStore.getState().resetSkillAllocations()` (además de progression + foundation).

    Mínimo 4 test cases (arcane-resolved + arcane-blocker + L1-prestige-still-blocked + al menos una base class sigue reachable en L9).

    Test 1 (happy path arcane resolved):
    ```ts
    it('L9 con Mago 8 niveles: fila Caballero Arcano NO muestra blocker arcane-spell', () => {
      setupL9WithClassProgression('class:wizard' as CanonicalId);
      render(createElement(ClassPicker));
      const row = document.querySelector('[data-class-id="class:caballero-arcano"]');
      expect(row).not.toBeNull();
      expect(row?.textContent).not.toMatch(/Requiere conjuros arcanos de nivel 5/);
      expect(row?.textContent).not.toMatch(/Requisitos en revisión/); // enriched=true, no más fail-closed
    });
    ```

    Test 2 (negative arcane):
    ```ts
    it('L9 con Guerrero 8 niveles: fila Caballero Arcano muestra arcane-spell blocker', () => {
      setupL9WithClassProgression('class:fighter' as CanonicalId);
      render(createElement(ClassPicker));
      const row = document.querySelector('[data-class-id="class:caballero-arcano"]');
      expect(row?.textContent).toMatch(/Requiere conjuros arcanos de nivel 5/);
    });
    ```
  </behavior>
  <action>
    Crear el archivo exactamente como describe `<behavior>`. Ejecutar con `pnpm exec vitest run tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` para confirmar que FALLA contra la implementación actual (Test 1 falla porque row.textContent contiene 'Requisitos en revisión' debido al fail-closed actual). Commit RED: `test(quick-260422-g7s): RED — prestige gate reachability wiring spec`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/phase-12.4/class-picker-prestige-reachability.spec.tsx</automated>
  </verify>
  <done>Spec existe, al menos 1 test falla con assertion clara contra 'Requisitos en revisión' en estado RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Cablear toClassPrereqInput + gateBuildState + enriched flag en class-picker.tsx</name>
  <files>apps/planner/src/features/level-progression/class-picker.tsx</files>
  <behavior>
    - Test 1 (arcane resolved) ahora pasa: la fila de Caballero Arcano con Mago 8 ya no muestra el copy 'Requisitos en revisión' ni el blocker 'arcane-spell'. Los 25 `requiredFeats` de MARTIAL_WEAPON_PROFICIENCIES seguirán sin estar satisfechos (no los pre-seedeamos), por lo que la fila mostrará labels tipo 'Requiere dote: Competencia con arma marcial (espada corta)' — eso es correcto y esperado.
    - Test 2 (negative arcane) pasa: la fila muestra exactamente 'Requiere conjuros arcanos de nivel 5' cuando `highestArcaneSpellLevel` es 0 (Guerrero no es caster).
    - Test L1 prestige regresión sigue verde (rama 2 del gate dispara antes que rama 4).
    - Fixture spec `tests/phase-12.4/prestige-gate.fixture.spec.ts` sigue 100% verde (no se toca rules-engine).
    - Spec 12.4-06 `tests/phase-12.4/class-picker-grouping.spec.tsx` sigue 100% verde (aria-labelledby, data-class-id, fallback `enriched=false` para prestige sin override).
  </behavior>
  <action>
    Editar `apps/planner/src/features/level-progression/class-picker.tsx` con los siguientes cambios puntuales:

    **1. Imports nuevos** (añadir después de los imports existentes de `@rules-engine` + `@planner`):
    ```typescript
    import { useFeatStore } from '@planner/features/feats/store';
    import { useSkillStore } from '@planner/features/skills/store';

    import {
      buildPrestigeGateBuildState,
      type PrestigeGateBuildState,
    } from './prestige-gate-build';
    import { getPrestigeDecodedPrereqs } from './prestige-prereq-data';
    ```

    **2. Header comment** (líneas 1-18): reemplazar las líneas 14-17 (el párrafo "Extractor prereq enrichment for prestige is deferred to Phase 13.x ... fails-closed to `Requisitos en revisión` at L2+.") por un párrafo que diga exactamente (comentario JSDoc dentro del bloque existente):
    ```
    * Prestige prereq enrichment is wired via planner-local overrides in
    * `prestige-prereq-data.ts` (`getPrestigeDecodedPrereqs`) — keeps the
    * rules-engine gate framework-agnostic per CLAUDE.md "Prescriptive Shape".
    * Runtime build state (bab / featIds / classLevels / skillRanks /
    * abilityScores / raceId / highestArcaneSpellLevel / highestSpellLevel) is
    * derived once per render via `buildPrestigeGateBuildState` and forwarded
    * to every row. Prestige classes without an override still fail-closed to
    * `Requisitos en revisión` (branch 3 of `reachableAtLevelN`).
    ```
    NO dejar ninguna mención a "Phase 13.x" o "deferred" en el header.

    **3. `toClassPrereqInput` (líneas 46-54)**: extender para adjuntar `decodedPrereqs` cuando `option.kind === 'prestige'`:
    ```typescript
    function toClassPrereqInput(option: ClassOptionView): ClassPrereqInput {
      const record = getPhase04ClassRecord(option.id);
      const isBase = (record?.kind ?? option.kind) === 'base';
      const decodedPrereqs = isBase
        ? undefined
        : getPrestigeDecodedPrereqs(option.id);
      return {
        id: option.id as string,
        isBase,
        ...(decodedPrereqs ? { decodedPrereqs } : {}),
      };
    }
    ```

    **4. `ClassPicker` body**: después de las líneas existentes que leen `useLevelProgressionStore` + `useCharacterFoundationStore`, añadir los dos hooks nuevos y derivar el gate build state:
    ```typescript
    const featState = useFeatStore();
    const skillState = useSkillStore();
    const gateBuildState = buildPrestigeGateBuildState(
      progressionState,
      foundationState,
      featState,
      skillState,
      activeLevel,
    );
    ```
    Nota de orden: `activeLevel` se calcula DESPUÉS de leer progressionState; mover la llamada a `buildPrestigeGateBuildState` a después de `const activeLevel = ...` para que la variable exista.

    **5. Propagar `gateBuildState` a cada `ClassPickerRow`**: añadir prop `gateBuildState` en ambos `.map` (baseOptions y prestigeOptions).

    **6. Extender `ClassPickerRowProps`** con `gateBuildState: PrestigeGateBuildState`.

    **7. `ClassPickerRow` body** (líneas 130-148): reemplazar el objeto pasado a `reachableAtLevelN` para usar el build state real y calcular `enriched` dinámicamente:
    ```typescript
    const classRow = toClassPrereqInput(option);
    const gateResult: PrestigeGateResult = reachableAtLevelN({
      classRow,
      level,
      abilityScores: gateBuildState.abilityScores,
      bab: gateBuildState.bab,
      raceId: gateBuildState.raceId,
      skillRanks: gateBuildState.skillRanks,
      featIds: gateBuildState.featIds,
      classLevels: gateBuildState.classLevels,
      highestArcaneSpellLevel: gateBuildState.highestArcaneSpellLevel,
      highestSpellLevel: gateBuildState.highestSpellLevel,
      enriched: classRow.decodedPrereqs !== undefined,
    });
    ```
    **Por qué `enriched = classRow.decodedPrereqs !== undefined`**: es el invariante del contrato — si no hay overrides, `decodedPrereqs` queda `undefined` y el gate cae a la rama 3 (fail-closed 'Requisitos en revisión'); si hay overrides, `enriched=true` hace que el gate decodifique los blockers reales. El comentario existente en las líneas 134-138 ("we always pass `enriched: false` for prestige") se reemplaza por un comentario más corto: `// enriched reflects whether planner-local overrides supplied decodedPrereqs.`

    **8. Eliminar el comentario obsoleto** en `toClassPrereqInput` (líneas 51-53) que dice "decodedPrereqs: extractor enrichment deferred to Phase 13.x ..." — reemplazarlo por `// decodedPrereqs attached for prestige rows when planner-local overrides exist (see prestige-prereq-data.ts).`

    **Restricciones duras** (CLAUDE.md "Prescriptive Shape"):
    - NO tocar `packages/rules-engine/src/progression/prestige-gate.ts` — el gate queda shape-stable.
    - NO tocar `prestige-prereq-data.ts` ni `prestige-gate-build.ts` — solo consumirlos.
    - NO tocar CSS, NO añadir tokens, NO cambiar copy visible (labels siguen viniendo del gate helper).
    - `raceId` debe pasarse al gate (la rama 4 lo usa para `requiredAnyRaceIds`) — está en `gateBuildState.raceId`. El código actual NUNCA pasaba raceId al gate (lo omitía entero) — este cableado lo restaura.

    Ejecutar `pnpm exec vitest run tests/phase-12.4/class-picker-prestige-reachability.spec.tsx tests/phase-12.4/class-picker-grouping.spec.tsx tests/phase-12.4/prestige-gate.fixture.spec.ts` para confirmar que los 3 specs pasan. Commit GREEN: `feat(quick-260422-g7s): wire prestige gate reachability in class-picker`.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/phase-12.4/class-picker-prestige-reachability.spec.tsx tests/phase-12.4/class-picker-grouping.spec.tsx tests/phase-12.4/prestige-gate.fixture.spec.ts</automated>
  </verify>
  <done>
    Los 3 specs (nuevo + 12.4-06 picker + 12.4-06 gate fixture) pasan 100% verdes. `grep "Phase 13.x"` en class-picker.tsx devuelve 0 matches. `grep "enriched: false"` en class-picker.tsx devuelve 0 matches (ya no se hardcodea). `grep "buildPrestigeGateBuildState"` en class-picker.tsx devuelve ≥ 1 match.
  </done>
</task>

</tasks>

<verification>
```bash
# Full Phase 12.4 suite (locks no cross-phase regressions)
pnpm exec vitest run tests/phase-12.4/

# New spec alone (sanity)
pnpm exec vitest run tests/phase-12.4/class-picker-prestige-reachability.spec.tsx

# Typecheck (new hooks + prop additions)
pnpm exec tsc --noEmit

# Invariant greps
grep -c "Phase 13.x" apps/planner/src/features/level-progression/class-picker.tsx  # expect 0
grep -c "enriched: false" apps/planner/src/features/level-progression/class-picker.tsx  # expect 0
grep -c "buildPrestigeGateBuildState" apps/planner/src/features/level-progression/class-picker.tsx  # expect >= 1
grep -c "getPrestigeDecodedPrereqs" apps/planner/src/features/level-progression/class-picker.tsx  # expect >= 1

# Rules-engine framework purity (must stay 0)
grep -rn "from '@planner" packages/rules-engine/src/  # expect 0
grep -rn "from 'react'" packages/rules-engine/src/  # expect 0
```
</verification>

<success_criteria>
- En L9 con progression `class:wizard` x8 (L1..L8), la fila `[data-class-id="class:caballero-arcano"]` en el `<ClassPicker />` ya NO muestra el texto 'Requisitos en revisión' ni 'Requiere conjuros arcanos de nivel 5'. (El blocker de feats 'Requiere dote: Competencia con arma marcial (...)' SÍ aparece y es correcto — el usuario deberá satisfacer también esos prereqs separadamente.)
- En L9 con progression `class:fighter` x8, esa misma fila muestra 'Requiere conjuros arcanos de nivel 5' (verbatim del `arcaneSpellLabel(5)` del gate).
- En L1, todas las filas de prestige siguen mostrando 'Clase de prestigio: no disponible en nivel 1; revisa sus requisitos' (rama 2 intacta).
- Spec nuevo `tests/phase-12.4/class-picker-prestige-reachability.spec.tsx` pasa 100% verde.
- Specs existentes `prestige-gate.fixture.spec.ts` (rules-engine puro) y `class-picker-grouping.spec.tsx` (UI + CLAS-03 bridge + L1 fail-closed) siguen 100% verdes — sin regresiones.
- `grep "from '@planner'" packages/rules-engine/src/` devuelve 0 matches (CLAUDE.md "Prescriptive Shape" respetado — rules-engine sigue puro).
- `pnpm exec tsc --noEmit` sin errores en archivos tocados.
</success_criteria>

<output>
After completion, create `.planning/quick/260422-g7s-cablear-prestige-gate-reachability-en-cl/260422-g7s-SUMMARY.md` following the standard GSD summary template: objective, behavior changes, files touched, verification evidence (vitest output), and any follow-ups (e.g. if user later wants Caballero Arcano fully unlockable without hand-seeding MARTIAL_WEAPON_PROFICIENCIES, that's a separate UX gap outside this quick's scope).
</output>
