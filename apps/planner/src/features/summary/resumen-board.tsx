// Task 3 placeholder. Task 4 replaces this file with the full implementation
// (header + action bar + ResumenTable + Save/Load dialogs + hidden file input).
// Keeping a minimal component here so center-content.tsx and the lang-03 spec
// can import from this path without a forward-ref dangling.
export function ResumenBoard() {
  return <section className="resumen-board" aria-label="Resumen" />;
}
